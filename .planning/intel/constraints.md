# Constraints (from SPECs)

No SPEC-type documents were present in this ingest set (`CLASSIFICATIONS_DIR`
contained 5 documents, all classified `PRD`). No constraints extracted.

Note: individual PRDs contain their own "Constraints" subsections (performance,
compatibility, security, skill-quality-gate, etc.), but per the type taxonomy
those stay embedded in the PRD's requirement entries in `requirements.md`
rather than being lifted here — this file is reserved for SPEC-type documents.
