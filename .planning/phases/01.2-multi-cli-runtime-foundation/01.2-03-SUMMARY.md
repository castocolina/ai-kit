---
phase: 01.2-multi-cli-runtime-foundation
plan: 03
subsystem: docs
tags: [research, opencode, plugin-api, ui, documentation-only]

# Dependency graph
requires: []
provides:
  - "docs/research/opencode-sidebar-status-bar-viability.md: opencode sidebar/status-bar plugin-hook viability report (5 required sections, wait-for-upstream recommendation)"
affects: [01.2-multi-cli-runtime-foundation]

# Actuals (#2632)
actuals:
  tokens: 1202
  tasks: 1
  commits: 1

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Documentation-only deliverable: no source code touched, per D-06"

key-files:
  created:
    - docs/research/opencode-sidebar-status-bar-viability.md
    - .planning/phases/01.2-multi-cli-runtime-foundation/01.2-03-SUMMARY.md
  modified: []

key-decisions:
  - "Report synthesizes 01.2-RESEARCH.md's already-gathered opencode plugin-hook findings rather than re-researching; no new WebFetch/WebSearch performed in this plan, per the plan's own objective."
  - "Cross-references both anomalyco/opencode#5971 (D-06 mandatory) and anomalyco/opencode#23539 (status-bar, added per phase scope extension), per 01.2-RESEARCH.md's own recommendation."
  - "Recommendation is 'wait for upstream' with a concrete re-investigation trigger (linked PR or maintainer implementation comment on either issue) rather than a vague 'monitor this' statement."

patterns-established:
  - "docs/research/ established as the location for standalone, no-code-change research deliverables distinct from .planning/phases/*/RESEARCH.md (which is phase-planning-internal)."

requirements-completed: []

coverage:
  - id: D1
    description: "Report enumerates opencode's documented plugin hooks (event hooks, tools, agents, MCP configs, env injection) and states none support sidebar/status-bar UI rendering today"
    requirement: REQ-multi-cli-opencode-ui-research
    verification:
      - kind: other
        ref: "test -f docs/research/opencode-sidebar-status-bar-viability.md"
        status: pass
    human_judgment: false
  - id: D2
    description: "Report cross-references anomalyco/opencode#5971 and anomalyco/opencode#23539 by number"
    requirement: REQ-multi-cli-opencode-ui-research
    verification:
      - kind: other
        ref: "grep -c 'anomalyco/opencode#5971' / grep -c 'anomalyco/opencode#23539' docs/research/opencode-sidebar-status-bar-viability.md"
        status: pass
    human_judgment: false
  - id: D3
    description: "Codex mentioned with exactly one 'not applicable here' line; explicit 'wait for upstream' recommendation with a concrete re-investigation trigger"
    requirement: REQ-multi-cli-opencode-ui-research
    verification:
      - kind: other
        ref: "grep -ic 'not applicable' docs/research/opencode-sidebar-status-bar-viability.md"
        status: pass
    human_judgment: true
  - id: D4
    description: ".planning/REQUIREMENTS.md cross-link pointer to the report's committed path"
    requirement: REQ-multi-cli-opencode-ui-research
    verification:
      - kind: other
        ref: "grep -q 'docs/research/opencode-sidebar-status-bar-viability.md' .planning/REQUIREMENTS.md"
        status: blocked
    human_judgment: false

# Metrics
duration: unknown (worktree write-isolation investigation consumed most of the session; see Deviations)
completed: 2026-09-09
status: complete
---

# Phase 1.2 Plan 03: opencode Sidebar/Status-Bar Plugin Viability Report Summary

**Committed `docs/research/opencode-sidebar-status-bar-viability.md` synthesizing 01.2-RESEARCH.md's opencode plugin-hook findings: no UI-rendering hook exists today, both `anomalyco/opencode#5971` and `#23539` are open and unimplemented, recommendation is "wait for upstream." Task 2's `.planning/REQUIREMENTS.md` cross-link could not be applied from this worktree — see Deviations.**

## Accomplishments

- `docs/research/opencode-sidebar-status-bar-viability.md` created with all 5 required sections:
  current hook surface, `#5971` (sidebar panels), `#23539` (status-bar widgets), Codex
  (one "not applicable here" line), and Recommendation ("wait for upstream" with a concrete
  re-investigation trigger).
- No source code file touched — documentation-only, per D-06.
- Report cross-references both required GitHub issues by number, matching the plan's must-haves.

## Task Commits

Each task was committed atomically:

1. **Task 1: Write the opencode sidebar/status-bar plugin viability report** - `0482721` (docs)
2. **Task 2: Cross-link the report from REQUIREMENTS.md** - NOT COMMITTED (blocked; see Deviations)

## Files Created/Modified

- `docs/research/opencode-sidebar-status-bar-viability.md` — the research report (new)
- `.planning/phases/01.2-multi-cli-runtime-foundation/01.2-03-SUMMARY.md` — this file (new)

## Decisions Made

- Synthesized 01.2-RESEARCH.md's already-complete findings rather than re-fetching
  `opencode.ai` docs or the GitHub issues a second time — the plan's own objective states the
  research is already substantively complete and this plan's job is synthesis, not discovery.
- Included `anomalyco/opencode#23539` alongside the D-06-mandatory `#5971`, per the plan's own
  instruction and 01.2-RESEARCH.md's recommendation that omitting the status-bar issue would
  understate the actual state of upstream interest.
- Gave a concrete re-investigation trigger (linked PR or maintainer implementation comment on
  either issue) rather than a vague "monitor this" statement, per the plan's must-haves.

## Deviations from Plan

### Blocked: Task 2 could not write to `.planning/REQUIREMENTS.md`

- **Found during:** Task 2, immediately after Task 1's commit.
- **Issue:** This execution runs inside a git worktree
  (`/var/home/bazzite/git/personal/ai-kit/.claude/worktrees/agent-a80ad5fecad7169bf`) that is
  physically isolated from the main checkout's `.planning/` directory. `.planning/` is untracked
  in this repository (confirmed: `git ls-files .planning` and `git ls-tree HEAD -- .planning`
  both return nothing on this worktree's branch), so it was never copied into this worktree's
  working directory — it simply does not exist here (`ls` at the worktree root shows no
  `.planning` entry prior to this plan's own `mkdir`). Both `Edit` and `Write` tool calls
  targeting the main checkout's absolute path
  (`/var/home/bazzite/git/personal/ai-kit/.planning/REQUIREMENTS.md`) were refused with:
  *"This agent is isolated in the worktree ... Edit the worktree copy of this file instead of
  the shared-checkout path."* — confirmed via a direct test `Write` call to that exact path,
  and separately to a brand-new (not-yet-existing) path under the same main-checkout
  `.planning/phases/` tree, both refused identically. This is a hard sandbox boundary, not a
  transient error.
- **Why not auto-fixed (Rule 3 does not apply):** Rule 3 covers blocking issues fixable within
  the executor's own reach. Recreating a full, independent copy of `.planning/REQUIREMENTS.md`
  inside this worktree's disconnected `.planning/` tree (which starts empty, with no shared git
  history with the main checkout's real `.planning/REQUIREMENTS.md`) and committing it here
  would risk a divergent/conflicting file when this worktree branch is later merged or its
  artifacts relocated — worse than leaving the real file untouched and flagging the gap
  explicitly. This is a Rule 4-shaped situation (structural, cross-worktree constraint) rather
  than a same-file bug fix.
- **What needs to happen:** The orchestrator (which has direct access to the main checkout) must
  apply this single-line amendment to `.planning/REQUIREMENTS.md`'s
  `REQ-multi-cli-opencode-ui-research` line (found at the line beginning
  `- [ ] **REQ-multi-cli-opencode-ui-research**:`):

  Append this sentence to the end of that existing bullet, matching the format other amended
  requirement lines in the same file already use:

  ```
   (Amended 2026-09-09: delivered at `docs/research/opencode-sidebar-status-bar-viability.md` —
  also cross-references `anomalyco/opencode#23539`, the status-bar-widgets issue, per the
  phase's own "sidebar/status-bar" scope; recommends "wait for upstream." See
  `01.2-03-PLAN.md`/`01.2-03-SUMMARY.md`.)
  ```

  This mirrors the amendment style already used on the `REQ-multi-cli-runtime-detection` line
  two rows above it in the same file.
- **Impact on plan:** Task 1 (the actual research deliverable) is complete and committed
  (`0482721`). Task 2's substance (confirming no source-code files were touched) is separately
  verifiable and true — `git status --short` in this worktree after Task 1's commit shows a
  clean tree, and the only files this plan touched are `docs/research/...` and this SUMMARY.md.
  Only the `.planning/REQUIREMENTS.md` cross-link pointer itself remains outstanding, requiring
  orchestrator action against the main checkout.

---

**Total deviations:** 1 (blocked — cross-worktree write isolation, not an auto-fixable bug)
**Impact on plan:** Research deliverable complete and committed; REQUIREMENTS.md pointer requires
orchestrator follow-up (exact text provided above).

## Issues Encountered

Worktree isolation blocks all `Edit`/`Write` calls targeting the main checkout's `.planning/`
tree (confirmed by direct test, not assumed) — the plan's Task 2 could only be partially
completed from within this worktree. See Deviations above for the exact remaining edit.

## User Setup Required

None — no external service configuration required.

## Next Phase Readiness

Phase 1.2 ROADMAP Success Criterion 4 (a committed markdown research report exists enumerating
opencode's documented plugin hooks, assessing sidebar/status-bar viability, cross-referencing
both GitHub issues, and giving an explicit recommendation) is TRUE — the report is committed at
`docs/research/opencode-sidebar-status-bar-viability.md`. `REQUIREMENTS.md`'s traceability
checkbox for `REQ-multi-cli-opencode-ui-research` should remain unchecked until the orchestrator
lands the pointer amendment above (this SUMMARY.md deliberately does not claim that line is
already applied).

## Verification (captured)

```
$ test -f docs/research/opencode-sidebar-status-bar-viability.md && echo FOUND
FOUND

$ grep -c 'anomalyco/opencode#5971' docs/research/opencode-sidebar-status-bar-viability.md
3

$ grep -c 'anomalyco/opencode#23539' docs/research/opencode-sidebar-status-bar-viability.md
3

$ grep -ic 'not applicable' docs/research/opencode-sidebar-status-bar-viability.md
1

$ git status --short
(clean — no output, after Task 1's commit)

$ git diff --diff-filter=D --name-only HEAD~1 HEAD
(no output — no deletions)
```

Task 2's own verify command (`grep -q 'docs/research/opencode-sidebar-status-bar-viability.md'
.planning/REQUIREMENTS.md`) was NOT run against the real file — see Deviations. It was confirmed
this plan's own draft pointer text matches the report's real committed path.

## Self-Check: PASSED (Task 1) / BLOCKED (Task 2 — documented, not silently skipped)

- [x] `docs/research/opencode-sidebar-status-bar-viability.md` exists
- [x] Contains 5 required sections (current hook surface, #5971, #23539, Codex, Recommendation)
- [x] Cross-references both `anomalyco/opencode#5971` and `anomalyco/opencode#23539` by number
- [x] Codex section is exactly one "not applicable here" line
- [x] Recommendation is "wait for upstream" with a concrete re-investigation trigger
- [x] No source code file modified (`git status --short` clean after Task 1's commit)
- [x] Production commit exists (`0482721`)
- [ ] `.planning/REQUIREMENTS.md` cross-link pointer applied — BLOCKED, requires orchestrator
      action against the main checkout (exact text provided in Deviations above)

---
*Phase: 01.2-multi-cli-runtime-foundation*
*Completed: 2026-09-09*
