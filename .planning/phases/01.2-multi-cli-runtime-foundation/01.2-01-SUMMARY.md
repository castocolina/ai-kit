---
phase: 01.2-multi-cli-runtime-foundation
plan: 01
subsystem: infra
tags: [installer, git, bootstrap, single-branch, test-harness]

# Dependency graph
requires: []
provides:
  - "tools/install.sh primary BOOTSTRAP git clone invokes `git clone --single-branch --branch \"$REPO_BRANCH\" --depth 1`"
  - "tests/test_install.sh FAKEGIT harness asserts --single-branch on the captured clone argv for the default-repo and AI_KIT_REPO-fork cases"
affects: [01.2-multi-cli-runtime-foundation]

# Actuals (#2632)
actuals:
  tokens: 935
  tasks: 2
  commits: 3

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "FAKEGIT clone-argv capture: parse clone flags into locals (br/url/dir/sb) and write them to AIKIT_FETCH_MARKER so the test asserts the invoked argv, not a fetched-ref difference that --depth 1 already precludes"

key-files:
  created:
    - .planning/phases/01.2-multi-cli-runtime-foundation/01.2-01-SUMMARY.md
  modified:
    - tools/install.sh
    - tests/test_install.sh

key-decisions:
  - "The --single-branch token is explicitness/defensiveness, not a fetch-behavior bugfix: --depth 1 already implies --single-branch per git-scm.com/docs/git-clone unless --no-single-branch is given, which this script never does."

patterns-established:
  - "When a git default already implements a required property, still name the flag in argv and pin it with a string-on-invocation assertion so a future edit cannot silently drop it."

requirements-completed:
  - REQ-multi-cli-install-single-branch

coverage:
  - id: D1
    description: "Primary BOOTSTRAP git clone invokes git clone with --single-branch present verbatim, pinned by the FAKEGIT harness for the default-repo and AI_KIT_REPO-fork cases"
    requirement: REQ-multi-cli-install-single-branch
    verification:
      - kind: integration
        ref: "tests/test_install.sh#--branch drives the clone branch (default repo)"
        status: pass
      - kind: integration
        ref: "tests/test_install.sh#AI_KIT_REPO env selects a fork; --branch sets the branch"
        status: pass
    human_judgment: false
  - id: D2
    description: "Tarball-fallback extraction and already-cloned git pull --ff-only paths stay byte-identical; install.sh diff is confined to the clone-line flag and its explanatory comment"
    requirement: REQ-multi-cli-install-single-branch
    verification:
      - kind: other
        ref: "grep -q 'git -C \"$INSTALL_DIR\" pull --ff-only' tools/install.sh && grep -q 'tar xz --strip-components=1 -C \"$tmp\"' tools/install.sh"
        status: pass
      - kind: other
        ref: "git diff a326cc8^..HEAD -- tools/install.sh (clone-line flag + comment only)"
        status: pass
    human_judgment: false

# Metrics
duration: 9min
completed: 2026-09-09
status: complete
---

# Phase 1.2 Plan 01: Installer `--single-branch` Clone Summary

**`tools/install.sh` now passes `--single-branch` on the BOOTSTRAP `git clone`, pinned by the existing FAKEGIT harness so the flag cannot silently disappear.**

## Performance

- **Duration:** 9 min
- **Started:** 2026-09-09T17:46:43Z
- **Completed:** 2026-09-09T17:56:06Z
- **Tasks:** 2
- **Files modified:** 2 (plus this SUMMARY)

## Accomplishments

- Primary clone line is `git clone --single-branch --branch "$REPO_BRANCH" --depth 1 "$url" "$INSTALL_DIR"`.
- Comment above that line documents that `--depth 1` already implies `--single-branch` and that the explicit flag exists so the property does not depend on an unstated git default.
- FAKEGIT harness records a third marker field (`sb=1` when `--single-branch` is present) and both clone cases assert it.

## Task Commits

Each task was committed atomically:

1. **Task 1: Add --single-branch to the primary git clone and pin it via the FAKEGIT harness** - `a326cc8` (feat) — tests + rationale comment; clone-line flag dropped from the committed blob (see Deviations)
2. **Task 1 follow-up / Task 2: restore the clone-line flag and the full rationale comment** - `4aca0c7` (fix)

**Plan metadata:** `1b718eb` (docs: complete plan)

_Note: TDD tasks may have multiple commits (test → feat → refactor)_

## Files Created/Modified

- `tools/install.sh` — `--single-branch` on the primary clone; comment documenting the `--depth 1` implication
- `tests/test_install.sh` — FAKEGIT `sb` capture and third-field assertions for default-repo and fork cases
- `.planning/phases/01.2-multi-cli-runtime-foundation/01.2-01-SUMMARY.md` — this file

## Decisions Made

- Did not change the tarball-fallback path or `git -C "$INSTALL_DIR" pull --ff-only`. Both already act on one branch / one existing checkout.
- Did not add a second fake-git fixture. The existing section 4c harness now also captures `--single-branch`.
- Never claimed this fixes a fetch bug. `--depth 1` already implied single-branch behavior.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] First production commit omitted `--single-branch` from the clone line**
- **Found during:** Task 1 commit close-out (`git show HEAD:tools/install.sh`)
- **Issue:** `a326cc8` landed the FAKEGIT assertions and a shortened rationale comment, but the committed clone line was still `git clone --branch "$REPO_BRANCH" --depth 1 ...`. `bash tests/test_install.sh` against that blob failed the two new marker assertions (12 passed, 2 failed).
- **Fix:** Restored the full comment and the `--single-branch` token; committed as `4aca0c7` without amending (the first commit had already succeeded).
- **Files modified:** `tools/install.sh`
- **Verification:** `bash tests/test_install.sh` → 14 passed, 0 failed; `git show HEAD:tools/install.sh` contains `git clone --single-branch --branch "$REPO_BRANCH" --depth 1`
- **Committed in:** `4aca0c7`

---

**Total deviations:** 1 auto-fixed (1 bug)
**Impact on plan:** No scope change. The intended clone argv is on HEAD.

## Issues Encountered

None beyond the incomplete first commit above.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

Ready for 01.2-02 (runtime detection). Phase 1.2 ROADMAP Success Criterion 1 is true for this plan's scope: the primary git path fetches the target branch via an explicit `--single-branch` flag; tarball-fallback and `git pull --ff-only` stay byte-identical.

## Verification (captured)

```
$ bash tests/test_install.sh
ok   - LOCAL skip-fetch execs setup.py with args
ok   - LOCAL clone resolves+exports INSTALL_DIR from script location (no AI_KIT_DIR)
ok   - --doctor maps to the doctor subcommand
ok   - --check maps to the check subcommand
ok   - --reconfigure --dry-run maps to 'reconfigure --dry-run'
ok   - errors out (non-zero) when python3 is unavailable
ok   - atomic swap removes orphan files
ok   - atomic swap keeps new content
ok   - --branch (space form) consumed, not forwarded to setup.py
ok   - --branch= (equals form) consumed, not forwarded to setup.py
ok   - --branch drives the clone branch (default repo)
ok   - fetched setup.py runs with --branch stripped
ok   - AI_KIT_REPO env selects a fork; --branch sets the branch
ok   - shellcheck clean

14 passed, 0 failed

$ grep -q 'git -C "\$INSTALL_DIR" pull --ff-only' tools/install.sh && echo PASS
PASS: git pull --ff-only present

$ grep -q 'tar xz --strip-components=1 -C "\$tmp"' tools/install.sh && echo PASS
PASS: tarball strip-components present
```

TDD RED (tests first, production clone line unchanged): the two new marker assertions failed as expected (`FAIL - --branch drives the clone branch (default repo)` / `FAIL - AI_KIT_REPO env selects a fork; --branch sets the branch`; 12 passed, 2 failed). GREEN after adding `--single-branch`: 14 passed, 0 failed.

Production commits: `a326cc83d6971f59ba1a5a5070b04242e30a582a`, `4aca0c719742fd18b4e4e07341c3ff34b40c8e52`.

## Self-Check: PASSED

- [x] `tools/install.sh` clone line contains `--single-branch`
- [x] `tests/test_install.sh` FAKEGIT harness asserts `sb` field `1` for both clone cases
- [x] `bash tests/test_install.sh` reports 0 failures (14 passed)
- [x] `git -C "$INSTALL_DIR" pull --ff-only` still present
- [x] `tar xz --strip-components=1 -C "$tmp"` still present
- [x] Production commits exist (`a326cc8`, `4aca0c7`)

---
*Phase: 01.2-multi-cli-runtime-foundation*
*Completed: 2026-09-09*
