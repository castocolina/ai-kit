---
phase: 01.2-multi-cli-runtime-foundation
plan: 04
type: execute
wave: 2
depends_on:
  - 01.2-02
files_modified:
  - skills/ai-kit-spec-review/ai_kit_spec/cli.py
  - skills/ai-kit-spec-config/SKILL.md
  - tests/test_ai_kit_spec.py
  - .planning/ROADMAP.md
  - .planning/phases/01.2-multi-cli-runtime-foundation/01.2-CONTEXT.md
autonomous: true
cross_ai: true
requirements:
  - REQ-multi-cli-runtime-detection

estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "ai-kit-spec-config/SKILL.md's Step 2.7 native-entry ask computes native_runtime via a new `detect-current-runtime` CLI subcommand and shows it inline as `(running under: <value>)` next to the native candidate's prompt, per D-08's exact example wording."
    - "Step 3's write description documents that native_runtime is persisted onto the native [[reviewers]] entry (never on a CLI-sourced entry), mirroring how purpose is already carried forward from Step 2.7 into Step 3's write."
    - "ai-kit-spec-config/SKILL.md passes the skill-judge review loop (no remaining Critical/Important finding, or a recorded score >= 96/120 with no regression vs. baseline) after this change, per AGENTS.md's skill quality gate and REQ-multi-cli-runtime-detection's own acceptance criterion."
    - "`.planning/ROADMAP.md`'s Phase 1.2 Success Criterion 3 and `.planning/phases/01.2-multi-cli-runtime-foundation/01.2-CONTEXT.md`'s D-08 each carry a dated (2026-09-09) amendment note recording that 'ranked candidate output' resolves to Step 2.7's native-entry ask, not Step 2.2's ranked CLI-candidate block (which never contains a native row) — mirroring 01.2-02-PLAN.md's own dated SC2 amendment precedent, per AGENTS.md's Workflow rule."
    - "When `detect-current-runtime` returns `\"unknown\"`, Step 2.7's inline annotation still renders literally as `(running under: unknown)` — never omitted, never reworded to something softer — per D-04's never-silently-omitted rule, applied explicitly to display, not just persistence."
    - "Step 2.7's host-identity prose (previously an unconditional 'Today that host is Claude Code...') is reworded to defer to the live `(running under: <value>)` annotation, so it never contradicts a non-claude detected value once this plan's annotation ships."
  artifacts:
    - skills/ai-kit-spec-config/SKILL.md
    - skills/ai-kit-spec-review/ai_kit_spec/cli.py
  key_links:
    - "cli.py's new detect-current-runtime subcommand -> ai-kit-spec-config/SKILL.md Step 2.7's `python3 \"$TOOLS_PY\" detect-current-runtime` call"
    - "Step 2.7's captured native_runtime value -> Step 3's written [[reviewers]] entry's native_runtime field, validated by config_io.py's validate_reviewer_fields (01.2-02)"
---

<objective>
Wire `detect_current_runtime()` (built in 01.2-02) into `ai-kit-spec-config`'s wizard so the
native-entry flow actually surfaces and persists `native_runtime`, per D-08's inline-annotation
requirement, then pass the `skill-judge` quality gate REQ-multi-cli-runtime-detection's own
acceptance criteria require. This is the last piece making the D-08/Success-Criterion-3 half of
REQ-multi-cli-runtime-detection functionally reachable — 01.2-02 built the validated schema and
the detection function; this plan is the thin CLI-subcommand + SKILL.md wiring connecting them
end-to-end.

**SC3/D-08 wording amendment (per this plan's own cross-AI review, 01.2-REVIEWS.md):**
ROADMAP.md's Success Criterion 3 and CONTEXT.md's D-08 both say the wizard's "ranked candidate
output" gets the inline `native_runtime` annotation. But a native entry never goes through Step
2.2's ranked CLI-candidate flow — `SKILL.md` states this plainly — so there is no ranked native
row to annotate there. This plan integrates the annotation into Step 2.7's native-entry ask
instead (the integration point 01.2-RESEARCH.md's Open Question 2 recommended), and formally
amends both files with a dated note recording that resolution, mirroring 01.2-02-PLAN.md's own
dated SC2 amendment for the analogous `cli: None` wording gap.

Purpose: give the wizard's native-entry flow a real, live-detected runtime label instead of a
schema no code path ever populates.
Output: a new `detect-current-runtime` CLI subcommand; `ai-kit-spec-config/SKILL.md`'s Step 2.7/
Step 3 updated to compute, display, and persist `native_runtime`; a passing skill-judge review.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01.2-multi-cli-runtime-foundation/01.2-CONTEXT.md
@.planning/phases/01.2-multi-cli-runtime-foundation/01.2-02-SUMMARY.md
@skills/ai-kit-spec-config/SKILL.md
@skills/ai-kit-spec-review/ai_kit_spec/cli.py
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Add the detect-current-runtime CLI subcommand and wire it into Step 2.7's native-entry ask (D-08)</name>
  <files>skills/ai-kit-spec-review/ai_kit_spec/cli.py, skills/ai-kit-spec-config/SKILL.md, tests/test_ai_kit_spec.py, .planning/ROADMAP.md, .planning/phases/01.2-multi-cli-runtime-foundation/01.2-CONTEXT.md</files>
  <action>
  In `cli.py`, add `detect_current_runtime` to the existing
  `from ai_kit_spec.detection import (...)` import block (alphabetically, between
  `cache_runtimes_path` and `detect_tool_availability`). Register a new
  `sub.add_parser("detect-current-runtime")` (zero arguments — mirrors `detect-tools`'s
  zero-argument shape), placed next to the existing `detect-tools`/`detect-runtimes` parsers.
  Add its handler, placed next to the existing `detect-tools`/`detect-runtimes` handlers,
  printing `json.dumps({"native_runtime": detect_current_runtime(env_fn())})` and returning 0 —
  same `env_fn()` injection convention `main()` already uses elsewhere in this file (no
  caching/TTL wrapper, per D-03; this call is cheap and always live).

  Add a `test_detect_current_runtime_prints_json` method to `TestMainCli` in
  `tests/test_ai_kit_spec.py`, mirroring `test_detect_runtimes_prints_json_snapshot`'s shape,
  but calling `rs.main(["detect-current-runtime"], env_fn=lambda: {"CLAUDECODE": "1"})` and
  asserting the captured stdout parses to `{"native_runtime": "claude"}`. Add a second method,
  `test_detect_current_runtime_prints_json_for_unknown`, calling
  `rs.main(["detect-current-runtime"], env_fn=lambda: {})` (no matching signal present) and
  asserting the captured stdout parses to the literal `{"native_runtime": "unknown"}` — this
  pins the "unknown" case at the subcommand's own JSON boundary, one layer below where Step
  2.7's prose renders it as `(running under: unknown)`.

  Then edit `skills/ai-kit-spec-config/SKILL.md`'s Step 2.7 (the native-entry ask, the section
  beginning "Also ask, explicitly -- do not skip this: whether to register one or more
  **native**..."): immediately before the ask, add an instruction to run
  `python3 "$TOOLS_PY" detect-current-runtime` and capture its `native_runtime` value. Phrase
  the native-entry prompt to include the inline annotation `(running under: <native_runtime>)`
  immediately after naming the current host runtime, matching D-08's exact example style
  verbatim — e.g. "...dispatched via whatever runtime is hosting *this* session (running under:
  claude), not any one specific model or vendor..." Add one sentence stating that
  `native_runtime` (including the literal `"unknown"` value) is captured for this entry and
  carried forward into Step 3's write, exactly the same way `purpose` already is per this same
  Step's existing final paragraph (D-04: never silently omitted). State explicitly, as its own
  sentence, that when `detect-current-runtime` returns `"unknown"` the annotation still renders
  literally as `(running under: unknown)` — never omitted, never softened to something like "not
  detected" or dropped from the prompt entirely — extending D-04's never-silently-omitted rule
  from persistence (already covered) to display.

  Also revise this Step's existing "Today that host is Claude Code, whose own dispatch mechanism
  is the `Agent` tool's four tier aliases (`sonnet`/`opus`/`haiku`/`fable`)" sentence, which
  currently asserts Claude Code as an unconditional fact. Now that this Step computes and
  displays the live-detected `native_runtime` value via the `(running under: <value>)`
  annotation added above, that unconditional claim would read as self-contradictory the moment
  the annotation shows a non-claude value (e.g. `(running under: opencode)`). Reword it to defer
  to the live-detected value instead of asserting a fixed host — e.g. "The `(running under:
  <native_runtime>)` annotation above names the actual host for this run. When that host is
  Claude Code, its own dispatch mechanism is the `Agent` tool's four tier aliases
  (`sonnet`/`opus`/`haiku`/`fable`) — a different host substitutes its own alias/selection
  mechanism here." Apply the same host-conditional framing to the later "For each native entry
  the user wants **on this Claude-Code-hosted run**, `model` must be one of the four `Agent`-tool
  aliases" sentence: make the alias-list instruction explicitly conditional on the detected
  `native_runtime` actually being `"claude"` (the existing parenthetical "A different host
  runtime would substitute its own alias set here" already hedges this — promote it from an
  afterthought to the operative rule the mandatory-aliases sentence is gated on).

  Finally, edit Step 3's write description (the bullet describing what each `reviewers` object
  in the JSON shape carries: currently `purpose`, then `is_router`/`fallback_quota`) to add
  `native_runtime` — sourced from Step 2.7's `detect-current-runtime` call, set ONLY on native
  entries (never on a CLI-sourced entry from Step 2.2), always written as an explicit string
  including `"unknown"`, never omitted.

  Then formally amend two files with a dated note (2026-09-09), per AGENTS.md's own Workflow
  rule and mirroring 01.2-02-PLAN.md's own dated-amendment task for SC2 — this plan's own review
  (01.2-REVIEWS.md) found that SC3/D-08's literal "ranked candidate output" wording was never
  amended even though this plan integrates at Step 2.7, where no ranked native row exists
  (`SKILL.md` states plainly that "a native entry never goes through Step 2.2's catalog/ranking
  flow"):
  - `.planning/ROADMAP.md`'s Phase 1.2 Success Criterion 3 line — append a dated
    (2026-09-09) note recording that "ranked candidate output" resolves to Step 2.7's
    native-entry ask, not Step 2.2's ranked CLI-candidate block, because no native row exists
    there to annotate.
  - `.planning/phases/01.2-multi-cli-runtime-foundation/01.2-CONTEXT.md`'s D-08 bullet — append
    the same dated note under a new "### Amendment (2026-09-09, during Phase 1.2 cross-AI plan
    review)" heading, matching the existing D-09 amendment heading's style in the same file.
  Append short notes; do not rewrite the original wording out of either file.
  </action>
  <verify>
    <automated>python3 -m unittest tests.test_ai_kit_spec.TestMainCli.test_detect_current_runtime_prints_json tests.test_ai_kit_spec.TestMainCli.test_detect_current_runtime_prints_json_for_unknown -v</automated>
  </verify>
  <done>
  `python3 "$TOOLS_PY" detect-current-runtime` prints `{"native_runtime": "<value>"}`, including
  the literal `{"native_runtime": "unknown"}` when no signal matches; Step 2.7 of
  `ai-kit-spec-config/SKILL.md` shows the `(running under: <value>)` inline annotation next to
  the native entry's ask (rendering `(running under: unknown)` literally, never omitted, in the
  unknown case), matching D-08's example wording; Step 2.7's host-identity prose defers to that
  live-detected value instead of unconditionally asserting Claude Code; Step 3's write
  description documents `native_runtime`'s persistence on native entries only;
  `.planning/ROADMAP.md` SC3 and `01.2-CONTEXT.md` D-08 each carry a dated 2026-09-09 amendment
  note resolving "ranked candidate output" to Step 2.7's native-entry ask.
  </done>
</task>

<task type="auto">
  <name>Task 2: Run the skill-judge review loop against ai-kit-spec-config/SKILL.md</name>
  <files>skills/ai-kit-spec-config/SKILL.md</files>
  <action>
  Invoke the `skill-judge` skill against the updated `skills/ai-kit-spec-config/SKILL.md`,
  recording its baseline (pre-Task-1) score if one is available from a prior run, and the
  post-change score. Address every remaining Critical/Important finding directly in `SKILL.md`
  and re-run `skill-judge` until either no Critical/Important finding remains, or the score
  reaches >= 96/120 with no regression versus baseline — per AGENTS.md's Non-Negotiable Rules
  skill quality gate ("Any new or materially updated SKILL.md passes the skill-judge review loop
  ... before being marked complete") and REQ-multi-cli-runtime-detection's own acceptance
  criterion ("ai-kit-spec-config/SKILL.md documents the labeling and passes skill-judge review").
  Record the final pre/post scores and finding counts in this task's commit message.
  </action>
  <verify>
    <automated>N/A — skill-judge is an LLM-driven review skill, not a scriptable command; the gate is the recorded score/finding-count in this task's commit message, matching AGENTS.md's own skill-quality-gate definition and 01.2-RESEARCH.md's Validation Architecture table, which classifies this exact check as "manual (skill-judge review loop) ... not an automated test."</automated>
  </verify>
  <done>
  `skill-judge` shows zero remaining Critical/Important findings against
  `ai-kit-spec-config/SKILL.md` (or a recorded score >= 96/120 with no regression vs. its
  pre-change baseline); the pre/post scores are recorded in the commit message.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Process environment (via `detect_current_runtime()`) → CLI subcommand stdout → wizard-displayed prompt text → written `review-spec.toml` entry | An env-var-sourced value flows through a new CLI subcommand into user-facing wizard text and, on confirmation, into a persisted config file. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01.2-05 | Spoofing | `ai-kit-spec-config/SKILL.md` Step 2.7 native_runtime display + `cli.py`'s `detect-current-runtime` subcommand | low | accept | Same informational-only constraint as 01.2-02's T-01.2-02 — a spoofed env var only changes a displayed label and, at most, the value written to `native_runtime`, never which entry gets written, its `model`/`vendor`/`cli`/`command` fields, or how it's dispatched (D-05). |
| T-01.2-06 | Tampering | `cli.py`'s `detect-current-runtime` output, consumed by `SKILL.md` and written into `review-spec.toml` | low | mitigate | The printed value is validated by `config_io.py`'s `validate_reviewer_fields` (01.2-02, `_VALID_NATIVE_RUNTIME`) before `cfg_render_toml` ever writes it — an unexpected value would be rejected-and-reported (a stderr `WARNING:` line, Step 4 of the wizard already surfaces these), never silently written, per `cfg_render_toml`'s existing per-entry rejection contract. |

</threat_model>

<verification>
`python3 -m unittest tests.test_ai_kit_spec.TestMainCli.test_detect_current_runtime_prints_json
tests.test_ai_kit_spec.TestMainCli.test_detect_current_runtime_prints_json_for_unknown -v`
passes. `skill-judge` run against `ai-kit-spec-config/SKILL.md` shows zero remaining Critical/
Important findings (or score >= 96/120, no regression). Full suite (`make test`) stays green.
</verification>

<success_criteria>
Phase 1.2 ROADMAP Success Criterion 3 is true, per this plan's own dated amendment resolving
"ranked candidate output" to Step 2.7's native-entry ask (the block Step 2.2's ranked CLI-candidate
output never contains a native row for): `ai-kit-spec-config`'s wizard visibly labels
`native_runtime` — including the literal `unknown` case — next to the native-entry ask, and the
change passes `skill-judge` review.
</success_criteria>

<output>
Create `.planning/phases/01.2-multi-cli-runtime-foundation/01.2-04-SUMMARY.md` when done
</output>
