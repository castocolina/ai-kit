---
phase: 01.2
reviewers: [opencode]
reviewed_at: 2026-09-09T18:12:00Z
plans_reviewed: [01.2-01-PLAN.md, 01.2-02-PLAN.md, 01.2-03-PLAN.md, 01.2-04-PLAN.md]
models:
  opencode: "router-env/my-plan-review (reasoning=high)"
model_sources:
  opencode: "pinned"
---

# Cross-AI Plan Review — Phase 01.2

<!-- gsd:plan-revision-conflicts:begin -->
## Plan-Revision Conflicts
(none — this is a convergence cycle 2 re-review of plans 01.2-02 and 01.2-04, revised by commit `9a0c143` to address cycle 1's 6 actionable findings; plans 01.2-01 and 01.2-03 were untouched)
<!-- gsd:plan-revision-conflicts:end -->

## OpenCode Review

I'll verify these plans against the live repo. Let me gather the actual source files first.
Now let me read the test harness, validators, and SKILL.md sections.
Verified the harness and validators. Now the SKILL.md, cli.py imports, tests, and quota/dispatch paths.
SKILL.md Step 2.7 and cli.py verified. Now the test file, quota/execute_dispatch, and planning docs.
All source claims verified. Now let me pin down line numbers in the two revised plan files for the cycle-1 fix citations.
All evidence gathered. Here is the cycle-2 review.

---

# Phase 1.2 Plan Review — Cycle 2 (Convergence Verification)

All 4 plans re-verified against live source this session (`tools/install.sh`, `tests/test_install.sh`, `ai_kit_spec/detection.py`/`model_catalog.py`/`config_io.py`/`cli.py`/`quota.py`/`execute_dispatch.py`, `tests/test_ai_kit_spec.py`, `skills/ai-kit-spec-config/SKILL.md`, `AGENTS.md`, `.planning/ROADMAP.md`, `.planning/REQUIREMENTS.md`). Line numbers below are current on-disk lines, re-read this cycle — not cycle-1 citations carried forward.

## 01.2-01

**Summary:** Unchanged since cycle 1 (correctly not touched by commit `9a0c143`). Re-verified against live source: every code-level claim is accurate, and the plan honestly frames the change as behavior-neutral explicitness rather than a bug fix.

**Strengths:**
- The edit target is real and exactly as described: primary clone at `tools/install.sh:103` (`git clone --branch "$REPO_BRANCH" --depth 1 ...`), tarball fallback at `tools/install.sh:113/115`, `git pull --ff-only` at `tools/install.sh:100` — all three paths confirmed byte-for-byte as the plan describes them.
- The FAKEGIT harness extension is grounded: `tests/test_install.sh:112-130` is exactly the `case`-statement fake git the plan says to extend (`br`/`url`/`dir` parsing at lines 115-124, marker write `printf '%s %s\n' "$url" "$br"` at line 125), and the two existing assertions to update are at `tests/test_install.sh:141-142` (default repo) and `tests/test_install.sh:150-151` (`AI_KIT_REPO` fork). Adding `--single-branch) sb=1 ;;` needs no extra `shift` — the loop already shifts at line 123 — so the suggested arm is mechanically correct.
- Task 2's verify greps (`'git -C "$INSTALL_DIR" pull --ff-only'`, `'tar xz --strip-components=1 -C "$tmp"'`) are single-quoted literals that match the live lines verbatim; the diff-scope discipline is real, not aspirational.

**Concerns:** None new. The flag-presence (not ref-count) test design remains correct given `--depth 1` already implies `--single-branch`.

**Suggestions:** None.

**Risk Assessment:** **LOW** — a one-token shell change pinned by an existing, verified test harness.

## 01.2-02

**Summary:** The detection-core + schema plan, revised to add machine-enforced `native_runtime`/`cli` mutual exclusion, a single-invocation probe harness, and an empty-string-signal contract. All three cycle-1 findings are functionally resolved; the Open Question 1 resolution (both files, honestly tiered) remains the strongest part of the plan.

**Cycle-1 finding verdicts:**

- **MEDIUM — SC2 "never set on entries with a real `cli` value" not enforced → RESOLVED.**
  - Plan `01.2-02-PLAN.md:31` (must_have) and the Task 2 `<action>` add a real cross-field check to **both** validators: `entry.get("cli") is not None` + `native_runtime` present → rejection reason. The check is implementable exactly as written — traced both insertion points in live code: `validate_reviewer_fields` at `config_io.py:99-117` (where `cli` is already a known field, `_KNOWN_REVIEWER_FIELDS` at `config_io.py:13`) and `validate_catalog_entry` at `model_catalog.py:52` (where `entry.get("cli")` reads the raw dict, so it works even though `cli` is not in `_FIELD_TYPES` at `model_catalog.py:18-32` — confirmed, `cli` appears nowhere in that table).
  - The enforcement mechanism is real, not prose: `cfg_render_toml` drops a rejected entry at `config_io.py:136-143` and emits the stderr `WARNING:` at `config_io.py:180-183` — so a hand-edited `review-spec.toml` with both fields genuinely cannot be written.
  - Test coverage is specified with concrete method names in the correct existing classes: `test_native_runtime_rejected_when_cli_is_present` in `TestValidateCatalogEntry` (class confirmed at `tests/test_ai_kit_spec.py:371`, with the `name_declared_purpose` mirror tests at 432/448), and `test_render_toml_rejects_native_runtime_with_non_null_cli` / `test_render_toml_keeps_native_runtime_when_cli_is_absent` in `TestRenderAndWriteToml` (class at `tests/test_ai_kit_spec.py:219`, purpose-mirror tests at 287/293/301 — all three referenced test names exist exactly as cited).
  - The positive case (`native_runtime` + `cli` absent → kept) is also covered, which is what makes this a real invariant rather than a blanket rejection.

- **MEDIUM-LOW — Task 3 verify double-ran each probe / 90s timeout → RESOLVED.**
  - `01.2-02-PLAN.md:317` is now a single `subprocess.run` per command (walrus-bound `r`, both `.stdout[:300]` and `.stderr[:300]` read off the same result) with `timeout=180` — matching the commit-message claim of 90s→180s. `01.2-02-PLAN.md:311` additionally instructs the executor in prose to never re-invoke a CLI to check the second stream, with the right rationale (blocked runs are exactly where quota/rate-limit budget matters). Cycle 1's suggested rewrite is implemented in substance, with a longer timeout as it suggested.

- **LOW — no empty-string signal behavior case → RESOLVED.**
  - Behavior case at `01.2-02-PLAN.md:105-106` (`CLAUDECODE=""` → `"unknown"`), docstring contract mandated at `01.2-02-PLAN.md:138`, must_have at line 28, and the `<done>` gate at lines 154-156 all pin it. The plan also correctly mandates truthiness (`if env.get(var):`) over bare key-presence, and documents it as a deliberate contract rather than an accident.

**Strengths:**
- The `detection.py` claims all check out: `KNOWN_CLIS` with `"cursor-agent"` at `detection.py:13`, `detect_installed_clis` at `detection.py:20`, and the `env=None` injectable convention via `resolve_agents_tooling_path(env=None)` at `detection.py:113` — the sibling-function shape is exactly as described.
- The test-wiring claim ("this file already imports `detection` bare... no edit to the `rs` shim's name list is needed") is verified: `tests/test_ai_kit_spec.py:21` (`from ai_kit_spec import (...)`) binds the `detection` module, and the module-qualified convention is already in live use at `tests/test_ai_kit_spec.py:826` (`detection.detect_tool_availability(...)`). The rs shim at lines 99-117 indeed needs no edit for a module-qualified call.
- The `quota.py` claim is verified: `extra={k: v for k, v in entry.items() if k not in _KNOWN_REVIEWER_FIELDS}` at `quota.py:48` — `native_runtime` lands in `extra` automatically if kept out of `_KNOWN_REVIEWER_FIELDS`, exactly as the plan states.
- The amendment targets exist: AGENTS.md's `cli: None` architecture bullet at `AGENTS.md:61-63`, ROADMAP SC2 at `.planning/ROADMAP.md:70`, REQUIREMENTS at `.planning/REQUIREMENTS.md:20`. The type-check-before-membership ordering claim is verified against the live `purpose` precedent at `config_io.py:106-113`.

**Concerns:**
- **LOW (new, wording)** — Task 2's model_catalog behavior bullet says "a hand-crafted entry dict containing an unexpected `cli` key must not slip past validation just because the schema doesn't name that field." As specified, the cross-field check only rejects `cli` when `native_runtime` is co-present; a bare `cli` key with no `native_runtime` still passes — which is correct (it preserves the schema's documented permissive-unrecognized-field contract, `model_catalog.py:15-17`), but the sentence could mislead an executor into rejecting `cli` keys outright. Suggest tightening to "...must not slip past *when combined with `native_runtime`*..."
- **LOW (accepted residual)** — Task 3's verify still asserts nothing (prints only). Cycle 1 named this; the revision deliberately keeps it as a diagnostic with honest recording (real command + real outcome in the commit message and SUMMARY, per the `<done>` criteria) plus a `<human-check>` for later re-verification. Given a blocked probe is a legitimate outcome, this is defensible as-is.

**Suggestions:**
- Apply the wording tightening above to the Task 2 behavior bullet (one clause, no structural change).

**Risk Assessment:** **LOW** — all three cycle-1 findings are functionally resolved with traced, testable mechanisms; the remaining items are a wording nit and a consciously-accepted diagnostic-only smoke test.

## 01.2-03

**Summary:** Unchanged since cycle 1 (correctly not touched). Re-verified: documentation-only deliverable, requirements coverage confirmed, no code surface.

**Strengths:**
- The requirement line it satisfies exists verbatim at `.planning/REQUIREMENTS.md:21` (cross-reference `anomalyco/opencode#5971`, explicit recommendation, no code changes); ROADMAP SC4 at `.planning/ROADMAP.md` §Phase 1.2 names the same deliverable.
- The content source (01.2-RESEARCH.md's opencode plugin-hooks section, both issues fetched directly this research cycle) is genuinely complete — the plan is synthesis, not discovery, and says so.
- The verify commands are mechanically sound (`grep -c`/`grep -ic` exit non-zero only on zero matches), and Task 2's diff-scope check ("no `.py`/`.sh`/skill `.md` in the diff") is a real, checkable constraint.

**Concerns:** None new. The deliberate extension beyond D-06's single mandatory cross-reference (adding #23539) remains well-justified by the phase goal's own "sidebar/status-bar" wording.

**Suggestions:** None.

**Risk Assessment:** **LOW** — a markdown report with no runtime surface.

## 01.2-04

**Summary:** The wizard-wiring plan, revised to add the dated SC3/D-08 amendment, host-conditional prose, and a literal-`unknown` display contract. All three cycle-1 findings are resolved with mechanisms traced into the live SKILL.md and cli.py; the plan is now internally consistent end-to-end.

**Cycle-1 finding verdicts:**

- **MEDIUM — SC3/D-08 "ranked candidate output" wording unamended → RESOLVED.**
  - `01.2-04-PLAN.md:30` (must_have) and the Task 1 action (lines 145-157) add a dated 2026-09-09 amendment to both `.planning/ROADMAP.md`'s SC3 (live wording confirmed at `.planning/ROADMAP.md:71`: "visibly labels `native_runtime` in its ranked candidate output") and `01.2-CONTEXT.md`'s D-08, using a new "### Amendment (2026-09-09, ...)" heading matching the existing D-09 amendment heading style in that file. The `<done>` (line 170) and `<success_criteria>` (line 225) gates both require it, so it can't be silently skipped at execution time.
  - The factual basis is live-verified: "A native entry never goes through Step 2.2's catalog/ranking flow" appears verbatim at `skills/ai-kit-spec-config/SKILL.md:520` — there is genuinely no native row in the ranked block to annotate, so Step 2.7 is the only defensible integration point and the amendment records exactly that.

- **MEDIUM-LOW — "Today that host is Claude Code..." self-contradiction → RESOLVED.**
  - `01.2-04-PLAN.md:32` (must_have) and the Task 1 action (lines 120-130) reword the sentence, confirmed to exist verbatim at `skills/ai-kit-spec-config/SKILL.md:516`, to defer to the live `(running under: <value>)` annotation. Crucially, the revision also covers the **second** hardcoded sentence cycle 1 flagged — "For each native entry the user wants **on this Claude-Code-hosted run**, `model` must be one of the four `Agent`-tool aliases" at `SKILL.md:518` — by promoting its existing parenthetical hedge ("A different host runtime would substitute its own alias set here", same line) to the operative rule the mandatory-alias instruction is gated on. Both contradicting sentences are addressed, not just the first.

- **LOW — `unknown` display behavior unspecified → RESOLVED.**
  - `01.2-04-PLAN.md:31` (must_have) and the Task 1 action (line 116) mandate the literal `(running under: unknown)` rendering — "never omitted, never softened" — and extend D-04's never-silently-omitted rule explicitly from persistence to display. It is also **tested** at the layer below the prose: `test_detect_current_runtime_prints_json_for_unknown` (plan lines 98-102, run in the verify at line 159) pins `{"native_runtime": "unknown"}` at the subcommand's JSON boundary via `rs.main(["detect-current-runtime"], env_fn=lambda: {})`, mirroring the verified `test_detect_runtimes_prints_json_snapshot` shape at `tests/test_ai_kit_spec.py:1743-1753` (`TestMainCli` confirmed at line 1742; `main` is in the rs shim at line 114).

**Strengths:**
- Every cli.py claim verified against live source: the import block at `skills/ai-kit-spec-review/ai_kit_spec/cli.py:22-29` does run alphabetically `cache_runtimes_path` (line 25) → `detect_tool_availability` (line 26), so the prescribed `detect_current_runtime` placement is exact; `detect-tools`/`detect-runtimes` parsers exist at `cli.py:488-500` and handlers at `cli.py:700-713`; the `env_fn=lambda: dict(os.environ)` convention is real in `main()`'s signature at `cli.py:477`.
- The Step 3 edit point is verified: the write description carrying `purpose` → `is_router`/`fallback_quota` exists at `SKILL.md:541-558`, and `python3 "$TOOLS_PY"` is the file's established invocation pattern (`SKILL.md:500, 561`).
- Wave/dependency ordering is correct (`wave: 2`, `depends_on: [01.2-02]`) — the wiring consumes the schema and detection function the earlier plan builds, and its threat model correctly relies on `validate_reviewer_fields` (built in 01.2-02) as the write-side mitigation.
- The skill-judge gate is honestly classified as manual with the AGENTS.md-mandated pass condition (no Critical/Important, or ≥ 96/120, no regression) — no fake automatability claim.

**Concerns:**
- **LOW (accepted residual)** — the prose-level rendering of `(running under: unknown)` inside SKILL.md instructions is inherently not machine-testable; the plan compensates by pinning the value at the JSON boundary and gating the prose via skill-judge. Nothing more is achievable for a markdown skill file; noted, not actionable.
- **LOW (new, minor)** — the objective cites "01.2-REVIEWS.md" as the source of the SC3/D-08 finding; that file exists and contains exactly those findings, so the citation is accurate — no issue, just confirming the revision didn't invent a reference.

**Suggestions:** None required.

**Risk Assessment:** **LOW** — all three cycle-1 findings resolved with traced mechanisms; the remaining residuals are inherent to markdown-skill testing and are mitigated by design.

---

## Cross-Plan Assessment

- **Coverage:** SC1→01, SC2→02, SC3→04, SC4→03; all three REQ IDs mapped; no orphaned success criteria. The two amendments (SC2 in plan 02, SC3 in plan 04) target disjoint files/lines (`ROADMAP.md:70` vs `:71`, plus plan 04's CONTEXT.md D-08 edit), so the two wave-1/wave-2 plans cannot conflict on the amendment writes.
- **Consistency between the revisions:** plan 02's must_have (line 30) and plan 04's key_links agree on the attachment resolution (config_io.py = functionally reachable path, model_catalog.py = forward-compatible only) — the two plans now tell one coherent story about where `native_runtime` lives.
- **No new issues introduced by the revision** beyond the one LOW wording nit on plan 02's model_catalog behavior bullet; no scope creep (the AGENTS.md/REQUIREMENTS/ROADMAP edits are mandated by AGENTS.md's own Workflow rule, and the plan cites it).

## Overall Risk Assessment

**LOW.** All six cycle-1 findings are **RESOLVED** — each with a mechanism traced into live source (validators at `config_io.py:99-117`/`model_catalog.py:52`, drop-and-warn at `config_io.py:136-143`+`180-183`, the single-run probe harness, the amendment targets at `ROADMAP.md:70-71` and `SKILL.md:516/518/520`, the subcommand test boundary). Every file, line, class, and test-name claim checked across all four plans was accurate against the current codebase — including claims re-verified fresh rather than inherited from cycle 1. The plans are ready for execution; the single optional pre-execution edit is the one-clause wording tightening on plan 02's Task 2 model_catalog behavior bullet.

---

## Consensus Summary

Only one reviewer lane (OpenCode, `router-env/my-plan-review` at high reasoning) was available for this cycle. The primary route (`opencode run --model router-env/my-plan-review`) was probed live before the run and answered — no fallback was needed.

The reviewer re-verified every plan against live source (not just plan text or cycle-1 citations) and covered all 4 plan ids (01.2-01 through 01.2-04), explicitly grading each of the 6 cycle-1 findings as RESOLVED/PARTIALLY RESOLVED/NOT RESOLVED with `path:line` evidence, satisfying the mandatory plan-coverage requirement and this cycle's convergence-verification brief.

### Agreed Strengths
(single-reviewer pass — no cross-reviewer agreement to report; see per-plan Strengths sections above)

### Agreed Concerns
(single-reviewer pass — no cross-reviewer agreement to report. All 6 cycle-1 findings are graded RESOLVED. Two new LOW-severity notes were raised: a wording nit on plan 02's Task 2 model_catalog behavior bullet, and a confirmatory (non-actionable) note on plan 04's citation accuracy — see per-plan Concerns sections above.)

### Divergent Views
(single-reviewer pass — not applicable)
