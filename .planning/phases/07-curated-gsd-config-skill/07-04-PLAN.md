---
phase: 07-curated-gsd-config-skill
plan: 04
type: execute
wave: 4
depends_on: ["07-03"]
cross_ai: true
autonomous: true
requirements: [REQ-cfg-skill-pipeline]
files_modified:
  - skills/ai-kit-gsd-config/SKILL.md
  - skills/ai-kit-gsd-config/ai-kit-gsd-config.py
  - skills/ai-kit-gsd-config/ai_kit_gsd_config/*.py
  - tests/test_ai_kit_gsd_config.py
  - Makefile
  - .pre-commit-config.yaml
  - pyproject.toml
  - README.md

estimate:
  tokens: 58000
  raw_tokens: 58000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "SKILL.md is actually scaffolded via `/superpowers:writing-skills` (not hand-authored from scratch), and its NEVER section explicitly states it does not replace or wrap `gsd-settings`/`gsd-config` (D-10) — both remain the deep-cut entry points this skill intentionally does not duplicate."
    - "`/skill-judge` run against the finished SKILL.md reports zero remaining Critical or Important findings, or a score at or above 96/120 with no regression against the first run."
    - "`/naming-analyzer` is actually invoked (not skipped/assumed) against the finished SKILL.md; whatever name it recommends — whether or not it differs from the provisional `ai-kit-gsd-config` — becomes the skill's real directory, package, entrypoint, and `SKILL.md` `name:` field, with the decision recorded either way (ROADMAP SC4)."
    - "Every one of the four registration surfaces (`Makefile` `test:`/`lint:`, `.pre-commit-config.yaml`'s `ruff`/`py-compile`/`unittest` hooks, `pyproject.toml`'s `[tool.pyright]`/`[tool.vulture]` paths) is registered under the FINAL post-naming-analyzer name — this is the skill's first-ever registration in these four files, not a rename of an earlier provisional entry, since no prior plan in this phase touched any of them."
    - "`make test`, `make lint`, `make validate`, and `make e2e-docker` are all green AFTER registration/rename, proving the new skill is fully wired into this repo's real quality gate, not just internally self-consistent."
    - "README.md's Contents table gains exactly one new row for the final skill name, linking to its `SKILL.md`."
  artifacts:
    - skills/ai-kit-gsd-config/SKILL.md (pre-rename path; final path is whatever /naming-analyzer chooses)
  key_links:
    - "`Makefile`'s `test:`/`lint:` targets, `.pre-commit-config.yaml`'s three per-file-pattern hooks, and `pyproject.toml`'s two path lists all reference the SAME final skill directory name — `make validate` passing after registration is the proof that all four were updated consistently, not just the obviously-visible ones."
---

<objective>
Close the REQ-cfg-skill-pipeline loop: scaffold the skill's `SKILL.md` via
`/superpowers:writing-skills`, run `/skill-judge` and fix every Critical/
Important finding, then run `/naming-analyzer` to pick the skill's real,
final name, rename accordingly if it differs from the provisional
`ai-kit-gsd-config`, register the skill in all four local quality-gate
surfaces for the first time, and re-verify the full gate suite.

Purpose: ROADMAP Phase 7 Success Criterion 4 requires all three pipeline
steps to actually run, in order, against the real artifact Plans 01-03
built — never a self-reported "looks good," and never a skill that works in
isolation but is invisible to `make test`/`make lint`/`make validate`.

Output: a `/skill-judge`-cleared, `/naming-analyzer`-named skill with a real
`SKILL.md`, registered in `Makefile`/`.pre-commit-config.yaml`/`pyproject.toml`,
a README row, and a final commit closing Phase 7.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@AGENTS.md
@.planning/phases/07-curated-gsd-config-skill/07-CONTEXT.md
@.planning/phases/07-curated-gsd-config-skill/07-01-SUMMARY.md
@.planning/phases/07-curated-gsd-config-skill/07-02-SUMMARY.md
@.planning/phases/07-curated-gsd-config-skill/07-03-SUMMARY.md
</context>

<tracked_source_paths>
`skills/ai-kit-gsd-config/` is tracked once Plans 01-03's commits land in
this phase (confirmed via `git ls-files skills/ai-kit-gsd-config/` at
planning time for this plan — empty now, populated by the time this plan
executes). `Makefile`, `.pre-commit-config.yaml`, `pyproject.toml`,
`README.md` are pre-existing tracked files this plan edits with `Edit`
(scoped), never `Write` — confirmed via `git ls-files` at planning time
(all four non-empty). The final, post-naming-analyzer skill path is not yet
known (that is the entire point of Task 2) — if it changes, it will be a
`git mv`'d version of an already-tracked tree, not a new untracked location.
</tracked_source_paths>

<failing_direction_contract>
Every runnable `<automated>` command below is followed by a `<fails_when>`
sibling naming the observable output that constitutes failure.
</failing_direction_contract>

<source_audit>
## Multi-Source Coverage Audit (Phase 7, Plan 04 portion)

| # | Source | Item | Covered by |
|---|--------|------|-----------|
| SC4 | ROADMAP SC4 | SKILL.md scaffolded via `/superpowers:writing-skills`, reviewed via `/skill-judge`, named via `/naming-analyzer` | Task 1 (scaffold + skill-judge), Task 2 (naming-analyzer + registration) |
| REQ-4 | REQUIREMENTS.md | REQ-cfg-skill-pipeline | Task 1, Task 2 |
| CTX-D10 | 07-CONTEXT.md | Coexist with `gsd-settings`/`gsd-config`, not replace/wrap | Task 1 (SKILL.md NEVER section) |
| AGENTS.md rule | AGENTS.md | README currency after shipping a new skill | Task 2 |

This closes the phase: every GOAL/REQ/CONTEXT item from Plan 01's whole-phase
audit (see `07-01-PLAN.md`'s `<source_audit>`) now maps to a task somewhere
across all four plans. **Result: 0 unplanned items across the whole phase.**
</source_audit>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold SKILL.md and run the skill-judge loop</name>
  <files>skills/ai-kit-gsd-config/SKILL.md</files>

  <precondition>
    `~/.claude/skills/skill-judge/SKILL.md` is installed and invocable, and
    `/superpowers:writing-skills` is available as the scaffolding entry
    point (both confirmed present on this machine during this phase's
    discussion) — if either cannot be invoked, stop and surface that rather
    than hand-authoring SKILL.md from scratch or self-grading it from memory.
  </precondition>

  <read_first>
    - `.planning/phases/07-curated-gsd-config-skill/07-01-SUMMARY.md`,
      `07-02-SUMMARY.md`, `07-03-SUMMARY.md` — the real, final CLI
      subcommand surface (`ensure-project`, `apply-profile`,
      `apply-critical-agents`, `detect-execution-candidate`,
      `detect-review-candidate`, `apply-execution`, `apply-review`,
      `detect-claude-md-path`, `detect-frontend`, `apply-claude-md-path`,
      `apply-workflow-defaults`) SKILL.md must document accurately — do not
      describe subcommands from the PLAN files' proposed design if the
      executed SUMMARYs recorded any deviation.
    - `.planning/phases/07-curated-gsd-config-skill/07-CONTEXT.md` D-10 —
      the exact coexist-not-replace framing this SKILL.md's NEVER section
      must state explicitly.
    - `skills/ai-kit-spec-config/SKILL.md`, `~/.claude/skills/gsd-settings/
      SKILL.md`, `~/.claude/skills/gsd-config/SKILL.md` — the sibling/
      adjacent skills this one coexists alongside; confirm the new SKILL.md's
      own frontmatter `description:` makes the distinction legible to a
      future reader choosing between them (when to reach for the curated
      skill vs. the full interrogation).
    - `.planning/milestones/v1.0-phases/02-opencode-provider-management/02-02-PLAN.md`
      Task 3 — this project's own precedent for running the skill-judge loop
      and recording scores.
  </read_first>

  <action>
Invoke `/superpowers:writing-skills` to scaffold
`skills/ai-kit-gsd-config/SKILL.md` for the already-built CLI (the skill
wraps the `ai-kit-gsd-config.py` entrypoint Plan 01 created; scaffolding
documents an EXISTING tool, it does not invent new behavior). Ensure the
scaffolded document: names every real subcommand from the three executed
SUMMARYs with its actual flags; states the curated question set is ONE
question (`model_profile`) plus the preset defaults applied without asking
(high effort for heavy-tier/named-top-up agents, Haiku for the research/
execution floor, cross-AI execution on, plan-review convergence on) per
07-CONTEXT.md D-03; and includes an explicit NEVER section stating, citing
D-10 directly, that this skill does NOT replace or wrap `gsd-settings`/
`gsd-config` — those remain the entry points for power-user knobs
(`--advanced`) and API keys/integrations (`--integrations`) this curated
skill intentionally skips; it is a faster ALTERNATIVE entry point, invoked
instead of them, never through them.

Invoke the `skill-judge` skill against the scaffolded SKILL.md. Record the
numeric score and every finding verbatim in this plan's own SUMMARY (ai-kit's
own process record, not a target-repo artifact). Address every Critical and
every Important finding by editing SKILL.md, then re-run `skill-judge` and
record the new score. Loop until there is no remaining Critical or Important
finding, or the score is at or above 96/120 with no regression against the
first run's score. Any edit that changes a documented command, flag, or CLI
output shape must be re-checked against `cli.py`'s actual behavior (per the
SUMMARYs read above) before the loop closes — the document is only correct
if it still matches the code Plans 01-03 actually shipped.
  </action>

  <verify>
    <automated>test -f skills/ai-kit-gsd-config/SKILL.md && grep -c "gsd-settings\|gsd-config" skills/ai-kit-gsd-config/SKILL.md</automated>
    <fails_when>the file does not exist, or the grep count is `0` — SKILL.md never mentions the coexistence relationship D-10 requires, meaning the NEVER section was dropped or never written.</fails_when>
    <automated>python3 -m unittest tests.test_ai_kit_gsd_config -v 2>&1 | tail -30</automated>
    <fails_when>any test errors or fails (non-zero exit, `FAILED`/`ERROR` in output) — proves SKILL.md edits made during the skill-judge loop never required changing the CLI's actual behavior; if they did and the CLI was touched, this catches a regression.</fails_when>
    <human-check>Record the skill-judge run: the numeric score of the first run, every Critical/Important finding and how each was addressed, and the numeric score of the final run. Confirm the final run has no remaining Critical or Important finding, or scores at or above 96/120 with no regression.</human-check>
  </verify>

  <done>
SKILL.md exists, was produced via `/superpowers:writing-skills` (not hand-
authored), documents every real subcommand accurately, explicitly states
the D-10 coexistence boundary in its NEVER section, and clears the
skill-judge bar with the loop's first and final scores recorded in the plan
summary.
  </done>
</task>

<task type="auto">
  <name>Task 2: naming-analyzer, rename-if-needed, first-time registration in all four gates, README row, close the phase</name>
  <files>skills/ai-kit-gsd-config/SKILL.md, skills/ai-kit-gsd-config/ai-kit-gsd-config.py, skills/ai-kit-gsd-config/ai_kit_gsd_config/*.py, tests/test_ai_kit_gsd_config.py, Makefile, .pre-commit-config.yaml, pyproject.toml, README.md</files>

  <precondition>
    `~/.claude/skills/naming-analyzer/SKILL.md` is installed and invocable
    (confirmed present on this machine during this phase's discussion);
    Task 1 of this plan has already closed with no remaining Critical/
    Important skill-judge finding.
  </precondition>

  <read_first>
    - `skills/ai-kit-gsd-config/SKILL.md` — the frontmatter `description:`
      and body `naming-analyzer` reasons from.
    - `README.md`'s `## Contents` table — the exact row format
      (`| [\`name\`](skills/name/SKILL.md) | skill | one-line use case |`)
      to append to, matching the existing `ai-kit-spec-execute-gsd` row's
      style.
    - `Makefile` (`test:`/`lint:` targets), `.pre-commit-config.yaml` (the
      `ruff`/`py-compile` `files:` regex alternations and the `unittest`
      hook's test-module list), `pyproject.toml` (`[tool.vulture] paths` and
      `[tool.pyright] include`) — confirmed at planning time that NONE of
      these four files mention `ai-kit-gsd-config`/`ai_kit_gsd_config`/
      `test_ai_kit_gsd_config` yet (no earlier plan in this phase registered
      them) — this task performs the FIRST-EVER registration, directly under
      whatever name `naming-analyzer` settles on, not a rename of a prior
      entry.
  </read_first>

  <action>
Invoke the `naming-analyzer` skill against the finished
`skills/ai-kit-gsd-config/SKILL.md` (its frontmatter `description:`, its
Scope section, and what it actually does — a curated, short question set
that writes/merges a schema-validated `.planning/config.json` via gsd-tools,
reusing `ai_kit_spec`'s detection pipeline, coexisting with `gsd-settings`/
`gsd-config`) and record its recommended name verbatim. Do not default back
to `ai-kit-gsd-config` merely because it is convenient — if `naming-analyzer`
proposes a different name, that is the one this task ships; if its top
recommendation IS functionally the same name, record that the analysis was
run and concluded no change was warranted (a reasoned match, not a skipped
step).

IF the analyzer's chosen name (`<final>`) differs from `ai-kit-gsd-config`:
producing a package name `ai_kit_<final_snake>` via the same `ai-kit-<kebab>`
/ `ai_kit_<snake>` convention every other skill in this repo already follows
— `git mv skills/ai-kit-gsd-config skills/<final>`; `git mv` the entrypoint
script to `<final>.py`; `git mv` the `ai_kit_gsd_config/` package directory
to `ai_kit_<final_snake>/`; update the entrypoint's `from
ai_kit_gsd_config.cli import main` line to the new package name; update
every internal import inside the package's own modules that references
`ai_kit_gsd_config` by name (the `sys.path`-injection imports from
`ai_kit_spec` established in Plan 02 reference a DIFFERENT, sibling skill's
package name and are NOT renamed by this step — only this skill's OWN
package name changes); update `SKILL.md`'s frontmatter `name:` field and
every internal self-reference to the old name/path; `git mv
tests/test_ai_kit_gsd_config.py tests/test_ai_kit_<final_snake>.py` and
update every `import`/module-loader string inside it referencing the old
package/module paths. IF the analyzer confirms `ai-kit-gsd-config` stands
unchanged, skip all renaming — `<final>` is simply `ai-kit-gsd-config` and
every subsequent step below registers under that name as-is.

Register the skill for the first time in all four gate surfaces, under
`<final>`: append `tests.test_ai_kit_<final_snake>` to `Makefile`'s `test:`
target's module list (alongside the existing `tests.test_ai_kit_usage_metrics`
entry) and append `skills/<final>/<final>.py skills/<final>/ai_kit_<final_snake>/*.py`
to `lint:`'s `py_compile` argument list, matching the existing
`ai-kit-usage-metrics` entries' exact style. In `.pre-commit-config.yaml`,
add `skills/<final>/.*` to both the `ruff` and `py-compile` hooks' `files:`
regex alternation (matching the existing `skills/ai-kit-opencode-providers/.*`
style), and append `tests.test_ai_kit_<final_snake>` to the `unittest`
hook's entry module list. In `pyproject.toml`, append `"skills/<final>"` to
both `[tool.vulture] paths` and `[tool.pyright] include`, matching the
existing `"skills/ai-kit-usage-metrics"` entry's style.

Add one row to `README.md`'s `## Contents` table for `<final>`, matching the
existing row format, with a one-line use case naming the curated-question-
set + schema-validated-config-write + cross-AI-CLI-reuse behavior (not a
restated SKILL.md description verbatim — concise, per AGENTS.md's own
"no excess documentation" standard, applied here to ai-kit's own docs).

Grep the whole tree for the literal provisional strings `ai-kit-gsd-config`
and `ai_kit_gsd_config` after all edits (only relevant if a rename happened)
and confirm zero remaining matches outside this phase's own `.planning/`
artifacts (which correctly keep the provisional name as historical planning
record — never rewrite PLAN.md/CONTEXT.md/SUMMARY.md history).
  </action>

  <verify>
    <automated>make test && make lint && make validate</automated>
    <fails_when>any of the three exits non-zero, `make test`'s output contains `FAILED`, or `make validate`'s pre-commit output contains `Failed` for any hook — this is the proof that registration actually wired the skill into the real local gate, not just that its own isolated test suite passes.</fails_when>
    <automated>make e2e-docker 2>&1 | tail -40</automated>
    <fails_when>the build or container run exits non-zero, or the tail output contains `FAILED`/`ERROR` — the new skill's test module is unreachable or failing inside a genuinely clean-room container with no pre-existing `~/.claude`/`~/.cursor`/`~/.config/opencode`/`~/.codex`.</fails_when>
    <automated>! grep -rlE "ai-kit-gsd-config|ai_kit_gsd_config" --include="*.py" --include="Makefile" --include="*.yaml" --include="*.toml" --include="SKILL.md" . 2>/dev/null | grep -v "^\./.planning/"</automated>
    <fails_when>the grep finds any remaining match outside `.planning/` AND a rename actually happened this task — a leftover provisional-name reference in live code/config means the rename is incomplete. (If `naming-analyzer` confirmed no rename was needed, this check is expected to find the still-current `ai-kit-gsd-config` references and is not applicable — note that outcome in the summary instead of treating a match as a failure in that branch.)</fails_when>
  </verify>

  <done>
The skill is registered, for the first time, in `Makefile`,
`.pre-commit-config.yaml`, and `pyproject.toml` under its final
`/naming-analyzer`-confirmed name; it lives at that final path (renamed via
`git mv` if the analyzer chose a different name, left in place if not); all
four local gates (`make test`, `make lint`, `make validate`, `make
e2e-docker`) are green; `README.md` documents the new skill; and Phase 7's
whole-phase source audit (started in `07-01-PLAN.md`) shows zero unplanned
items across all four plans.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This plan performs a documentation-scaffolding, review, and registration
pass over artifacts Plans 01-03 already threat-modeled (T-07-01 through
T-07-09). No new trust boundary is introduced: no new input source, no new
write target beyond this repo's own tracked files (`git mv`, `Edit`), and no
new runtime behavior — the skill's logic is unchanged, only its
documentation, (possibly) its name/path, and its gate registration change.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-10 | Tampering | Incomplete first-time registration leaves the skill's tests unreachable from `make test`/`.pre-commit-config.yaml` (silent gate gap — the skill ships but is never actually checked by CI/commit hooks going forward) | medium | mitigate | Task 2's `<verify>` runs `make test && make lint && make validate` AND `make e2e-docker` directly, in a genuinely clean-room container for the last one — a registration that is merely textually present but wired to the wrong path/module name fails at least one of these four real executions, not just a grep-based sanity check. |
| T-07-11 | Tampering | A rename (if `naming-analyzer` picks a new name) leaves a stale provisional-name reference in a registration surface alongside the new one | low | mitigate | The final `<verify>` step's repo-wide grep for the literal old name/package string outside `.planning/` catches a partial rename even if `make validate` happens to still pass by accident (e.g. a duplicated-but-harmless regex alternation). |

No package-manager install tasks exist in this plan, so the Package
Legitimacy Gate does not apply.
</threat_model>

<verification>
- `skill-judge`'s final run against the finished `SKILL.md` (Task 1) shows
  no remaining Critical/Important finding (or ≥96/120, no regression).
- `naming-analyzer` was actually invoked and its output is what the final
  name is — recorded, not assumed.
- `make test`, `make lint`, `make validate`, `make e2e-docker` are all green
  AFTER first-time registration (and rename, if one happened).
- Zero remaining `ai-kit-gsd-config`/`ai_kit_gsd_config` references outside
  `.planning/` IF a rename happened; otherwise the provisional name is the
  final name and remains throughout, by design.
- `README.md` documents the skill under its final name.
</verification>

<success_criteria>
- ROADMAP Phase 7 Success Criterion 4 is fully satisfied.
- REQ-cfg-skill-pipeline is complete.
- D-10's coexistence boundary is documented in SKILL.md, not just honored in
  code.
- Phase 7 has zero unplanned items across all four plans' source audits.
</success_criteria>

<output>
Create `.planning/phases/07-curated-gsd-config-skill/07-04-SUMMARY.md` when done
</output>
