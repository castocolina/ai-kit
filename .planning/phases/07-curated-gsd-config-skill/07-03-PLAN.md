---
phase: 07-curated-gsd-config-skill
plan: 03
type: execute
wave: 3
depends_on: ["07-02"]
cross_ai: true
autonomous: true
requirements: [REQ-cfg-curated-questions, REQ-cfg-writes-config]
files_modified:
  - skills/ai-kit-gsd-config/ai_kit_gsd_config/claude_md_detect.py
  - skills/ai-kit-gsd-config/ai_kit_gsd_config/frontend_detect.py
  - skills/ai-kit-gsd-config/ai_kit_gsd_config/workflow_defaults.py
  - skills/ai-kit-gsd-config/ai_kit_gsd_config/cli.py
  - tests/test_ai_kit_gsd_config.py

estimate:
  tokens: 86000
  raw_tokens: 86000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "`claude_md_detect.detect_claude_md_path` checks `AGENTS.md`, `CLAUDE.local.md`, `AGENTS.local.md`, `CLAUDE.md` in that exact priority order and returns `None` — never a fabricated default — when none exist in the target repo (D-06)."
    - "`frontend_detect.detect_frontend_present` is a concrete, testable heuristic (package-manifest dependency scan plus a README framework-name scan) that returns a plain `bool` and never raises when `package.json`/`README.md` are absent (D-08, Claude's Discretion)."
    - "`workflow.ui_phase` and `workflow.ui_review` are written to the SAME boolean value the frontend-detection function returns — never hardcoded `true` regardless of what is actually detected (D-08)."
    - "The D-07 workflow-flag bundle is applied unconditionally every run, sourced verbatim from this repo's own `.planning/config.json` `workflow.*` section at planning time, and deliberately EXCLUDES `cross_ai_execution`/`cross_ai_command`/`cross_ai_timeout`/`plan_review_convergence` (owned by Plan 02) and `ui_phase`/`ui_review` (owned by this plan's own frontend-detection task) — no key in this bundle is ever double-written by two different plans."
    - "An end-to-end integration test proves D-09's merge-mode contract: applying this plan's writes against an EXISTING `.planning/config.json` fixture (never ai-kit's own real file) with unrelated pre-existing keys leaves every one of those unrelated keys byte-identical after the run; applying against NO existing config file correctly falls through `config-new-project` first (reusing Plan 01's `ensure-project`), never hand-writes a fresh file."
  artifacts:
    - skills/ai-kit-gsd-config/ai_kit_gsd_config/claude_md_detect.py
    - skills/ai-kit-gsd-config/ai_kit_gsd_config/frontend_detect.py
    - skills/ai-kit-gsd-config/ai_kit_gsd_config/workflow_defaults.py
  key_links:
    - "`cli.py`'s `apply-claude-md-path`/`apply-workflow-defaults` subcommands write exclusively through Plan 01's `gsd_write.config_set` — no new write path is introduced in this plan."
    - "The D-09 integration test chains Plan 01's `ensure-project` subcommand as its fresh-project entry point before this plan's own subcommands run against it, proving the Plan 01 to Plan 03 subcommand chain coheres end-to-end, not just unit-by-unit."
---

<objective>
Add the two remaining detection helpers (`claude_md_path` resolution per D-06,
frontend presence for `ui_phase`/`ui_review` per D-08), the fixed D-07
workflow-flag defaults bundle, and a merge-mode end-to-end integration test
proving D-09's "touch only curated keys, never clobber the rest" contract
across the full Plan 01 to Plan 03 subcommand chain.

Purpose: ROADMAP Phase 7 Success Criterion 1's remaining preset pieces
(instruction-file path, UI-phase activation) plus Success Criterion 2's
merge-safety guarantee — the one property every other plan's writes in this
phase implicitly depend on holding true.

Output: `claude_md_detect.py`, `frontend_detect.py`, `workflow_defaults.py`,
and three new CLI subcommands (`detect-claude-md-path`, `detect-frontend`,
`apply-claude-md-path`, `apply-workflow-defaults`).
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@AGENTS.md
@.planning/phases/07-curated-gsd-config-skill/07-CONTEXT.md
</context>

<tracked_source_paths>
`skills/ai-kit-gsd-config/ai_kit_gsd_config/{claude_md_detect,frontend_detect,
workflow_defaults}.py` return empty via `git ls-files` at planning time (new
files under the tracked-tree package Plan 01 created). This plan's own
`.planning/config.json` read (for grounding the D-07 bundle's literal values,
done once already at planning time — see the `<action>` below) is a
PLANNING-TIME reference lookup only; no task in this plan reads or writes
that real file at runtime — every runtime read/write in this plan's tests
targets a disposable `tempfile.mkdtemp()` fixture.
</tracked_source_paths>

<failing_direction_contract>
Every runnable `<automated>` command below is followed by a `<fails_when>`
sibling naming the observable output that constitutes failure.
</failing_direction_contract>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: claude_md_path and frontend-presence detection</name>
  <files>skills/ai-kit-gsd-config/ai_kit_gsd_config/claude_md_detect.py, skills/ai-kit-gsd-config/ai_kit_gsd_config/frontend_detect.py, tests/test_ai_kit_gsd_config.py</files>

  <read_first>
    - `.planning/phases/07-curated-gsd-config-skill/07-CONTEXT.md` D-06 and D-08 (the exact priority order for the instruction-file check, and the explicit "Claude's Discretion" framing for the frontend heuristic's implementation details — no further user input exists for either, this task's own judgment is the spec).
    - `skills/ai-kit-gsd-config/ai_kit_gsd_config/gsd_catalog.py` (from Plan 01 — whichever `isdir_fn`/DI-parameter naming convention it already established; this task's two new modules MUST use the same `which_fn=shutil.which`/`isfile_fn=os.path.isfile`-style dependency-injection convention, not a divergent one).
  </read_first>

  <behavior>
    - `claude_md_detect.detect_claude_md_path(project_dir, isfile_fn=os.path.isfile)` checks, IN THIS EXACT ORDER, whether `AGENTS.md`, then `CLAUDE.local.md`, then `AGENTS.local.md`, then `CLAUDE.md` exists directly under `project_dir`; returns the first hit as a `"./<name>"`-prefixed relative string (matching ai-kit's own config.json's existing `"./CLAUDE.md"` literal convention); returns `None` when none of the four exist.
    - `frontend_detect.detect_frontend_present(project_dir, isfile_fn=os.path.isfile, read_fn=_read_text)` returns `True` when `package.json` exists under `project_dir` AND its `dependencies`/`devDependencies` object contains any of a fixed key set (`"react"`, `"vue"`, `"svelte"`, `"next"`, `"@angular/core"`, `"solid-js"`), OR when a `README.md` exists and its text contains a case-insensitive, word-boundary match for any of `"react"`, `"vue"`, `"svelte"`, `"angular"`, `"next.js"`; returns `False` when neither file exists, neither contains a match, or `package.json` exists but is malformed JSON (never raises on a malformed manifest — treat it as "no signal", not an error).
  </behavior>

  <action>
Implement `claude_md_detect.py` per `<behavior>` — a pure function taking an
injected `isfile_fn` (default `os.path.isfile`) so the priority-order logic
is testable against a fake filesystem with zero real I/O.

Implement `frontend_detect.py` per `<behavior>`. Add a small private
`_read_text(path)` helper (`open(path, encoding="utf-8").read()`, the
DEFAULT for the injected `read_fn` parameter) so tests can inject a fake
reader returning canned fixture text instead of real files. Parse
`package.json` with `json.loads`, wrapped in `try/except (json.JSONDecodeError,
OSError)` returning `False` from that branch alone (the README branch still
runs independently — a malformed `package.json` must not short-circuit the
README check). Use a bounded regex (`\b` word boundaries, case-insensitive)
for the README scan, mirroring the same bounded-match discipline
`ai_kit_spec.model_heuristics._hint_matches` already established elsewhere in
this codebase — do not import that helper here (it is string-API-shaped for
model IDs, not multi-line document text); write a small local equivalent.
ALWAYS build each hint's pattern via `re.escape(hint)` before wrapping it in
`\b...\b` (fixes cross-AI review Cycle 2 LOW: a naive, unescaped
`\bnext.js\b` treats the literal `.` in `"next.js"` as a wildcard, so a
string like `"nextzjs"` would false-positive; `re.escape` is required for
every hint, not just `"next.js"`, since it is a general correctness property
of this helper, not a special case for one hint value — `07-REVIEWS.md`
Cycle 2 self-verification review). Add a `test_next_js_dot_is_literal_not_a_wildcard`
case asserting a README containing only `"nextzjs"` (no literal `"next.js"`)
does NOT satisfy the frontend-detection match, alongside the existing
`"react"`-vs-`"reactive"` word-boundary test.

Add to `tests/test_ai_kit_gsd_config.py`: `TestDetectClaudeMdPath` (AGENTS.md-
only; CLAUDE.local.md present alongside a LOWER-priority CLAUDE.md, asserting
CLAUDE.local.md wins; all four absent, asserting `None`). `TestDetectFrontendPresent`
(a `package.json` fixture with `"react"` in `dependencies`; a `README.md`
fixture mentioning `"Next.js"` case-insensitively; a `package.json` containing
the substring `"reactive"` in an unrelated dependency name, asserting this
does NOT false-positive on the bounded `"react"` check; a malformed
`package.json` paired with a clean README match, asserting the README branch
still returns `True`; neither file present, asserting `False` with no
exception raised).
  </action>

  <verify>
    <automated>python3 -m unittest tests.test_ai_kit_gsd_config -v 2>&1 | tail -40</automated>
    <fails_when>the final summary line is not `OK`, or the output contains `FAILED`, `ERROR`, or `Traceback`.</fails_when>
    <automated>cd skills/ai-kit-gsd-config && python3 -c "from ai_kit_gsd_config import frontend_detect as f; assert f.detect_frontend_present('/nonexistent-dir-xyz') is False; print('FRONTEND_DEFAULT_FALSE_OK')"</automated>
    <fails_when>stdout does not contain the literal line `FRONTEND_DEFAULT_FALSE_OK`, or the call raises instead of returning cleanly for a directory that does not exist at all.</fails_when>
  </verify>
  <done>
`detect_claude_md_path` returns the correct priority-ordered match or `None`
for every fixture in `TestDetectClaudeMdPath`; `detect_frontend_present`
never raises regardless of missing/malformed input files and only returns
`True` on a genuine bounded-match signal. All new tests in
`tests/test_ai_kit_gsd_config.py` pass.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: D-07 workflow-defaults bundle, wiring, and the merge-mode end-to-end integration test</name>
  <files>skills/ai-kit-gsd-config/ai_kit_gsd_config/workflow_defaults.py, skills/ai-kit-gsd-config/ai_kit_gsd_config/cli.py, tests/test_ai_kit_gsd_config.py</files>

  <read_first>
    - `.planning/config.json` (this repo's own file, read-only reference — already fully read at planning time; re-confirm the literal `workflow.*` values below still match if re-running this plan later, since D-07 treats this file as a point-in-time baseline, not a live lookup) — the exact source of every value in `WORKFLOW_DEFAULTS` below. NEVER open this path from test or production code (Non-Negotiable Rule: never use real config files as test fixtures; this plan's tests use disposable `tempfile.mkdtemp()` fixtures only).
    - `skills/ai-kit-gsd-config/ai_kit_gsd_config/gsd_write.py` (Plan 01/02) — confirms `config_set(project_dir, key_path, value, run_fn=...)`'s exact signature this task's `apply-workflow-defaults` subcommand calls once per bundle key.
  </read_first>

  <behavior>
    - `workflow_defaults.WORKFLOW_DEFAULTS` is a fixed, literal `dict` of exactly these 27 `workflow.<key>` entries, with NO `ui_phase`/`ui_review`/`cross_ai_execution`/`cross_ai_command`/`cross_ai_timeout`/`plan_review_convergence` key present (those five live entirely in other tasks/plans): `research: true`, `plan_check: true`, `verifier: true`, `nyquist_validation: true`, `auto_advance: false`, `node_repair: true`, `node_repair_budget: 2`, `ui_safety_gate: true`, `ai_integration_phase: true`, `tdd_mode: false`, `human_verify_mode: "end-of-phase"`, `text_mode: false`, `research_before_questions: false`, `discuss_mode: "discuss"`, `skip_discuss: false`, `code_review: true`, `code_review_depth: "standard"`, `code_review_command: null`, `pattern_mapper: true`, `plan_bounce: false`, `plan_bounce_script: null`, `plan_bounce_passes: 2`, `auto_prune_state: false`, `post_planning_gaps: true`, `security_enforcement: true`, `security_asvs_level: 1`, `security_block_on: "high"`.
    - `cli.py apply-workflow-defaults --project-dir <dir> --ui-phase <true|false> --ui-review <true|false>` writes every `WORKFLOW_DEFAULTS` entry unconditionally via `gsd_write.config_set`, THEN writes `workflow.ui_phase`/`workflow.ui_review` from the two required CLI flags (never a hardcoded `true` — the caller, per Claude's Discretion D-08, is expected to pass `detect-frontend`'s own boolean result here; this subcommand does not call `detect_frontend_present` itself, keeping detection and application separate exactly like every other subcommand pair in this plan set).
    - `cli.py apply-claude-md-path --project-dir <dir> --path <path-or-empty>` writes `claude_md_path` to `--path` only when it is non-empty; an empty `--path` (the caller's way of passing through `detect_claude_md_path`'s `None`) leaves the key untouched rather than writing an empty string.
  </behavior>

  <action>
Implement `workflow_defaults.py` per `<behavior>` — module-level constant,
documented with a comment citing D-07 and the exact date/commit context this
bundle was sourced from (this repo's own `.planning/config.json`,
2026-09-10), and the explicit exclusion list (the five keys owned by Plan 02
and this plan's own frontend-detection task) so a future reader never
"helpfully" re-adds one of those five here and creates a double-write.

Extend `cli.py` with `detect-claude-md-path --project-dir <dir>` (prints
`{"path": "./AGENTS.md"}` or `{"path": null}`), `detect-frontend
--project-dir <dir>` (prints `{"frontend_present": bool}`), `apply-claude-md-
path`, and `apply-workflow-defaults` per `<behavior>`. Every subcommand exits
0; a `None`/empty detection result is informational, never a CLI failure.

Add to `tests/test_ai_kit_gsd_config.py`: `TestWorkflowDefaults` (asserts the
dict has exactly 27 keys, asserts NONE of the five excluded keys are present
— this is the one assertion most likely to catch an accidental future
double-write regression). `TestApplyWorkflowDefaultsCli` (DI through a
`fake_run`; asserts all 27 bundle keys get a `config_set` call PLUS
`ui_phase`/`ui_review` from the two flags — one case with `--ui-phase true
--ui-review false`, asserting the two values land independently, not
coupled). `TestApplyClaudeMdPathCli` (one case with a real `--path`, one case
with an empty `--path`, asserting the empty case issues ZERO `config_set`
calls).

**D-09 integration test** (`TestMergeModeEndToEnd`, a single test reusing
`tempfile.mkdtemp()` + `shutil.rmtree` in `tearDown`): (1) Create a fresh
temp dir with NO `.planning/` at all; invoke Plan 01's `ensure-project` CLI
entrypoint against it (real subprocess, `skipUnless` guarded on gsd-tools
actually being resolvable, same skip pattern as Plan 01's own real-integration
test) then this plan's `apply-workflow-defaults`/`apply-claude-md-path`
against the now-existing file; assert the resulting `.planning/config.json`
contains the full `WORKFLOW_DEFAULTS` bundle. (2) In a SECOND temp dir, hand-
write (via plain `json.dump`, not gsd-tools) a synthetic fixture
`.planning/config.json` containing ONLY a handful of unrelated keys
(`{"some_other_key": "untouched", "workflow": {"some_other_workflow_key":
"keep-me"}}` — never values copied from any real project's actual config);
run `apply-workflow-defaults` against it; assert `some_other_key` and
`workflow.some_other_workflow_key` are BOTH still present and byte-identical
to their original values afterward, proving merge mode never clobbers
unrelated keys (D-09).
  </action>

  <verify>
    <automated>python3 -m unittest tests.test_ai_kit_gsd_config -v 2>&1 | tail -60</automated>
    <fails_when>the final summary line is not `OK`, or the output contains `FAILED`, `ERROR`, or `Traceback`.</fails_when>
    <automated>cd skills/ai-kit-gsd-config && python3 -c "from ai_kit_gsd_config.workflow_defaults import WORKFLOW_DEFAULTS as w; assert len(w) == 27, len(w); assert not ({'ui_phase','ui_review','cross_ai_execution','cross_ai_command','cross_ai_timeout','plan_review_convergence'} & set(w)); print('BUNDLE_OK')"</automated>
    <fails_when>stdout does not contain the literal line `BUNDLE_OK`, or either `assert` raises first (the bundle's key count drifted, or one of the five cross-plan-owned keys leaked in).</fails_when>
  </verify>
  <done>
`WORKFLOW_DEFAULTS` has exactly 27 keys with zero overlap against the five
keys owned by Plan 02/this-plan's-own-frontend-task. `apply-workflow-defaults`
writes the bundle plus `ui_phase`/`ui_review` from caller-supplied flags (never
hardcoded). `apply-claude-md-path` writes only on a non-empty path. The D-09
merge-mode integration test passes against both a brand-new project (via
Plan 01's `ensure-project`) and an existing synthetic fixture with unrelated
keys proven untouched. All tests in `tests/test_ai_kit_gsd_config.py` pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Target-repo file contents → frontend/claude_md detection | `package.json`/`README.md` text in the TARGET project (not this one) feeds directly into two boolean/string config values — untrusted input from whatever repo the skill is run against. |
| Workflow-defaults bundle → `.planning/config.json` | A fixed, planning-time-sourced bundle written through gsd-tools' own validated `config-set` — same boundary as every other write in this phase. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-08 | Tampering | `frontend_detect`'s package.json/README scan | low | accept | A crafted dependency name or README string can only flip a UI-workflow activation boolean (`ui_phase`/`ui_review`) — no security-relevant effect, no code execution, no file write beyond the two booleans themselves. |
| T-07-09 | Tampering | Merge-mode write accidentally clobbering unrelated existing keys | medium | mitigate | `TestMergeModeEndToEnd`'s byte-identity assertion against a synthetic fixture with unrelated keys directly tests the exact failure mode D-09 exists to prevent — this is the phase's single most consequential regression to catch, since every other plan's writes in this phase rely on the same merge-mode write path never widening its blast radius. |

Package-legitimacy gate: not applicable — this plan installs no new package.
</threat_model>

<verification>
Run `python3 -m unittest tests.test_ai_kit_gsd_config -v` — all tests pass,
including both tasks' new classes and the D-09 end-to-end integration test.
Run the two `<automated>` hygiene checks above directly. Confirm `cli.py
--help` lists `detect-claude-md-path`, `detect-frontend`, `apply-claude-md-
path`, `apply-workflow-defaults` alongside every subcommand from Plans 01-02.
</verification>

<success_criteria>
- [ ] `claude_md_detect.py` resolves the correct instruction-file path in
      priority order or `None` — never a fabricated default (D-06).
- [ ] `frontend_detect.py` returns a real, bounded-match-based boolean signal,
      never hardcoded and never raising on missing/malformed input (D-08).
- [ ] `workflow.ui_phase`/`workflow.ui_review` always reflect the actual
      detection result passed in, never a literal `true`.
- [ ] The D-07 bundle is exactly 27 keys with zero overlap against the five
      keys owned elsewhere in this phase.
- [ ] D-09's merge-mode integration test proves both the fresh-project
      fallback path and the existing-file non-clobber guarantee.
- [ ] All tests pass; no test or production code in this plan opens this
      repo's own real `.planning/config.json`.
</success_criteria>

<output>
Create `.planning/phases/07-curated-gsd-config-skill/07-03-SUMMARY.md` when done
</output>
