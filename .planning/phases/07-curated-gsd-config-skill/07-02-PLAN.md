---
phase: 07-curated-gsd-config-skill
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
cross_ai: true
autonomous: true
requirements: [REQ-cfg-curated-questions, REQ-cfg-writes-config]
files_modified:
  - skills/ai-kit-gsd-config/ai_kit_gsd_config/model_detect.py
  - skills/ai-kit-gsd-config/ai_kit_gsd_config/preference_match.py
  - skills/ai-kit-gsd-config/ai_kit_gsd_config/cross_ai_build.py
  - skills/ai-kit-gsd-config/ai_kit_gsd_config/cli.py
  - skills/ai-kit-gsd-config/ai_kit_gsd_config/gsd_write.py
  - tests/test_ai_kit_gsd_config.py

estimate:
  tokens: 88000
  raw_tokens: 88000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Detecting candidate models reuses `ai_kit_spec`'s own `detect-runtimes`/`group-models` CLI subcommands via subprocess — this plan never re-implements CLI/model enumeration from scratch (D-02)."
    - "The execution preference ladder (contains `coding`/`executor`, `composer`>=2.5, `grok`>=4.6, `deepseek-flash`, `luna`, each delimiter-bounded) is evaluated in that literal priority order over the opencode-then-cursor candidate pool; the first rule that matches ANY candidate wins (D-03 Specifics)."
    - "The plan-review preference ladder (contains `plan-review`, `gpt-<ver>-sol`, `glm-5.2`/`glm-5.3`, each delimiter-bounded, then a native `claude`/`opus` last resort) is evaluated the same way, and the native last resort ALWAYS resolves to something — `best_review_candidate` never returns a no-match signal (D-03 Specifics: 'opus as last resort')."
    - "`workflow.cross_ai_execution` and `workflow.plan_review_convergence` are written `true` UNCONDITIONALLY — the boolean presets apply even when no installed CLI matches any preference pattern (ROADMAP SC1: 'applies the preset defaults ... without prompting')."
    - "`workflow.cross_ai_command`, when written, is built from the BARE command name resolved via PATH at runtime (`opencode`, `cursor-agent`, etc.) — it never bakes in a venv/nvm/brew-resolved absolute path for the executable (D-05, the observed-regression landmine)."
    - "When neither opencode nor cursor-agent is installed (or neither has any model matching any execution-ladder rule), `cross_ai_command` is left UNWRITTEN rather than invented — the skill's final summary states this plainly instead of claiming a working cross-AI command exists (Claude's Discretion: graceful degradation, never a fabricated command)."
    - "`review.default_reviewers`/`review.models.<slug>`/`review.effort.<slug>` are written as STRUCTURED config keys for plan-review, never as a constructed command string (D-04 — the explicit execution/plan-review asymmetry)."
  artifacts:
    - skills/ai-kit-gsd-config/ai_kit_gsd_config/model_detect.py
    - skills/ai-kit-gsd-config/ai_kit_gsd_config/preference_match.py
    - skills/ai-kit-gsd-config/ai_kit_gsd_config/cross_ai_build.py
  key_links:
    - "`preference_match.py` consumes `model_detect.py`'s output (a plain `{cli_name: [model_id, ...]}` pool) — the detection layer and the decision layer are separate modules, so the decision logic is unit-testable against a synthetic pool with zero subprocess calls."
    - "`cli.py`'s `apply-execution`/`apply-review` subcommands take an already-resolved `(cli, model)` pair as CLI flags, not a live detection call inside themselves — detection and application are separate subcommands, composable and independently testable, mirroring Plan 01's `ensure-project`/`apply-profile` split."
---

<objective>
Add live model/CLI detection (reusing `ai_kit_spec`'s existing pipeline, D-02)
and the two literal preference ladders from 07-CONTEXT.md's Specific Ideas
(execution: coding/executor-named or version-gated router models; plan-review:
plan-review-named or specific-family models, with a native opus last resort),
then wire the winning candidates into `workflow.cross_ai_command`
(D-04/D-05 — bare executable name only) and `review.default_reviewers`/
`review.models.<slug>`/`review.effort.<slug>` (D-04 — structured keys, never
a command string) respectively.

Purpose: ROADMAP Phase 7 Success Criterion 1's "cross-AI execution enabled,
plan-review convergence enabled" preset, applied against whatever is
genuinely installed on the machine running the skill — never a guess, never
a hardcoded model name.

Output: `model_detect.py`, `preference_match.py`, `cross_ai_build.py`, and
four new CLI subcommands (`detect-execution-candidate`,
`detect-review-candidate`, `apply-execution`, `apply-review`).
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@AGENTS.md
@.planning/phases/07-curated-gsd-config-skill/07-CONTEXT.md
@skills/ai-kit-spec-review/ai_kit_spec/model_heuristics.py
</context>

<tracked_source_paths>
`skills/ai-kit-gsd-config/ai_kit_gsd_config/{model_detect,preference_match,cross_ai_build}.py`
return empty via `git ls-files` at planning time (new files under the
tracked-tree package Plan 01 created). `skills/ai-kit-spec-review/ai_kit_spec/`
is tracked and modified by NO task in this plan — this plan only subprocess-
invokes its already-installed CLI, never imports or edits its modules
directly (mirrors D-02's own "reuses ... instead of building new detection/
ranking logic" framing: reuse via the CLI boundary, not via a cross-skill
Python import, since `ai_kit_spec` has no declared dependency contract for
being imported by a sibling skill).
</tracked_source_paths>

<failing_direction_contract>
Every runnable `<automated>` command below is followed by a `<fails_when>`
sibling naming the observable output that constitutes failure.
</failing_direction_contract>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Detection pool + the two literal preference ladders</name>
  <files>skills/ai-kit-gsd-config/ai_kit_gsd_config/model_detect.py, skills/ai-kit-gsd-config/ai_kit_gsd_config/preference_match.py, skills/ai-kit-gsd-config/ai_kit_gsd_config/cli.py, tests/test_ai_kit_gsd_config.py</files>

  <read_first>
    - `skills/ai-kit-spec-review/ai_kit_spec/detection.py` — `build_runtimes_snapshot()` (the `{"clis": {name: {"installed": bool, "path"?: str, "models"?: [str]}}}` shape this task's detection layer consumes) and `detect_opencode_models`/`detect_cursor_agent_models` (confirms ONLY `opencode` and `cursor-agent` carry a `"models"` list — `claude`/`codex`/`grok` never do, since they are not multi-model-enumerable CLIs).
    - `skills/ai-kit-spec-review/ai_kit_spec/cli.py` — the `detect-runtimes [--save PATH] [--if-stale PATH]` and `group-models --runtimes-json PATH --cli <id>` subparsers (confirms the exact flag shapes this task's `model_detect.py` shells out with).
    - `skills/ai-kit-spec-review/ai_kit_spec/model_heuristics.py` — `_hint_matches(hint, lowered)` (the delimiter-bounded `\bhint\b` regex helper — `"review"` must never fire on `"preview"`) and `_PURPOSE_NAME_HINTS` (the EXISTING, narrower hint tuples `{"review": ("review","plan-review"), "execute": ("coding","execute")}` — note this task's OWN ladders use a DIFFERENT, wider literal set per 07-CONTEXT.md D-03 Specifics (`"executor"` not `"execute"`, no bare `"review"`) — import and reuse `_hint_matches` for the bounded-match MECHANISM only, define this task's own hint tuples separately; do not assume the existing `_PURPOSE_NAME_HINTS` tuples are the right literals for this phase's locked preference patterns.
    - `skills/ai-kit-spec-execute-gsd/SKILL.md` Step 0 — the multi-candidate skill-directory resolution pattern (`CLAUDE_PLUGIN_ROOT` / `~/.claude/skills/<name>` / sibling-of-this-SKILL.md) this task's `model_detect.py` mirrors when locating `ai_kit_spec`'s own CLI entrypoint (`ai-kit-spec.py`), since this skill has no guaranteed `CLAUDE_PLUGIN_ROOT` either.
  </read_first>

  <behavior>
    - `model_detect.build_candidate_pool(runtimes_snapshot)` returns `{"opencode": [model_id, ...], "cursor-agent": [model_id, ...]}`, omitting a CLI key entirely when that CLI's `installed` is `false` or it has no `"models"` list at all (never an empty-list placeholder for a CLI that was never enumerable in the first place — `claude`/`codex`/`grok` never appear as keys).
    - `preference_match.best_execution_candidate(pool)` walks the pool in declared rule order (coding/executor bounded-substring; composer>=2.5; grok>=4.6; deepseek-flash bounded-substring; luna bounded-substring), searching `pool.get("opencode", [])` then `pool.get("cursor-agent", [])` for EACH rule before moving to the next rule — returns the FIRST `(cli, model, rule_label)` match found in that nested order, or `None` if no rule matches anything in either list.
    - `preference_match.best_review_candidate(pool)` walks its own three rules (plan-review bounded-substring; `gpt-<digits[.digits]>-sol` bounded pattern; glm-5.2/glm-5.3 bounded-substring) the same nested way; when NONE of the three match anything, it returns the fixed native fallback `("claude", "opus", "native-last-resort")` — this function has no "no match" return value at all.
    - `preference_match._version_at_least(model_id, family, min_version)` extracts the numeric dotted version immediately following a bounded `family-` prefix and returns `True` only when that version, compared component-wise as integers (shorter tuple zero-padded), is `>=` the parsed `min_version` tuple; a model id with no such bounded family-version match returns `False` (never raises on a non-numeric or absent segment).
  </behavior>

  <action>
In `model_detect.py`, mirror `ai-kit-spec-execute-gsd/SKILL.md`'s Step 0
candidate list (host-agnostic: `CLAUDE_PLUGIN_ROOT`-relative, then
`~/.claude/skills/ai-kit-spec-review`, then a sibling-of-this-skill's-own-
directory fallback `../ai-kit-spec-review`) to implement
`resolve_ai_kit_spec_path(this_skill_dir, env=None, isdir_fn=os.path.isdir)`
returning the resolved `ai-kit-spec.py` absolute path or `None`. Implement
`detect_runtimes_snapshot(python_bin, ai_kit_spec_path, run_fn=subprocess.run)`:
runs `[python_bin, ai_kit_spec_path, "detect-runtimes"]` (no `--save`/
`--if-stale` — this task always wants a fresh, unpersisted snapshot, since a
stale cached snapshot could hide a just-installed CLI), `json.loads`s stdout,
returns the parsed dict or `None` on any failure (non-zero exit, bad JSON).
Implement `build_candidate_pool(runtimes_snapshot)` per `<behavior>` — a pure
function, no I/O, taking the ALREADY-parsed snapshot dict (detection and pool-
building stay separate so the pure half is independently testable without
ever shelling out).

In `preference_match.py`, import `_hint_matches` from
`ai_kit_spec.model_heuristics` (confirm this import resolves by adding
`ai_kit_spec`'s OWN skill directory, resolved via `model_detect.
resolve_ai_kit_spec_path`'s sibling-dir logic, onto `sys.path` before the
import — document in the module docstring that this is the one deliberate
exception to "this skill stays self-contained," scoped to reusing ONE proven
regex helper, never `ai_kit_spec`'s detection/ranking classes wholesale,
consistent with D-02's "reuses ... for model/CLI detection and preference-
pattern matching"). Define `EXECUTION_RULES` as an ordered tuple of
`(label, predicate)` pairs exactly matching the bullet order in `<behavior>`,
where each `predicate(model_id)` is either a `_hint_matches`-based bounded-
substring check (for `coding`/`executor`, `deepseek-flash`, `luna`) or a call
into `_version_at_least(model_id, family, min_version)` (for
`composer`>=`"2.5"`, `grok`>=`"4.6"`). Define `REVIEW_RULES` the same shape
for `plan-review` (bounded substring), the `gpt-<ver>-sol` pattern (a bounded
regex, NOT `_version_at_least` — `sol` is a fixed suffix token, not a
version-threshold comparison), and `glm-5.2`/`glm-5.3` (two bounded
substrings, either one matching). Implement `_version_at_least` per
`<behavior>` using a regex anchored on a word boundary before `family` and
capturing the trailing `[0-9]+(?:\.[0-9]+)*` run; parse both the captured and
`min_version` strings into `tuple(int(p) for p in s.split("."))`, zero-pad
the shorter tuple on the right to the longer's length, and compare with a
plain `>=` on the padded tuples. Implement `best_execution_candidate(pool)`
and `best_review_candidate(pool)` per `<behavior>`, iterating
`("opencode", "cursor-agent")` as the fixed runtime search order for both
(07-CONTEXT.md's Specifics section states this order for plan-review
explicitly and gives no contrary order for execution, so this task applies
the same order to both for one consistent, documented search policy rather
than inventing a second one).

Extend `cli.py` with `detect-execution-candidate` and `detect-review-candidate`
(no arguments beyond the usual `node`/`ai_kit_spec` resolution): each runs the
detection + pool-build + matcher chain and prints the winning
`{"cli": ..., "model": ..., "rule": ...}` as JSON (or, for execution only,
`{"cli": null, "model": null, "rule": null}` when `best_execution_candidate`
returned `None` — review never needs this null shape since it always
resolves). Exit 0 in every case — a "no candidate" result is informational,
not a CLI failure.

Add to `tests/test_ai_kit_gsd_config.py`: `TestBuildCandidatePool` (a handful
of synthetic snapshot fixtures: opencode-only, cursor-only, neither installed,
an installed-but-models-absent CLI). `TestVersionAtLeast` (composer-2.4 fails
`>=2.5`; composer-2.5 and composer-2.6 both pass; composer-2.5.1 passes;
grok-4.5 fails `>=4.6`; a model id with no trailing version segment at all
returns `False`, never raises). `TestBestExecutionCandidate` and
`TestBestReviewCandidate`: one case per rule (a pool whose ONLY candidate
matches exactly that rule and no earlier one), one case proving PRIORITY
ORDER (a pool where a later-rule-only candidate sits in the list ahead of an
earlier-rule candidate — assert the earlier rule still wins), one case
proving the `cursor-agent`-only fallback when `opencode` has no match, and
(review only) one case proving the `("claude", "opus", "native-last-resort")`
fallback when the pool is completely empty.
  </action>

  <verify>
    <automated>python3 -m unittest tests.test_ai_kit_gsd_config -v 2>&1 | tail -40</automated>
    <fails_when>the final summary line is not `OK`, or the output contains `FAILED`, `ERROR`, or `Traceback`.</fails_when>
    <automated>cd skills/ai-kit-gsd-config && python3 -c "from ai_kit_gsd_config import preference_match as p; r = p.best_review_candidate({}); print(r); assert r == ('claude', 'opus', 'native-last-resort')"</automated>
    <fails_when>the command exits non-zero or raises — `best_review_candidate` failed to resolve the unconditional native fallback against a completely empty pool.</fails_when>
    <automated>cd skills/ai-kit-gsd-config && python3 -c "from ai_kit_gsd_config import preference_match as p; assert p._version_at_least('composer-2.5', 'composer', '2.5') is True; assert p._version_at_least('composer-2.4', 'composer', '2.5') is False; assert p._version_at_least('composer', 'composer', '2.5') is False; print('VERSION_OK')"</automated>
    <fails_when>stdout does not contain the literal line `VERSION_OK`, or any `assert` raises `AssertionError` first (a bounded-version comparison resolved the wrong way).</fails_when>
  </verify>
  <done>
`model_detect.build_candidate_pool` returns a pool keyed only by CLIs that
both report `installed: true` and carry a `"models"` list; `preference_match.
best_execution_candidate` walks its five rules in declared order over
opencode-then-cursor-agent and returns `None` only when nothing matches
anything; `preference_match.best_review_candidate` never returns `None` —
an empty pool resolves to the native `("claude", "opus", "native-last-resort")`
tuple; `cli.py detect-execution-candidate`/`detect-review-candidate` print
valid JSON and exit 0 regardless of match/no-match. All new/updated tests in
`tests/test_ai_kit_gsd_config.py` pass.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire the winning candidates into config — cross-AI execution command and structured plan-review keys</name>
  <files>skills/ai-kit-gsd-config/ai_kit_gsd_config/cross_ai_build.py, skills/ai-kit-gsd-config/ai_kit_gsd_config/cli.py, skills/ai-kit-gsd-config/ai_kit_gsd_config/gsd_write.py, tests/test_ai_kit_gsd_config.py</files>

  <read_first>
    - `skills/ai-kit-spec-review/ai_kit_spec/commands.py` — `build_execute_command(cli, **params)` (the ALREADY-LIVE-VERIFIED per-CLI execute-mode command-string factory this task reuses instead of hand-writing opencode/cursor-agent invocation syntax — confirmed: `_build_opencode_execute_command` returns `"opencode run -m {model} --dir {quoted_target_dir} --auto"`, `_build_cursor_agent_execute_command` returns `"cursor-agent --sandbox enabled --workspace {quoted_target_dir} -p --output-format text --model {model} --trust --force"`; both raise `ValueError` if `target_dir` is falsy, and leave `{model}` as a literal, unfilled placeholder for the caller to `.format()` — never string-interpolate `target_dir` any other way, it is ALREADY `shlex.quote`-wrapped by `_require_target_dir` inside the builder).
    - `skills/ai-kit-spec-execute-gsd/ai_kit_spec_gsd/gsd_cross_ai.py` — `build_cross_ai_command(resolved_candidate, target_dir, execute_command_fn=build_execute_command)` (the EXISTING, separate skill's own thin wrapper around the same builder — confirms the `.format(model=...)` call pattern and the "catch `ValueError`, return `None`" degradation shape this task's `cross_ai_build.py` mirrors; this task does NOT import from `ai_kit_spec_gsd` — it is a sibling skill with its own install-root resolution, not a dependency — it only mirrors the proven pattern in a self-contained function).
    - `.planning/config.json` (this repo's own file, read-only reference) — confirms the real, live shape: `workflow.cross_ai_command: "opencode run --model router-env/my-coding"`, `review.default_reviewers: ["opencode"]` (a JSON array of reviewer slugs), `review.models.opencode: "xai/grok-4.6"` — ground every written value's shape against this, but NEVER copy its literal strings into a test fixture or assertion (Non-Negotiable Rule: never use real config files as test fixtures) and NEVER let any task in this plan open or write this path directly.
    - `~/.claude/gsd-core/bin/lib/capability-registry.cjs` (grep only, do not load in full) — confirms the reviewer-slug vocabulary for `review.models.<slug>`/`review.effort.<slug>`: `opencode` and `cursor` are both real, live slugs (`review.models.opencode`, `review.models.cursor`) — note the CLI name `cursor-agent` maps to the SHORTER slug `cursor`, never the raw CLI binary name; `claude` is also a live slug, used for the native-last-resort case.
    - `~/.claude/gsd-core/bin/lib/config.cjs` — `cmdConfigSet`'s value-parsing branch (`val.startsWith('[') || val.startsWith('{')` triggers `JSON.parse`) — confirms `gsd_write.config_set` must pass an ALREADY `json.dumps`-encoded string for `review.default_reviewers`, never a Python list object or a bare comma-joined string.
  </read_first>

  <behavior>
    - `cross_ai_build.build_execution_command(cli, model, project_dir, execute_command_fn=build_execute_command)` returns the fully-rendered command string (e.g. a real `opencode run -m <model> --dir <project_dir> --auto`-shaped string with NO unfilled `{model}` placeholder remaining) when `execute_command_fn` successfully returns a template for `cli`; returns `None` (never raises) when `execute_command_fn` raises `ValueError` for an unimplemented CLI, and the returned string NEVER contains `project_dir` rendered as anything other than `shlex.quote`d (delegated entirely to `execute_command_fn` — this function itself never re-quotes or re-escapes).
    - `cross_ai_build.CLI_TO_REVIEWER_SLUG` maps `{"opencode": "opencode", "cursor-agent": "cursor", "claude": "claude"}` — the only three CLI names this plan's two ladders can ever resolve to; looking up any other key raises `KeyError` (never silently substitutes the raw CLI name as a fallback slug, which would write an invalid key `config-set` would reject anyway).
    - `gsd_write.config_get(project_dir, key_path, run_fn=subprocess.run)` returns the JSON-decoded current value at `key_path` (e.g. `["opencode"]`), or `None` when the key is absent/unset (gsd-tools' own `config-get` reports this distinctly from an error — treat a clean "not set" result and any other non-zero-exit failure identically as `None`, since this task only ever uses the result to decide whether to append, and appending into an absent list degrades safely to a fresh one-element list either way).
  </behavior>

  <action>
Add `build_execution_command` and `CLI_TO_REVIEWER_SLUG` to `cross_ai_build.py`
per `<behavior>`, importing `build_execute_command` from `ai_kit_spec.commands`
using the SAME `sys.path`-injection-via-`model_detect.resolve_ai_kit_spec_path`
pattern Plan 02 Task 1's `preference_match.py` already established (confirm by
reading that module's import block — do not re-derive a second, divergent
resolution strategy for the same sibling skill).

Extend `gsd_write.py` with `config_get(project_dir, key_path, run_fn=
subprocess.run)` per `<behavior>`, invoking `config-get <key_path>
--project-dir <project_dir> --raw` (mirror Plan 01's `config_set`'s own
subprocess-invocation shape — same `resolve_gsd_tools_path`/
`resolve_node_binary` plumbing from `gsd_catalog.py`, no duplicated path-
resolution logic).

Extend `cli.py` with two subcommands:
- `apply-execution --project-dir <dir> --cli <cli> --model <model>` (cli/model
  already resolved by the caller — per this plan's own `detect-execution-
  candidate`, or omitted entirely when that returned `cli: null`). Always
  writes `workflow.cross_ai_execution` to `true` first (D-04/ROADMAP SC1's
  unconditional preset — this happens even when `--cli`/`--model` are both
  omitted, since "cross-AI execution enabled" as a boolean preset is
  independent of whether a live command could be built today). When `--cli`/
  `--model` ARE given, calls `cross_ai_build.build_execution_command(cli,
  model, project_dir)`; if it returns a string, writes it verbatim to
  `workflow.cross_ai_command`; if it returns `None` (unimplemented builder)
  or `--cli` was omitted, `cross_ai_command` is left untouched. Prints
  `{"cross_ai_execution_written": true, "cross_ai_command_written": bool,
  "degraded_reason": "no_candidate"|"no_builder"|null}` and exits 0 in every
  branch — D-05's bare-command-name guarantee is automatically satisfied
  here because `build_execute_command`'s templates only ever contain literal
  CLI names (`opencode`, `cursor-agent`) as written in `commands.py`'s source,
  never a resolved filesystem path; this subcommand performs no `shutil.
  which`/path-resolution step of its own that could reintroduce one.
- `apply-review --project-dir <dir> --cli <cli> --model <model>` (cli/model
  always present when called — `best_review_candidate` never returns a no-
  match signal, so the caller always has a real pair, even if it is the
  native `("claude", "opus", "native-last-resort")` tuple). Always writes
  `workflow.plan_review_convergence` to `true` and the named top-up
  `review.effort.opencode` to `"high"` FIRST, UNCONDITIONALLY, regardless of
  which `--cli` was resolved (D-03's explicit, independent top-up — "may be a
  no-op confirmation", written every run regardless of the winning
  candidate). Then resolves `slug = cross_ai_build.CLI_TO_REVIEWER_SLUG[cli]`,
  reads the current `review.default_reviewers` via `gsd_write.config_get`
  (`None` treated as `[]`), appends `slug` only if not already present
  (dedupe, preserve existing entries — D-09's merge-mode: this skill only
  ever ADDS its own resolved slug to an existing reviewer list, never
  replaces or removes another slug a human already configured), writes the
  merged array back as a `json.dumps`-encoded string, and writes
  `review.models.<slug>` to `model`. Prints `{"plan_review_convergence_
  written": true, "default_reviewers": [...], "review_effort_opencode_
  written": true}` and exits 0.

Add to `tests/test_ai_kit_gsd_config.py`: `TestBuildExecutionCommand` (a fake
`execute_command_fn` returning a template string for `"opencode"`, asserting
the returned string has `{model}` fully substituted and no literal `{model}`
substring remains; a fake raising `ValueError` for an unknown CLI, asserting
`None` comes back, never a raised exception). `TestCliToReviewerSlug` (asserts
`cursor-agent` maps to the literal string `"cursor"`, never `"cursor-agent"`
itself — this is the single assertion most likely to silently regress if
someone "simplifies" the map to `cli == slug`). `TestApplyExecutionCli` (DI
through a `fake_run`/fake `cross_ai_build.build_execution_command`; one case
with a real candidate asserting both `workflow.cross_ai_execution` and
`workflow.cross_ai_command` get written calls; one case with no candidate
asserting ONLY `workflow.cross_ai_execution` gets a write call and
`cross_ai_command` sees none). `TestApplyReviewCli` (one case where
`config_get` returns an existing one-element list containing a DIFFERENT slug,
asserting the merged write contains BOTH the existing and the new slug, in a
deterministic order; one case where `config_get` returns `None`, asserting the
write is a fresh one-element list; every case asserts `review.effort.opencode`
and `workflow.plan_review_convergence` both get written regardless).
  </action>

  <verify>
    <automated>python3 -m unittest tests.test_ai_kit_gsd_config -v 2>&1 | tail -50</automated>
    <fails_when>the final summary line is not `OK`, or the output contains `FAILED`, `ERROR`, or `Traceback`.</fails_when>
    <automated>cd skills/ai-kit-gsd-config && python3 -c "from ai_kit_gsd_config import cross_ai_build as c; assert c.CLI_TO_REVIEWER_SLUG['cursor-agent'] == 'cursor'; assert c.CLI_TO_REVIEWER_SLUG['opencode'] == 'opencode'; print('SLUG_OK')"</automated>
    <fails_when>stdout does not contain the literal line `SLUG_OK`, or either `assert` raises first (the cursor-agent-to-cursor slug rename regressed).</fails_when>
    <automated>grep -rn "os\.path\.expanduser\|shutil\.which(" skills/ai-kit-gsd-config/ai_kit_gsd_config/cross_ai_build.py | grep -v '^#' | grep -c . || true</automated>
    <fails_when>the count is greater than `0` — `cross_ai_build.py` resolved an absolute executable path itself anywhere, which is exactly the D-05 landmine this plan exists to avoid (the bare command name must come only from `commands.py`'s own literal template strings, never from a local path-resolution call in this file).</fails_when>
  </verify>
  <done>
`apply-execution` always writes `workflow.cross_ai_execution: true`, and
writes `workflow.cross_ai_command` only when a real candidate and a working
builder both exist — never a fabricated or path-baked command.
`apply-review` always writes `workflow.plan_review_convergence: true` and
`review.effort.opencode: "high"`, and always (since the matcher never returns
no-match) writes a dedupe-merged `review.default_reviewers` plus
`review.models.<slug>` for the resolved candidate. All new/updated tests in
`tests/test_ai_kit_gsd_config.py` pass. `cross_ai_build.py` contains zero
local path-resolution calls.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Installed-CLI enumeration → preference matcher | `ai_kit_spec`'s `detect-runtimes` output is locally-sourced (reads the machine's own PATH/installed binaries) but still externally-influenced — a malicious or unexpected binary on PATH under a matched name feeds directly into what gets written into `workflow.cross_ai_command`. |
| `ai_kit_spec.commands` import boundary | This plan imports a LIVE CODE MODULE from a sibling skill at runtime (not just its CLI output) — a change to that module's `build_execute_command` signature or template shape propagates directly into this skill's generated command string. |
| Resolved config write → `.planning/config.json` | Same boundary as Plan 01 — writes go through gsd-tools' own `config-set`, never a direct file write. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-05 | Tampering | `build_execution_command`'s reliance on `ai_kit_spec.commands.build_execute_command`'s template strings | medium | mitigate | `cross_ai_build.py` never re-interpolates `target_dir`/`model` itself — all quoting happens inside the reused, already-live-verified builder; this plan's own tests assert the returned string has no literal `{model}` left unfilled, catching a future signature drift immediately rather than shipping a malformed command. |
| T-07-06 | Spoofing | A same-named-but-different binary earlier on PATH satisfying a preference-ladder substring match (e.g. a dev's local shim literally named `opencode`) | low | accept | Out of this skill's control — identical trust boundary to every other PATH-based CLI dispatch already used throughout this repo's existing `ai_kit_spec` pipeline (D-02's reuse target); no new exposure introduced here. |
| T-07-07 | Tampering | `review.default_reviewers` merge-read-write race (two concurrent skill runs) | low | accept | The skill is a single-operator, interactively-invoked CLI tool, never a background daemon — concurrent invocation against the same `.planning/config.json` is not a realistic usage pattern for this phase's scope. |

Package-legitimacy gate: not applicable — this plan installs no new package,
only imports an already-installed sibling skill's module at runtime.
</threat_model>

<verification>
Run `python3 -m unittest tests.test_ai_kit_gsd_config -v` — all tests pass,
including every new class from both tasks. Run the two slug/path-hygiene
`<automated>` checks above directly. Confirm `cli.py detect-execution-
candidate`, `detect-review-candidate`, `apply-execution`, `apply-review` all
exist as real argparse subcommands (`python3 ai-kit-gsd-config.py --help`
lists all four alongside Plan 01's `ensure-project`/`apply-profile`/
`apply-critical-agents`).
</verification>

<success_criteria>
- [ ] `model_detect.py` builds a CLI/model pool from `ai_kit_spec`'s own
      `detect-runtimes` output — no re-implemented detection logic (D-02).
- [ ] `preference_match.py`'s two ladders match the literal patterns and
      priority order from 07-CONTEXT.md's Specifics section exactly.
- [ ] `best_review_candidate` never returns a no-match signal.
- [ ] `cross_ai_command`, when written, is always a bare-command-name string
      (D-05) — confirmed by the AST/grep hygiene check finding zero local
      path-resolution calls.
- [ ] `review.default_reviewers`/`review.models.<slug>`/`review.effort.<slug>`
      are written as structured keys, never a constructed command string
      (D-04).
- [ ] All tests pass; `.planning/config.json` itself is never opened or
      written directly by any test or production code path in this plan.
</success_criteria>

<output>
Create `.planning/phases/07-curated-gsd-config-skill/07-02-SUMMARY.md` when done
</output>