---
phase: 06-agents-md-rules-checker-skill
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
cross_ai: true
autonomous: true
requirements: [REQ-agtmd-wrapper, REQ-agtmd-makefile-rules]
files_modified:
  - skills/ai-kit-agents-md-checker/ai_kit_agents_md_checker/remediation.py
  - skills/ai-kit-agents-md-checker/ai_kit_agents_md_checker/stack_cache.py
  - skills/ai-kit-agents-md-checker/ai_kit_agents_md_checker/cli.py
  - tests/test_ai_kit_agents_md_checker.py
  - skills/ai-kit-agents-md-checker/SKILL.md

estimate:
  tokens: 100000
  raw_tokens: 100000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "For a detected stack with no fresh `stack_cache` entry, the CLI's `cache-update` path lets an agent persist a research subagent's findings — structured as a dict keyed by names drawn from `stack_cache.ALL_TOOLING_KEYS` (`REQUIRED_STATIC_TARGETS` UNION `RESEARCH_CATEGORIES` — e.g. `\"setup-env\"`, `\"arch-test\"`, AND `\"formatter\"`, `\"security-scanner\"`), never a free-form key outside that combined set — and a subsequent `check`/`remediate` call against the same stack then reports `needs_research: False` with a real recommendation; the cache-with-staleness model D-04 requires is exercised end to end with a schema that actually round-trips through Plan 01's lookup, not just architected (D-04; fixes cross-AI review Round 1 HIGH: the research prompt previously asked for category names that could never satisfy Plan 01's target-name-keyed lookup, `06-REVIEWS.md` L69@02ca473; fixes cross-AI review Cycle 2 HIGH: the schema still could not hold the formatter/security/dead-code categories the research prompt itself asks for, `06-REVIEWS.md` Cycle 2 06-02 Concerns)."
    - "`cache-update` validates the parsed JSON BEFORE writing it to cache: the root must be a JSON object, every key must be a member of `stack_cache.ALL_TOOLING_KEYS`, and every value must be a non-empty string — an unknown key, a non-string value, or a non-object root is rejected with a one-line stderr error and exit 1, never silently cached and later breaking a `.get()` call or surfacing an unusable recommendation (fixes cross-AI review Cycle 2 HIGH: `cache-update` accepted structurally invalid JSON across the LLM-to-cache trust boundary, `06-REVIEWS.md` Cycle 2 06-02 Concerns)."
    - "The `cache-update` subcommand takes an explicit, documented test-only `--cache-root <path>` flag (never silent `os.environ`/monkeypatch reliance) so a test can assert `needs_research=False` without writing into the real `~/.cache` tree (fixes cross-AI review Round 1 MEDIUM, `06-REVIEWS.md` L72@02ca473)."
    - "The remediation payload built from a `check` report is ordered most-critical-first ACROSS BOTH `workflow` and `makefile` findings together (D-02, now possible because Plan 01's Cycle-2 fix gives every finding a `criticality`) and carries an `auto_apply` flag that is `True` for every `missing` finding (and every Makefile finding with a concrete, non-`None` `recommendation`, hook-derived, structural, or tooling-category) and `False` for every `near_miss` finding — never the reverse (D-03, D-08)."
    - "Every `remediate`-list entry near-miss confirmation (Step A, Task 2) can point at real evidence: `build_remediation()` preserves the `matched_signals` field Plan 01's `rule_checker.py` now attaches to every workflow finding, so the agent names the literal phrases already found in the target file, not just an id/status/summary triple with nothing to point at (fixes cross-AI review Cycle 2 HIGH: Step A asked for 'current near-miss evidence' that no payload field actually carried, `06-REVIEWS.md` Cycle 2 06-02 Concerns)."
    - "Invoking this skill is observably different from invoking plain `/agent-md-refactor`: the SKILL.md documents that this skill ALWAYS runs the house-rule check first and, only when the remediation payload is non-empty, feeds it into EXACTLY ONE `/agent-md-refactor` invocation as additional input to that skill's own Phase 2 (Identify the Essentials) — never a separate invocation per rule — whereas `/agent-md-refactor` alone never runs a house-rule check and has no house-rule input of its own (ROADMAP SC-3; fixes cross-AI review Round 1 HIGH: `/agent-md-refactor` is a 5-phase whole-file refactor with its own user-confirmation gate and no callable/per-rule insertion interface — confirmed by reading `~/.claude/skills/agent-md-refactor/SKILL.md` directly, not assumed — so ~16 separate full-refactor invocations was never a viable design, `06-REVIEWS.md` L70@02ca473)."
    - "SKILL.md instructs the invoking agent to paraphrase each rule's `summary` concisely when handing it to `/agent-md-refactor`, never to paste the todo file's rule text verbatim (D-01). D-01's 'programmatic invoke' is satisfied by a CONCRETE host-native skill-invocation contract: invoke `/agent-md-refactor` via whichever skill-invocation mechanism the current host provides (naming Claude Code's `Agent`/subagent-dispatch tool as ONE worked example, never a requirement), falling back explicitly to reading that skill's own SKILL.md instructions directly into context and following them manually on a host with no dispatch mechanism at all — never silently degrading, per AGENTS.md's own cross-host skill-portability rule (fixes cross-AI review Round 1 MEDIUM, `06-REVIEWS.md` L71@02ca473; fixes cross-AI review Cycle 2 MEDIUM: the handoff still named no concrete invocation mechanism, `06-REVIEWS.md` Cycle 2 06-02 Concerns)."
    - "SKILL.md's research-dispatch instructions describe dispatching a research sub-task host-neutrally (a subagent/Task-dispatch primitive if the host provides one — Claude Code's `Agent` tool is named as an example only — otherwise performing the research directly inline), and never hardcode a `~/.claude/...` path to `agent-md-refactor`'s SKILL.md — it is resolved via the same host-neutral candidate-list pattern `ai-kit-usage-metrics/SKILL.md`'s entrypoint-resolution section already establishes (fixes cross-AI review Cycle 2 HIGH: the generated SKILL.md hardcoded Claude-specific tool names and paths, conflicting with AGENTS.md's cross-host skill-portability rule, `06-REVIEWS.md` Cycle 2 06-02 Concerns)."
    - "Every `auto_apply: false` (near-miss) entry is confirmed or declined by the user BEFORE `/agent-md-refactor` is invoked at all — the confirm gate is owned by this wrapping skill itself, not repurposed from `/agent-md-refactor`'s own Phase 1 (which is about internal file contradictions, a different concern) (D-03, D-08)."
    - "SKILL.md instructs the invoking agent to present every finding inline in the conversation and never write a persisted report file into the target repo (D-05)."
    - "`make test`, `make lint`, and `make validate` are all green with the new module and SKILL.md content in place."
  artifacts:
    - skills/ai-kit-agents-md-checker/ai_kit_agents_md_checker/remediation.py
    - skills/ai-kit-agents-md-checker/SKILL.md
  key_links:
    - "`remediation.build_remediation(report)` reads `criticality`/`summary` off EVERY finding (both `workflow` and `makefile` kind now carry both, per Plan 01's Cycle-2 schema-unification fix — fixes cross-AI review Cycle 2 CRITICAL: this line previously claimed `makefile_checker.py` already attached these fields when it did not, `06-REVIEWS.md` Cycle 2 06-02 Concerns, confirmed by this orchestrator's own spot-check) plus each kind's own identifying/decision fields (`id`/`status` for `workflow`; `target`-or-`category`/`recommendation`/`needs_research`/`stacks` for `makefile`) — it adds no new lookup into `rules.py`."
    - "`cli.py`'s `cache-update` subcommand calls `stack_cache.write_stack_cache()` directly — the same atomic-write function Plan 01 wrote, not a second writer."
    - "SKILL.md's wrapping instructions name `cli.py`'s `check`, `remediate`, and `cache-update` subcommands by their literal invocation form, so the executing agent never has to guess the CLI surface from prose alone."
---

<objective>
Build the remediation payload (D-01/D-02/D-03/D-08) that turns a `check`
report into an ordered, auto-apply-annotated hand-off for
`/agent-md-refactor`; close D-04's cache loop by giving the CLI a
`cache-update` path a research subagent's findings can be written through;
and author the full `SKILL.md` that makes this skill's wrapping behavior —
not just its checking logic — concrete and distinct from plain
`/agent-md-refactor` (ROADMAP SC-3).

Purpose: Plan 01 proved the checker can name gaps and route to cached or
hook-derived recommendations. This plan proves the OTHER half of the
wrapper contract: that missing/near-miss findings become a real, ordered,
auto-apply-policy-tagged payload (REQ-agtmd-wrapper), and that the skill's
own documentation — not just its Python package — makes the wrap mechanism
and its D-05 inline-only reporting rule explicit to whichever agent invokes
it.

Output: `remediation.py`; `cli.py` gains `remediate` and `cache-update`
subcommands; a fully-authored (not stub) `skills/ai-kit-agents-md-checker/SKILL.md`
covering entrypoint resolution, the ruleset, the wrap mechanism, the
auto-apply/confirm split, and the inline-only reporting rule; green
`make test`/`make lint`/`make validate`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/ONESHOT-RULES.md
@AGENTS.md
@.planning/phases/06-agents-md-rules-checker-skill/06-CONTEXT.md
@.planning/phases/06-agents-md-rules-checker-skill/06-01-SUMMARY.md
</context>

<tracked_source_paths>
`skills/ai-kit-agents-md-checker/ai_kit_agents_md_checker/remediation.py` is
new (confirmed empty `git ls-files` result at planning time). Every other
path listed in `files_modified` was created by Plan 01 in this same phase
and is already tracked once Plan 01's commit lands; this plan only modifies
files that plan itself introduced — no install-mirror or gitignored path is
touched.
</tracked_source_paths>

<failing_direction_contract>
Every runnable `<automated>` command below is followed by a `<fails_when>`
sibling naming the observable output that constitutes failure.
</failing_direction_contract>

<source_audit>
## Multi-Source Coverage Audit (Phase 6, Plan 02 portion)

| # | Source | Item | Covered by |
|---|--------|------|-----------|
| SC-1 (remainder) | ROADMAP SC-1 | Per-language tool rec for static targets, sourced via real research, not invented | Task 1 (`cache-update`) |
| SC-3 | ROADMAP SC-3 | Observably distinct from plain `/agent-md-refactor` | Task 2 |
| REQ-2 | REQUIREMENTS.md | REQ-agtmd-wrapper | Task 1 (payload), Task 2 (SKILL.md wrap narrative) |
| REQ-1 (remainder) | REQUIREMENTS.md | REQ-agtmd-makefile-rules (research-dispatch half of D-04) | Task 1 |
| D-01 | 06-CONTEXT.md | Feed missing rules into `/agent-md-refactor` programmatically, paraphrased; costly reversibility | Task 2 |
| D-02 | 06-CONTEXT.md | Most-critical-first insertion ordering | Task 1 (`build_remediation` ordering) |
| D-03 | 06-CONTEXT.md | Near-miss is recommend/warn only, never forced rewrite | Task 1 (`auto_apply=False` for `near_miss`) |
| D-04 (remainder) | 06-CONTEXT.md | Research-subagent dispatch + cache write, closing the loop Plan 01 architected | Task 1 |
| D-05 | 06-CONTEXT.md | Findings presented inline only, never a persisted report file | Task 2 |
| D-08 | 06-CONTEXT.md | Auto-apply clear gaps, confirm-only for near-miss; reversible | Task 1 (flag), Task 2 (SKILL.md documents the confirm gate) |

**Deferred to Plan 03 (not a gap here):** `/skill-judge` review loop,
`/naming-analyzer` final naming and rename, README row.
</source_audit>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Remediation payload builder and the stack-cache research-dispatch write path</name>
  <files>skills/ai-kit-agents-md-checker/ai_kit_agents_md_checker/remediation.py, skills/ai-kit-agents-md-checker/ai_kit_agents_md_checker/stack_cache.py, skills/ai-kit-agents-md-checker/ai_kit_agents_md_checker/cli.py, tests/test_ai_kit_agents_md_checker.py</files>

  <read_first>
    - `skills/ai-kit-agents-md-checker/ai_kit_agents_md_checker/rule_checker.py`, `makefile_checker.py`, `stack_cache.py` — the exact finding-dict shapes this task consumes and the `get_stack_tooling`/`write_stack_cache` signatures Plan 01 already committed; do not redefine a competing shape.
    - `06-CONTEXT.md` D-01 through D-04 and D-08, and Claude's Discretion note on the cache file format.
    - `skills/ai-kit-spec-review/SKILL.md` lines ~164-310 (Step 1 dispatch section) — this repo's own convention for describing a subagent dispatch inside a `SKILL.md` (the `Agent` tool, `subagent_type: general-purpose`); Task 2 of this plan writes the prose version of this for the research dispatch, this task only needs to know the shape of what that prose will call into (a CLI write path, not a Python-level dispatch call — dispatching an LLM subagent is not something this task's unit tests invoke).
  </read_first>

  <behavior>
    - `build_remediation(report)` returns a DICT `{"remediate": [...], "blocked": [...]}`, never a bare list (fixes cross-AI review Cycle 2 MEDIUM: this behavior block previously described a bare-list return while the action's own implementation contract returned the dict shape, `06-REVIEWS.md` Cycle 2 06-02 Concerns). Against a report with one `missing` R01 finding (criticality 1) and one `near_miss` R17 finding (criticality 3), `build_remediation(report)["remediate"]` is ordered `[R01, R17]`, with `R01["auto_apply"] is True` and `R17["auto_apply"] is False`.
    - `build_remediation(report)["remediate"]` interleaves `workflow` and `makefile` findings in ONE combined criticality-ascending order (e.g. a `makefile` gap and a `workflow` gap both at `CRITICALITY_CRITICAL` sort ahead of a `CRITICALITY_HIGH` `workflow` finding) — now possible because Plan 01's Cycle-2 fix gives every `makefile` finding a `criticality` too, not just `workflow` findings.
    - `build_remediation(report)` never includes a `makefile`-or-category finding whose `needs_research` is `True` and has no `recommendation` — a research-blocked gap is surfaced separately under a `blocked` key, carrying forward Plan 01's `"stacks"` list verbatim (every detected stack still lacking a usable cache entry, sorted — never collapsed to one), not silently folded into the same auto-apply list as a workflow-rule gap.
    - `build_remediation(report)` includes a Plan 01 `validate`-chain-incomplete or `test-unit`-not-aliased finding (which carries `present: True` but a concrete `recommendation`) in the SAME ordered `remediate` list with `auto_apply=True` — the `present` field is never consulted by this function, only `recommendation`/`needs_research`.
    - Every `remediate` entry built from a `workflow` finding carries a `matched_signals` field copied from that finding (the literal phrases Plan 01's `rule_checker.py` found in the target text), so a near-miss confirmation has real evidence to name.
    - After `cli.py cache-update python --cache-root <scratch> <path-to-json>` (writing valid tooling data keyed by names from `stack_cache.ALL_TOOLING_KEYS`), a subsequent `get_stack_tooling("python", cache_root=<same scratch>)` returns `needs_research=False` and a recommendation for each key present in the written JSON.
    - `cli.py cache-update python --cache-root <scratch> <path-to-json>` against a JSON file containing an unknown key (not in `stack_cache.ALL_TOOLING_KEYS`), a non-string value, or a JSON array instead of an object exits 1 with a one-line stderr error and writes NOTHING to the cache file.
  </behavior>

  <action>
Implement `remediation.py`: `build_remediation(report)` takes the
`{"makefile": [...], "workflow": [...]}` shape `cli.check()` returns and
builds ONE combined, criticality-ordered `remediate` list plus a `blocked`
list — fixing cross-AI review Cycle 2 CRITICAL: a prior draft of this
function's contract assumed every finding already carried
`criticality`/`status`/`summary`, which was true for `workflow` findings but
NOT for Plan 01's `makefile` findings; Plan 01's Cycle-2 fix now attaches
`criticality`/`summary` to every `makefile` finding too (static-target,
hook-derived, chain/alias, and tooling-category), so this function finally
has one real, consistent contract to read (`06-REVIEWS.md` Cycle 2 06-02
Concerns, confirmed by this orchestrator's own spot-check of
`06-01-PLAN.md`/`06-02-PLAN.md`). For `workflow` findings with `status` in
`("missing", "near_miss")` (skip `"not_applicable"` and any `"present"`
entries a caller passed with `include_present=True`), build an entry
`{"kind": "workflow", "id": finding["id"], "status": finding["status"],
"criticality": finding["criticality"], "summary": finding["summary"],
"matched_signals": finding.get("matched_signals", []), "auto_apply":
finding["status"] == "missing"}` — D-08's auto-apply-missing/confirm-
near-miss split, made a structural field instead of a comment, and
`matched_signals` carried forward so a near-miss confirmation (SKILL.md
Step A) has real evidence to name instead of an id/status/summary triple
with nothing to point at (fixes cross-AI review Cycle 2 HIGH, `06-REVIEWS.md`
Cycle 2 06-02 Concerns). For `makefile`-kind findings (both `"target"`- and
`"category"`-keyed — this function treats them identically, branching ONLY
on `recommendation`/`needs_research`, never on `present` or on which
identifying key is set): any finding with a non-`None` `recommendation`
builds an entry `{"kind": "makefile", "id": finding.get("target",
finding.get("category")), "criticality": finding["criticality"], "summary":
finding["summary"], "auto_apply": True}` (a concretely-recommended gap —
whether a missing target, an incomplete `validate` chain, a missing alias,
or a tooling-category gap — is exactly as "clearly actionable" as a
workflow rule with a known fix, D-08's own rationale, extended
consistently); any finding with `needs_research=True` and no recommendation
goes into a separate `blocked` list, carrying its `criticality`/`summary`
plus its `target`-or-`category` and `"stacks"` list (Plan 01's sorted list
of every stack still lacking a usable cache entry) forward verbatim — these
must never be handed to `/agent-md-refactor` as if they were actionable,
because there is nothing concrete to insert yet. Build BOTH kinds' entries
into the SAME `remediate` list, then sort that ONE list by `criticality`
ascending (D-02, now spanning workflow AND makefile findings together,
not two separately-ordered lists). Return `{"remediate": [...ordered...],
"blocked": [...]}` — a dict, never a bare list.

Extend `cli.py`: `remediate(repo_root, agents_md_path=None)` calls
`check(...)` then `remediation.build_remediation(...)`, returns the result.
Add the `remediate` subcommand to `main`'s dispatch dict (`argv[1]` is
`repo_root`, same convention Plan 01 established for `check`), printing
`json.dumps(remediate(...), indent=2)`. Add a `cache-update` subcommand
taking positional args (`stack`, `json_path`) PLUS an explicit,
always-available `--cache-root <path>` option (parsed by hand — no
`argparse` — scan `argv` for the literal token `"--cache-root"` and consume
the following element; default `None`, meaning `stack_cache.CACHE_ROOT`):
reads `json_path` as JSON; VALIDATES it before writing (fixes cross-AI
review Cycle 2 HIGH: `cache-update` previously accepted structurally
invalid JSON across the LLM-to-cache trust boundary, `06-REVIEWS.md` Cycle 2
06-02 Concerns) — the parsed value must be a `dict` (reject a list or
scalar), every key must be a member of `stack_cache.ALL_TOOLING_KEYS`
(reject any other key), and every value must be a non-empty `str` (reject
`None`/numbers/nested objects); on ANY validation failure, print a one-line
error to stderr naming which check failed and return 1, writing nothing. On
valid input, calls `stack_cache.write_stack_cache(stack, data,
cache_root=cache_root)`, prints a one-line confirmation naming the stack and
the resolved cache file path, returns 0; on a missing/unreadable `json_path`
or syntactically invalid JSON (fails to parse at all), prints a one-line
error to stderr and returns 1 rather than raising a traceback. Document
`--cache-root` in `cache-update`'s own docstring as a TEST-ONLY escape hatch
— never mentioned to an end user in `SKILL.md` (Task 2), which always
invokes `cache-update` without it — resolving cross-AI review Round 1
MEDIUM's "your choice" ambiguity with one definite answer rather than
leaving it open (`06-REVIEWS.md` L72@02ca473). This is the path `SKILL.md`
(Task 2) instructs an agent to call after a research subagent returns its
findings structured as a dict keyed by `stack_cache.ALL_TOOLING_KEYS` names,
so it must fail predictably, never crash a wrapping conversation.

Add to `tests/test_ai_kit_agents_md_checker.py`: `TestRemediation`
covering the `build_remediation` behaviors above: the combined
cross-kind criticality ordering (a `makefile` finding and a `workflow`
finding interleaved by `criticality`, not two separately-ordered lists);
`auto_apply` correctness for `missing`/`near_miss`; `matched_signals`
carried forward onto a `workflow` entry; the `present: True`-but-recommended
structural finding flowing into `remediate` not `blocked`; and the `blocked`
split preserving `"stacks"` using a `makefile` finding with
`needs_research: True, recommendation: None, stacks: ["go", "python"]`.
`TestCacheUpdateCLI`: write a scratch JSON tooling file keyed by names from
`stack_cache.ALL_TOOLING_KEYS` (e.g. `{"setup-env": "...", "formatter":
"..."}`), invoke `cli.main(["cache-update", "python", <scratch-json-path>,
"--cache-root", <scratch-root>])`, assert return code 0, then call
`stack_cache.get_stack_tooling("python", cache_root=<same scratch root>)`
and assert `needs_research is False` and the returned tooling has both the
`"setup-env"` and `"formatter"` keys. `TestCacheUpdateValidation`: three
cases — a JSON array instead of an object, an unknown key not in
`ALL_TOOLING_KEYS`, and a non-string value — each asserted to exit 1 with a
stderr message and to leave the target cache file absent/unwritten.
  </action>

  <verify>
    <automated>python3 -m unittest tests.test_ai_kit_agents_md_checker -v</automated>
    <fails_when>any test errors or fails (non-zero exit, "FAILED" or "ERROR" in output)</fails_when>
  </verify>

  <reversibility rating="reversible">
    D-08's auto-apply-vs-confirm split is a plain boolean field on a
    structured payload. Changing the policy later (e.g. requiring
    confirmation for `missing` too) is a one-line change to
    `build_remediation`'s `auto_apply` expression, not a migration.
  </reversibility>

  <done>
    `build_remediation` produces a criticality-ordered, auto-apply-flagged
    payload that cleanly separates research-blocked Makefile gaps from
    actionable ones, and `cache-update` lets a research subagent's findings
    close the D-04 cache loop end to end.
  </done>
</task>

<task type="auto">
  <name>Task 2: Author the full SKILL.md — wrap mechanism, inline-only reporting, distinctness from plain agent-md-refactor</name>
  <files>skills/ai-kit-agents-md-checker/SKILL.md</files>

  <read_first>
    - `skills/ai-kit-agents-md-checker/SKILL.md` (the Plan 01 stub) — replace its body, keep the provisional-name note.
    - `~/.claude/skills/agent-md-refactor/SKILL.md` — the skill this SKILL.md wraps; read it directly (ground truth, not assumed) and confirm two facts before writing the wrap section: (1) its actual Phase names (Find Contradictions -> Identify the Essentials -> Group the Rest -> Create the File Structure -> Flag for Deletion), and (2) it is a Markdown-only, prose-instructed skill with NO CLI, NO callable/importable interface, and NO per-rule "insert this one thing" mode — it is a single whole-file refactor pass whose own Phase 1 ends with "Ask the user to resolve before proceeding" on CONTRADICTIONS specifically (not missing/near-miss house rules). Design the wrap mechanism around this real shape (fixes cross-AI review Round 1 HIGH: the prior design assumed a per-rule insertion API and ~16 separate invocations, neither of which the real skill supports, `06-REVIEWS.md` L70@02ca473). Do NOT hardcode this `~/.claude/...` path into the SKILL.md text being written — see the next bullet.
    - `skills/ai-kit-usage-metrics/SKILL.md`'s "Resolve the entrypoint" section — the exact host-neutral candidate-list shape (checking `$HOME/.claude/skills/...`, `${XDG_CONFIG_HOME:-$HOME/.config}/opencode/skills/...`, `$HOME/.agents/skills/...` in turn) to mirror TWICE in this task: once for this skill's own entrypoint, and once for locating `agent-md-refactor/SKILL.md` itself — `AGENTS.md`'s own "Skills under `skills/` follow the Agent Skills open standard so they work unmodified across hosts... flag the gap explicitly instead of silently degrading" rule forbids hardcoding a single-host path for either (fixes cross-AI review Cycle 2 HIGH: the generated SKILL.md hardcoded a `~/.claude/skills/agent-md-refactor/SKILL.md` path and Claude-specific `Agent` tool/`subagent_type` wording, `06-REVIEWS.md` Cycle 2 06-02 Concerns).
    - `skills/ai-kit-spec-review/SKILL.md` lines ~25-45 ("Step 0 — Persist before dispatch") and ~164-230 (dispatch shape) — this project's own precedent for describing a subagent dispatch from inside a SKILL.md; note it names Claude Code's `Agent` tool (`subagent_type: general-purpose`) as the NATIVE-dispatch branch of a two-branch design that also has a separate external-CLI branch for non-native cases — cite it here as ONE worked example of a dispatch mechanism, never as a requirement every host must support, and write this task's research-dispatch instructions with an explicit portable fallback (read `agent-md-refactor`'s own instructions into context and follow them directly, or perform the stack research inline, when no subagent-dispatch mechanism exists) rather than assuming the `Agent` tool is universal.
    - `06-CONTEXT.md` D-01, D-03, D-05, D-08 in full.
  </read_first>

  <action>
Write `SKILL.md`'s frontmatter: `name: ai-kit-agents-md-checker`
(provisional — restate the Plan 03 naming note), `description:` third-person,
starting "Use when...", naming concrete triggers (checking an AGENTS.md/
CLAUDE.md file against a house ruleset, auditing a project's Makefile target
shape, "is my AGENTS.md missing any workflow rules") — never summarizing the
skill's process (writing-skills' own Skill Discovery Optimization rule).

Body sections, in order: (1) **Scope** — one paragraph stating this skill
checks a target repo's AGENTS.md/CLAUDE.md-style file and Makefile/
`.pre-commit-config.yaml` against ai-kit's own 17-rule house ruleset
(`rules.py`), and that the 17 rules themselves are NOT a general-purpose
rules engine for other projects (D-06) — they are ai-kit's own accumulated
convention, applied to whichever repo is being checked. (2) **Resolve the
entrypoint** — the host-neutral candidate-list bash snippet, adapted from
`ai-kit-usage-metrics/SKILL.md`, pointing at
`ai-kit-agents-md-checker.py`. (3) **Run the check** — the literal
invocation `python3 <entrypoint> check <repo_root>` and
`... remediate <repo_root>`, with one real example of the JSON shape each
returns (trimmed, not the full 17-rule dump). (4) **Wrap mechanism (what
makes this different from plain `/agent-md-refactor`)** — state explicitly,
citing ROADMAP SC-3 in spirit (not by literal citation text, just the
requirement): invoking plain `/agent-md-refactor` only runs its own five-
phase structural refactor and has no house-rule awareness; invoking THIS
skill always runs `check`/`remediate` FIRST, and only when `remediate`'s
payload is non-empty does it proceed toward `/agent-md-refactor`. Because
`/agent-md-refactor` has no CLI/callable/per-rule-insertion interface (it is
a single Markdown-only, whole-file refactor pass — confirmed above, not
assumed), this skill invokes it EXACTLY ONCE per wrapping run, never once
per rule. Document the hand-off precisely per D-01 as a three-step sequence:
**Step A (confirm near-misses first, owned by THIS skill):** for every
entry in `remediate` with `auto_apply: false` (a `near_miss`), STOP and ask
the user to confirm or decline individually — name the rule id, its current
near-miss evidence (the entry's own `matched_signals` list — the literal
phrases Plan 01's checker already found in the target file, never an
unsupported "trust me" claim; fixes cross-AI review Cycle 2 HIGH: this step
previously asked for evidence no field in the payload actually carried,
`06-REVIEWS.md` Cycle 2 06-02 Concerns), and what would be inserted if
confirmed (D-03/D-08).
This confirmation is asked BEFORE `/agent-md-refactor` is invoked at all,
and is owned entirely by this wrapping skill — it is deliberately NOT
routed through `/agent-md-refactor`'s own Phase 1 ("Find Contradictions"),
because that phase exists for a different concern (conflicting instructions
already in the file), not for confirming whether to insert a missing house
rule; conflating the two would misuse a gate that isn't shaped for this
question. **Step B (assemble one ordered list):** build the FINAL ordered
list = every `auto_apply: true` entry, plus every user-confirmed near-miss
entry, in the same most-critical-first order `remediate` already returned
(D-02) — declined near-miss entries are dropped and reported inline as
skipped (D-05). **Step C (one invocation, named injection point):** invoke
`/agent-md-refactor` against the target file EXACTLY ONCE, explicitly
telling the invoking agent to treat the Step B list's `summary` strings (the
paraphrase already in the payload — never the todo file's verbatim rule
text, D-01) as ADDITIONAL "essential content" candidates for that skill's
OWN Phase 2 ("Identify the Essentials") and Phase 3 ("Group the Rest"),
preserving the list's most-critical-first order as the priority if Phase 2
has to choose what earns root-file placement — this is what makes D-01's
"programmatic invoke, not just documentation of a two-step workflow"
concrete without a callable interface: the hand-off names an exact
injection point and exact ordering, not a vague "then run
agent-md-refactor" instruction (fixes cross-AI review Round 1 HIGH + MEDIUM,
`06-REVIEWS.md` L70-71@02ca473). State this as a CONCRETE host-native
skill-invocation contract, not prose-only (fixes cross-AI review Cycle 2
MEDIUM: D-01 remained only a prose description with no concrete invocation
mechanism, `06-REVIEWS.md` Cycle 2 06-02 Concerns): invoke `/agent-md-refactor`
via WHICHEVER skill-invocation mechanism the current host provides for
invoking an already-installed skill it knows about (a slash command, a
named subagent/Task dispatch, or loading that skill's own SKILL.md
instructions directly into context — Claude Code's `Agent` tool with
`subagent_type: general-purpose` is named here as ONE worked example, never
a requirement); resolve `agent-md-refactor/SKILL.md`'s own location with the
SAME host-neutral candidate-list pattern used for this skill's own
entrypoint (Section 2), never a hardcoded `~/.claude/...` path. The PORTABLE
FALLBACK, stated explicitly per `AGENTS.md`'s cross-host skill-portability
rule rather than silently degraded: on a host with no subagent-dispatch
mechanism available at all, read `agent-md-refactor/SKILL.md`'s instructions
directly into context and follow them manually for this one invocation.
There is NO Python import or subprocess call into `agent-md-refactor`
anywhere — it has no such interface; the hand-off is necessarily a
documented instruction sequence for whichever agent is executing this
skill, never code. For the `blocked` list (Makefile/tooling-category gaps
with `needs_research: true`), document the D-04 research-dispatch step,
deferred to a LATER, separate invocation of this wrapping skill (blocked
entries have no concrete content yet, so they are never fed into the Step
C `/agent-md-refactor` call): dispatch ONE independent research sub-task per
distinct stack named in `blocked` entries' `"stacks"` lists — on a host that
provides a subagent/Task-dispatch primitive, use it (Claude Code's `Agent`
tool, `subagent_type: general-purpose`, is named as an example, never a
requirement); on a host with no such primitive, perform the research
directly in the current conversation instead of assuming one exists (fixes
cross-AI review Cycle 2 HIGH: the generated SKILL.md hardcoded the `Agent`
tool/`subagent_type` as THE mechanism, conflicting with `AGENTS.md`'s
cross-host skill-portability rule, `06-REVIEWS.md` Cycle 2 06-02 Concerns).
The prompt/task asks it to research, for that specific stack, current (not
training-data-stale — cite rule 14's spirit) recommendations for: a
formatter, a security-leak scanner, a code-smell/duplicate-code detector, a
dead-code detector, unit/integration/e2e/architecture-test tooling, which
checks are cheapest/fail-fastest (for `validate`'s ordering), and the
pre-commit-vs-pre-push split — and instructs it to return its findings as a
**flat JSON object keyed by names drawn from
`stack_cache.ALL_TOOLING_KEYS`** (`REQUIRED_STATIC_TARGETS` for a category
that maps 1:1 onto an existing/needed Makefile target, e.g. `"setup-env"`,
`"arch-test"`; `RESEARCH_CATEGORIES` for one that does not, e.g.
`"formatter"`, `"security-scanner"`, `"dead-code-detector"`), e.g.
`{"setup-env": "<tool + install command>", "arch-test": "<tool +
invocation>", "security-scanner": "<tool + invocation>", ...}` — never a
free-form key outside that combined set. Show the FULL combined key list
(all 7 `REQUIRED_STATIC_TARGETS` plus all 7 `RESEARCH_CATEGORIES`) in the
prompt template itself so the research sub-task cannot guess wrong (fixes
cross-AI review Round 1 HIGH: the prior prompt asked for category names
that could never satisfy the target-name-keyed cache lookup,
`06-REVIEWS.md` L69@02ca473; fixes cross-AI review Cycle 2 HIGH: even after
that fix the schema still had no field for the formatter/security/dead-code
categories the prompt itself asked for, `06-REVIEWS.md` Cycle 2 06-02
Concerns). Then write the findings to a scratch JSON file under `./tmp/`
(this repo's own rule 10 convention, applied to whichever repo is running
the check) and call `cache-update <stack> <that file>` to persist it (which
now also validates the JSON against `ALL_TOOLING_KEYS` before writing, per
Task 1); re-running `remediate` afterward (same or a later invocation) now
picks up the fresh recommendation. (5) **Reporting rule (D-05)** — one explicit,
unambiguous sentence: findings and remediation results are reported INLINE
in the conversation only; this skill never writes a report file into the
target repo. (6) **Ruleset reference** — a short pointer to `rules.py` as
the source of truth for the 17 rules, not a restated copy of their text
(avoids the exact duplication problem D-06 exists to prevent).

Keep the whole document readably close to `writing-skills`'s
"frequently-loaded... under 500 words" guidance for a non-`getting-started`
skill, but the word count is secondary to correctness: if Plan 03's
`/skill-judge` loop later requires expanding a section to resolve a
Critical/Important finding, the quality bar wins — do not under-document
the wrap mechanism or the D-04 research-prompt key schema merely to hit a
word target (fixes cross-AI review Round 1 LOW: word-budget vs. quality-gate
tension was unaddressed, `06-REVIEWS.md` L74@02ca473). Reference
`agent-md-refactor/SKILL.md` (resolved via the host-neutral candidate-list
pattern, never a hardcoded `~/.claude/...` path — see above) and this
project's own `rules.py` rather than re-explaining either.
  </action>

  <verify>
    <automated>python3 -c "
import re, sys
text = open('skills/ai-kit-agents-md-checker/SKILL.md', encoding='utf-8').read()
lower = text.lower()
assert text.startswith('---')
assert 'agent-md-refactor' in text
assert 'inline' in lower
assert 'auto_apply' in text or 'auto-apply' in lower
assert 'exactly once' in lower, 'wrap mechanism must state agent-md-refactor is invoked exactly once, never per-rule'
assert 'identify the essentials' in lower, 'wrap mechanism must name the real Phase 2 injection point'
assert 'all_tooling_keys' in lower, 'research-dispatch prompt must cite the combined static-target + research-category cache-key schema'
assert 'host-native' in lower or 'host-neutral' in lower, 'D-01 invocation contract must be stated host-concretely, not Claude-only prose'
assert 'portable fallback' in lower or 'no subagent-dispatch' in lower, 'research-dispatch and agent-md-refactor invocation must document an explicit fallback for a host with no dispatch mechanism'
print('ok')
"</automated>
    <fails_when>the script raises `AssertionError` or a non-zero exit (frontmatter missing, or the document omits the agent-md-refactor hand-off, the inline-only reporting rule, the auto-apply/confirm split, the single-invocation wrap mechanism, the real Phase 2 injection point, the extended cache-key schema, or the host-native-with-portable-fallback invocation contract)</fails_when>
  </verify>
  <verify>
    <automated>make test && make lint && make validate</automated>
    <fails_when>any of the three exits non-zero, or `make test`'s output contains "FAILED", or `make validate`'s pre-commit output contains "Failed" for any hook</fails_when>
  </verify>

  <reversibility rating="costly">
    D-01's wrap mechanism (check-then-conditionally-feed-agent-md-refactor,
    confirm-near-misses-first, ONE `/agent-md-refactor` invocation per
    wrapping run with the ordered payload injected into that skill's own
    Phase 2, most-critical-first) is the architectural contract the whole
    skill is built around — corrected in this round against
    `/agent-md-refactor`'s real, verified (no-CLI, single-pass) shape rather
    than the previously-assumed per-rule insertion API. Changing how the
    checker and `/agent-md-refactor` hand off to each other later means
    re-plumbing this section and every caller of `remediate()`'s payload
    shape, not a local wording tweak — per 06-CONTEXT.md D-01's own
    reversibility note.
  </reversibility>

  <done>
    `SKILL.md` fully documents entrypoint resolution, the check/remediate
    CLI surface, the confirm-first/single-invocation wrap-into-
    agent-md-refactor mechanism with its auto-apply/confirm split, its
    `matched_signals`-backed near-miss evidence, and its named Phase 2
    injection point, a CONCRETE host-native invocation contract for
    `/agent-md-refactor` (with an explicit portable fallback for a host with
    no subagent-dispatch mechanism — never a silently Claude-only
    assumption), the D-04 research-dispatch step (with its full
    `ALL_TOOLING_KEYS` cache-key schema covering both static targets and
    tooling categories) for blocked gaps, and the inline-only reporting rule
    — and `make test`/`make lint`/`make validate` are all green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| checker output -> LLM agent -> `/agent-md-refactor` | The remediation payload's `summary` strings are ai-kit's own canonical text (from `rules.py`), but the `blocked`/Makefile `recommendation` strings and any research-subagent output cached via `cache-update` are LLM-generated text that gets surfaced to the user and, indirectly, shapes what gets inserted into a target file |
| research subagent -> stack cache -> future invocations | A cache entry written once is reused, unreviewed, by every later `check`/`remediate` call against that stack until it goes stale |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-04 | Tampering | stack-research cache poisoning (a bad/malicious tool recommendation gets cached and silently reused) | medium | mitigate | Cache entries are informational tool-NAME recommendations only — `cache-update`/`get_stack_tooling` never execute, install, or shell out to anything they return; a recommendation is always presented inline to a human-in-the-loop before any install/setup-env action is taken, and SKILL.md's Step C never instructs auto-running a recommended tool. |
| T-06-05 | Elevation of Privilege | `auto_apply: true` entries are included in the single `/agent-md-refactor` invocation without a per-change confirmation gate | medium | accept | Deliberate, user-locked departure from PROJECT.md's "every config write is atomic and explicitly confirmed" Core Value (D-08) — that Core Value is scoped to runtime AI-CLI config files (opencode.jsonc, Codex TOML, Claude JSON), a different artifact class from a git-tracked, human-reviewable, revertible documentation file. D-08 itself is rated `reversible`. Every `near_miss` entry still gets an explicit Step A confirmation before it ever reaches the Step C invocation. |
| T-06-06 | Tampering | Prompt injection: target AGENTS.md content could contain text shaped like an instruction, read by the checker and then, indirectly, by the agent orchestrating `/agent-md-refactor` | high | mitigate | SKILL.md's Step C hand-off names only the Step B ordered list's `summary` strings as the additional-essentials content fed into `/agent-md-refactor`'s Phase 2 — the target file's raw text is used ONLY as classification input (Plan 01's keyword match), never copied into an instruction to another skill/tool. Nothing in the target file can become something `/agent-md-refactor` is told to DO. A `workflow` finding's `summary` is always `rules.py`'s own canonical text; a `makefile` finding's `summary` can embed a literal fragment of the target repo's OWN `.pre-commit-config.yaml` `entry:` command (a hook-derived recommendation) — this is still only ever surfaced as inert display/insertion text for `/agent-md-refactor`'s Phase 2, never executed, shelled out to, or installed by this checker (same non-execution guarantee T-06-04 states for cached tooling recommendations). |
| T-06-07 | Information Disclosure | Inline-only reporting (D-05) vs. a persisted report file | low | accept | Already the design: this skill never writes a findings report into the target repo, closing the leak vector a persisted artifact would otherwise create. |

No package-manager install tasks exist in this plan (no new third-party
dependency is added), so the Package Legitimacy Gate does not apply.
</threat_model>

<verification>
- `python3 -m unittest tests.test_ai_kit_agents_md_checker -v` is green.
- `make test`, `make lint`, `make validate` are all green.
- `SKILL.md` documents the confirm-first/single-invocation wrap mechanism
  (Steps A/B/C), the auto-apply/confirm split, the `matched_signals`-backed
  near-miss evidence, the D-04 research-dispatch step and its full
  `ALL_TOOLING_KEYS` cache-key schema, the host-native-with-portable-fallback
  invocation contract, and the inline-only reporting rule, confirmed by the
  structural grep check, not by eyeballing the diff.
- `cache-update` rejects structurally invalid JSON (non-object root, unknown
  key, non-string value) before ever writing to cache, proven by
  `TestCacheUpdateValidation`.
</verification>

<success_criteria>
- ROADMAP Phase 6 Success Criterion 3 is satisfied: invoking this skill is
  documented as observably distinct from invoking plain `/agent-md-refactor`.
- REQ-agtmd-wrapper is fully implemented at the payload + documentation
  level (skill-judge/naming/final packaging remain for Plan 03).
- D-01, D-02, D-03, D-04, D-05, D-08 are all implemented, not just recorded
  as intent.
</success_criteria>

<output>
Create `.planning/phases/06-agents-md-rules-checker-skill/06-02-SUMMARY.md` when done
</output>

## Review Dispositions Ledger

### Round 1 — 02ca473

### Review Feedback Addressed

| Concern | Severity | How Addressed |
|---------|----------|----------------|
| D-04 research prompt asked for category names (formatter/scanner/...), which can never satisfy Plan 01's target-name-keyed cache lookup (L69) | HIGH | Task 1's `cache-update` section and Task 2's research-dispatch prompt now both require a flat JSON object keyed by the exact `stack_cache.REQUIRED_STATIC_TARGETS` names, shown verbatim in the prompt template the subagent receives. |
| `/agent-md-refactor` is a 5-phase whole-file refactor with its own user-confirmation gate and no CLI/per-rule insertion interface — running it ~16 times autonomously risks each pass pruning the previous insert (L70) | HIGH | Task 2's wrap mechanism redesigned: `/agent-md-refactor` is invoked EXACTLY ONCE per wrapping run; the ordered, confirmed remediation list is injected as additional "essentials" input into that skill's own Phase 2 ("Identify the Essentials")/Phase 3 ("Group the Rest"), never as 16 separate full-refactor passes. Near-miss confirmation (Step A) happens BEFORE the single invocation and is owned by this wrapping skill, not repurposed from `/agent-md-refactor`'s own Phase 1. |
| D-01 "programmatic invoke" vs. `/agent-md-refactor` being Markdown-only with no callable interface — the plan never said so explicitly (L71) | MEDIUM | Task 2's action now states explicitly, in the wrap-mechanism section itself, that there is no Python import/subprocess call into `agent-md-refactor` — `SKILL.md` (natural-language invocation) is the only interface it has; the "programmatic" half of D-01 is satisfied by the structured, ordered, auto-apply-flagged data hand-off into a named phase, not a function call. |
| `cache-update` vs. default `CACHE_ROOT` — test isolation left as "your choice" (L72) | MEDIUM | Resolved with one definite answer: `cache-update` takes an explicit, always-available, test-only `--cache-root` flag, documented as such in its own docstring and never mentioned to end users in SKILL.md. |
| Multi-stack `blocked` entries — "the stack it needs research for" was unspecified when multiple stacks match (L73) | MEDIUM | `build_remediation`'s `blocked` entries now carry forward Plan 01's sorted `"stacks"` list verbatim; the research-dispatch step dispatches one subagent per stack named in that list, never an arbitrary single pick. |
| Word-count guidance ("under 500 words") vs. the full research prompt + wrap contract risked under-documentation before the real gate (skill-judge) runs (L74) | LOW | Task 2's action now states explicitly that the quality bar (skill-judge, Plan 03) wins over the word-count guidance if the two conflict. |

### Review Feedback Deferred

| Concern | Reason |
|---------|--------|
| 06-03's rename/skill-judge loop cannot repair 06-01/06-02 contract bugs (L98) | Out of this plan's scope — this round fixes the underlying 06-01/06-02 bugs directly; 06-03's own ledger re-sequences its rename step to re-verify the now-fixed plans first. |

### Round 2 — Cycle 2 review (openai/gpt-5.6-sol fallback)

### Review Feedback Addressed

| Concern | Severity | How Addressed |
|---------|----------|----------------|
| CRITICAL field-schema mismatch: `build_remediation()` assumed every finding carried `criticality`/`status`/`summary`, but Plan 01's Makefile findings did not | CRITICAL | Fixed at the source in Plan 01 (every `makefile_checker.py` finding now carries `criticality`/`summary` too); `build_remediation()`'s own text and frontmatter `key_links` rewritten to describe the now-accurate, unified contract and to build ONE combined criticality-ordered list spanning both finding kinds. |
| Near-miss confirmation (Step A) requests "current near-miss evidence" that no payload field carried | HIGH | `rule_checker.py` (Plan 01) now attaches `matched_signals` to every workflow finding; `build_remediation()` carries it forward onto each `remediate` entry; Step A now names `matched_signals` as the concrete evidence to present. |
| `cache-update` accepted structurally invalid JSON across the LLM-to-cache trust boundary | HIGH | `cache-update` now validates the parsed JSON is an object, every key is in `stack_cache.ALL_TOOLING_KEYS`, and every value is a non-empty string, before writing — rejecting otherwise with a one-line stderr error and exit 1. Proven by new `TestCacheUpdateValidation`. |
| Generated SKILL.md hardcoded Claude-specific tool names (`Agent` tool, `subagent_type: general-purpose`) and a `~/.claude/...` path | HIGH | Research-dispatch and the `/agent-md-refactor` invocation are now described host-neutrally (whichever dispatch mechanism the host provides, Claude's `Agent` tool named only as one example) with an explicit portable fallback (inline research / reading the wrapped skill's instructions directly) for a host with none; `agent-md-refactor`'s location is resolved via the same host-neutral candidate-list pattern as this skill's own entrypoint, never a hardcoded path. |
| Cache schema still cannot represent the full research categories (formatter/security/dead-code/etc.) the prompt itself asks for | HIGH | Consumed Plan 01's new `RESEARCH_CATEGORIES`/`ALL_TOOLING_KEYS` schema: the research-dispatch prompt now shows the FULL combined key list, and `cache-update` validates against it. |
| D-01 "programmatic invoke" remained prose-only with no host-native skill-invocation contract | MEDIUM | Step C now states a concrete contract: invoke via whichever skill-invocation mechanism the host provides, with an explicit portable fallback — not just "no Python import/subprocess," which described an absence, not a mechanism. |
| `build_remediation()`'s behavior contract said "returns a list" while its implementation contract returned a dict | MEDIUM | `<behavior>` block rewritten to state the dict `{"remediate": [...], "blocked": [...]}` shape explicitly and consistently with the `<action>`. |

### Review Feedback Deferred

None — every Cycle 2 finding touching this plan is addressed above.

