---
phase: 06-agents-md-rules-checker-skill
plan: 01
type: execute
wave: 1
depends_on: []
cross_ai: true
autonomous: true
requirements: [REQ-agtmd-makefile-rules, REQ-agtmd-workflow-rules, REQ-agtmd-skill-pipeline]
files_modified:
  - skills/ai-kit-agents-md-checker/SKILL.md
  - skills/ai-kit-agents-md-checker/ai-kit-agents-md-checker.py
  - skills/ai-kit-agents-md-checker/ai_kit_agents_md_checker/__init__.py
  - skills/ai-kit-agents-md-checker/ai_kit_agents_md_checker/rules.py
  - skills/ai-kit-agents-md-checker/ai_kit_agents_md_checker/stack_detect.py
  - skills/ai-kit-agents-md-checker/ai_kit_agents_md_checker/stack_cache.py
  - skills/ai-kit-agents-md-checker/ai_kit_agents_md_checker/makefile_checker.py
  - skills/ai-kit-agents-md-checker/ai_kit_agents_md_checker/tool_presence.py
  - skills/ai-kit-agents-md-checker/ai_kit_agents_md_checker/rule_checker.py
  - skills/ai-kit-agents-md-checker/ai_kit_agents_md_checker/cli.py
  - tests/test_ai_kit_agents_md_checker.py
  - Makefile
  - .pre-commit-config.yaml
  - pyproject.toml

estimate:
  tokens: 155000
  raw_tokens: 155000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Running the checker's `check` subcommand against a repo whose Makefile lacks `arch-test` (or any of the other required static targets) reports that exact target name as missing, never a generic pass/fail (ROADMAP SC-1, REQ-agtmd-makefile-rules)."
    - "A missing per-hook Makefile target (one not 1:1 with a `.pre-commit-config.yaml` hook id) is recommended by reusing that hook's own `entry:` command — no stack research is needed or triggered for this case, because the target repo's own pre-commit config already names the tool (D-04's research/cache path is reserved for the static targets that are not hook-derived)."
    - "A missing STATIC target (`setup-env`, test-pyramid targets, etc.) is recommended from `stack_cache`'s per-stack data when a FRESH (never stale) cache entry exists for the detected stack, and is flagged `needs_research: true` — never a guessed/hardcoded tool name, and never trusting a stale entry merely because `tooling.get(target)` is non-`None` — when the cache entry is absent or older than `STALE_AFTER_DAYS` (D-04, costly reversibility; fixes cross-AI review Cycle 2 HIGH: stale-cache entries were being trusted because the staleness flag `get_stack_tooling` already returns was never actually consulted before setting `needs_research: false`, `06-REVIEWS.md` Cycle 2 06-01 Concerns)."
    - "`stack_cache`'s `tooling` dict is keyed by the exact hyphenated Makefile target names in `REQUIRED_STATIC_TARGETS` (e.g. `\"setup-env\"`, `\"arch-test\"`, `\"test-unit\"`) — one schema, defined once in `stack_cache.py` and imported by `makefile_checker.py`, never redefined or looked up under a different key shape (e.g. `tooling[\"setup_env\"]`) anywhere else. Plan 02's research-dispatch prompt is written against this exact schema so a fresh cache entry actually satisfies the lookup (fixes cross-AI review Round 1 HIGH: cache-key mismatch, `06-REVIEWS.md` L41/L69@02ca473)."
    - "When `detect_stacks` matches more than one stack for a repo, a static-target gap is `needs_research: false` only when EVERY detected stack has its OWN fresh cache entry for that target; if even one detected stack still lacks a fresh entry, the gap stays `needs_research: true` and its `stacks` field names every stack still lacking one, in deterministic sorted order — multi-stack detection never reports success from a single covered stack while hiding that a second detected stack's tooling is still unresearched (fixes cross-AI review Cycle 2 HIGH: multi-stack collapse — the prior design stopped at the first fresh match and silently ignored remaining detected stacks, `06-REVIEWS.md` Cycle 2 06-01 Concerns)."
    - "When `detect_stacks` matches NO stack at all (a repo with none of the recognized marker files), `_resolve_across_stacks` returns `needs_research: True` with an empty `stacks` list and a `summary` stating no stack was detected — it NEVER returns `needs_research: False`/a vacuous empty-string recommendation merely because an empty `missing` set made the 'every detected stack is covered' condition trivially true over zero stacks (fixes cross-AI review Cycle 3 HIGH: the prior version's `missing = sorted(s for s in detected_stacks if ...)` was empty whenever `detected_stacks` was empty, so 'fresh coverage' was reported for a repo the checker could not actually say anything about, `06-REVIEWS.md` Cycle 3 06-01 Concerns)."
    - "`tool_presence.check_tool_presence` reports EACH of the five `MODERN_CLI_TOOLS` (`rg`, `bat`, `sd`, `fd`, `eza`) under its own `modern-cli:<tool>` key, in addition to the aggregate `modern-cli` (any-of) key R06's `condition` still gates on; `rule_checker.check_workflow_rules` classifies R06 using GROUPED, tool-scoped signals (mirroring R04's grouping mechanism) where a group is only REQUIRED when its own tool is actually present — a group for an absent tool is never counted toward `missing`/`near_miss`/`present`, and R06 is `present` only when EVERY INSTALLED modern-CLI tool's own phrase is found in the text, never when a single `rg`-only mention satisfies guidance that should also have named an installed `bat`/`sd`/`fd`/`eza` (fixes cross-AI review Cycle 3 HIGH: R06 collapsed all five tools' presence into one aggregate boolean and its signals named only `rg`/`ripgrep`, so guidance could pass while never mentioning an installed `bat`/`sd`/`fd`/`eza`, `06-REVIEWS.md` Cycle 3 06-01 Concerns)."
    - "`RESEARCH_CATEGORIES` no longer includes `validate-order` or `precommit-vs-prepush-split` — both were keyword-matched categories with a deliberately empty keyword tuple that could NEVER be skipped, so even a fully compliant repo received a perpetual, unresolvable finding for each (fixes cross-AI review Cycle 3 HIGH: two research categories with empty keyword sets were perpetual false positives by construction, `06-REVIEWS.md` Cycle 3 06-01 Concerns). Both concerns are now DEDICATED STRUCTURAL checks instead of cache/research-backed category gaps: `check_validate_order` reuses the same chain-membership data the `validate`-chain check already computes and NEVER emits its own finding in either branch (fixes cross-AI review Cycle 5 MEDIUM: an earlier draft appended a second `validate_order_unverified` finding whenever `chain_incomplete` was already raised, duplicating the SAME gap into two overlapping `build_remediation()` entries, `06-REVIEWS.md` Cycle 5 06-01 Concerns) — the existing `chain_incomplete` finding is the sole source of truth when membership is incomplete, and the residual 'cost ORDER is not mechanically verified' limitation when membership IS complete is documented once in prose (this function's docstring and this plan's `<done>`/`success_criteria`), never re-asserted as a per-run finding that would fire on every invocation regardless of outcome — the same perpetual-finding failure shape already removed from the old research-category design; `check_precommit_prepush_split` classifies each present hook as \"slow\" via `SLOW_HOOK_KEYWORDS` and checks, PER SLOW HOOK, whether THAT hook's own YAML block carries a `stages:` value containing `pre-push`/`push` — never satisfied merely because SOME unrelated hook elsewhere in the file has a push stage (fixes cross-AI review Cycle 4 HIGH: the prior version was satisfied by any hook anywhere having a push stage, never confirming a slow hook specifically was moved there, `06-REVIEWS.md` Cycle 4 New Findings); no slow hook present at all means nothing to verify (satisfied, no finding); at least one slow hook missing its own push stage produces a finding with `needs_research: False` and a fixed structural recommendation naming every such hook (add `stages: [pre-push]` to those specific hooks) — no LLM research is needed for either structural check, because both are structural/process facts readable directly from the target repo's own config, not a tool pick."
    - "A pre-commit hook id declared with no inline `entry:` line in this repo's `.pre-commit-config.yaml` (e.g. a hook whose command lives entirely in an externally-referenced hook-repo manifest rather than a `repo: local` block) is still included in the per-hook Makefile-target audit — its recommendation falls back to `pre-commit run <id>` instead of being silently dropped from the audit merely because it has no inline `entry:` (fixes cross-AI review Cycle 2 HIGH: non-inline hooks invisible to the audit, `06-REVIEWS.md` Cycle 2 06-01 Concerns)."
    - "R04's three independently-mandated clauses (cross-AI plan-review, cross-AI cheap-model execution, and path-agnostic command generation — per REQUIREMENTS.md's REQ-agtmd-workflow-rules) are each their own required signal GROUP; R04 is classified `present` only when ALL THREE groups have evidence, never when review-only phrasing happens to clear a single flat aggregate threshold with zero execution/cheap-model/path-agnostic signal present (fixes cross-AI review Cycle 2 HIGH: compound-rule false positive, `06-REVIEWS.md` Cycle 2 06-01 Concerns)."
    - "R02 is checked on BOTH axes REQUIREMENTS.md requires: `makefile_checker.py` audits the structural Makefile-target shape, AND `rule_checker.py` (Task 2) audits R02's own prose clause (agents must not skip pre-commit checks except for the sanctioned red→green TDD bypass) via R02's own non-empty `signals` entry — R02 is never excluded from workflow-rule checking merely because its `category` is `\"makefile\"` (fixes cross-AI review Cycle 2 HIGH: R02 excluded from checking entirely, `06-REVIEWS.md` Cycle 2 06-01 Concerns)."
    - "Every finding in the `check` report's `makefile` list — a missing static target, a hook-derived gap, a `chain_incomplete`/`not_aliased` structural gap, or a tooling-category gap — carries the SAME `criticality` (R02's own criticality tier, since Makefile shape is R02's domain) and `summary` fields every `workflow` finding already carries: one unified finding schema across both checkers, so Plan 02's `build_remediation()` never has to special-case a finding kind missing fields it assumes are universally present (fixes cross-AI review Cycle 2 CRITICAL: field-schema mismatch between Plan 01 and Plan 02, confirmed by this orchestrator's own spot-check of `06-01-PLAN.md`/`06-02-PLAN.md`, `06-REVIEWS.md` Cycle 2 06-02 Concerns)."
    - "`stack_cache.py`'s schema also defines `RESEARCH_CATEGORIES` (formatter, security-scanner, code-smell-detector, duplicate-code-detector, dead-code-detector — exactly these FIVE, never `validate-order` or `precommit-vs-prepush-split`, which are dedicated structural checks per the preceding truth, not research-backed cache categories, so they are never members of `RESEARCH_CATEGORIES` itself; fixes cross-AI review Cycle 4 HIGH: this truth previously listed `RESEARCH_CATEGORIES` as still containing `validate-order`/`precommit-vs-prepush-split`, directly contradicting the implementation section below that removes both, `06-REVIEWS.md` Cycle 4 New Findings) alongside `REQUIRED_STATIC_TARGETS` — a `tooling` dict can hold a real recommendation for every category the D-04 research prompt asks for, not only the 7 Makefile-target names; `check_makefile_shape` surfaces a category gap (cache-sourced, staleness-gated exactly like a static-target gap) whenever no currently-present Makefile target or pre-commit hook id already appears, by a simple keyword match, to cover that category — stated as a heuristic, not a guarantee, the same documented-limitation posture as the `validate`-chain check (fixes cross-AI review Cycle 2 HIGH: cache schema could not represent the research categories Plan 02's own prompt asks for, `06-REVIEWS.md` Cycle 2 06-02 Concerns)."
    - "A Makefile target that IS present by name (e.g. `validate`, or both `test` and `test-unit`) but does not structurally satisfy REQ-agtmd-makefile-rules' chaining/alias requirement — `validate`'s recipe never references a present per-hook target by name (via word-boundary-aware matching, never a raw substring match) AND that target is never listed as one of `validate`'s Make PREREQUISITES either, or `test`'s recipe never invokes `test-unit` (same word-boundary-aware matching) AND `test-unit` is never listed as one of `test`'s Make PREREQUISITES either — is still flagged, with its own recommendation and `needs_research: false`, never silently treated as compliant merely because the target name exists, merely because a DIFFERENTLY-named target's recipe happens to contain the target name as a raw substring (e.g. `pylint` containing `lint`), or merely because the alias check only recognized a recipe-text reference (fixes cross-AI review Round 1 HIGH: presence-only check, `06-REVIEWS.md` L40@02ca473; fixes cross-AI review Cycle 3 HIGH: chain-membership previously checked ONLY recipe-text substrings and ignored genuine Make prerequisite chaining — `target: dep1 dep2` — which is an equally valid, arguably more idiomatic way to wire a chain, so a repo using prerequisites instead of recipe calls was falsely flagged as incomplete, `06-REVIEWS.md` Cycle 3 06-01 Concerns; fixes cross-AI review Cycle 4 HIGH: the alias check recognized only a recipe-text reference or identical recipe text, so a valid `test: test-unit` prerequisite alias was falsely reported `not_aliased`, and the shared recipe-matching helper used a raw substring check, so a target named `lint` could be falsely reported wired merely because the recipe invoked `pylint`, `06-REVIEWS.md` Cycle 4 New Findings). Mechanically verifying true cost-order ('lightest/cheapest first') is out of reach for a structural regex check and is NOT claimed; only chain/alias MEMBERSHIP (via word-boundary-aware recipe reference OR prerequisite listing) is verified — this limitation is stated explicitly in this task's `<done>` AND in this plan's `success_criteria`, which does not claim ROADMAP SC-1/REQ-agtmd-makefile-rules is unqualifiedly 'fully implemented' while that ordering gap stands (fixes cross-AI review Cycle 3 HIGH: the plan narrowed cost-ordering out of scope yet still claimed the REQ complete in its own success_criteria wording, `06-REVIEWS.md` Cycle 3 06-01 Concerns; fixes cross-AI review Cycle 4 HIGH: the plan's own success_criteria still opened by claiming ROADMAP SC-1 itself satisfied, contradicting this same narrowing, `06-REVIEWS.md` Cycle 4 New Findings)."
    - "`check_makefile_shape` and `check_workflow_rules`, called against a repo root whose `Makefile` and/or `.pre-commit-config.yaml` does not exist, return normally (never raise) — a missing Makefile is reported as EVERY `REQUIRED_STATIC_TARGETS` name missing (the maximal, most informative gap, since an absent Makefile also means every per-hook target is absent), and a missing `.pre-commit-config.yaml` is reported as zero hook-derived targets required but never crashes the static-target loop (fixes cross-AI review Cycle 3 HIGH: the parsers previously read both paths unconditionally with no absent-file handling, even though a missing Makefile/pre-commit config is the strongest form of the exact gap this skill exists to report, `06-REVIEWS.md` Cycle 3 06-01 Concerns)."
    - "`main()`'s `check` subcommand resolves the target instruction file by checking `AGENTS.md` first and falling back to `CLAUDE.md` when `AGENTS.md` does not exist in `repo_root` — a repository containing only `CLAUDE.md` is checked against its real content, never treated as having an empty instruction file merely because the default path assumes `AGENTS.md` (fixes cross-AI review Cycle 3 HIGH: the public CLI could not check a CLAUDE.md-only repository because `check()` defaulted exclusively to `AGENTS.md`, `06-REVIEWS.md` Cycle 3 06-01 Concerns)."
    - "The text `check_workflow_rules` classifies against is not limited to the root AGENTS.md/CLAUDE.md file's own text: `resolve_instruction_text()` also follows every same-repo relative Markdown link found in the root file's body (e.g. `[testing conventions](docs/testing.md)`) one level deep and concatenates each linked file's text into the same classification string — a rule `/agent-md-refactor` has moved out of the root file into a linked, progressive-disclosure file it still satisfies is found there and is never reported as missing merely because it is not physically in the root file (fixes cross-AI review Cycle 3 HIGH: the checker read only the root file, so a later run could falsely report a relocated rule missing and feed it back into `/agent-md-refactor` as a duplicate-insertion candidate, `06-REVIEWS.md` Cycle 3 06-01 Concerns; this root-cause fix lives here because `resolve_instruction_text()` is the one function both Plan 01's `cli.check()` and Plan 02's wrapper consume — no second implementation). Containment of a linked file under `repo_root` is checked via `os.path.realpath` on both sides, never lexical path comparison — a link that LOOKS like it resolves inside the repo but, via a symlink anywhere in its path, actually resolves outside `repo_root`'s real path is rejected and never read (fixes cross-AI review Cycle 4 MEDIUM: containment previously had no symlink-safe realpath contract, `06-REVIEWS.md` Cycle 4 New Findings)."
    - "Running `check` against an AGENTS.md missing several of the 17 house rules reports each missing rule individually by id, never one rolled-up verdict (ROADMAP SC-2, REQ-agtmd-workflow-rules)."
    - "The 17-rule house ruleset is structured Python data (id, category, criticality, condition, signals, summary) in `rules.py`, not freeform prose embedded in `SKILL.md` (D-06)."
    - "A rule whose `condition` is `tool-presence:<name>` is skipped/reframed rather than reported as a generic gap when that tool/marker is not live-detected on this machine or in this repo — mirroring Phase 3's live-verification-over-assumption pattern, reused rather than reinvented (D-07)."
    - "`stack_cache.read_stack_cache()` distinguishes absent / stale / fresh cache states from real timestamps, never treating an old cache entry as permanently trustworthy (D-04)."
    - "`stack_cache.cache_path(stack, cache_root=None)` rejects any `stack` value that is not a bare `[a-z0-9][a-z0-9_-]{0,63}` identifier — no `/`, no `.`, no absolute-path shape — by raising `ValueError` BEFORE joining it into a filesystem path, so a path-traversal-shaped `stack` argument (from Plan 02's `cache-update` CLI or any future caller) can never resolve to a location outside `cache_root`/`CACHE_ROOT` (fixes cross-AI review Cycle 3 HIGH — SECURITY: `cache-update` permitted path traversal through an unvalidated `stack` argument, allowing an atomic overwrite of an arbitrary JSON file under/outside the cache root, `06-REVIEWS.md` Cycle 3 06-02 Concerns; fixed at the shared `cache_path()` function so every caller is protected, not only the CLI surface that first exposed it)."
    - "`tests.test_ai_kit_agents_md_checker` is registered in the `Makefile` `test:` list and the `.pre-commit-config.yaml` `unittest` hook, and `skills/ai-kit-agents-md-checker/` is covered by the `ruff` and `py-compile` hook `files:` regexes and by `pyproject.toml`'s `[tool.pyright] include` / `[tool.vulture] paths` — registered once, in this plan, so every later plan's additions to the same modules/test file are gated automatically (mirrors Phase 3 Task 1's immediate-registration precedent)."
  artifacts:
    - skills/ai-kit-agents-md-checker/SKILL.md
    - skills/ai-kit-agents-md-checker/ai_kit_agents_md_checker/rules.py
    - skills/ai-kit-agents-md-checker/ai_kit_agents_md_checker/makefile_checker.py
    - skills/ai-kit-agents-md-checker/ai_kit_agents_md_checker/rule_checker.py
    - skills/ai-kit-agents-md-checker/ai_kit_agents_md_checker/cli.py
    - tests/test_ai_kit_agents_md_checker.py
  key_links:
    - "`cli.py`'s `check` command imports and calls `makefile_checker.check_makefile_shape()` and `rule_checker.check_workflow_rules()` and merges both into one JSON report — the CLI is the one place that wires `rules.py`, `stack_detect.py`, `stack_cache.py`, `makefile_checker.py`, `tool_presence.py`, and `rule_checker.py` together."
    - "`makefile_checker.py` calls `stack_cache.get_stack_tooling()`, never reimplements cache reading itself — one cache implementation, reused."
    - "`cli.py`'s `check` command calls `tool_presence.check_tool_presence()` exactly once and passes the resulting dict into `rule_checker.check_workflow_rules()`, which evaluates every `tool-presence:*` rule's `condition` against that already-computed dict — `rule_checker.py` itself never calls `check_tool_presence()`, and a rule is never marked `missing` solely because its condition tool is absent (fixes cross-AI review Cycle 2 MEDIUM: the key-link contract named `rule_checker.py` as the caller while the action placed the call in `cli.py`, `06-REVIEWS.md` Cycle 2 06-01 Concerns)."
    - "The new skill package is NOT imported from `tools/` and does NOT import `tools/hooks/detect.py` directly — `tool_presence.py` adapts that module's `shutil.which`-based pattern locally, the same layering choice Phase 3 made for `atomic_write.py` (no `tools/`<->`skills/` cross-import)."
---

<objective>
Stand up the new AGENTS.md house-rule checker skill: scaffold its skill
directory under a PROVISIONAL name (`ai-kit-agents-md-checker` — the real,
final name is picked later by `/naming-analyzer` per ROADMAP Success
Criterion 4 and Plan 03 of this phase, never assumed upfront), hardcode the
17-rule house ruleset as structured data (D-06), and implement the two
deterministic checkers the rest of the phase builds on: the Makefile
target-shape checker (REQ-agtmd-makefile-rules) and the workflow-rule
prose checker (REQ-agtmd-workflow-rules), both driven through one `cli.py`
`check` command.

Purpose: ROADMAP Phase 6 Success Criteria 1 and 2 — a missing Makefile
target or a missing house rule is named individually, with a per-language
tool recommendation for Makefile gaps that is either hook-derived (no
research needed) or sourced from a staleness-checked per-stack cache
(D-04) — never invented from training data. Establishing the cache
architecture now, even though the research-subagent dispatch that fills it
arrives in Plan 02, is what D-04's "costly to change later" reversibility
note requires.

Output: `skills/ai-kit-agents-md-checker/` (provisional name) holding a
stub `SKILL.md` plus the `ai_kit_agents_md_checker` package (`rules.py`,
`stack_detect.py`, `stack_cache.py`, `makefile_checker.py`,
`tool_presence.py`, `rule_checker.py`, `cli.py`); `tests/test_ai_kit_agents_md_checker.py`;
and the `Makefile`/`.pre-commit-config.yaml`/`pyproject.toml` registrations
that gate all of it (and everything Plans 02-03 add to the same files)
under `make test`/`make lint`/`make validate`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/ONESHOT-RULES.md
@AGENTS.md
@.planning/phases/06-agents-md-rules-checker-skill/06-CONTEXT.md
@.planning/todos/pending/2026-09-09-agents-md-rules-checker-skill-wrapping-agent-md-refactor.md
</context>

<tracked_source_paths>
Verified with `git ls-files` at planning time:

- `Makefile`, `.pre-commit-config.yaml`, `pyproject.toml`, `AGENTS.md`,
  `tools/hooks/detect.py`, `skills/ai-kit-usage-metrics/SKILL.md`,
  `tests/test_tool_substitution_hook.py` — all return a non-empty
  `git ls-files` result (tracked, used as read-only pattern references).
- `skills/ai-kit-agents-md-checker/SKILL.md` and every other path under
  `skills/ai-kit-agents-md-checker/`, plus
  `tests/test_ai_kit_agents_md_checker.py` — all return empty (do not exist
  yet); these are new files at their intended, tracked-tree paths. No path
  in this plan points at an install mirror or a gitignored directory.
</tracked_source_paths>

<failing_direction_contract>
Every runnable `<automated>` command below is followed by a `<fails_when>`
sibling naming the observable output that constitutes failure.
</failing_direction_contract>

<source_audit>
## Multi-Source Coverage Audit (Phase 6, Plan 01 portion)

| # | Source | Item | Covered by |
|---|--------|------|-----------|
| GOAL | ROADMAP Phase 6 Goal | Automated check against ai-kit's house ruleset, not just a generic refactor | Task 1 (scaffold + Makefile checker), Task 2 (workflow-rule checker) |
| SC-1 | ROADMAP SC-1 | Missing Makefile targets flagged by name + per-language tool rec | Task 1 |
| SC-2 | ROADMAP SC-2 | Missing workflow rules flagged individually | Task 2 |
| SC-4 (partial) | ROADMAP SC-4 | Scaffolded via `/superpowers:writing-skills` (naming/skill-judge land in Plan 03) | Task 1 |
| REQ-1 | REQUIREMENTS.md | REQ-agtmd-makefile-rules | Task 1 |
| REQ-3 | REQUIREMENTS.md | REQ-agtmd-workflow-rules | Task 2 |
| REQ-4 (partial) | REQUIREMENTS.md | REQ-agtmd-skill-pipeline (scaffold step only) | Task 1 |
| D-04 | 06-CONTEXT.md | Stack tooling NOT hardcoded to go/python/node/java; cache-with-staleness model, costly to change | Task 1 (`stack_detect.py`, `stack_cache.py`) |
| D-06 | 06-CONTEXT.md | 17-rule ruleset as structured data, not prose | Task 1 (`rules.py`) |
| D-07 | 06-CONTEXT.md | Per-rule applicability `condition`; reuse Phase 3's live tool-presence pattern | Task 2 (`tool_presence.py`) |
| D-03 (partial) | 06-CONTEXT.md | Near-miss phrasing is not force-rewritten; lenient first-pass heuristic | Task 2 (`rule_checker.py`'s `near_miss` status) |
| D-02 (partial) | 06-CONTEXT.md | Most-critical-first ordering | Task 2 (`criticality` field feeds Plan 02's ordering) |
| Discretion | 06-CONTEXT.md | Per-rule data schema and stack-cache file format are the planner/implementer's choice | Task 1 |

**Deferred to Plan 02 (not a gap in this plan):** the remediation payload
(D-01/D-02/D-03/D-08), the research-subagent dispatch that fills a cold
`stack_cache` entry, and `SKILL.md`'s full wrapping narrative. **Deferred to
Plan 03:** `/skill-judge` and `/naming-analyzer`.
</source_audit>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Tracer — scaffold the skill and wire one Makefile gap from PATH/file probe to a named, recommended finding</name>
  <files>skills/ai-kit-agents-md-checker/SKILL.md, skills/ai-kit-agents-md-checker/ai-kit-agents-md-checker.py, skills/ai-kit-agents-md-checker/ai_kit_agents_md_checker/__init__.py, skills/ai-kit-agents-md-checker/ai_kit_agents_md_checker/rules.py, skills/ai-kit-agents-md-checker/ai_kit_agents_md_checker/stack_detect.py, skills/ai-kit-agents-md-checker/ai_kit_agents_md_checker/stack_cache.py, skills/ai-kit-agents-md-checker/ai_kit_agents_md_checker/makefile_checker.py, skills/ai-kit-agents-md-checker/ai_kit_agents_md_checker/cli.py, tests/test_ai_kit_agents_md_checker.py, Makefile, .pre-commit-config.yaml, pyproject.toml</files>

  <read_first>
    - `.planning/todos/pending/2026-09-09-agents-md-rules-checker-skill-wrapping-agent-md-refactor.md` rule 2 — the full Makefile target-shape requirement (note its own frontmatter `../gitig/.pre-commit-config.yaml` path is stale; use this repo's real `Makefile`/`.pre-commit-config.yaml` instead, per `06-CONTEXT.md`'s canonical_refs).
    - `Makefile` and `.pre-commit-config.yaml` (this repo's own, in full) — the concrete "real Makefile the checker would currently flag" fixture `06-CONTEXT.md`'s code_context names: no `setup-env`, no 1:1 per-hook targets, no `test-unit`/`test-integration`/`e2e-test`/`arch-test` split. Copy (never mutate) this content into the test fixture.
    - `skills/ai-kit-usage-metrics/ai-kit-usage-metrics.py` and `skills/ai-kit-usage-metrics/ai_kit_usage_metrics/cli.py` (first ~30 lines) — the thin-entrypoint-imports-package-cli convention this skill's own entrypoint and `cli.py` must follow (argparse-free, `sys.argv`-dispatch style).
    - `skills/ai-kit-usage-metrics/SKILL.md` — the "Resolve the entrypoint" host-neutral candidate-list pattern this skill's `SKILL.md` stub must also use.
    - `tests/test_ai_kit_usage_metrics.py` lines 16-21 — the `sys.path.insert(0, os.path.join(os.path.dirname(__file__), "..", "skills", "ai-kit-usage-metrics"))` + `from ai_kit_usage_metrics import (...)` loading convention. This skill is a multi-module PACKAGE with intra-package imports (`cli` -> `makefile_checker` -> `stack_cache`, etc.), the same shape as `ai_kit_usage_metrics` — NOT a single-file module. `tests/test_setup.py`/`tests/test_tool_substitution_hook.py`'s `importlib.util.spec_from_file_location` convention loads ONE file by path and does not import a package's intra-package references correctly; it is the wrong precedent here and must not be used (fixes cross-AI review Round 1 HIGH: wrong test-loader precedent, `06-REVIEWS.md` L39@02ca473).
    - `tools/hooks/detect.py` (module docstring + `CURATED_SUBSTITUTIONS`/`detect_substitutions` shape) — the live-verification-over-assumption pattern this phase's `tool_presence.py` (Task 2) adapts; read now so `rules.py`'s `condition` field design (Task 1) is compatible with it.
    - `.pre-commit-config.yaml` lines for the `ruff`, `py-compile`, and `unittest` hooks, and `Makefile`'s `test:`/`lint:` targets, and `pyproject.toml`'s `[tool.pyright] include` / `[tool.vulture] paths` — the exact four registration surfaces this task extends.
  </read_first>

  <behavior>
    - `parse_makefile_targets(path)` on a Makefile with `test:\n\t...` and `.PHONY: test` returns `{"test"}` — `.PHONY` and recipe lines are never mistaken for targets.
    - `parse_precommit_hooks(path)` on a `.pre-commit-config.yaml` with one hook `id: ruff`, `entry: uv run ruff check` returns `{"ruff": "uv run ruff check"}`.
    - `parse_precommit_hooks(path)` on a `.pre-commit-config.yaml` with one hook declared `- id: "vulture"` (double-quoted) and another declared `- id: 'ruff'` (single-quoted) returns `{"vulture": ..., "ruff": ...}` keyed by the BARE, unquoted id string for both — neither quote form is dropped from the returned dict.
    - `check_makefile_shape(repo_root)` against a Makefile missing `arch-test` and missing a target for hook id `vulture` returns two findings: `{"target": "arch-test", "present": False, "needs_research": True or False, ...}` and `{"target": "vulture", "present": False, "recommendation": "...uv run vulture...", "needs_research": False}` — the hook-derived one never sets `needs_research`.
    - `stack_cache.read_stack_cache("python")` with no cache file returns a result whose state is `"absent"`; after `write_stack_cache("python", {...})` it returns `"fresh"`; with the cached timestamp manually backdated past `STALE_AFTER_DAYS` it returns `"stale"`.
    - `check_makefile_shape` against a Makefile where `validate`/`test`/`test-unit` and a hook-named target are all present BY NAME, but `validate`'s recipe never mentions the hook-named target and `test`'s recipe never mentions `test-unit`, returns a `{"target": "validate", "present": True, "issue": "chain_incomplete", ...}` finding AND a `{"target": "test-unit-alias", "present": True, "issue": "not_aliased", ...}` finding, each with a non-`None` `recommendation` and `needs_research: False`; when `validate`'s recipe DOES reference the hook-named target and `test`'s recipe DOES reference `test-unit`, neither finding appears; when `test`'s recipe instead lists `test-unit` as a Make PREREQUISITE (`test: test-unit`) with no recipe-text reference at all, the `not_aliased` finding also does not appear (prerequisite listing is a genuine alias mechanism, same as chain-membership).
    - `_recipe_mentions_target(recipe_text, name)` returns `False` for `name="lint"` against a recipe text that only contains `"uv run pylint src/"` (word-boundary-aware, never a raw substring match) and `True` for `name="test-unit"` against a recipe text containing the literal token `"test-unit"`.
    - `check_makefile_shape` against a repo matching two stacks where only ONE of the two detected stacks has a fresh cache entry for a given target returns `needs_research: True` for that target (the other detected stack still lacks coverage), with `"stacks"` listing only the stack(s) still lacking a fresh entry; only when EVERY detected stack has a fresh entry for that target does it return `needs_research: False` with a combined recommendation covering every stack.
    - `check_makefile_shape` against a `.pre-commit-config.yaml` hook id with no inline `entry:` line (e.g. declared under a non-`local` `repo:` block) still produces a finding for that hook's Makefile target, with `recommendation` falling back to `"pre-commit run <id>"`.
    - Every finding `check_makefile_shape` returns (static-target, hook-derived, chain/alias, or tooling-category) carries non-`None` `criticality` and `summary` keys.
  </behavior>

  <action>
Scaffold the skill via the `/superpowers:writing-skills` skill under the
provisional directory name `skills/ai-kit-agents-md-checker/`. Write a
SKILL.md stub now (frontmatter `name: ai-kit-agents-md-checker`,
third-person `description:` starting "Use when..." naming AGENTS.md/
CLAUDE.md house-rule checking and Makefile target-shape auditing as the
trigger — not a workflow summary, per `writing-skills`'s Skill Discovery
Optimization rules) plus a one-line body noting the skill is under active
construction across this phase's three plans; full authoring lands in Plan
02 Task 2. State explicitly, in a one-line HTML comment or note, that the
directory/package name is PROVISIONAL pending `/naming-analyzer` (Plan 03).

Create the `ai_kit_agents_md_checker` package. In `rules.py`, define module
constants `CONDITION_ALWAYS = "always"` and a `condition_tool_presence(name)`
helper returning `f"tool-presence:{name}"`, plus `CRITICALITY_CRITICAL = 1`,
`CRITICALITY_HIGH = 2`, `CRITICALITY_MEDIUM = 3`. Define `RULES` as a tuple
of 17 frozen dataclass or dict entries (`id`, `category` — `"makefile"` or
`"workflow"`, `criticality`, `condition`, `signals` — a tuple of short
lowercase phrases used by Task 2's keyword heuristic, `summary` — a concise
canonical paraphrase suitable for handing to `/agent-md-refactor`, never a
verbatim block-quote of the todo file per D-01). Number them `R01`-`R17`
matching the todo file's own 17 numbered items exactly (rule 2 is `R02`,
category `"makefile"`; every other rule is `"workflow"`). Assign EVERY one
of the 17 explicitly from this table — none are left to "read the todo and
decide," closing the ambiguity cross-AI review Round 1 flagged (R17's
criticality was required by Task 2 but never assigned in Task 1; the same
gap existed for R03-R11/R13-R17, `06-REVIEWS.md` L44@02ca473). `summary`
text is this task's own concise paraphrase of the todo's rule (D-01 — never
a verbatim block-quote); write each `summary` from the todo file directly,
not from memory of a different project's conventions:

| id | category | criticality | condition | signals (lowercase substrings) |
|----|----------|-------------|-----------|---------------------------------|
| R01 | workflow | `CRITICALITY_CRITICAL` | `CONDITION_ALWAYS` | `("english only", "english-only", "non-negotiable")` |
| R02 | makefile | `CRITICALITY_CRITICAL` | `CONDITION_ALWAYS` | `("--no-verify", "skip pre-commit checks", "red-green")` — these three signals check ONLY R02's separate prose clause ("agents must not skip pre-commit checks except for the sanctioned red→green TDD bypass"); the Makefile SHAPE half of R02 is read from the real Makefile by `makefile_checker.py`, never from AGENTS.md prose. `category` stays `"makefile"` for R02's own bookkeeping, but Task 2's `rule_checker.py` filter now includes R02 in workflow-prose checking because it has a non-empty `signals` tuple, not because of its `category` (fixes cross-AI review Cycle 2 HIGH: R02 excluded from checking entirely, `06-REVIEWS.md` Cycle 2 06-01 Concerns) |
| R03 | workflow | `CRITICALITY_HIGH` | `CONDITION_ALWAYS` | `("one coherent", "compaction", "logical commit")` |
| R04 | workflow | `CRITICALITY_HIGH` | `condition_tool_presence("gsd")` | `(("cross-ai review", "plan-review convergence", "cross-ai plan-review"), ("cross-ai execution", "cheap-model execution", "cheap model"), ("path-agnostic", "path agnostic"))` — GROUPED, not flat: three independently-required clauses (review / cheap-model execution / path-agnostic command, per REQUIREMENTS.md's REQ-agtmd-workflow-rules "both path-agnostic" wording), each satisfied by ANY ONE of its own phrases matching. R04 is the ONE rule in this ruleset using PLAIN phrase-groups (every group independently required, regardless of environment); R06 below uses a DIFFERENT, TOOL-SCOPED group shape where a group's requiredness itself depends on live tool presence (fixes cross-AI review Cycle 2 HIGH: a flat aggregate threshold let review-only phrasing satisfy all of R04 with zero execution/cheap-model/path-agnostic evidence present, `06-REVIEWS.md` Cycle 2 06-01 Concerns) |
| R05 | workflow | `CRITICALITY_MEDIUM` | `condition_tool_presence("rtk")` | `("rtk", "token killer", "tool substitution")` |
| R06 | workflow | `CRITICALITY_MEDIUM` | `condition_tool_presence("modern-cli")` | `(("rg", ("rg", "ripgrep")), ("bat", ("bat", "batcat")), ("sd", ("sd", "stream editor", "sed replacement")), ("fd", ("fd", "fd-find")), ("eza", ("eza", "exa")))` — TOOL-SCOPED groups, a DIFFERENT shape than R04's plain phrase-groups: each inner tuple is `(tool_name, phrases)`; `rule_checker.py` only REQUIRES a group whose `tool_name` is actually installed per `tool_presence`'s per-tool `modern-cli:<tool>` keys (fixes cross-AI review Cycle 3 HIGH: collapsing all 5 tools into one aggregate `modern-cli` boolean let guidance pass while never mentioning an installed `bat`/`sd`/`fd`/`eza`, `06-REVIEWS.md` Cycle 3 06-01 Concerns) |
| R07 | workflow | `CRITICALITY_MEDIUM` | `condition_tool_presence("codegraph")` | `("codegraph", "codegraph_explore", ".codegraph")` |
| R08 | workflow | `CRITICALITY_MEDIUM` | `condition_tool_presence("graphify")` | `("graphify", "graph.json", "graphify-out")` |
| R09 | workflow | `CRITICALITY_HIGH` | `CONDITION_ALWAYS` | `("orphan", "orphaned process", "kill")` |
| R10 | workflow | `CRITICALITY_MEDIUM` | `CONDITION_ALWAYS` | `("./tmp/", "tmp/", "ephemeral")` |
| R11 | workflow | `CRITICALITY_HIGH` | `CONDITION_ALWAYS` | `("readme", "docs currency", "nested docs")` |
| R12 | workflow | `CRITICALITY_CRITICAL` | `CONDITION_ALWAYS` | `("no absolute path", "absolute path", "relative path")` |
| R13 | workflow | `CRITICALITY_CRITICAL` | `CONDITION_ALWAYS` | `("no excuse", "deflection", "not my fault")` |
| R14 | workflow | `CRITICALITY_HIGH` | `CONDITION_ALWAYS` | `("stale knowledge", "gray-area", "verify before trusting")` |
| R15 | workflow | `CRITICALITY_HIGH` | `CONDITION_ALWAYS` | `("theory", "hypothesis", "spike")` |
| R16 | workflow | `CRITICALITY_HIGH` | `CONDITION_ALWAYS` | `("uncommitted", "working area", "untracked")` |
| R17 | workflow | `CRITICALITY_MEDIUM` | `CONDITION_ALWAYS` | `("concise", "no excess documentation", "narrative")` |

`R01`'s `summary="All written project artifacts (docs, commits, comments,
planning files) are English-only, regardless of the conversation's
language."`. `R12`'s `summary="Project assets, source, and tests never
hardcode absolute filesystem paths; use relative or repo-root-relative
resolution."`. Write the other 14 `summary` strings the same way: one
concise sentence paraphrasing that numbered item's intent, read directly
from the todo file just now, not invented from general conventions.

In `stack_detect.py`, implement `detect_stacks(repo_root)` returning a
`set[str]` from marker files: `go.mod` -> `"go"`; any of `requirements.txt`,
`pyproject.toml`, `setup.py`, `Pipfile` -> `"python"`; `package.json` ->
`"node"`; any of `pom.xml`, `build.gradle`, `build.gradle.kts` -> `"java"`;
`Cargo.toml` -> `"rust"`. A repo can match more than one stack.

In `stack_cache.py`, define `REQUIRED_STATIC_TARGETS = ("setup-env",
"validate", "test", "test-unit", "test-integration", "e2e-test",
"arch-test")` AND `RESEARCH_CATEGORIES = ("formatter", "security-scanner",
"code-smell-detector", "duplicate-code-detector", "dead-code-detector")`
HERE — together these are the single canonical schema for every key a
`tooling` dict may legitimately carry. `REQUIRED_STATIC_TARGETS` are the
exact Makefile target names `makefile_checker.py` checks presence for.
`RESEARCH_CATEGORIES` are the tool-category recommendations the D-04
research prompt (Plan 02) asks for that do NOT map 1:1 to one of the 7
static target names — e.g. a dedicated formatter or security scanner the
target repo may have no Makefile target OR pre-commit hook for at all yet
(fixes cross-AI review Cycle 2 HIGH: the prior single
`REQUIRED_STATIC_TARGETS`-only schema had no field to hold these categories
even though Plan 02's own research prompt asks for them, `06-REVIEWS.md`
Cycle 2 06-02 Concerns). `RESEARCH_CATEGORIES` deliberately does NOT include
`validate-order` or `precommit-vs-prepush-split` — those two are process/
structural facts readable directly from the target repo's own files, not a
tool pick that benefits from research, so they are implemented below as
dedicated structural checks (`check_validate_order`,
`check_precommit_prepush_split`) instead of cache/research-backed category
gaps (fixes cross-AI review Cycle 3 HIGH: a category with an empty keyword
tuple could never be skipped, so both were perpetual false positives by
construction even against a fully compliant repo, `06-REVIEWS.md` Cycle 3
06-01 Concerns). Define `ALL_TOOLING_KEYS = REQUIRED_STATIC_TARGETS +
RESEARCH_CATEGORIES` as the one combined set both `makefile_checker.py` and
Plan 02's `cache-update` validation check against.
A `tooling` dict's keys are ALWAYS drawn from `ALL_TOOLING_KEYS` verbatim
(hyphenated, matching each tuple's own values — e.g. `tooling["setup-env"]`,
`tooling["security-scanner"]` — never underscored, never a key outside this
combined set). `makefile_checker.py` imports both constants from
`stack_cache` rather than redefining them, so the schema is declared exactly
once and Plan 02's research-dispatch prompt can cite it by name (fixes
cross-AI review Round 1 HIGH: cache lookup key inconsistent between `name`
and `tooling["setup_env"]`, and the matching D-04 research-loop mismatch
where the research prompt asked for category names instead of target
names, `06-REVIEWS.md` L41/L69@02ca473). `CACHE_ROOT` resolves under
`os.environ.get("XDG_CACHE_HOME", os.path.expanduser("~/.cache"))` then
`ai-kit/agents-md-checker/stack-refs`; `STALE_AFTER_DAYS = 30`;
`cache_path(stack, cache_root=None)` FIRST validates `stack` against
`re.fullmatch(r"[a-z0-9][a-z0-9_-]{0,63}", stack)` — letters/digits/`_`/`-`
only, no `/`, no `.`, no leading `-`/`_`, 1-64 chars — and raises
`ValueError(f"invalid stack identifier: {stack!r}")` if it does not match,
BEFORE ever joining it into a path (fixes cross-AI review Cycle 3 HIGH —
SECURITY: `cache_path` previously joined an unvalidated `stack` argument
directly into a filesystem path, so a path-traversal-shaped value (e.g.
`"../../etc/passwd"` or an absolute path) could escape `cache_root` entirely
and let `write_stack_cache`/`cache-update` atomically overwrite an arbitrary
JSON file outside the cache directory, `06-REVIEWS.md` Cycle 3 06-02
Concerns — fixed here, the one shared function every caller including
Plan 02's `cache-update` CLI goes through, rather than only at the CLI
layer); ONLY after that check passes does it join `cache_root or
CACHE_ROOT` with `f"{stack}.json"`; `read_stack_cache(stack, cache_root=None)` returns a dict
`{"state": "absent"|"stale"|"fresh", "data": dict|None}` — `"absent"` when
the file does not exist, `"stale"` when `cached_at` (ISO-8601, UTC) is older
than `STALE_AFTER_DAYS`, `"fresh"` otherwise, `data` populated whenever the
file parses regardless of staleness (a stale cache is still returned, so a
caller CAN fall back to it, but must see the flag); `write_stack_cache(stack,
data, cache_root=None)` writes `{"cached_at": <iso now>, "stack": stack,
"tooling": data}` atomically (`tempfile.mkstemp` in the cache dir +
`os.replace`, creating parent dirs first) — reuse the
`tempfile.mkstemp`+`os.replace` shape `tools/setup.py`'s `_atomic_write_json`
already established, adapt it locally rather than importing across the
`tools/`<->`skills/` boundary; `get_stack_tooling(stack, cache_root=None)`
returns `(tooling_or_None, needs_research: bool)` — `needs_research` true
for both `"absent"` and `"stale"`.

In `makefile_checker.py`, `from .stack_cache import REQUIRED_STATIC_TARGETS,
RESEARCH_CATEGORIES` and `from .rules import CRITICALITY_CRITICAL` — do NOT
redefine any of these here (they are `stack_cache.py`'s and `rules.py`'s
schemas now, see above). All three parsing functions below return an EMPTY
result (`set()` / `{}`) when `path` does not exist on disk — checked with
`os.path.isfile(path)` at the top of each, before any `open()` call — rather
than letting `FileNotFoundError` propagate; a missing Makefile or
`.pre-commit-config.yaml` is the strongest form of the exact gap this skill
reports, never a crash (fixes cross-AI review Cycle 3 HIGH: the specified
parsers previously had no absent-file handling at all, `06-REVIEWS.md` Cycle
3 06-01 Concerns). `parse_makefile_targets(path)` reads the file and, for
each line, matches a target-definition line with a regex anchored at the
start of the line requiring a bare identifier (letters/digits/`_`/`-`/`.`)
followed by `:` NOT followed by `=` (excludes `VAR := value` / `VAR ?= value`
forms) and not starting with `.` (excludes `.PHONY:` and other special
targets); returns the `set` of matched names. `parse_makefile_prerequisites(path,
target)` uses the SAME target-definition regex to find that target's header
line, then returns the `set` of whitespace-separated tokens appearing AFTER
the `:` and before any line-ending `#` comment on that same header line (GNU
Make's own `target: prereq1 prereq2` prerequisite-list syntax) — `set()` if
the target is absent or declares no prerequisites; this is DISTINCT from
`parse_makefile_recipe` below (prerequisites are declared on the header
line itself, recipe lines are the indented lines that follow) and gives the
chain-membership check below a second, equally valid way a target can wire
another target in, alongside a recipe-text reference (fixes cross-AI review
Cycle 3 HIGH: chain-membership previously checked ONLY recipe-text
substrings, so a repo wiring `validate: lint test` via genuine Make
prerequisites — arguably the more idiomatic mechanism — was falsely flagged
as incomplete, `06-REVIEWS.md` Cycle 3 06-01 Concerns). `parse_makefile_recipe(path,
target)` returns the joined text of every indented/recipe line immediately
following that target's `target:` header line, up to (not including) the
next non-indented, non-blank line, or `""` if the target is absent — used
below for the `validate`/`test` structural checks, not just presence.
`parse_precommit_hooks(path)` scans EVERY `- id: <name>` line, matching the
id token whether it is BARE, DOUBLE-quoted, or SINGLE-quoted YAML
(`- id: ruff`, `- id: "ruff"`, and `- id: 'ruff'` must all parse to the same
bare id `"ruff"` — strip a matched pair of surrounding `"`/`'` from the
captured value before using it as a dict key; never match only the bare
unquoted form, fixes cross-AI review Cycle 4 HIGH: hook-id parsing
previously matched only a literal unquoted `- id: <name>` line, silently
dropping any hook declared with a quoted YAML id from the mandatory 1:1
target audit, `06-REVIEWS.md` Cycle 4 New Findings), regardless of
which `repo:` block it sits under (a non-`local` `repo:` entry declares only
`id:`, with the real command living in that external hook repo's own
manifest, never in this file) and, for each id, the nearest following
`entry: <cmd>` line before the next `- id:` or end of file; returns
`dict[str, str | None]` id->entry, with `None` when no inline `entry:` line
exists for that id (read this repo's own `.pre-commit-config.yaml` first to
confirm the actual indentation/ordering your regex must tolerate for the
hooks that DO have an inline `entry:`). Fixes cross-AI review Cycle 2 HIGH:
a hook with no inline `entry:` was previously invisible to the whole audit,
silently excluding it from the 1:1 per-hook-target requirement
(`06-REVIEWS.md` Cycle 2 06-01 Concerns).

Factor the per-stack lookup into one shared helper,
`_resolve_across_stacks(name, detected_stacks, get_tooling)`, used by BOTH
the static-target loop and the new tooling-category loop below — one
staleness-gating implementation, not two: FIRST, if `detected_stacks` is
empty, return `(None, True, [])` immediately — no stack was detected at
all, so there is nothing to resolve and the gap MUST stay `needs_research:
True` with a summary stating "no stack detected" (never fall through to the
membership computation below, which would vacuously treat zero required
stacks as zero missing stacks and report false success; fixes cross-AI
review Cycle 3 HIGH: the prior version's `missing = sorted(s for s in
detected_stacks if ...)` was trivially empty whenever `detected_stacks`
itself was empty, so `check_makefile_shape` reported `needs_research: False`
with an empty-string recommendation for a repo matching no recognized
stack, `06-REVIEWS.md` Cycle 3 06-01 Concerns). Otherwise, for each stack in
`sorted(detected_stacks)`, call `get_tooling(stack)` -> `(tooling,
needs_research)`; a stack "covers" `name` ONLY when `needs_research is
False` AND `tooling.get(name) is not None` — a STALE cache entry (where
`get_tooling` itself returned `needs_research=True`) is NEVER treated as
covering `name`, even though `tooling.get(name)` may still be non-`None`
(fixes cross-AI review Cycle 2 HIGH: stale-cache trust — the prior version
ignored the `needs_research` flag `get_tooling` already computed and
trusted any non-`None` entry regardless of freshness, `06-REVIEWS.md` Cycle
2 06-01 Concerns). Collect `covering = {stack: tooling[name] for stacks that
cover name}` and `missing = sorted(s for s in detected_stacks if s not in
covering)`. Return `(None, True, missing)` if `missing` is non-empty (ANY
detected stack still lacks fresh coverage — never declare success from one
covered stack while another detected stack is still unresearched; fixes
cross-AI review Cycle 2 HIGH: multi-stack collapse, `06-REVIEWS.md` Cycle 2
06-01 Concerns); otherwise return `("; ".join(f"{s}: {covering[s]}" for s in
sorted(covering))`, `False`, `[]`)` — a combined, per-stack-labeled
recommendation only once EVERY detected stack is covered.

`check_makefile_shape(repo_root, get_tooling=None)` (default
`get_tooling=stack_cache.get_stack_tooling`): parse both files under
`repo_root` — `parse_makefile_targets`/`parse_makefile_prerequisites` and
`parse_precommit_hooks` each return an EMPTY result (`set()`/`{}`) when the
underlying file does not exist at all, never raising `FileNotFoundError` —
an absent Makefile therefore yields an empty parsed-target set (every
`REQUIRED_STATIC_TARGETS` name reported missing, the maximal and most
informative gap) and an absent `.pre-commit-config.yaml` yields an empty
`hooks` dict (zero hook-derived targets required, static-target checking
still runs normally); both loops below are unaffected by which of the two
files is present (fixes cross-AI review Cycle 3 HIGH: the specified parsers
previously read both paths unconditionally with no absent-file handling,
`06-REVIEWS.md` Cycle 3 06-01 Concerns). Compute `required =
set(REQUIRED_STATIC_TARGETS) | set(hooks.keys())`; for each name in
`required` not present in the parsed target set, build a finding dict
`{"target": name, "present": False, "criticality": CRITICALITY_CRITICAL}`;
if `name in hooks`: let `cmd = hooks[name] if hooks[name] else
f"pre-commit run {name}"` (the fallback for a hook with no inline `entry:`),
add `"recommendation": f"add a \`{name}\` target running: {cmd}"`,
`"summary": f"Add Makefile target '{name}' running: {cmd}"`, and
`"needs_research": False` (hook-derived, no research needed — the target
repo's own `.pre-commit-config.yaml`, or the external hook repo's own
manifest for a non-inline hook, already names the tool); otherwise it is one
of `REQUIRED_STATIC_TARGETS` — call `detect_stacks(repo_root)` and
`recommendation, needs_research, missing_stacks =
_resolve_across_stacks(name, detect_stacks(repo_root), get_tooling)`; if
`needs_research` AND `detect_stacks(repo_root)` is non-empty, set
`"recommendation": None`, `"needs_research": True`, `"stacks":
missing_stacks`, `"summary": f"Makefile target '{name}' needs research for
stack(s): {', '.join(missing_stacks)}"`; if `needs_research` AND
`detect_stacks(repo_root)` is EMPTY (no recognized stack at all), set
`"recommendation": None`, `"needs_research": True`, `"stacks": []`,
`"summary": f"Makefile target '{name}' needs a detected stack before any
tooling recommendation is possible — no stack markers found in this repo"`
(distinct wording from the stack-research case, so a reader can tell "no
stack detected" apart from "stack detected, cache not fresh yet" — fixes
cross-AI review Cycle 3 HIGH, `06-REVIEWS.md` Cycle 3 06-01 Concerns); else
set `"recommendation": recommendation`, `"needs_research": False`,
`"summary": f"Add Makefile target '{name}': {recommendation}"`. Present
targets are NOT included in the findings list — only gaps are reported
(ROADMAP SC-1 "flags each gap by name").

THEN run a THIRD loop over `RESEARCH_CATEGORIES` (tooling-category gaps,
distinct from the static-target gaps above — these findings carry a
`"category"` key instead of `"target"`/`"present"`, so a consumer can tell
the two finding shapes apart): define a small per-category keyword map,
`CATEGORY_KEYWORDS = {"formatter": ("format", "fmt"), "security-scanner":
("security", "bandit", "safety", "gosec", "semgrep", "trivy"),
"code-smell-detector": ("lint", "pylint", "golangci", "eslint"),
"duplicate-code-detector": ("dup", "duplicate", "jscpd"),
"dead-code-detector": ("dead", "vulture", "unused", "deadcode")}` — note
this map covers exactly the 5 `RESEARCH_CATEGORIES` entries now that
`validate-order`/`precommit-vs-prepush-split` have been moved to the two
dedicated structural checks below (see their own paragraph) and removed
from `RESEARCH_CATEGORIES` entirely (fixes cross-AI review Cycle 3 HIGH: a
category with a deliberately empty keyword tuple could never be skipped,
making it a perpetual false positive even against a fully compliant repo,
`06-REVIEWS.md` Cycle 3 06-01 Concerns); for each category, if ANY
already-present Makefile target name OR pre-commit hook id contains
(case-insensitive substring) any of that category's keywords, SKIP it — a
heuristic best-effort "already covered under a differently-named target/
hook" check, not a guarantee, stated as a limitation in this task's `<done>`
exactly like the chain/alias check's cost-ordering limitation. For every
category not skipped, call `_resolve_across_stacks(category,
detect_stacks(repo_root), get_tooling)` (the SAME helper the static-target
loop uses) and build a finding `{"category": category, "criticality":
CRITICALITY_CRITICAL}` with `recommendation`/`needs_research`/`stacks`/
`summary` populated the same way as a static-target gap above, substituting
"tooling category" for "Makefile target" in the `summary` text. Fixes
cross-AI review Cycle 2 HIGH: the cache schema had no field for these
categories and no path to surface them as findings, `06-REVIEWS.md` Cycle 2
06-02 Concerns.

THEN run two DEDICATED STRUCTURAL checks for the two process concerns
removed from `RESEARCH_CATEGORIES` above — neither needs research or a
cache entry, because both are facts readable directly from the target
repo's own files: `check_validate_order(repo_root, chain_incomplete_found)`
takes the boolean already computed by the `validate`-chain-membership check
below (no second independent computation) and NEVER appends its own
finding in either branch (fixes cross-AI review Cycle 5 MEDIUM: an earlier
draft of this check appended a second `validate-order` finding whenever
`chain_incomplete_found` was True, duplicating the SAME underlying gap
`build_remediation()` already turns into one auto-apply entry from the
`chain_incomplete` finding itself, surfacing one Makefile gap as two
overlapping remediation entries, `06-REVIEWS.md` Cycle 5 06-01 Concerns) —
when chain membership is incomplete, the existing `chain_incomplete`
finding is the single source of truth for that gap and its remediation;
when chain membership is complete, cost ORDER among the chained targets is
still not mechanically verified, but this is a permanent, structural
limitation of a regex-based checker (not a transient gap a future research
cycle could resolve), so it is documented ONCE in this function's own
docstring/the skill's prose (and in this plan's `<done>`/`success_criteria`
wording) rather than re-asserted as a per-run finding that would report
the same non-actionable caveat on every single invocation, satisfied or
not (the exact "perpetual non-actionable finding" failure shape Cycle 3
already flagged and removed for the old `validate-order`/
`precommit-vs-prepush-split` research categories — this function exists
to keep that removal honest at the CALL-SITE level, not to reintroduce the
same failure shape as a hand-rolled structural check). Second,
`check_precommit_prepush_split(repo_root)` identifies which of the
ALREADY-PRESENT pre-commit hooks are "slow" by a keyword match against a
new fixed tuple `SLOW_HOOK_KEYWORDS = ("test", "pytest", "unittest", "mypy",
"pyright", "vulture", "integration", "e2e", "arch")` (case-insensitive
substring match against that hook's id OR its `entry:` command — the
heavier test-pyramid/static-analysis tiers REQ-agtmd-makefile-rules names,
distinct from a fast formatter/linter). For EACH hook classified "slow",
determine whether THAT SPECIFIC hook's own YAML block (the text from its
`- id:` line up to the next `- id:` line or end of file — read this repo's
own `.pre-commit-config.yaml` for the real per-hook block shape) contains a
`stages:` line whose value contains `pre-push` (current pre-commit syntax)
or `push` (legacy `stages: [push]` syntax); this is a PER-HOOK check against
that hook's own block, never "does ANY hook anywhere in the file have a
push stage" (fixes cross-AI review Cycle 4 HIGH: the prior version passed
this check merely because SOME hook, anywhere in the file, had a push
stage, without ever confirming a slow hook specifically was the one moved
there, `06-REVIEWS.md` Cycle 4 New Findings). If NO hook is classified
"slow" at all, this check has nothing to verify and is satisfied trivially
(no finding). If at least one slow hook exists and ANY slow hook's own
block lacks push staging, append ONE finding naming every such hook:
`{"category": "precommit-vs-prepush-split", "issue":
"slow_hooks_not_pushed", "slow_hooks_missing_push_stage": sorted(ids),
"recommendation": "split the slower hooks into a pre-push stage (e.g. add
\`stages: [pre-push]\` to: " + ", ".join(sorted(ids)) + ") so \`pre-commit\`
itself stays fast", "needs_research": False, "criticality":
CRITICALITY_MEDIUM, "summary": <the same recommendation text>}`; when every
detected slow hook already carries its own push stage, this check adds
NOTHING. The `SLOW_HOOK_KEYWORDS` classification is stated as a heuristic,
not a guarantee — the same documented-limitation posture as the
tooling-category keyword skip above — it can miss a genuinely slow hook
whose id/entry matches none of those keywords, but it never again reports
success merely because an unrelated fast hook elsewhere in the file happens
to carry a push stage. Both checks fix cross-AI review Cycle 3 HIGH: the
two categories they replace had empty keyword tuples and could never be
classified as covered by any repo, `06-REVIEWS.md` Cycle 3 06-01 Concerns.

Define one shared helper, `_recipe_mentions_target(recipe_text, name)`, used
by BOTH structural checks below — one matching implementation, not two:
returns `True` when `re.search(rf"\b{re.escape(name)}\b", recipe_text)`
matches — a WORD-BOUNDARY-AWARE check, never a raw substring check — so a
short target name like `"lint"` is never reported "wired" merely because
`validate`'s recipe happens to invoke `"pylint"` or `"golangci-lint"` (`\b`
does not match between two `\w` characters, so "lint" inside "pylint" has no
boundary on its left); `name`'s own internal hyphen (e.g. `"test-unit"`) is
still matched as one literal token because `\b` is evaluated only at the
two ends of `name`, not inside it (fixes cross-AI review Cycle 4 HIGH:
recipe-text matching previously used a raw case-sensitive substring check
for both the chain-membership and alias checks below, so a target literally
named `lint` could be marked wired purely because the recipe text contained
`pylint`, `06-REVIEWS.md` Cycle 4 New Findings).

THEN, regardless of the above loops, run two structural checks that a target
existing BY NAME does not by itself satisfy (fixes cross-AI review Round 1
HIGH: `REQUIRED_STATIC_TARGETS` was presence-only and would pass this repo's
own real Makefile despite missing the chaining/alias structure
REQUIREMENTS.md's REQ-agtmd-makefile-rules and ROADMAP SC-1 actually
require, `06-REVIEWS.md` L40@02ca473):
1. **`validate` chain-membership.** If `"validate"` is in the parsed target
   set, get its recipe via `parse_makefile_recipe` AND its prerequisite set
   via `parse_makefile_prerequisites` (GNU Make's `validate: dep1 dep2`
   syntax is an equally valid way to wire a chain, not just calling the
   target from inside the recipe body — fixes cross-AI review Cycle 3 HIGH:
   the prior version consulted ONLY the recipe text and ignored genuine Make
   prerequisite chaining entirely, falsely flagging a repo that wires
   `validate` via prerequisites, `06-REVIEWS.md` Cycle 3 06-01 Concerns); let
   `chainable = {hook_id for hook_id in hooks if hook_id in present_targets}`
   (every per-hook target name that the repo ALSO already defines as a real
   Makefile target — a hook target that doesn't exist at all is already
   reported by the presence loop above, so this check only judges targets
   that genuinely exist). A member of `chainable` is considered WIRED if
   `_recipe_mentions_target(validate_recipe, member)` is `True` (the
   word-boundary-aware check defined above, never a raw substring check) OR
   is a member of `validate`'s own prerequisite set — either mechanism
   satisfies chain membership. If `chainable` is non-empty and any member is
   wired by NEITHER mechanism, append a finding `{"target": "validate", "present":
   True, "issue": "chain_incomplete", "missing_chain_members": sorted(m for
   m in chainable if not wired(m)), "recommendation": "restructure
   \`validate\` to invoke each of: " + ", ".join(sorted(chainable)) + "
   explicitly (via its recipe or as a Make prerequisite; lightest/cheapest
   check first)", "needs_research": False, "criticality":
   CRITICALITY_CRITICAL, "summary": <the same recommendation text>}`.
   Mechanically verifying TRUE cost ordering ("lightest-first") from
   Makefile text alone is not attempted — this check only verifies chain
   MEMBERSHIP (every chainable hook target is actually invoked from
   `validate`, by either recipe reference or prerequisite listing), which is
   the part presence-checking was missing entirely; state this limitation
   verbatim in this task's `<done>`, not silently, and do not let this
   plan's `success_criteria` claim REQ-agtmd-makefile-rules is
   unconditionally "fully implemented" while that ordering gap stands (fixes
   cross-AI review Cycle 3 HIGH, `06-REVIEWS.md` Cycle 3 06-01 Concerns).
2. **`test`/`test-unit` alias.** If both `"test"` and `"test-unit"` are in
   the parsed target set, get both recipes via `parse_makefile_recipe` AND
   both prerequisite sets via `parse_makefile_prerequisites` (the SAME two
   wiring mechanisms the chain-membership check above uses — a genuine Make
   prerequisite listing, e.g. `test: test-unit`, is an equally valid alias
   mechanism, not only a recipe-text reference or textually-identical
   recipes; fixes cross-AI review Cycle 4 HIGH: alias detection previously
   recognized ONLY a recipe-text reference or identical recipe text, so a
   repo wiring `test: test-unit` via a genuine Make prerequisite — the same
   idiomatic mechanism already accepted for `validate`'s own chain — was
   falsely reported `not_aliased`, `06-REVIEWS.md` Cycle 4 New Findings); if
   NEITHER `_recipe_mentions_target(test_recipe, "test-unit")` NOR
   `_recipe_mentions_target(test_unit_recipe, "test")` is `True` (the same
   word-boundary-aware check the chain-membership check uses, never a raw
   substring check) AND `"test-unit"` is not in `test`'s own prerequisite set
   AND `"test"` is not in `test-unit`'s own prerequisite set AND the two
   recipes are not textually identical after stripping whitespace, append a
   finding `{"target": "test-unit-alias", "present": True, "issue":
   "not_aliased", "recommendation": "make \`test\` invoke \`test-unit\`
   (e.g. \`$(MAKE) test-unit\`), list it as a Make prerequisite (\`test:
   test-unit\`), or keep their recipes identical so the two names are a
   genuine alias, per REQ-agtmd-makefile-rules", "needs_research":
   False, "criticality": CRITICALITY_CRITICAL, "summary": <the same
   recommendation text>}`.

Every finding this function returns — static-target, hook-derived,
chain/alias, or tooling-category — now carries `criticality` and `summary`,
the SAME two fields every `workflow` finding (Task 2) already carries: one
unified finding schema across both checkers (fixes cross-AI review Cycle 2
CRITICAL: field-schema mismatch between this checker's output and Plan 02's
`build_remediation()`, which assumed every finding already had these two
fields, `06-REVIEWS.md` Cycle 2 06-02 Concerns).

In `cli.py`: `check(repo_root, agents_md_path=None)` calls
`makefile_checker.check_makefile_shape(repo_root)` and returns
`{"makefile": [...]}` (workflow key added in Task 2 — do not invent it
early with an empty placeholder; Task 2 adds the real key). `main(argv)`
dispatches `argv[0]` as the subcommand name via a `dict` of subcommand name
-> handler (no `argparse` dependency, matching `ai_kit_usage_metrics.cli`'s
style); UNLIKE `ai_kit_usage_metrics.cli.main` (which takes no positional
path argument, only an `env` dict), this CLI's subcommands need an explicit
target-repo path, so fix the argv shape here rather than leaving it
implicit (fixes cross-AI review Round 1 MEDIUM: `main()` argv wiring for
`check .` was unspecified, `06-REVIEWS.md` L42@02ca473): `argv[1]` (if
present) is `repo_root`, defaulting to `"."` when omitted; for now register
only `"check"`, calling `check(repo_root)` and printing
`json.dumps(check(...), indent=2)` to stdout, returning exit code 0, or
printing a one-line usage error and returning 2 for an unrecognized/missing
subcommand. Plan 02's `remediate`/`cache-update` subcommands follow this
same `argv[1:]`-are-positional-args convention, documented here so Plan 02
does not have to re-derive it. Create
`skills/ai-kit-agents-md-checker/ai-kit-agents-md-checker.py`: shebang,
`from ai_kit_agents_md_checker.cli import main`, `sys.exit(main(sys.argv[1:]))`
under the `__main__` guard — identical shape to
`ai-kit-usage-metrics.py`.

Register the new surface across all four gates NOW, so every module Plans
02-03 add under this same package/test-file is covered automatically: in
`.pre-commit-config.yaml`, extend the `ruff` hook's `files:` alternation and
the `py-compile` hook's `files:` alternation with
`skills/ai-kit-agents-md-checker/.*` (match the existing alternation
pattern's syntax exactly — each is one parenthesized `|`-joined group), and
add `tests.test_ai_kit_agents_md_checker` to the `unittest` hook's `entry:`
module list. In `Makefile`, add the same module to the `test:` target's
`python3 -m unittest` list, and add
`skills/ai-kit-agents-md-checker/ai-kit-agents-md-checker.py
skills/ai-kit-agents-md-checker/ai_kit_agents_md_checker/*.py` to the
`lint:` target's `py_compile` argument list. In `pyproject.toml`, add
`"skills/ai-kit-agents-md-checker"` to both `[tool.pyright] include` and
`[tool.vulture] paths`, matching how `ai-kit-usage-metrics` is already
listed in both.

Create `tests/test_ai_kit_agents_md_checker.py` using the
`sys.path.insert(0, os.path.join(os.path.dirname(__file__), "..", "skills",
"ai-kit-agents-md-checker"))` + `from ai_kit_agents_md_checker import
(...)` loading convention `tests/test_ai_kit_usage_metrics.py` lines 16-21
use — NOT `importlib.util.spec_from_file_location` (that single-file loader
is the wrong precedent for a multi-module package with intra-package
imports; fixes cross-AI review Round 1 HIGH, `06-REVIEWS.md` L39@02ca473).
Write the tracer test: build a scratch repo directory; ALSO write an empty
`requirements.txt` into that same scratch directory (so `detect_stacks`
actually matches `"python"` for this fixture — fixes cross-AI review Cycle
2 HIGH: the prior fixture seeded a `python` stack-cache entry but wrote no
Python marker file into the scratch repo, so `detect_stacks` on that
directory returned an empty set and the seeded cache was never even
reachable by the stack-iteration loop, `06-REVIEWS.md` Cycle 2 06-01
Concerns); write a Makefile that is a byte-for-byte copy of THIS repo's real
`Makefile` content with the `arch-test`-equivalent target line and the
`vulture` pre-commit hook's target both absent (construct this
synthetically in the test, do not read the live repo `Makefile` from disk at
test time — the copy lives in the test file as a string constant, so the
test result never depends on this repo's Makefile changing later); write a
matching scratch `.pre-commit-config.yaml` with a `vulture` hook entry
declared with a DOUBLE-QUOTED id (`- id: "vulture"`, not the bare
`- id: vulture` form — this same fixture doubles as the quoted-YAML-id
regression test, fixes cross-AI review Cycle 4 HIGH: hook-id parsing
previously matched only an unquoted `- id: <name>` line, silently omitting
a quoted id from the mandatory 1:1 target audit, `06-REVIEWS.md` Cycle 4
New Findings) PLUS
one hook id with NO `entry:` line at all (simulating a non-`local`
externally-referenced hook); call `check_makefile_shape` with `get_tooling`
left at its default; assert a finding for `arch-test` with `needs_research`
True (no cache seeded) and `criticality`/`summary` both non-`None`; a
finding for `vulture` with `needs_research` False and a `recommendation`
string containing the hook's own `entry:` text — proving the quoted id
`"vulture"` was parsed into the bare key `"vulture"` and matched against the
Makefile's missing `vulture` target, not silently dropped; and a finding for
the no-`entry:` hook's target with `recommendation` containing `"pre-commit
run"` followed by its id. Add a dedicated `test_parse_precommit_hooks_handles_quoted_ids`
case directly against `parse_precommit_hooks` (not only via the end-to-end
fixture above) asserting BOTH a double-quoted and a single-quoted id line
parse to the correct bare-string dict keys. Then seed a `stack_cache` fixture (write a fresh
`python` entry via `write_stack_cache` into a scratch `cache_root`, using
`stack_cache.REQUIRED_STATIC_TARGETS` keys — e.g. `{"setup-env": "uv venv &&
uv sync"}`, never `{"setup_env": ...}`) and re-run with
`get_tooling=functools.partial(stack_cache.get_stack_tooling,
cache_root=<scratch>)`, now asserting the `setup-env` finding (if present in
the fixture's gap set) carries `needs_research: False` and a non-`None`
recommendation sourced from the seeded cache. Add a `TestStaleCacheNotTrusted`
case: seed a `python` cache entry, then manually backdate its `cached_at`
past `STALE_AFTER_DAYS` in the written JSON — assert the matching target's
finding STILL carries `needs_research: True` and `recommendation: None`,
proving a stale entry is never silently trusted merely because
`tooling.get(name)` is non-`None`. Add a `TestMultiStackResolution` case: a
scratch repo matching two stacks (e.g. both `requirements.txt` and
`package.json` present) where only ONE of the two has a fresh cache entry
for a given target — assert `needs_research: True` and `"stacks"` lists
ONLY the stack that still lacks a fresh entry (not both); a second case
where BOTH stacks have a fresh entry for that target — assert
`needs_research: False` and a combined `recommendation` string mentioning
both stack names; a third case where NEITHER stack's cache is fresh —
assert `needs_research: True` and `"stacks"` lists both detected stack
names, sorted. Add a `TestToolingCategoryGaps` case: seed a fresh cache entry
for one `RESEARCH_CATEGORIES` key (e.g. `"security-scanner"`) for the one
detected stack, with NO existing Makefile target/hook name matching that
category's keywords — assert a finding with `"category": "security-scanner"`
and `needs_research: False` appears; a second case where an existing hook id
DOES match one of that category's keywords (e.g. a `"bandit"` hook present)
— assert NO finding for that category appears (the keyword-coverage skip).
Add a `TestValidateChainAndAlias` case: a scratch Makefile
with `validate`/`test`/`test-unit`/a hook-named target all present by name,
but `validate`'s recipe referencing none of them and `test`'s recipe not
referencing `test-unit` — assert both the `chain_incomplete` and
`not_aliased` findings appear with `needs_research: False` and a
non-`None` `recommendation`; a second case where `validate`'s recipe DOES
reference every chainable member and `test`'s recipe DOES invoke
`test-unit` — assert neither finding appears. Add a
`test_alias_via_prerequisite` case: a scratch Makefile where `test`'s recipe
never mentions `test-unit` at all, but `test` lists `test-unit` as a genuine
Make prerequisite instead (`test: test-unit`) — assert NO `not_aliased`
finding appears (prerequisite listing satisfies alias detection just as a
recipe reference would, mirroring `test_validate_chain_via_prerequisites`
below; fixes cross-AI review Cycle 4 HIGH: alias detection previously
recognized only a recipe-text reference or identical recipe text, falsely
reporting a valid `test: test-unit` prerequisite alias as `not_aliased`,
`06-REVIEWS.md` Cycle 4 New Findings). Add a
`test_wired_check_is_word_boundary_aware` case: a scratch Makefile with a
target literally named `lint` and a `validate` recipe that invokes
`"uv run pylint src/"` but never the standalone token `"lint"`, with `lint`
not listed as a `validate` prerequisite either — assert the `chain_incomplete`
finding'S `missing_chain_members` STILL includes `"lint"` (a raw substring
match on `"pylint"` must NOT satisfy chain membership; fixes cross-AI review
Cycle 4 HIGH: recipe matching previously used a raw substring check, so a
target named `lint` could be falsely reported wired merely because the
recipe invoked `pylint`, `06-REVIEWS.md` Cycle 4 New Findings); a second
sub-case where the recipe instead invokes the standalone token `"lint"` (not
as part of a longer word) — assert `"lint"` is NOT in `missing_chain_members`.
Add the three `stack_cache`
state tests from `<behavior>` (absent/fresh/stale, backdating
`cached_at` directly in the written JSON for the stale case). Add a
`TestCachePathValidation` case: `cache_path("python")` and
`cache_path("node-lts")` succeed; `cache_path("../../etc/passwd")`,
`cache_path("/etc/passwd")`, `cache_path("a/b")`, and `cache_path("")` each
raise `ValueError` — proving path-traversal-shaped and absolute-path-shaped
`stack` values are rejected before any path join happens (fixes cross-AI
review Cycle 3 HIGH — SECURITY, `06-REVIEWS.md` Cycle 3 06-02 Concerns). Add a
`TestGateRegistration` class covering the FULL surface, matching
`tests.test_ai_kit_usage_metrics.TestGateRegistration`'s own assertions (not
a thinner subset — fixes cross-AI review Round 1 MEDIUM, `06-REVIEWS.md`
L43@02ca473): `test_makefile_registers_module_and_package` asserts both
`"tests.test_ai_kit_agents_md_checker"` and
`"skills/ai-kit-agents-md-checker/"` appear in `Makefile`'s text;
`test_precommit_ruff_and_py_compile_match_package_path` asserts, for BOTH
`"ruff"` and `"py-compile"` hook ids, `re.match` of that hook's `files:`
regex against the REAL nested package path
`"skills/ai-kit-agents-md-checker/ai_kit_agents_md_checker/cli.py"`
succeeds (never the non-existent flat
`"skills/ai-kit-agents-md-checker/cli.py"` path the unrevised plan named),
and that the `unittest` hook's `entry:` text contains
`"tests.test_ai_kit_agents_md_checker"`; `test_pyright_include_contains_package_directory`
parses `pyproject.toml` with `tomllib` and asserts
`"skills/ai-kit-agents-md-checker"` is in `[tool.pyright] include`; add a
fourth assertion this repo's own precedent omits but this plan's frontmatter
`key_links` promises — `test_vulture_paths_contains_package_directory`
asserts the same string is in `[tool.vulture] paths`.
  </action>

  <verify>
    <automated>python3 -m unittest tests.test_ai_kit_agents_md_checker -v</automated>
    <fails_when>any test errors or fails, or the module fails to import (non-zero exit, "FAILED" or "ERROR" in output)</fails_when>
  </verify>
  <verify>
    <automated>python3 skills/ai-kit-agents-md-checker/ai-kit-agents-md-checker.py check . | python3 -c "import json,sys; d=json.load(sys.stdin); assert any(f['target']=='arch-test' for f in d['makefile'])"</automated>
    <fails_when>the script exits non-zero, prints non-JSON, or the assertion raises (no `arch-test` finding against this repo's own real, currently-non-compliant Makefile)</fails_when>
  </verify>

  <done>
    `check_makefile_shape` names every required-but-absent Makefile target
    (including a per-hook target whose hook has no inline `entry:`, via the
    `pre-commit run <id>` fallback), hook-derived gaps carry a recommendation
    with no research flag, and static-target gaps carry either a
    cache-sourced recommendation or an honest `needs_research: True` — never
    an invented tool name, and never trusting a stale cache entry (proven by
    `TestStaleCacheNotTrusted`). Multi-stack detection never reports success
    while ANY detected stack still lacks a fresh entry for a target (proven
    by `TestMultiStackResolution`'s three cases). The same staleness-gated,
    all-stacks-covered resolution also surfaces tooling-category gaps
    (formatter/security-scanner/etc.) not already covered by an existing
    target/hook name, proven by `TestToolingCategoryGaps`; the keyword-based
    "already covered" skip is stated as a heuristic, not a guarantee.
    `validate`-chain and `test`/`test-unit`-alias structural checks flag a
    present-by-name-but-not-actually-wired target — wired via EITHER a
    recipe reference OR a genuine Make prerequisite, proven by
    `TestValidateChainAndAlias`, `test_validate_chain_via_prerequisites`, and
    `test_alias_via_prerequisite` (a `test: test-unit` prerequisite alias is
    recognized, never falsely reported `not_aliased`); both checks' recipe
    matching is word-boundary-aware, never a raw substring match, proven by
    `test_wired_check_is_word_boundary_aware` (a target named `lint` is never
    falsely "wired" merely because the recipe invokes `pylint`).
    Mechanically verifying "lightest-first" COST ORDER (as opposed to
    chain/alias MEMBERSHIP) is explicitly NOT attempted by this checker and
    is not claimed as implemented — `check_validate_order` and
    `check_precommit_prepush_split` add two honest structural checks for the
    two process concerns that were previously unsatisfiable empty-keyword
    research categories, without ever claiming cost ORDER itself is
    verified. A repo with no Makefile or no `.pre-commit-config.yaml` at all
    is reported as the maximal gap (every required target missing), never a
    crash, proven by `test_check_missing_makefile_and_precommit_no_crash`. A
    repo matching no detected stack at all keeps every static-target gap
    `needs_research: True`, never a vacuous empty-recommendation success,
    proven by `test_no_detected_stack_stays_needs_research`. Every
    finding this function returns — static-target, hook-derived, chain/
    alias, or tooling-category — carries `criticality` and `summary`,
    matching the schema every `workflow` finding (Task 2) already carries.
    The new test module and skill path are live in all four registration
    surfaces (Makefile, `.pre-commit-config.yaml` ruff/py-compile/unittest,
    `pyproject.toml` pyright/vulture), proven by `TestGateRegistration`'s
    executed assertions, not prose.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Workflow-rule prose checker with live tool-presence conditioning</name>
  <files>skills/ai-kit-agents-md-checker/ai_kit_agents_md_checker/tool_presence.py, skills/ai-kit-agents-md-checker/ai_kit_agents_md_checker/rule_checker.py, skills/ai-kit-agents-md-checker/ai_kit_agents_md_checker/cli.py, tests/test_ai_kit_agents_md_checker.py</files>

  <read_first>
    - `skills/ai-kit-agents-md-checker/ai_kit_agents_md_checker/rules.py` (just written in Task 1) — the full `RULES` tuple, especially each entry's `condition` and `signals`.
    - `tools/hooks/detect.py` — `detect_substitutions`'s `shutil.which(name, path=search_path)` resolution pattern (never touches `os.environ` directly, so tests can inject a scratch PATH) — adapt this shape, do not import it.
    - `tests/test_tool_substitution_hook.py`'s `fake_bin(root, names, body)` / `scoped_env(bin_dir, home)` helpers — the scratch-PATH test harness this task's tool-presence tests reuse the pattern of (re-implement locally in this test file; do not import across test modules).
    - `06-CONTEXT.md` D-03 and D-07 — near-miss leniency and condition-based skip/reframe.
  </read_first>

  <behavior>
    - `check_tool_presence(search_path=None, repo_root=None)` returns `{"rtk": False, "modern-cli": False, "modern-cli:rg": False, "modern-cli:bat": False, "modern-cli:sd": False, "modern-cli:fd": False, "modern-cli:eza": False, "codegraph": False, "graphify": False, "gsd": False}` against an empty scratch PATH and repo root.
    - With a scratch PATH containing `rg` and `bat` stubs (but not `sd`/`fd`/`eza`) and a repo root containing a `.codegraph/` dir, the same call returns `modern-cli: True` (aggregate any-of), `modern-cli:rg: True`, `modern-cli:bat: True`, `modern-cli:sd: False`, `modern-cli:fd: False`, `modern-cli:eza: False`, and `codegraph: True`.
    - `check_workflow_rules(text, tool_presence)` against AGENTS.md text containing none of R01's `signals` returns `status="missing"` for R01; against text containing at least half of its `signals` returns `status="present"`; against text containing some but under half returns `status="near_miss"`.
    - `check_workflow_rules` against a `tool-presence:rtk` rule when `tool_presence["rtk"]` is `False` returns `status="not_applicable"` regardless of text content — never `"missing"`.
    - `check_workflow_rules` against R04 (plain grouped signals) with text containing ONLY its review-clause phrasing returns `status="near_miss"`, never `"present"`; with text containing at least one phrase from EACH of its three groups, returns `status="present"`; with text containing none of any group, returns `status="missing"`.
    - `check_workflow_rules` against R06 (tool-scoped grouped signals) when `tool_presence` shows `rg` and `bat` installed (`sd`/`fd`/`eza` not) and the text mentions `"ripgrep"` but never `"bat"`: returns `status="near_miss"` for R06 (one of the two REQUIRED groups — `rg`, `bat` — is satisfied, the other is not; `sd`/`fd`/`eza`'s groups are not required at all since those tools aren't installed, so their absence from the text does not affect the classification); when the text also mentions `"bat"`, returns `status="present"`; when `tool_presence["modern-cli"]` is `False` (no modern-cli tool installed at all), returns `status="not_applicable"` regardless of text.
    - `check_workflow_rules` includes R02 in its output despite `category == "makefile"`, because R02 has a non-empty `signals` tuple; every returned finding carries a `"matched_signals"` list naming which literal phrases were found (empty for `"missing"`).
  </behavior>

  <action>
Implement `tool_presence.py`: `MODERN_CLI_TOOLS = ("rg", "bat", "sd", "fd",
"eza")`. `check_tool_presence(search_path=None, repo_root=None)` returns a
`dict[str, bool]` keyed by the same condition names used in `rules.py`
(`"rtk"`, `"modern-cli"`, `"codegraph"`, `"graphify"`, `"gsd"`), PLUS one
additional per-tool key `f"modern-cli:{tool}"` for EACH of the 5
`MODERN_CLI_TOOLS` (fixes cross-AI review Cycle 3 HIGH: the aggregate-only
`"modern-cli"` boolean let R06 activate on any single installed tool while
its flat signal list only ever checked for `rg`, so guidance could pass
while never mentioning an installed `bat`/`sd`/`fd`/`eza`, `06-REVIEWS.md`
Cycle 3 06-01 Concerns): `"rtk"` = `shutil.which("rtk", path=search_path) is
not None`; `"modern-cli"` = any of `shutil.which(name, path=search_path)`
across `MODERN_CLI_TOOLS` (the aggregate, still used for R06's rule-level
`condition` — whether the RULE applies at all); `f"modern-cli:{name}"` =
`shutil.which(name, path=search_path) is not None` for each `name` in
`MODERN_CLI_TOOLS` individually (used by `rule_checker.py` to decide which
of R06's tool-scoped groups are actually REQUIRED); `"codegraph"` =
`os.path.isdir(os.path.join(repo_root or ".", ".codegraph"))`; `"graphify"`
= `shutil.which("graphify", path=search_path) is not None` OR
`os.path.isdir(os.path.join(repo_root or ".", "graphify-out"))`; `"gsd"` =
`os.path.isdir(os.path.join(repo_root or ".", ".planning"))`. Resolve every
binary through the `path=` parameter, never by mutating `os.environ`, so
tests stay hermetic on a scratch PATH exactly like `tools/hooks/detect.py`.

Implement `rule_checker.py`: `evaluate_condition(condition, tool_presence)`
returns `True` for `rules.CONDITION_ALWAYS`, otherwise parses
`"tool-presence:<name>"` and looks up `tool_presence.get(name, False)`.
`check_workflow_rules(agents_md_text, tool_presence)` iterates `rules.RULES`
filtered to rules with a non-empty `signals` tuple — NOT filtered by
`category == "workflow"` (R02 has `category == "makefile"` but a non-empty
`signals` tuple for its own prose clause, so this filter change is what
brings R02 into workflow-prose checking; fixes cross-AI review Cycle 2 HIGH:
R02 excluded entirely, `06-REVIEWS.md` Cycle 2 06-01 Concerns). For a rule
whose `evaluate_condition` is `False`, emit `{"id": rule.id, "status":
"not_applicable", "reason": f"condition '{rule.condition}' not met in this
environment", "matched_signals": []}` and do NOT run the keyword heuristic
at all (D-07: skip or reframe, never flag as a generic gap). For an
applicable rule, lowercase `agents_md_text` once. If `rule.signals`'s first
element is itself a tuple, distinguish TWO grouped shapes before falling
through to FLAT handling: (a) PLAIN groups (currently only R04) — each inner
element is a tuple of phrases — treat each as one independently-required
clause: a group is "satisfied" if ANY ONE of its own phrases is found as a
substring; classify zero satisfied groups -> `"missing"`, all groups
satisfied -> `"present"`, otherwise -> `"near_miss"` (fixes cross-AI review
Cycle 2 HIGH: a flat aggregate threshold let R04's review-only phrasing
satisfy the whole rule with zero execution/cheap-model/path-agnostic
evidence, `06-REVIEWS.md` Cycle 2 06-01 Concerns); `"matched_signals"` is the
sorted union of every matched phrase across all groups. (b) TOOL-SCOPED
groups (currently only R06) — each inner element is a 2-tuple `(tool_name:
str, phrases: tuple)`, detected by checking the inner element's first member
is itself a `str` (PLAIN groups' inner elements are tuples-of-tuples/
tuples-of-str with no such string-first shape): a group is REQUIRED only
when `tool_presence.get(f"modern-cli:{tool_name}", False)` is `True` —
a group for a tool that is not actually installed is DROPPED from
consideration entirely, never counted toward `missing`/`near_miss`/
`present` and never demanded in the text (fixes cross-AI review Cycle 3
HIGH: collapsing all 5 modern-CLI tools into one aggregate `"modern-cli"`
boolean let guidance pass while silently omitting an installed tool's own
mention, `06-REVIEWS.md` Cycle 3 06-01 Concerns); among the REQUIRED groups
only, a group is "satisfied" if ANY ONE of its own `phrases` is found as a
substring; classify zero required groups (can only happen if the rule's
`condition` aggregate was somehow `True` while no individual tool key is —
treat this edge case as `"not_applicable"`, never crash), zero SATISFIED
required groups -> `"missing"`, all required groups satisfied ->
`"present"`, otherwise -> `"near_miss"`; `"matched_signals"` is the sorted
union of matched phrases across required groups, and the finding ALSO
carries `"missing_tools"`: sorted list of required-but-unsatisfied groups'
`tool_name`s (empty for `"present"`), so a near-miss/missing result names
exactly which INSTALLED tool(s) still need a mention, not just "R06 is
incomplete." Otherwise (FLAT signals — every other rule, including R02),
count how many of `rule.signals` appear as a substring, and classify:
zero matches -> `"missing"`; all matches, or at least
`math.ceil(len(signals)/2)` when there are 3+ signals -> `"present"`;
otherwise -> `"near_miss"`; `"matched_signals"` is the sorted list of
matched phrases. Each finding also carries `"criticality"` and `"summary"`
copied from the rule, so Plan 02's remediation builder needs no second
lookup into `rules.py`. Return only non-`"present"` findings by default
(mirrors `check_makefile_shape` reporting gaps, not passes) via a
`include_present=False` keyword defaulting to that; when `True`, include
every rule's status (useful for a future `--verbose`, not required by this
plan's CLI surface).

Implement `resolve_instruction_text(repo_root, agents_md_path=None)` in a
new small module-level function in `cli.py` (used by `check()` below, and
re-exported for Plan 02's wrapper to import — one implementation, not two,
per the shared-root-cause note in this plan's `must_haves`): if
`agents_md_path` is given, use it as-is. Otherwise resolve by checking, IN
ORDER, `os.path.join(repo_root, "AGENTS.md")` then
`os.path.join(repo_root, "CLAUDE.md")` — the FIRST one that exists wins; if
NEITHER exists, fall back to the `AGENTS.md` path (so the absent-file
behavior below still applies) (fixes cross-AI review Cycle 3 HIGH: the
public CLI could not check a CLAUDE.md-only repository because the default
path assumed `AGENTS.md` exclusively, treating a real `CLAUDE.md`'s content
as an empty instruction file, `06-REVIEWS.md` Cycle 3 06-01 Concerns). Read
the resolved path's text (empty string if it does not exist — an absent
instruction file is the maximal-gap case, not an error). THEN, regardless of
which file was resolved, scan that text for Markdown links of the form
`[label](relative/path.md)` whose target is a relative path (never an
absolute path or a URL containing `://`) that resolves, relative to the
resolved file's OWN directory, to an existing file — CONTAINMENT is checked
via `os.path.realpath`, never lexical path comparison: compute
`real_repo_root = os.path.realpath(repo_root)` once, then for each candidate
link target compute `real_link = os.path.realpath(candidate_path)` and
require `real_link == real_repo_root or real_link.startswith(real_repo_root
+ os.sep)` before treating it as "under `repo_root`" — a candidate whose
LEXICAL path looks like it is under `repo_root` but whose real path (after
resolving any symlink anywhere in the path, including the candidate file
itself or an intermediate directory) escapes outside `real_repo_root`
(e.g. a symlink planted inside the target repo pointing at `/etc/passwd` or
a sibling directory) is REJECTED and never read (fixes cross-AI review
Cycle 4 MEDIUM: containment previously had no symlink-safe realpath
contract, so a lexically in-repo-looking link could resolve outside the
repository via a symlink, `06-REVIEWS.md` Cycle 4 New Findings); the
`os.sep`-suffixed `startswith` check (not a bare prefix check) also avoids
the classic sibling-directory false-positive where `real_repo_root` is a
string prefix of an unrelated path (e.g. `/repo` vs `/repo-other`) — for
each such link found (deduplicated, capped at 20 to bound a pathological
file), read that linked file's text too and APPEND it (each separated by a
blank line) to the text returned for rule classification. This follows
EXACTLY the progressive-disclosure shape `/agent-md-refactor` itself
produces (a root file with links out to topic files it moved content into)
one level deep — deep/transitive link-following is explicitly NOT
attempted, since `/agent-md-refactor`'s own structure is at most one hop
from the root file by design (fixes cross-AI review Cycle 3 HIGH: the
checker previously classified ONLY the root file's own text, so a rule
`/agent-md-refactor` had already relocated into a linked file it still
satisfied was falsely reported missing on a later run, and fed back into
`/agent-md-refactor` as a duplicate-insertion candidate, `06-REVIEWS.md`
Cycle 3 06-01 Concerns). Return the combined text.

Extend `cli.py`'s `check(repo_root, agents_md_path=None)`: call
`resolve_instruction_text(repo_root, agents_md_path)` to get the combined
text (root file plus any one-hop linked files) rather than reading
`AGENTS.md` directly; call
`tool_presence.check_tool_presence(repo_root=repo_root)`; call
`rule_checker.check_workflow_rules(text, presence)`; sort the combined
`workflow` findings by `criticality` ascending (D-02, most-critical-first —
`CRITICALITY_CRITICAL` is the smallest int) before returning; return
`{"makefile": [...], "workflow": [...]}`.

Add to `tests/test_ai_kit_agents_md_checker.py`: a local `fake_bin`/
`scoped_env` pair (re-implemented, not imported, matching
`test_tool_substitution_hook.py`'s shape) for `TestToolPresence`'s two
`<behavior>` cases. A `TestRuleChecker` class covering the three
classification cases from `<behavior>` using R01 (3+ signals) and a
`tool-presence:rtk` rule's `not_applicable` case with `tool_presence =
{"rtk": False, ...}`; a `test_r04_grouped_signals_require_every_group` case
covering all three sub-cases (review-only text -> `near_miss`, all-three
groups present -> `present`, none present -> `missing`); a
`test_r06_tool_scoped_groups_only_require_installed_tools` case with a
`tool_presence` dict showing `rg`/`bat` installed and `sd`/`fd`/`eza` not —
text mentioning only `"ripgrep"` -> `status="near_miss"` with
`missing_tools == ["bat"]`; text mentioning both `"ripgrep"` and `"bat"` ->
`status="present"` with `missing_tools == []` (never penalized for omitting
the three uninstalled tools); a second sub-case with no modern-CLI tool
installed at all -> `status="not_applicable"` regardless of text (fixes
cross-AI review Cycle 3 HIGH, `06-REVIEWS.md` Cycle 3 06-01 Concerns); a
`test_r02_included_by_signals_not_category` case asserting R02's `id`
appears in `check_workflow_rules`'s output (with `include_present=True`)
against text containing none of its signals, proving the filter change
actually includes it; a `test_matched_signals_field` case asserting a
`near_miss` finding's `"matched_signals"` list is non-empty and a subset of
that rule's own `signals`. A `test_check_combines_and_orders_by_criticality`
exercising `cli.check()` end to end against a scratch repo + scratch
AGENTS.md text missing R01 (criticality 1) and R17 (criticality 3), and
asserting the returned `workflow` list has R01's entry before R17's. A
`test_resolve_instruction_text_falls_back_to_claude_md` case: a scratch repo
with NO `AGENTS.md` but a real `CLAUDE.md` containing R01's signals —
assert `cli.check(scratch_repo)`'s `workflow` list does NOT include R01
(because `resolve_instruction_text` found and classified the `CLAUDE.md`
content, not an empty string), proving a CLAUDE.md-only repo is actually
checked rather than treated as having no instruction file (fixes cross-AI
review Cycle 3 HIGH, `06-REVIEWS.md` Cycle 3 06-01 Concerns). A
`test_resolve_instruction_text_follows_one_hop_links` case: a scratch
`AGENTS.md` containing a Markdown link `[testing](docs/testing.md)` and NONE
of R03's signals itself, plus a scratch `docs/testing.md` containing ALL of
R03's signals — assert `cli.check(scratch_repo)`'s `workflow` list does NOT
include R03 (found via the one-hop link, not the root file alone), and a
second case where `docs/testing.md` is absent entirely (a dangling link) —
assert `check()` still returns normally (no crash), with R03 correctly
reported missing. A `test_resolve_instruction_text_rejects_symlink_escape`
case (requires `os.symlink`, skip on a platform where symlinks are
unsupported): a scratch `AGENTS.md` linking to `docs/escape.md`, where
`docs/escape.md` is a real symlink (created via `os.symlink`) pointing at a
file OUTSIDE the scratch repo root entirely (e.g. a sibling scratch
directory's file containing R03's signals) — assert
`cli.check(scratch_repo)`'s `workflow` list STILL reports R03 as missing
(the symlinked-outside target is never read, never folded into the
classification text), proving containment is checked against the REAL,
resolved path, not the lexical one (fixes cross-AI review Cycle 4 MEDIUM:
containment previously had no symlink-safe realpath contract, allowing a
lexically in-repo-looking link to resolve outside the repository,
`06-REVIEWS.md` Cycle 4 New Findings). A `test_check_missing_makefile_and_precommit_no_crash`
case: a scratch repo with no `Makefile` and no `.pre-commit-config.yaml` at
all — assert `cli.check(scratch_repo)` returns normally (no exception) with
EVERY `REQUIRED_STATIC_TARGETS` name present in the `makefile` findings list
(fixes cross-AI review Cycle 3 HIGH: missing Makefile/pre-commit config had
no defined non-crashing behavior, `06-REVIEWS.md` Cycle 3 06-01 Concerns).
A `test_validate_chain_via_prerequisites` case: a scratch Makefile where
`validate`'s recipe never mentions a chainable hook-named target, but
`validate: <hook-target-name>` lists it as a genuine Make prerequisite
instead — assert NO `chain_incomplete` finding appears (prerequisite wiring
satisfies chain membership just as a recipe reference would, fixes
cross-AI review Cycle 3 HIGH, `06-REVIEWS.md` Cycle 3 06-01 Concerns). A
`test_no_detected_stack_stays_needs_research` case: a scratch repo with NONE
of the recognized stack marker files — assert every static-target finding's
`needs_research` is `True` with an empty `"stacks"` list and a summary
mentioning "no stack detected," never `needs_research: False` with an empty
recommendation (fixes cross-AI review Cycle 3 HIGH: vacuous fresh coverage
over zero detected stacks, `06-REVIEWS.md` Cycle 3 06-01 Concerns). A
`test_validate_order_and_precommit_prepush_split` case covering both new
structural checks: (a) a scratch Makefile/pre-commit pair with
`chain_incomplete` already raised — assert `check_validate_order` returns
no finding (the existing `chain_incomplete` finding remains the sole
source of truth for that gap, fixes cross-AI review Cycle 5 MEDIUM: an
earlier draft appended a second, duplicate `validate-order` finding here,
`06-REVIEWS.md` Cycle 5 06-01 Concerns); with chain membership complete —
assert `check_validate_order` STILL returns no finding (the residual
cost-order limitation is documented once in prose, never re-asserted as a
per-run finding in either branch); (b) a scratch `.pre-commit-config.yaml`
with a SLOW hook
(e.g. id `"unittest"`, matching `SLOW_HOOK_KEYWORDS`) that has NO
`stages: [pre-push]`/`stages: [push]` on its OWN block, PLUS an unrelated
FAST hook (e.g. id `"ruff"`) that DOES carry `stages: [push]` on its own
block — assert a `precommit-vs-prepush-split` finding STILL appears naming
the slow hook in `slow_hooks_missing_push_stage` (proving the fast hook's
unrelated push stage does NOT satisfy this check — fixes cross-AI review
Cycle 4 HIGH: the prior version was satisfied by any hook anywhere having a
push stage, `06-REVIEWS.md` Cycle 4 New Findings); a second sub-case where
the SAME slow hook's OWN block carries `stages: [pre-push]` — assert no such
finding appears; a third sub-case with NO hook matching `SLOW_HOOK_KEYWORDS`
at all — assert no such finding appears (nothing to verify); and assert
NEITHER `"validate-order"` nor `"precommit-vs-prepush-split"` ever appears as
an entry in `RESEARCH_CATEGORIES` itself (fixes cross-AI review Cycle 3
HIGH: both were previously unsatisfiable empty-keyword research categories,
`06-REVIEWS.md` Cycle 3 06-01 Concerns).
  </action>

  <verify>
    <automated>python3 -m unittest tests.test_ai_kit_agents_md_checker -v</automated>
    <fails_when>any test errors or fails (non-zero exit, "FAILED" or "ERROR" in output)</fails_when>
  </verify>
  <verify>
    <automated>python3 skills/ai-kit-agents-md-checker/ai-kit-agents-md-checker.py check . | python3 -c "
import json, sys
d = json.load(sys.stdin)
workflow = d['workflow']
assert isinstance(workflow, list)
assert all(f['status'] in ('missing', 'near_miss', 'not_applicable') for f in workflow)
assert all(f.get('status') != 'present' for f in workflow)
print('ok', len(workflow))
"</automated>
    <fails_when>the command errors, prints non-JSON, or any `workflow` entry carries `status == "present"` — `include_present=False` must exclude satisfied rules even against this real repo's actual AGENTS.md. This is a STRUCTURAL sanity cross-check only (does the real CLI run end-to-end and honor the exclusion contract against real, messy input) — it deliberately does NOT assert which SPECIFIC rule id is missing/near_miss/not_applicable, because this repo's own real AGENTS.md content is expected to evolve independently of this checker and achieving full 17-rule compliance in AGENTS.md is explicitly not a Phase 6 deliverable (06-CONTEXT.md code_context's Makefile note applies the same logic here). The scratch-fixture unit tests above (`TestRuleChecker`, `test_check_combines_and_orders_by_criticality`) are the sole source of truth for per-rule-id classification correctness — fixes cross-AI review Round 1 CRITICAL: the previous version of this check asserted `'R01' in ids`, which is FALSE today because `AGENTS.md:20,22` already literally contains R01's `"english only"`/`"non-negotiable"` signals (2 of 3, meeting the `>= ceil(3/2)` present threshold), so R01 is correctly EXCLUDED from `workflow` by design — the old assertion was asserting the opposite of what correct behavior produces (`06-REVIEWS.md` L38@02ca473).</fails_when>
  </verify>

  <done>
    `check_workflow_rules` reports every non-applicable, non-present rule
    individually with a status distinguishing `missing`/`near_miss`/
    `not_applicable`, tool-presence conditions are live-verified (never
    assumed) — including each modern-CLI tool individually, so R06 never
    collapses `rg`/`bat`/`sd`/`fd`/`eza` presence into one boolean — and the
    combined CLI report is ordered most-critical-first. `resolve_instruction_text`
    checks `AGENTS.md` then falls back to `CLAUDE.md`, so a CLAUDE.md-only
    repo is actually checked against its real content, and follows one hop
    of same-repo relative Markdown links out of the root file — containment
    checked via `os.path.realpath` on both sides, so a symlink-escaped link
    is rejected rather than lexically trusted — so a rule
    `/agent-md-refactor` relocated into a linked, progressive-disclosure file
    is still found there rather than falsely reported missing, proven by
    `test_resolve_instruction_text_rejects_symlink_escape`. The live
    cross-check against this repo's real AGENTS.md asserts the structural
    exclusion contract only (no `"present"` entries leak into `workflow`),
    never a specific rule id's outcome — per-rule-id correctness is proven by
    the scratch-fixture unit tests, which are immune to this repo's
    AGENTS.md content changing over time.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| target repo filesystem -> checker | `Makefile`, `.pre-commit-config.yaml`, and `AGENTS.md` in the repo being checked are untrusted, arbitrary, possibly adversarial text that this skill parses and later (Plan 02/03) surfaces to an LLM agent and to `/agent-md-refactor` |
| stack cache -> checker | `~/.cache/ai-kit/agents-md-checker/stack-refs/*.json` is local, but its contents originate from an LLM research pass (Plan 02) and are read back as trusted-enough-to-recommend data |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-01 | Tampering | `makefile_checker.parse_makefile_targets`/`parse_precommit_hooks`, `rule_checker.check_workflow_rules` | high | mitigate | Target-repo content (Makefile, `.pre-commit-config.yaml`, `AGENTS.md` text) is parsed with fixed regexes into plain strings/booleans ONLY — no `eval`, no shelling out to `make`/`pre-commit` against the target repo, no following of any instruction-shaped text found inside it. This plan's functions never execute anything the target repo's own files say; they only classify. (Plan 02/03 carry the corresponding rule for the remediation-payload and SKILL.md layers, which are the parts that hand content to an LLM agent.) |
| T-06-02 | Information Disclosure | `stack_cache` files under `~/.cache` | low | accept | Cache entries hold only tool-name/category strings for a detected stack (e.g. "python" -> formatter name) — no repo-specific secrets, paths, or content are ever written into the cache; the cache key is the stack name, not the repo path. |
| T-06-03 | Denial of Service | `parse_makefile_targets`/`parse_precommit_hooks` against a huge or malformed file | low | accept | Both are single-pass line scans with no backtracking-prone regex and no recursion; a pathological input at worst costs linear time proportional to file size, and these files are developer-authored config, not attacker-supplied network input. |

No package-manager install tasks exist in this plan (no new third-party
dependency is added — Makefile/`.pre-commit-config.yaml` parsing is
hand-rolled regex, matching this repo's existing "hand-rolled scanner over
new dependency" precedent), so the Package Legitimacy Gate does not apply.
</threat_model>

<verification>
- `python3 -m unittest tests.test_ai_kit_agents_md_checker -v` is green.
- `python3 -m py_compile` over every new `.py` file under
  `skills/ai-kit-agents-md-checker/` succeeds with no output.
- The four registration surfaces (`Makefile` `test:`/`lint:`,
  `.pre-commit-config.yaml` `ruff`/`py-compile`/`unittest`, `pyproject.toml`
  `pyright`/`vulture`) all name the new path/module, confirmed by the
  `TestGateRegistration` test, not by eyeballing the diff.
</verification>

<success_criteria>
- ROADMAP Phase 6 Success Criterion 2 (workflow-rule checking) is satisfied
  at the checker level. Success Criterion 1 (Makefile target-shape
  checking) is satisfied for every clause EXCEPT one: SC-1's own text
  requires a `validate` target "chaining all hook targets lightest-first,"
  and this plan mechanically verifies chain/prerequisite MEMBERSHIP only —
  true cost ORDER ("lightest-first") is NOT mechanically verified and is
  NOT claimed satisfied (see the explicit limitation below); SC-1 is
  therefore satisfied only PARTIALLY, not claimed fully/unqualifiedly met,
  consistent with how REQ-agtmd-makefile-rules' own cost-ordering
  limitation is already stated further down in this section (fixes
  cross-AI review Cycle 4 HIGH: this bullet previously claimed SC-1
  unqualifiedly "satisfied" while the requirement-level bullet below it
  already narrowed the same claim for cost ordering — an internal
  contradiction within this plan's own success criteria, `06-REVIEWS.md`
  Cycle 4 New Findings). (Remediation/wrapping and the skill pipeline
  finish in Plans 02-03.)
- REQ-agtmd-workflow-rules is fully implemented at the checker level,
  including R02's own prose clause (checked via signals, not category),
  R04's three independently-required plain signal groups, and R06's
  tool-scoped signal groups (never collapsing per-tool modern-CLI presence
  into one aggregate boolean).
  REQ-agtmd-makefile-rules is implemented at the checker level for
  target-name presence (including non-inline-entry hooks via the
  `pre-commit run <id>` fallback, and a missing Makefile/pre-commit config
  itself, reported as the maximal gap rather than crashing), hook-derived/
  cache-sourced recommendations (staleness-gated and aggregated across
  every detected stack — including the zero-detected-stacks case, which
  stays `needs_research: True` rather than vacuously succeeding — never
  just the first match), tooling-category gaps (formatter/security-scanner/
  etc., heuristically deduplicated against already-present targets/hooks),
  the dedicated `validate-order`/`precommit-vs-prepush-split` structural
  checks (replacing the two previously-unsatisfiable empty-keyword research
  categories), AND the `validate`-chain-membership (via recipe reference OR
  genuine Make prerequisite) / `test`-`test-unit`-alias structural checks.
  REQ-agtmd-makefile-rules is EXPLICITLY **NOT** claimed fully/unqualifiedly
  implemented on ONE specific axis: mechanically verifying
  "lightest/cheapest-first" COST ORDERING (as opposed to chain/prerequisite
  MEMBERSHIP, which IS verified) remains out of scope for a structural
  regex checker and is documented as a named, standing limitation, not
  silently claimed complete (06-REVIEWS.md Round 1 HIGH; reaffirmed
  Cycle 3 HIGH — this plan narrows the claim rather than overstating it).
  REQ-agtmd-skill-pipeline's scaffold step is done (skill-judge/
  naming-analyzer remain).
- `rules.py` holds all 17 rules as structured data; no rule text is
  duplicated as prose anywhere else in this plan's artifacts.
- The public `check` CLI path resolves `AGENTS.md` OR `CLAUDE.md` (whichever
  exists) and follows one hop of same-repo relative Markdown links out of
  the resolved root file, so a rule `/agent-md-refactor` has relocated into
  a linked, progressive-disclosure file is never falsely reported missing.
</success_criteria>

<output>
Create `.planning/phases/06-agents-md-rules-checker-skill/06-01-SUMMARY.md` when done
</output>

## Review Dispositions Ledger

### Round 1 — 02ca473

### Review Feedback Addressed

| Concern | Severity | How Addressed |
|---------|----------|----------------|
| Task 2's live `assert 'R01' in ids` verify fails against this repo's real AGENTS.md, which already satisfies R01's signals (L38) | CRITICAL | Task 2's second `<verify>` rewritten to assert only the structural exclusion contract (no `workflow` entry has `status == "present"`) against the real repo — never a specific rule id's outcome; per-rule-id correctness stays with the scratch-fixture unit tests. |
| Wrong test-loader precedent: `importlib.util.spec_from_file_location` is a single-file loader, wrong for this multi-module package (L39) | HIGH | Task 1's `<action>`/`<read_first>` now specify `sys.path.insert` + package import, matching `tests/test_ai_kit_usage_metrics.py:16-21` verbatim. |
| `REQUIRED_STATIC_TARGETS` check is presence-only; would pass this repo's real Makefile despite missing chaining/alias structure, yet the plan claimed the REQ "fully implemented" (L40) | HIGH | Task 1's `<action>` adds explicit `validate`-chain-membership and `test`/`test-unit`-alias structural checks (new findings with `issue`/`recommendation` keys), proven by new `TestValidateChainAndAlias` tests. Success criteria now state the honest scope: chain/alias MEMBERSHIP is checked; "lightest-first" cost ORDERING is not, and is named as a documented limitation rather than silently assumed. |
| Cache lookup key inconsistent: findings use `name`, the worked example looked up `tooling["setup_env"]` (L41) | HIGH | `REQUIRED_STATIC_TARGETS` moved to `stack_cache.py` as the single schema source; `makefile_checker.py` imports it and looks up `tooling.get(name)` using the exact hyphenated target name (e.g. `"setup-env"`), never an underscored variant. |
| `main()` argv wiring for `check .` unspecified — `ai_kit_usage_metrics.cli.main` has no path argument precedent (L42) | MEDIUM | Task 1's `cli.py` paragraph now explicitly defines `argv[1]` as the optional `repo_root` (default `"."`), documented once so Plan 02's `remediate`/`cache-update` subcommands inherit the same convention. |
| `TestGateRegistration` thinner than the `ai-kit-usage-metrics` precedent; regex-tests a non-existent flat `cli.py` path (L43) | MEDIUM | Task 1's test-writing paragraph now specifies the full registration surface (Makefile, ruff+py-compile against the real nested package path, unittest entry, pyright include, vulture paths), mirroring `tests.test_ai_kit_usage_metrics.TestGateRegistration`. |
| R17's criticality is required by Task 2 but never assigned in Task 1; same gap for R03-R11/R13-R17 (L44) | MEDIUM | Task 1's `<action>` now gives a complete 17-row table assigning `criticality`/`condition`/`signals` to every rule explicitly — nothing left to "read the todo and decide" at implementation time. |
| Multi-stack detection resolution rule unspecified (two stacks match, one cache fresh) (L73) | MEDIUM | Task 1's `<action>` specifies deterministic sorted-stack iteration, taking the first stack with a usable cache entry; a genuinely blocked gap names every stack still lacking one, sorted. |

### Review Feedback Deferred

| Concern | Reason |
|---------|--------|
| D-04 research-loop cache-key mismatch (category names vs target names) (L69) | Addressed jointly with the cache-key-schema fix above; the matching research-prompt wording fix lives in 06-02's ledger since the prompt itself is authored in Plan 02. |
| `/agent-md-refactor` is not a per-rule insertion API (L70) | Out of this plan's scope — the wrap mechanism is designed and documented in Plan 02; see 06-02's ledger. |
| 06-03 rename loop cannot repair 06-01/06-02 contract bugs (L98) | Out of this plan's scope — addressed by actually fixing the bugs (this round) plus 06-03's own ledger entry re-sequencing its rename step. |

### Round 2 — Cycle 2 review (openai/gpt-5.6-sol fallback)

### Review Feedback Addressed

| Concern | Severity | How Addressed |
|---------|----------|----------------|
| Stale cache entries trusted: `get_stack_tooling` returns `needs_research=True` for stale data, but `check_makefile_shape` selected any non-`None` entry and overrode it to `False` | HIGH | Introduced the shared `_resolve_across_stacks()` helper — a stack only "covers" a target when `get_tooling` itself returned `needs_research=False` AND the key is present; a stale entry is never trusted regardless of whether `tooling.get(name)` is non-`None`. Proven by new `TestStaleCacheNotTrusted`. |
| Multi-stack repositories collapse to the first stack with a usable entry instead of covering every detected stack | HIGH | `_resolve_across_stacks()` requires EVERY detected stack to have fresh coverage before reporting `needs_research: False`; any stack still lacking coverage keeps the gap `needs_research: True` and names only the stacks still missing in `"stacks"`. Proven by `TestMultiStackResolution`'s three cases (one fresh, all fresh, none fresh). |
| Non-inline pre-commit hooks (no `entry:`, e.g. a non-`local` `repo:` block) are invisible to the audit | HIGH | `parse_precommit_hooks` now returns `None` for a hook with no inline `entry:` instead of omitting it; `check_makefile_shape`'s hook-derived branch falls back to `"pre-commit run <id>"` as the recommendation. Proven by a new scratch fixture case with a no-`entry:` hook. |
| Compound rule R04 can pass its signal threshold with zero execution/cheap-model/path-agnostic signal present | HIGH | R04's `signals` restructured into three independently-required GROUPS (review / cheap-model execution / path-agnostic); `rule_checker.py`'s classifier requires ALL groups satisfied for `"present"`, not a flat aggregate threshold. Proven by `test_r04_grouped_signals_require_every_group`. |
| R02 excluded from checking entirely despite being in the required ruleset | HIGH | R02 given a non-empty `signals` tuple for its own prose clause (don't skip pre-commit except TDD red→green); `rule_checker.py`'s filter changed from `category == "workflow"` to "has non-empty signals," so R02 is checked without disturbing its `category == "makefile"` bookkeeping. Proven by `test_r02_included_by_signals_not_category`. |
| The seeded-cache tracer fixture has no Python stack marker, so `detect_stacks` never matches `"python"` and the seeded cache is unreachable | HIGH | Task 1's test-writing paragraph now has the scratch repo also write an empty `requirements.txt`, so `detect_stacks` actually matches `"python"` for the fixture that seeds a `python` cache entry. |
| CRITICAL field-schema mismatch: Plan 02's `build_remediation()` assumes every finding carries `criticality`/`status`/`summary`, but Plan 01's Makefile findings carried only `target`/`present`/`recommendation`/`needs_research`/`stacks` | CRITICAL | Unified the schema at the source: every `makefile_checker.py` finding (static-target, hook-derived, chain/alias, and the new tooling-category findings) now also carries `criticality` (R02's own tier) and `summary` — the same two fields every `workflow` finding already carried. Plan 02's consumer no longer special-cases a finding kind missing fields it assumes are universal. |
| Cache schema cannot represent all research categories the prompt asks for (formatter/security/dead-code/etc.) | HIGH | Added `RESEARCH_CATEGORIES` to `stack_cache.py` alongside `REQUIRED_STATIC_TARGETS`, plus `ALL_TOOLING_KEYS`; added a third `check_makefile_shape` loop surfacing a tooling-category gap (reusing `_resolve_across_stacks()`) whenever no existing target/hook name already matches that category's keywords. Proven by `TestToolingCategoryGaps`. |
| Call-path contract disagreement: `key_links` named `rule_checker.py` as the caller of `check_tool_presence()`, but the action placed that call in `cli.py` | MEDIUM | Corrected the `key_links` entry to state `cli.py` calls `check_tool_presence()` once and passes the result into `rule_checker.check_workflow_rules()`. |

### Review Feedback Deferred

| Concern | Reason |
|---------|--------|
| REQ-agtmd-makefile-rules' "lightest/cheapest-first" cost ORDERING and full per-target recipe-content inspection beyond chain/alias membership | Already formally narrowed in this plan's own `<done>`/success criteria as a documented limitation of a structural regex checker (not silently omitted); this round did not expand that narrowing further, per the review's own "either formally narrow or implement" framing — the narrowing route is the one this plan takes. |

### Round 3 — Cycle 3 review (openai/gpt-5.6-sol)

### Review Feedback Addressed

| Concern | Severity | How Addressed |
|---------|----------|----------------|
| Public CLI cannot check a CLAUDE.md-only repository; `check()` defaulted exclusively to `AGENTS.md` | HIGH | New `resolve_instruction_text()` checks `AGENTS.md` first, falls back to `CLAUDE.md` when absent, and is the one function both `cli.check()` and Plan 02's wrapper now consume. Proven by `test_resolve_instruction_text_falls_back_to_claude_md`. |
| The checker cannot see rules `agent-md-refactor` moves into linked/progressive-disclosure files; a later run can falsely report them missing and reinsert duplicates | HIGH | `resolve_instruction_text()` also follows one hop of same-repo relative Markdown links out of the resolved root file and concatenates linked files' text into the same classification string. Proven by `test_resolve_instruction_text_follows_one_hop_links` (including a dangling-link no-crash case). |
| No defined non-crashing behavior when the Makefile or pre-commit config is missing | HIGH | `parse_makefile_targets`/`parse_makefile_prerequisites`/`parse_precommit_hooks` now check `os.path.isfile` first and return an empty result instead of raising; a missing Makefile is reported as every required target missing (the maximal, most informative gap). Proven by `test_check_missing_makefile_and_precommit_no_crash`. |
| Make prerequisite chaining ignored, lightest-first ordering explicitly omitted, while still claiming `REQ-agtmd-makefile-rules` complete | HIGH | New `parse_makefile_prerequisites()` gives the `validate`-chain-membership check a second, equally valid wiring mechanism (Make's own `target: dep1 dep2` syntax) alongside the recipe-text check. `success_criteria` rewritten to explicitly NOT claim the requirement unqualifiedly "fully implemented" — cost ORDERING remains a named, standing limitation, chain/prerequisite MEMBERSHIP is what's verified. Proven by `test_validate_chain_via_prerequisites`. |
| Multi-stack resolver produces vacuous "fresh coverage" when no stack is detected at all | HIGH | `_resolve_across_stacks()` now checks `detected_stacks` for emptiness FIRST and returns `(None, True, [])` immediately, before the `missing` computation that was previously trivially empty over zero stacks. Proven by `test_no_detected_stack_stays_needs_research`. |
| Modern-CLI rule collapses per-tool presence into one boolean, so guidance can pass while omitting installed tools | HIGH | `tool_presence.check_tool_presence` now reports each of `rg`/`bat`/`sd`/`fd`/`eza` under its own `modern-cli:<tool>` key; R06's `signals` restructured into TOOL-SCOPED groups (a new shape alongside R04's plain groups) so only INSTALLED tools' groups are required, each independently. Proven by `test_r06_tool_scoped_groups_only_require_installed_tools`. |
| Two new research categories (`validate-order`, `precommit-vs-prepush-split`) have empty keyword sets and can never be classified covered — perpetual false positives | HIGH | Both removed from `RESEARCH_CATEGORIES` entirely and replaced with dedicated structural checks (`check_validate_order`, `check_precommit_prepush_split`) that read facts directly from the target repo's own files — no cache/research involved, so neither can ever be an unsatisfiable research category again. Proven by `test_validate_order_and_precommit_prepush_split`. |
| SECURITY — `cache-update`'s CLI (Plan 02) permits path traversal through an unvalidated `stack` argument, allowing an atomic overwrite of an arbitrary JSON file under/outside the cache root | HIGH | Fixed at the shared root cause: `stack_cache.cache_path()` now validates `stack` against a bare-identifier regex and raises `ValueError` before any path join, so the protection applies to every caller (Plan 02's CLI included), not a CLI-layer patch alone. Proven by `TestCachePathValidation`. (Filed against 06-02 in the review since `cache-update` is what first exposed the unvalidated argument; fixed here because `cache_path()` is the one function every writer goes through — see 06-02's own ledger for the CLI-layer error-handling half of this fix.) |

### Review Feedback Deferred

None — every Cycle 3 finding touching this plan is addressed above.

### Round 4 — Cycle 4 review (openai/gpt-5.6-sol)

### Review Feedback Addressed

| Concern | Severity | How Addressed |
|---------|----------|----------------|
| Lightest-first ordering remains unverified despite being mandatory, while the plan's own success_criteria still claimed ROADMAP SC-1 satisfied | HIGH (PARTIALLY RESOLVED from Cycle 3) | `success_criteria`'s first bullet rewritten: SC-2 is satisfied; SC-1 is satisfied for every clause EXCEPT cost ordering, stated explicitly as a partial satisfaction rather than an unqualified "satisfied," consistent with the cost-ordering limitation already stated further down in the same section. |
| A must-have still listed `RESEARCH_CATEGORIES` as containing `validate-order`/`precommit-vs-prepush-split`, contradicting the implementation section that removes both | HIGH (PARTIALLY RESOLVED from Cycle 3) | The must-have's parenthetical now lists exactly the 5 real `RESEARCH_CATEGORIES` entries and explicitly states the two structural-check names are never members, cross-referencing the truth that already stated this correctly. |
| A valid prerequisite alias (`test: test-unit`) was falsely reported `not_aliased` because alias detection checked only recipe substrings/identical recipes, never Make prerequisite listing | HIGH | The alias check now also consults `parse_makefile_prerequisites` for both `test` and `test-unit`, the same mechanism already accepted for `validate`'s chain. Proven by new `test_alias_via_prerequisite`. |
| The pre-commit/pre-push split check passed when ANY hook had a push stage, without confirming a SLOW hook specifically moved there; the research category it used to map to isn't consumable through `ALL_TOOLING_KEYS` | HIGH | `check_precommit_prepush_split` now classifies hooks via `SLOW_HOOK_KEYWORDS` and checks each slow hook's OWN YAML block for its own push stage, never "any hook anywhere." Plan 02's research prompt no longer asks for a topic with no `ALL_TOOLING_KEYS` home. Proven by an expanded `test_validate_order_and_precommit_prepush_split`. |
| Pre-commit hook-id parsing matched only literal unquoted `- id: <name>` lines, omitting quoted YAML ids | HIGH | `parse_precommit_hooks` now matches bare, double-quoted, and single-quoted id forms, normalizing to the bare string key. Proven by the tracer fixture's quoted `vulture` id plus a dedicated `test_parse_precommit_hooks_handles_quoted_ids`. |
| Validate-chain recipe matching used raw substrings, so a target named `lint` could appear wired merely because the recipe contains `pylint` | HIGH | New shared `_recipe_mentions_target()` helper uses word-boundary-aware regex matching, used by both the chain-membership and alias checks. Proven by new `test_wired_check_is_word_boundary_aware`. |
| Linked-file containment lacked a symlink-safe realpath contract | MEDIUM | `resolve_instruction_text()`'s containment check now compares `os.path.realpath` of both the candidate link and `repo_root`, with an `os.sep`-suffixed `startswith` (not a bare prefix check). Proven by new `test_resolve_instruction_text_rejects_symlink_escape`. |

### Review Feedback Deferred

None — every Cycle 4 finding touching this plan is addressed above.

