---
phase: 06-agents-md-rules-checker-skill
plan: 03
type: execute
wave: 3
depends_on: ["06-02"]
cross_ai: true
autonomous: true
requirements: [REQ-agtmd-skill-pipeline, REQ-agtmd-wrapper]
# NOTE: every path below is the PRE-RENAME, provisional-name path (fixes
# cross-AI review Round 1 LOW: this list was previously unflagged as
# pre-rename, 06-REVIEWS.md L103@02ca473). Task 2 `git mv`s most of this
# tree to /naming-analyzer's chosen final name — this list is NOT the final
# tree; see Task 2's <action> for the rename's real destinations.
files_modified:
  - skills/ai-kit-agents-md-checker/SKILL.md
  - skills/ai-kit-agents-md-checker/ai-kit-agents-md-checker.py
  - skills/ai-kit-agents-md-checker/ai_kit_agents_md_checker/__init__.py
  - skills/ai-kit-agents-md-checker/ai_kit_agents_md_checker/rules.py
  - skills/ai-kit-agents-md-checker/ai_kit_agents_md_checker/stack_detect.py
  - skills/ai-kit-agents-md-checker/ai_kit_agents_md_checker/stack_cache.py
  - skills/ai-kit-agents-md-checker/ai_kit_agents_md_checker/makefile_checker.py
  - skills/ai-kit-agents-md-checker/ai_kit_agents_md_checker/tool_presence.py
  - skills/ai-kit-agents-md-checker/ai_kit_agents_md_checker/rule_checker.py
  - skills/ai-kit-agents-md-checker/ai_kit_agents_md_checker/remediation.py
  - skills/ai-kit-agents-md-checker/ai_kit_agents_md_checker/cli.py
  - tests/test_ai_kit_agents_md_checker.py
  - Makefile
  - .pre-commit-config.yaml
  - pyproject.toml
  - README.md

estimate:
  tokens: 62000
  raw_tokens: 62000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Before Task 1 invokes `/skill-judge` at all, Plans 01 and 02's own `<verify>` commands are ALL re-run under the provisional name and confirmed green — not just the unittest suite, but also Plan 01's live `check .` structural-exclusion assertion and Plan 02's SKILL.md structural-grep assertion and `make test && make lint && make validate` — each wired as an actual `<automated>` entry in Task 1's own `<verify>` block, not described in prose alone (fixes cross-AI review Round 1 HIGH: a rename/close-out plan cannot repair contract bugs in the plans it depends on, `06-REVIEWS.md` L98@02ca473; fixes cross-AI review Cycle 2 HIGH: the prior revision's action claimed this re-verification but only the unittest command was actually wired as a runnable step, `06-REVIEWS.md` Cycle 2 06-03 Concerns)."
    - "`/skill-judge` run against the finished SKILL.md reports ZERO remaining entries under the report's own `## Critical Issues` section — the literal, stricter bar ROADMAP Phase 6 Success Criterion 4 and REQUIREMENTS.md's REQ-agtmd-skill-pipeline set for THIS phase ('no remaining Critical/Important finding'), not satisfied merely by a score at or above 96/120 while a Critical Issue remains listed. The installed `/skill-judge` skill's actual report format (confirmed by reading `~/.agents/skills/skill-judge/SKILL.md` directly) has exactly one severity-named section, `## Critical Issues`, and no distinct 'Important' tier — so 'Important' maps to the same `## Critical Issues` section, not to a second threshold this plan invents. The numeric score is still recorded each run for visibility, but never substitutes for a clean `## Critical Issues` section (fixes cross-AI review Cycle 2 HIGH: the prior gate's OR-score-threshold escape was weaker than the phase's own success criterion, and 'Important' was undefined against the real skill-judge vocabulary, `06-REVIEWS.md` Cycle 2 06-03 Concerns)."
    - "Neither Task 1's `/skill-judge` nor Task 2's `/naming-analyzer` is ever satisfied by self-grading from memory against the published rubric — `autonomous: true` on this plan's frontmatter does NOT authorize silently skipping either invocation; if either skill cannot actually be invoked in the execution environment, the task stops and surfaces that fact as a blocker instead of approximating the result (fixes cross-AI review Round 1 MEDIUM: no fallback existed beyond 'stop and surface,' making a silent checkbox-skip easy under autonomous execution, `06-REVIEWS.md` L99@02ca473)."
    - "After the `/skill-judge` loop closes AND after the rename, `/skill-judge` is re-run once more against the final, post-rename SKILL.md — a name-only change still touches the frontmatter `name:` field and every internal self-reference, so it is re-checked rather than assumed exempt (fixes cross-AI review Round 1 MEDIUM, `06-REVIEWS.md` L101@02ca473)."
    - "`/naming-analyzer` is actually invoked (not skipped/assumed) and its chosen name — not `ai-kit-agents-md-checker` by default — becomes the skill's real directory, package, entrypoint, and `SKILL.md` `name:` field (ROADMAP SC-4: 'never assumed upfront'). When `naming-analyzer`'s own 'Suggested Renaming' output lists more than one candidate that could plausibly name THIS skill, the HIGHEST-PRIORITY bucket (High > Medium > Low, the analyzer's own real priority grouping) wins deterministically; if multiple candidates remain tied within that same bucket, the task stops and asks the user to pick rather than guessing (fixes cross-AI review Cycle 2 MEDIUM: no tie-break/confirmation existed for multiple returned alternatives, `06-REVIEWS.md` Cycle 2 06-03 Concerns)."
    - "The git-mv rename, the internal path/reference updates, and the zero-old-name-occurrence grep are ALL conditional on `naming-analyzer`'s chosen name actually differing from the provisional `ai-kit-agents-md-checker` — when the analyzer confirms the provisional name stands unchanged, Task 2 skips every move/update step and the grep-for-leftovers check is understood to find the still-current provisional name everywhere (expected, not a failure) rather than demanding zero matches of a name that was never changed (fixes cross-AI review Cycle 2 HIGH: the prior revision's 'unchanged-name' branch was logically impossible under its own unconditional `git mv` + zero-occurrence grep, `06-REVIEWS.md` Cycle 2 06-03 Concerns)."
    - "The final package name is derived by first stripping any leading `ai-kit-`/`ai_kit_` the analyzer's chosen name might already carry, THEN re-applying the `ai-kit-<kebab>` / `ai_kit_<snake>` convention exactly once — `naming-analyzer` returning an already-prefixed name never produces a doubled `ai-kit-ai-kit-*` / `ai_kit_ai_kit_*` result (fixes cross-AI review Cycle 2 MEDIUM: the prior conversion formula could double-prefix, `06-REVIEWS.md` Cycle 2 06-03 Concerns)."
    - "After the rename, `make test`, `make lint`, and `make validate` are all green, proving every one of the four registration surfaces Plan 01 wired was updated to the final name, not left pointing at the provisional one."
    - "ROADMAP SC-3's 'observably distinct from plain /agent-md-refactor' claim is backed by an actual automated `<verify>` command (not action-only prose) that runs the renamed checker's `check .` and asserts it returns at least one real, non-empty finding, PLUS the existing automated confirmation that `agent-md-refactor`'s own SKILL.md contains no house-rule awareness — both are wired as executed `<verify>` entries, not a SUMMARY sentence alone (fixes cross-AI review Round 1 MEDIUM, `06-REVIEWS.md` L100@02ca473; fixes cross-AI review Cycle 2 MEDIUM: the renamed-checker half of this evidence was still only described in the action's prose with no corresponding `<verify>`, `06-REVIEWS.md` Cycle 2 06-03 Concerns)."
    - "The zero-leftover-reference grep scans `*.md` (not just `SKILL.md`), so README.md and any other Markdown file are actually covered, and ALSO separately checks for the slash-delimited cache-namespace string (`ai-kit/agents-md-checker`, from `CACHE_ROOT`'s path-join — a different literal substring than the hyphenated `ai-kit-agents-md-checker` the main grep already catches) — a rename is never declared complete while this second, differently-shaped leftover string still exists in `stack_cache.py` (fixes cross-AI review Cycle 2 MEDIUM: the prior grep's extension filter excluded ordinary Markdown and could never detect the slash-delimited cache-namespace leftover, `06-REVIEWS.md` Cycle 2 06-03 Concerns)."
    - "README.md's Contents table gains one row for the final skill name, linking to its `SKILL.md` (AGENTS.md's README-currency rule, applied to this project's own README after shipping a new skill); the plan's own SUMMARY notes, in one line, that `ai-kit-usage-metrics` is ALSO currently missing from that table and that backfilling it is out of this phase's scope, not silently inconsistent (fixes cross-AI review Round 1 LOW, `06-REVIEWS.md` L102@02ca473)."
  artifacts:
    - skills/ai-kit-agents-md-checker/SKILL.md (pre-rename path; final path is whatever /naming-analyzer chooses)
  key_links:
    - "Every Makefile/.pre-commit-config.yaml/pyproject.toml entry Plan 01 wrote under the provisional name is updated, not duplicated, by the rename — `make validate` passing after the rename is the proof that no stale provisional-name entry was left behind alongside a new one."
    - "`stack_cache.py`'s `CACHE_ROOT` cache-namespace segment (`\"ai-kit/agents-md-checker/stack-refs\"`) is updated to the final name alongside every other reference — the slash-delimited form is a DIFFERENT literal string than the hyphenated `ai-kit-agents-md-checker` the main rename grep matches, so it needs its own explicit edit, not an assumption that the main grep already covers it."
---

<objective>
Close the REQ-agtmd-skill-pipeline loop: run `/skill-judge` against the
SKILL.md Plan 02 authored and fix every Critical/Important finding, then
run `/naming-analyzer` to pick the skill's real, final name and rename
every path/reference from the provisional `ai-kit-agents-md-checker` to
that name — never assuming the provisional name was the answer.

Purpose: ROADMAP Phase 6 Success Criterion 4 requires BOTH gates to
actually run, in order, against the real artifact — not a self-reported
"looks good." The rename step is the part most likely to silently half-
finish (a forgotten Makefile line, a stale import), so this plan's own
`<verify>` re-runs the full local gate suite after the rename, not just
after the skill-judge pass.

Output: a `/skill-judge`-cleared, `/naming-analyzer`-named skill at its
final path, every registration surface pointing at that final name, a
README row, and a final commit closing Phase 6.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@AGENTS.md
@.planning/phases/06-agents-md-rules-checker-skill/06-CONTEXT.md
@.planning/phases/06-agents-md-rules-checker-skill/06-02-SUMMARY.md
</context>

<tracked_source_paths>
Every path under `skills/ai-kit-agents-md-checker/` is tracked once Plans
01-02's commits land in this same phase. `README.md`, `Makefile`,
`.pre-commit-config.yaml`, and `pyproject.toml` are pre-existing tracked
files this plan edits with `Edit` (scoped), never `Write`. The final,
post-rename skill path is not yet known (that is the entire point of Task
2) — it will be a `git mv`'d version of an already-tracked tree, not a new
untracked location.
</tracked_source_paths>

<failing_direction_contract>
Every runnable `<automated>` command below is followed by a `<fails_when>`
sibling naming the observable output that constitutes failure.
</failing_direction_contract>

<source_audit>
## Multi-Source Coverage Audit (Phase 6, Plan 03 portion)

| # | Source | Item | Covered by |
|---|--------|------|-----------|
| SC-4 | ROADMAP SC-4 | skill-judge clean + naming-analyzer name, never assumed | Task 1 (skill-judge), Task 2 (naming-analyzer + rename) |
| REQ-4 | REQUIREMENTS.md | REQ-agtmd-skill-pipeline | Task 1, Task 2 |
| REQ-2 (remainder) | REQUIREMENTS.md | REQ-agtmd-wrapper (final, demonstrated distinctness after rename) | Task 2 |
| AGENTS.md rule 11 | AGENTS.md | README currency after adding a feature | Task 2 |

This closes the phase: every GOAL/REQ/CONTEXT item from Plans 01-02's audits
plus this plan's own is now covered by a task somewhere in the phase.
**Result: 0 unplanned items across the whole phase.**
</source_audit>

<tasks>

<task type="auto">
  <name>Task 1: skill-judge review loop</name>
  <files>skills/ai-kit-agents-md-checker/SKILL.md</files>

  <precondition>
    `~/.claude/skills/skill-judge/SKILL.md` is installed and invocable
    (confirmed present on this machine during Phase 6's discussion) — if
    `/skill-judge` cannot be invoked, stop and surface that rather than
    self-grading the SKILL.md against the rubric from memory.
  </precondition>

  <read_first>
    - `skills/ai-kit-agents-md-checker/SKILL.md` — the file under review.
    - `~/.agents/skills/skill-judge/SKILL.md` (its Evaluation Protocol and
      Step 5 report template) — read it directly, not from memory: its real
      report format has exactly ONE severity-named section, `## Critical
      Issues`, and no distinct "Important" tier anywhere in its report
      template or grading rubric. ROADMAP Phase 6 Success Criterion 4 and
      REQUIREMENTS.md's REQ-agtmd-skill-pipeline both require "no remaining
      Critical/Important finding" — a STRICTER, literal bar than `AGENTS.md`'s
      own general-purpose OR-score-threshold baseline (next bullet) — so this
      task's own pass gate is: zero entries under the report's `## Critical
      Issues` section, full stop, not a score alone (fixes cross-AI review
      Cycle 2 HIGH: the prior gate's OR-score-threshold escape was weaker
      than this phase's own success criterion, and "Important" had no mapping
      onto the real skill-judge vocabulary, `06-REVIEWS.md` Cycle 2 06-03
      Concerns).
    - `AGENTS.md` Non-Negotiable Rules — the general baseline bar every
      SKILL.md in this repo must clear ("no remaining Critical/Important
      finding, or score ≥ 96/120, no regression vs. baseline"); this phase's
      OWN criterion above is stricter and wins for this plan — a phase is
      always free to hold itself to more than the repo-wide floor.
    - `.planning/milestones/v1.0-phases/02-opencode-provider-management/02-02-PLAN.md`
      Task 3 — this project's own precedent for running this exact loop and
      recording scores.
  </read_first>

  <action>
FIRST, before invoking `skill-judge` at all: re-run EVERY ONE of Plan 01's
and Plan 02's own `<verify>` commands under the still-provisional name —
not just the unittest suite, but the FULL set, each wired below as its own
`<automated>` entry in this task's `<verify>` block (fixes cross-AI review
Cycle 2 HIGH: the prior revision's action claimed this full re-verification
but only the unittest command was actually wired as a runnable step,
`06-REVIEWS.md` Cycle 2 06-03 Concerns): (1) `python3 -m unittest
tests.test_ai_kit_agents_md_checker -v`; (2) Plan 01's live `check .`
structural-exclusion assertion (the exact command from 06-01-PLAN.md Task
2's second `<verify>`); (3) Plan 02's SKILL.md structural-grep assertion
(the exact command from 06-02-PLAN.md Task 2's first `<verify>`); (4) `make
test && make lint && make validate`. This plan's rename/review work in Task
2 is built on top of whatever Plans 01-02 actually shipped — never assume
they are sound just because they merged (fixes cross-AI review Round 1
HIGH, `06-REVIEWS.md` L98@02ca473). If ANY of these four fail, STOP this
plan and surface the failure rather than proceeding into the
skill-judge/naming/rename sequence on top of a broken base.

THEN invoke the `skill-judge` skill against
`skills/ai-kit-agents-md-checker/SKILL.md`. Record the numeric score and
every finding verbatim (in the plan summary, not in a new repo file —
this is ai-kit's own process record, not a target-repo artifact, so it is
fine for it to live in the SUMMARY; it is not the D-05 inline-only rule,
which governs what the CHECKER SKILL ITSELF does when run against a
target repo, a different thing from this meta-review of the skill's own
documentation). Address every finding listed under the report's `##
Critical Issues` section by editing SKILL.md, then re-run `skill-judge` and
record the new score. Loop until the `## Critical Issues` section is EMPTY
— zero listed issues. Do NOT close the loop on a high score alone while
`## Critical Issues` still lists an entry; the score is recorded for
visibility only. Any edit that changes a documented command, flag, or CLI
output shape must be re-checked against `cli.py`'s actual behavior before
the loop closes — the document is only correct if it still matches the
code.
  </action>

  <verify>
    <automated>python3 -m unittest tests.test_ai_kit_agents_md_checker -v</automated>
    <fails_when>any test errors or fails (non-zero exit, "FAILED" or "ERROR" in output) — proves SKILL.md edits made during the loop did not drift from the CLI's real behavior, and (run first, per this task's action) proves Plan 01 is still sound before this plan builds on it</fails_when>
  </verify>
  <verify>
    <automated>python3 skills/ai-kit-agents-md-checker/ai-kit-agents-md-checker.py check . | python3 -c "
import json, sys
d = json.load(sys.stdin)
workflow = d['workflow']
assert isinstance(workflow, list)
assert all(f['status'] in ('missing', 'near_miss', 'not_applicable') for f in workflow)
assert all(f.get('status') != 'present' for f in workflow)
print('ok', len(workflow))
"</automated>
    <fails_when>the command errors, prints non-JSON, or any `workflow` entry carries `status == "present"` — proves Plan 01's own structural live-check still holds under the provisional name before this plan extends it</fails_when>
  </verify>
  <verify>
    <automated>python3 -c "
import re, sys
text = open('skills/ai-kit-agents-md-checker/SKILL.md', encoding='utf-8').read()
lower = text.lower()
assert text.startswith('---')
assert 'agent-md-refactor' in text
assert 'inline' in lower
assert 'auto_apply' in text or 'auto-apply' in lower
assert 'exactly once' in lower
assert 'identify the essentials' in lower
assert 'all_tooling_keys' in lower
print('ok')
"</automated>
    <fails_when>the script raises `AssertionError` or a non-zero exit — proves Plan 02's own SKILL.md structural contract still holds under the provisional name before this plan extends it</fails_when>
  </verify>
  <verify>
    <automated>make test && make lint && make validate</automated>
    <fails_when>any of the three exits non-zero, `make test`'s output contains "FAILED", or `make validate`'s pre-commit output contains "Failed" for any hook — proves the full local gate suite is green BEFORE this plan's rename work begins, not just the skill's own isolated test suite</fails_when>
    <human-check>Record the skill-judge run: the numeric score of the first run, every `## Critical Issues` finding and how each was addressed, and the numeric score of the final run. Confirm the final run's `## Critical Issues` section is empty — a high score alone does not close this loop while that section still lists an entry.</human-check>
  </verify>

  <done>
    All four of Plan 01's and Plan 02's own verify commands were
    re-confirmed green FIRST, under the provisional name, each as its own
    executed `<automated>` entry above — not described in prose alone.
    SKILL.md then clears the skill-judge bar defined as zero remaining
    entries under the report's `## Critical Issues` section (the literal,
    stricter bar this phase's own ROADMAP/REQUIREMENTS.md set — not
    satisfied by a high score alone), with the loop's first and final
    scores and every finding's resolution recorded in the plan summary.
    This task is NOT satisfied by self-grading SKILL.md against the rubric
    from memory — if `/skill-judge` could not actually be invoked, the task
    stopped and surfaced that instead of approximating a passing score;
    `autonomous: true` never authorizes skipping the invocation.
  </done>
</task>

<task type="auto">
  <name>Task 2: naming-analyzer, rename, re-verify all four gates, README row, close the phase</name>
  <files>skills/ai-kit-agents-md-checker/SKILL.md, skills/ai-kit-agents-md-checker/ai-kit-agents-md-checker.py, skills/ai-kit-agents-md-checker/ai_kit_agents_md_checker/*.py, tests/test_ai_kit_agents_md_checker.py, Makefile, .pre-commit-config.yaml, pyproject.toml, README.md</files>

  <precondition>
    `~/.claude/skills/naming-analyzer/SKILL.md` is installed and invocable
    (confirmed present on this machine during Phase 6's discussion); Task 1
    of this plan has already closed with an empty `## Critical Issues`
    section from `/skill-judge`. As with Task 1's `/skill-judge` invocation,
    `autonomous: true` does not authorize skipping `/naming-analyzer` or
    self-choosing a name from memory — if it cannot actually be invoked,
    stop and surface that rather than approximating.
  </precondition>

  <read_first>
    - `skills/ai-kit-agents-md-checker/SKILL.md` — the frontmatter
      `description:` and body `naming-analyzer` reasons from.
    - `~/.agents/skills/naming-analyzer/SKILL.md`'s "Suggested Renaming"
      output section (its real report groups candidates into High/Medium/Low
      Priority buckets, one `old -> new` suggestion per identified name) —
      read it directly so this task's tie-break rule (below) matches the
      analyzer's REAL output shape rather than an invented "ranked list."
    - `README.md`'s `## Contents` table — the exact row format
      (`| [\`name\`](skills/name/SKILL.md) | skill | one-line use case |`)
      to append to, matching the existing `ai-kit-opencode-providers` row's
      style.
    - `Makefile`, `.pre-commit-config.yaml`, `pyproject.toml` — every place
      Plan 01 registered the provisional name; this task must find and
      update EVERY one, not just the obviously-visible ones.
    - `skills/ai-kit-agents-md-checker/ai_kit_agents_md_checker/stack_cache.py`'s
      `CACHE_ROOT` definition — the slash-delimited cache-namespace string
      (`"ai-kit/agents-md-checker/stack-refs"`) this task must ALSO rename; it
      is a different literal substring than the hyphenated
      `ai-kit-agents-md-checker` the main rename grep matches, so it needs
      its own explicit edit and its own grep check (see below).
  </read_first>

  <action>
Invoke the `naming-analyzer` skill against the finished
`skills/ai-kit-agents-md-checker/SKILL.md` (its frontmatter `description:`,
its Scope section, and what it actually does — check/remediate a house
ruleset, wrapping `/agent-md-refactor`) and record its recommended name(s)
verbatim. Do not default back to `ai-kit-agents-md-checker` merely because
it is convenient — if `naming-analyzer` proposes a different name, that is
the one this task ships; if its top recommendation IS functionally the
same name, record that the analysis was run and concluded no change was
warranted (a reasoned match, not a skipped step). TIE-BREAK RULE (fixes
cross-AI review Cycle 2 MEDIUM: no tie-break existed for multiple returned
alternatives, `06-REVIEWS.md` Cycle 2 06-03 Concerns): the analyzer's real
"Suggested Renaming" output groups candidates into High/Medium/Low Priority
buckets — if more than one candidate plausibly renames THIS skill (its
top-level identifier, not an incidental internal variable/function name the
analyzer also flagged), take the one in the HIGHEST-priority bucket; if
still tied within that same bucket, STOP and ask the user to pick between
the tied candidates rather than guessing.

Determine `<final>` and its derived forms IDEMPOTENTLY (fixes cross-AI
review Cycle 2 MEDIUM: a naive "always prepend ai-kit-" formula could
double-prefix a name the analyzer already returned with the prefix intact,
`06-REVIEWS.md` Cycle 2 06-03 Concerns): let `final_suffix` = the analyzer's
chosen name with any leading `ai-kit-` (or `ai_kit_`, normalized to
hyphenated form first) stripped EXACTLY ONCE if present, otherwise the name
unchanged; `final_skill_name` = `"ai-kit-" + final_suffix` (the skill
directory/SKILL.md `name:` always carries the `ai-kit-` prefix exactly
once, matching every other skill in this repo); `final_package_name` =
`"ai_kit_" + final_suffix.replace("-", "_")`. This never produces
`ai-kit-ai-kit-*` / `ai_kit_ai_kit_*` regardless of whether the analyzer's
raw output already included the prefix.

IF `final_skill_name` differs from the provisional `ai-kit-agents-md-checker`
(fixes cross-AI review Cycle 2 HIGH: the prior revision's rename/grep steps
ran unconditionally, which is logically impossible to reconcile with a
"name unchanged" outcome, `06-REVIEWS.md` Cycle 2 06-03 Concerns): `git mv
skills/ai-kit-agents-md-checker skills/<final_skill_name>`; `git mv` the
entrypoint script to `<final_skill_name>.py`; `git mv` the
`ai_kit_agents_md_checker/` package directory to
`<final_package_name>/`; update the entrypoint's `from
ai_kit_agents_md_checker.cli import main` line to `<final_package_name>`;
update `SKILL.md`'s frontmatter `name:` field and every internal
self-reference to the old name/path. Update `.pre-commit-config.yaml`'s
`ruff`/`py-compile` `files:` alternations and `unittest` hook's
`tests.test_ai_kit_agents_md_checker` entry (rename the TEST FILE itself
too: `git mv tests/test_ai_kit_agents_md_checker.py
tests/test_<final_package_name>.py`, and update every `sys.path.insert`
scratch-path string and `from ai_kit_agents_md_checker import ...` line
inside it that references the old package/module paths — this test module
uses the `sys.path.insert` + package-import convention per Plan 01's Round
1 fix, never `spec_from_file_location`). Update `Makefile`'s `test:` module
list and `lint:` py_compile argument list. Update `pyproject.toml`'s
`[tool.pyright] include` and `[tool.vulture] paths`. ALSO update
`stack_cache.py`'s `CACHE_ROOT` cache-namespace segment from
`"ai-kit/agents-md-checker/stack-refs"` to
`f"ai-kit/{final_suffix}/stack-refs"` — this slash-delimited string is a
DIFFERENT literal substring than `ai-kit-agents-md-checker` and will NOT be
caught by a grep for that hyphenated form (fixes cross-AI review Cycle 2
MEDIUM: the rename audit could not detect this leftover shape,
`06-REVIEWS.md` Cycle 2 06-03 Concerns). Grep the whole tree for the literal
string `ai-kit-agents-md-checker`, `ai_kit_agents_md_checker`, AND
`ai-kit/agents-md-checker` (the slash form) after all edits and confirm
zero remaining matches outside this plan's own `.planning/` artifacts
(which correctly keep the provisional name as historical record — do not
rewrite PLAN.md/CONTEXT.md history).

IF `final_skill_name` equals the provisional `ai-kit-agents-md-checker`
(the analyzer confirmed no change was warranted): skip every `git mv`/
path-update/cache-namespace-rename step above entirely — there is nothing
to move. The grep-for-leftovers check below is expected to find the
still-current provisional name everywhere in this case; that is correct
behavior, not a failure, and must be noted as such in the plan summary.

Add one row to `README.md`'s `## Contents` table for `<final_skill_name>`,
matching the existing row format, with a one-line use case naming the
house-rule check + Makefile-shape audit + `/agent-md-refactor` wrap (not a
restated SKILL.md description verbatim — concise, per rule 17's own "no
excess documentation" standard, applied here to ai-kit's own docs). Note in
the plan summary, in one line, that `README.md`'s `## Contents` table is
ALREADY missing a row for `ai-kit-usage-metrics` today (a pre-existing
gap) — adding this row is still correct; backfilling the unrelated
`ai-kit-usage-metrics` gap is out of this phase's scope, stated explicitly
rather than left to look like an inconsistency this plan introduced (fixes
cross-AI review Round 1 LOW, `06-REVIEWS.md` L102@02ca473).

Re-run `/skill-judge` once more against the FINAL, post-rename (or
confirmed-unchanged) SKILL.md and record the score — do not treat a
name-only change, or a confirmed-no-change outcome, as exempt from
re-review (fixes cross-AI review Round 1 MEDIUM: no post-rename skill-judge
re-run existed, `06-REVIEWS.md` L101@02ca473). If this surfaces a new entry
under `## Critical Issues`, fix it before this task closes — per Task 1's
literal gate (zero entries under that section), not a score threshold.

Finally, demonstrate ROADMAP SC-3's distinctness with ACTUAL AUTOMATED
evidence, not a SUMMARY sentence alone (fixes cross-AI review Round 1
MEDIUM: the prior plan only asked for a narrative note, `06-REVIEWS.md`
L100@02ca473; fixes cross-AI review Cycle 2 MEDIUM: the renamed-checker
half of this evidence was still only described in this action's prose with
no corresponding `<verify>` entry, `06-REVIEWS.md` Cycle 2 06-03 Concerns —
see the new fourth `<verify>` block below, which is the actual acceptance
mechanism): run `python3 <final-entrypoint> check .` against this repo and
capture its JSON output (a real, non-empty `makefile` and/or `workflow`
findings list); separately, run a command that reads
`agent-md-refactor/SKILL.md`'s text directly (resolved via the same
host-neutral candidate-list pattern Plan 02 uses, never a hardcoded
`~/.claude/...` path) and asserts it contains neither `"house"` nor a
reference to this project's `rules.py` — record BOTH outputs verbatim in
the plan summary as the acceptance evidence for "observably distinct": one
skill demonstrably runs a house-rule check and returns findings; the
other, read directly, contains no such capability.
  </action>

  <verify>
    <automated>make test && make lint && make validate</automated>
    <fails_when>any of the three exits non-zero, `make test`'s output contains "FAILED", or `make validate`'s pre-commit output contains "Failed" for any hook — this is the proof that every registration surface was actually updated, not just the obviously-visible ones</fails_when>
  </verify>
  <verify>
    <automated>! grep -rlE "ai-kit-agents-md-checker|ai_kit_agents_md_checker|ai-kit/agents-md-checker" --include="*.py" --include="Makefile" --include="*.yaml" --include="*.toml" --include="*.md" . 2>/dev/null | grep -v "^\./.planning/"</automated>
    <fails_when>a rename actually happened this task (the analyzer chose a different name) AND the grep finds any remaining match outside `.planning/` — a leftover provisional-name OR leftover slash-delimited cache-namespace reference in live code/config/docs means the rename is incomplete. If `naming-analyzer` confirmed no rename was needed, this check is EXPECTED to find the still-current provisional-name references and is not applicable — note that outcome in the summary instead of treating a match as a failure in that branch (fixes cross-AI review Cycle 2 HIGH: the prior unconditional version of this check was logically impossible to pass in the no-rename branch, `06-REVIEWS.md` Cycle 2 06-03 Concerns; `*.md` added so README.md and other Markdown are actually covered, and the slash form added so the cache-namespace leftover is actually detectable, `06-REVIEWS.md` Cycle 2 06-03 Concerns)</fails_when>
  </verify>
  <verify>
    <automated>python3 -c "
import glob, json, subprocess
cands = [c for c in glob.glob('skills/*/ai_kit_*/stack_cache.py') if 'REQUIRED_STATIC_TARGETS' in open(c, encoding='utf-8').read()]
assert len(cands) == 1, f'expected exactly one checker skill under skills/, found {cands}'
skill_dir = cands[0].split('/')[1]
entry = f'skills/{skill_dir}/{skill_dir}.py'
out = subprocess.run(['python3', entry, 'check', '.'], capture_output=True, text=True, check=True)
d = json.loads(out.stdout)
total = len(d.get('makefile', [])) + len(d.get('workflow', []))
assert total > 0, 'expected at least one real finding from the renamed checker against this repo'
print('ok', skill_dir, total)
"</automated>
    <fails_when>the script errors (wrong count of matching skill dirs, non-JSON CLI output, or a non-zero CLI exit) or asserts `total == 0` — proves the checker at its ACTUAL post-rename (or confirmed-unchanged) path still runs end-to-end and returns real findings, the first half of the SC-3 distinctness evidence, as an executed `<verify>` that discovers the real path dynamically rather than a claim made only in the action's prose or a literal `<final>` placeholder (fixes cross-AI review Cycle 2 MEDIUM, `06-REVIEWS.md` Cycle 2 06-03 Concerns)</fails_when>
  </verify>
  <verify>
    <automated>FOUND=""; for d in "$HOME/.claude/skills/agent-md-refactor" "${XDG_CONFIG_HOME:-$HOME/.config}/opencode/skills/agent-md-refactor" "$HOME/.agents/skills/agent-md-refactor"; do [ -f "$d/SKILL.md" ] && FOUND="$d/SKILL.md" && break; done; [ -n "$FOUND" ] && python3 -c "
text = open('$FOUND', encoding='utf-8').read().lower()
assert 'house' not in text
assert 'rules.py' not in text
print('confirmed: agent-md-refactor has no house-rule awareness ($FOUND)')
" || { echo "err: no agent-md-refactor/SKILL.md candidate found" >&2; exit 1; }</automated>
    <fails_when>no candidate `agent-md-refactor/SKILL.md` path exists on this machine (exits 1, stop and surface, do not assume), or the found text DOES mention "house" or "rules.py" (the Python assertion fails) — either means the distinctness claim is not automatically verifiable and must be re-examined, not asserted in prose alone</fails_when>
  </verify>

  <done>
    The skill lives at its `/naming-analyzer`-chosen final name (or stays at
    the provisional name with that outcome explicitly recorded as a reasoned
    match, never silently skipped), every registration surface and internal
    reference — including `stack_cache.py`'s cache-namespace segment — points
    at that name, all four local-gate `<verify>` entries above are green,
    `/skill-judge` was re-run once more post-rename with its score recorded
    against the SAME literal zero-`## Critical Issues` gate as Task 1,
    README.md documents the new skill (with the pre-existing
    `ai-kit-usage-metrics` gap noted, not silently inconsistent), and the
    recorded evidence — the renamed skill's real `check` output (proven by
    an executed `<verify>`, not prose) plus the automated confirmation that
    `agent-md-refactor` itself has no house-rule awareness — shows this
    skill's behavior is observably distinct from invoking plain
    `/agent-md-refactor`, backed by automated checks rather than narrative
    alone.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This plan performs a mechanical rename/review pass over artifacts Plans
01-02 already threat-modeled (T-06-01 through T-06-07). No new trust
boundary is introduced: no new input source, no new write target beyond
this repo's own tracked files (`git mv`, `Edit`), and no new runtime
behavior — the checker's logic is unchanged, only its name and path move.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-08 | Tampering | Incomplete rename leaves a stale provisional-name entry alongside a new one in a registration surface (silent gate drift) | low | mitigate | Task 2's second `<verify>` is a repo-wide grep for the literal old name/package string (hyphenated, underscored, AND the slash-delimited cache-namespace form) across `*.py`/`Makefile`/`*.yaml`/`*.toml`/`*.md` outside `.planning/`, run AFTER the `make validate` pass — a partial rename fails this check even if `make validate` happens to still pass by accident (e.g. a duplicated-but-harmless regex alternation). The check is explicitly conditional on a rename having actually happened — a confirmed-no-change outcome from `naming-analyzer` is a correct, non-failing result, never conflated with an incomplete rename. |

No package-manager install tasks exist in this plan, so the Package
Legitimacy Gate does not apply.
</threat_model>

<verification>
- Plans 01-02's own FOUR `<verify>` commands (unittest, Plan 01's live
  structural-exclusion check, Plan 02's SKILL.md structural-grep check, and
  `make test && make lint && make validate`) were ALL re-confirmed green,
  under the provisional name, before this plan's skill-judge/rename work
  began — each as an executed `<automated>` entry, not prose.
- `skill-judge`'s final pre-rename run against the finished `SKILL.md`
  shows zero entries under `## Critical Issues` (this phase's own literal
  ROADMAP/REQUIREMENTS.md bar, stricter than AGENTS.md's general
  ≥96/120-OR-score baseline), AND `skill-judge` was re-run once more
  post-rename (or confirmed-unchanged) with its score recorded.
- `naming-analyzer` was actually invoked and its output is what the final
  name is — recorded, not assumed, with a defined tie-break for multiple
  candidates and an idempotent name-derivation that never double-prefixes.
- `make test`, `make lint`, `make validate` are all green AFTER the rename
  (or confirmed-no-rename).
- Zero remaining `ai-kit-agents-md-checker`/`ai_kit_agents_md_checker`/
  `ai-kit/agents-md-checker` references outside `.planning/` — conditional
  on a rename having actually happened.
- `README.md` documents the skill under its final name (with the
  pre-existing `ai-kit-usage-metrics` README gap noted as out of scope, not
  silently inconsistent).
- ROADMAP SC-3's distinctness claim is backed by two EXECUTED `<verify>`
  entries, not prose alone: the renamed skill's real `check` findings
  (discovered dynamically, never a literal `<final>` placeholder), and a
  direct read of `agent-md-refactor/SKILL.md` confirming it has no
  house-rule awareness.
</verification>

<success_criteria>
- ROADMAP Phase 6 Success Criterion 4 is fully satisfied.
- REQ-agtmd-skill-pipeline is complete.
- REQ-agtmd-wrapper's distinctness requirement is demonstrated with
  recorded evidence, not just asserted.
- Phase 6 has zero unplanned items across all three plans' source audits.
</success_criteria>

<output>
Create `.planning/phases/06-agents-md-rules-checker-skill/06-03-SUMMARY.md` when done
</output>

## Review Dispositions Ledger

### Round 1 — 02ca473

### Review Feedback Addressed

| Concern | Severity | How Addressed |
|---------|----------|----------------|
| Rename/close-out of a potentially-broken 06-01/06-02 surface: `make test && make lint && make validate` after rename only proves the name moved, not that the underlying checks are correct (L98) | HIGH | This round fixes the underlying 06-01/06-02 bugs directly (CRITICAL + HIGH findings resolved in those plans' own ledgers). Task 1's first action step now also explicitly re-runs Plans 01-02's own `<verify>` commands under the provisional name and stops this plan if they fail, rather than assuming soundness. |
| `skill-judge`/`naming-analyzer` under `autonomous: true` have no fallback beyond "stop and surface," making a silent checkbox-skip easy (L99) | MEDIUM | Added explicit must_haves truth + `<done>`/precondition language on both tasks: neither gate may be satisfied by self-grading from memory, and `autonomous: true` never authorizes skipping either invocation. |
| SC-3 "observably distinct" evidence was a SUMMARY sentence, not a test (L100) | MEDIUM | Task 2's action and a new third `<verify>` now require two automated outputs: the renamed skill's real `check` findings, and a direct read of `agent-md-refactor/SKILL.md` confirming it has no house-rule awareness. |
| No skill-judge re-run after rename, even though frontmatter `name:` and every self-reference change (L101) | MEDIUM | Task 2's action adds an explicit post-rename `/skill-judge` re-run step, with its score recorded; a name-only change is never treated as exempt. |
| README.md already omits `ai-kit-usage-metrics` — adding this skill's row is correct, but the existing gap should be noted (L102) | LOW | Task 2's action and the must_haves truth now require a one-line note in the plan summary stating this pre-existing gap is out of this phase's scope, rather than looking like an inconsistency this plan introduced. |
| `files_modified` lists the provisional, pre-rename path without flagging it as such (L103) | LOW | Added an explicit YAML comment directly above `files_modified` stating every path is pre-rename and pointing to Task 2 for the real destinations. |

### Review Feedback Deferred

None — every actionable Round 1 finding touching this plan is addressed above; the remaining Round 1 findings belong to 06-01/06-02 and are recorded in their own ledgers.

### Round 2 — Cycle 2 review (openai/gpt-5.6-sol fallback)

### Review Feedback Addressed

| Concern | Severity | How Addressed |
|---------|----------|----------------|
| The "name unchanged" branch was logically impossible: the `git mv` and zero-occurrence grep ran unconditionally, which can never coexist with a confirmed-no-change outcome | HIGH | Task 2's rename step is now explicitly `IF final_skill_name differs... / IF final_skill_name equals...` — the move/update steps and the zero-leftover grep only run in the differs-branch; the equals-branch explicitly skips them and notes the expected (non-failing) grep outcome. |
| The claimed full pre-rename re-verification was not wired as runnable `<automated>` steps — only the unittest command was | HIGH | Task 1's `<verify>` block now has four `<automated>` entries: the unittest suite, Plan 01's live structural-exclusion check, Plan 02's SKILL.md structural-grep check, and `make test && make lint && make validate` — copied verbatim from the plans they re-verify. |
| The skill-judge pass threshold (96/120) was weaker than the phase's own "no remaining Critical/Important finding" criterion, and "Important" is not a defined severity class in the installed skill-judge | HIGH | Read `~/.agents/skills/skill-judge/SKILL.md` directly: its real report format has exactly one severity section, `## Critical Issues`, no "Important" tier. Task 1's gate is now literally "zero entries under `## Critical Issues`" — the score is recorded for visibility but never substitutes for that. |
| Final-name conversion could double-prefix (`ai_kit_ai_kit_*`) if the analyzer's chosen name already contained `ai-kit-` | MEDIUM | Name derivation now strips any existing `ai-kit-`/`ai_kit_` prefix exactly once before re-applying it, via an explicit `final_suffix` intermediate value. |
| No tie-break/confirmation when `naming-analyzer` returns multiple name alternatives | MEDIUM | Read the analyzer's real "Suggested Renaming" output shape (High/Medium/Low Priority buckets); added a deterministic tie-break (highest-priority bucket wins; still-tied candidates within a bucket stop and ask the user). |
| Distinctness/rename-audit verify claims were narrower than stated: the live-check assertion was prose-only, and the content audit excluded ordinary Markdown like README.md | MEDIUM | Added a fourth `<verify>` that dynamically discovers the (possibly renamed) checker and asserts its real `check .` output is non-empty; the leftover-reference grep now includes `*.md` and the slash-delimited cache-namespace string, and is explicitly conditioned on whether a rename actually happened. |

### Review Feedback Deferred

None — every Cycle 2 finding touching this plan is addressed above.
