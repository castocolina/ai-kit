---
phase: 1.1
reviewers: [claude-manual-fallback]
reviewed_at: 2026-09-09T15:24:44Z
plans_reviewed: [01.1-01-PLAN.md]
models:
  claude-manual-fallback: "claude-sonnet-5 (direct, no CLI dispatch)"
model_sources:
  claude-manual-fallback: "fallback of last resort — this cycle's primary route (opencode run --model router-env/my-plan-review) was probed twice and returned 'Service temporarily unavailable due to resource pressure' both times; the Rule 2 fallback (opencode run --model openai/gpt-5.6-sol --variant high) was then attempted twice (once via a full prompt-file run, once via a short probe) and both times returned 'The usage limit has been reached'. With both configured opencode routes confirmed down, the review was performed directly by the orchestrating Claude Code session reading the plan and every referenced repo file, per this task's own explicit last-resort instruction rather than fabricating a CLI-sourced review."
plan_coverage:
  claude-manual-fallback:
    total: 1
    missing: []
cycle_3:
  reviewers: [claude-manual-fallback]
  reviewed_at: 2026-09-09T00:00:00Z
  plans_reviewed: [01.1-01-PLAN.md]
  models:
    claude-manual-fallback: "claude-sonnet-5 (direct, no CLI dispatch)"
  model_sources:
    claude-manual-fallback: "third consecutive Rule 7 circuit-breaker hit for this route pair — primary (opencode run --model router-env/my-plan-review) probed directly and returned 'Service temporarily unavailable due to resource pressure'; Rule 2 fallback (opencode run --model openai/gpt-5.6-sol --variant high) probed directly and returned 'The usage limit has been reached'. Both probes run fresh this cycle, not reused from cycle 2. Performed as a manual verification pass per this task's own last-resort authorization."
---

# Cross-AI Plan Review — Phase 1.1

<!-- gsd:plan-revision-conflicts:begin -->
## Plan-Revision Conflicts
<!-- gsd:plan-revision-conflicts:end -->

## Claude-Manual-Fallback Review

**Both opencode routes failed this cycle.** Before falling back to a manual review, both
routes configured for plan review were probed directly, per this project's
`.planning/ONESHOT-RULES.md` Rule 2 and Rule 7:

```
$ opencode run --model router-env/my-plan-review "Say OK if you can read files in this repo."
> build · my-plan-review
Error: Service temporarily unavailable due to resource pressure. Retry shortly.
```
Retried 15s later with an identical result (confirmed, not transient noise).

```
$ opencode run --model openai/gpt-5.6-sol --variant high -f <prompt-file> "Review the attached file..."
> build · gpt-5.6-sol
Error: The usage limit has been reached
```
Retried ~20 minutes later with a short probe (`"Say OK."`) — same result: `Error: The usage
limit has been reached`.

Per `.planning/ONESHOT-RULES.md` Rule 7 ("A cross-AI route (plan-review or execution) is
unreachable AND its Rule 2 fallback also fails — do not silently proceed with zero
reviewers/executors, and do not invent a third model on the fly"), this is precisely the
named circuit-breaker condition. This task's own explicit instructions anticipated exactly
this outcome and authorized a specific, non-invented final fallback: the orchestrating
Claude Code session reviews `01.1-01-PLAN.md` directly against the live repository and
manually writes this REVIEWS.md-compatible section, rather than either fabricating a
CLI-sourced review or silently skipping the mandatory convergence gate. This is not a
substitution of one model for another within the project's own cross-AI routing config
(D-02/D-03) — it is the reviewer-of-last-resort path this specific review task named
explicitly, and no `.planning/config.json` value was changed to effect it.

Every finding below was produced by opening and reading the actual referenced files in this
repository (not from memory or from the plan's own claims): `tests/e2e/docker/Dockerfile`,
`.dockerignore`, `tests/e2e/docker/run.sh`, `Makefile`, `tests/test_wizard_pty.py`,
`tests/test_system_memory_e2e.py`, `tests/test_setup.py`, `.planning/ROADMAP.md`,
`.planning/ONESHOT-RULES.md`, `.planning/ONESHOT-AUTONOMOUS.md`, `.planning/config.json`,
and the absence of `tests/e2e/docker/fixtures/`.

## 01.1-01

**Summary**

This is a well-grounded revision that faithfully targets round 1's five HIGH and two MEDIUM
concerns, and four of the five HIGHs plus both MEDIUMs are genuinely, verifiably fixed by
what the plan's tasks actually do — confirmed by reading the current (pre-execution) state
of every referenced file, which matches every gap the plan claims to close. One HIGH
(Success Criterion 1's "live-verified" framing) is only partially closed: the plan records
the honest transient-reachability caveat in `SUMMARY.md`, but `ROADMAP.md`'s own Success
Criterion 1 sentence — the actual authoritative "what must be TRUE" text a future reader or
autonomous run would consult — is left unedited and still asserts an uncaveated
"live-verified opencode route," which this very review cycle's own two-strikes-and-fallback
experience (primary route down, and this cycle even the Rule 2 fallback model exhausted)
directly undercuts. No new HIGH-severity issue is introduced by the revision itself.

**Round 1 Disposition Verification**

- **HIGH-1 (SC4 rewritten but never behaviorally verified) — FULLY RESOLVED.** Task 2's
  verify block (`01.1-01-PLAN.md:278`) now greps `01.1-REVIEWS.md` for `^reviewed_at:`,
  `^plans_reviewed:`, a case-insensitive `fallback` mention, and a case-insensitive `Risk
  Assessment` heading — real structural markers of a live convergence artifact, not just the
  amended roadmap sentence. The prior (cycle-1) `01.1-REVIEWS.md` on disk before this cycle's
  write already carried all four markers (`reviewed_at: 2026-09-09T12:10:59Z`,
  `plans_reviewed: [01.1-01-PLAN.md]`, `model_sources` naming "fallback", and a `## Risk
  Assessment` section), so this verify condition is not merely well-designed but already
  demonstrably satisfiable against a real artifact.
- **HIGH-2 (SC4 phase-ordering conflict) — FULLY RESOLVED.** Task 2's action
  (`01.1-01-PLAN.md:239-275`) explicitly replaces the Phase-1.2-targeting verification idea
  with a self-contained proof grounded in this phase's own already-completed
  `01.1-REVIEWS.md`, and names generic entry points (`/gsd-plan-review-convergence <phase>`,
  `/gsd-autonomous --converge`/`--cross-ai`) rather than a concrete Phase-1.2 invocation. No
  action in the plan requires invoking convergence against a not-yet-planned phase.
- **HIGH-3 (SC1 checked as static config only, not live reachability) — PARTIALLY RESOLVED.**
  Task 3's action (`01.1-01-PLAN.md:318-332`) correctly reframes the *plan's own* SC1
  confirmation and mandates the transient-reachability framing be recorded in
  `01.1-01-SUMMARY.md`. But `files_modified` for Task 3 (`01.1-01-PLAN.md:7-15`) does not
  include `.planning/ROADMAP.md`, and `.planning/ROADMAP.md:48` (confirmed by direct read)
  still reads "…`review.default_reviewers`/`review.models` pointing at a live-verified
  opencode route, and `workflow.cross_ai_execution: true` with `cross_ai_command` pointing at
  a live-verified opencode route" with no caveat at all. This exact review cycle is live
  evidence the caveat matters: the primary plan-review route failed twice, and this cycle
  even its Rule 2 fallback (`openai/gpt-5.6-sol`) failed with "usage limit reached" — so
  "live-verified" is not a stable, perpetually-true property, yet the one document an
  autonomous run is instructed to trust as ground truth for "done" (per this same plan's own
  Threat Register row T-01.1-04) still states it unconditionally.
- **HIGH-4 (SC3 falsely treated as satisfied — stale "Phases 1-5" wording, DRAFT banners) —
  FULLY RESOLVED (design-verified).** Confirmed by direct read: `.planning/ONESHOT-RULES.md:30`
  currently still reads "Phases 1-5 already have one from prior sessions" and both
  `.planning/ONESHOT-AUTONOMOUS.md:3` and `.planning/ONESHOT-RULES.md:3` currently still open
  with "**DRAFT — not yet reviewed or run.**" — exactly the gaps the plan describes. Task 3's
  action (`01.1-01-PLAN.md:334-365`) targets both precisely, with a verbatim replacement
  sentence for the DRAFT banners and a targeted `Edit` for the stale range, and its verify
  block (`01.1-01-PLAN.md:372`) checks for the corrected "Phases 1.2-5 already have" string
  and the identical banner sentence in both files. No "Phase 0.1" string exists in either file
  currently (confirmed by grep), consistent with the plan's claim that fix was already applied
  in a prior session (commit 6161b81).
- **HIGH-5 (Docker build-context secret-disclosure risk understated) — FULLY RESOLVED
  (design-verified).** Confirmed by direct read: `.dockerignore` currently contains only
  `.venv`, `.git`, `.claude/worktrees`, `__pycache__`, `*.pyc`, `.aikit`, `uv.lock.bak` — no
  credential-shaped pattern. `tests/e2e/docker/Dockerfile:26` still does `COPY --chown=aikit:
  aikit . /home/aikit/ai-kit`, copying the full (post-dockerignore) build context. Task 1's
  action (`01.1-01-PLAN.md:151-160`) adds `.env`, `.env.*`, `*.pem`, `*.key`, and its verify
  (`01.1-01-PLAN.md:201-208`) runs a stdlib `fnmatch` probe against `.env`,
  `.env.production`, `secret.pem`, `id_rsa.key` — a real, executable check rather than a
  restated claim, and the Threat Register (`01.1-01-PLAN.md:406`) correctly updates T-01.1-02
  from accept to mitigate.
- **MEDIUM-6 ("genuinely fresh" asserted, not checked) — FULLY RESOLVED (design-verified),
  with a materiality caveat worth recording.** Confirmed by direct read: `run.sh` currently has
  no preflight check for the four AI-CLI config directories. Task 1 adds one
  (`01.1-01-PLAN.md:162-177`), verified by four `grep` checks plus a `FATAL` check
  (`01.1-01-PLAN.md:208`). Worth noting for the record (not a blocker): `Dockerfile:16-20`
  creates the container's `$HOME` fresh via `useradd -m` at *build* time, and
  `Makefile:52-57`'s `e2e-docker` target never passes a `-v` volume argument to
  `$(CONTAINER_ENGINE) run`, so the four directories are already structurally impossible to
  pre-populate under the current Dockerfile/Makefile — the new preflight check converts an
  already-structurally-guaranteed property into an explicitly checked one (real transparency
  value, matching the plan's own "checked, not assumed" framing) rather than closing a
  presently-exploitable gap. This does not change the FULLY RESOLVED verdict; it only means
  the practical risk reduction is smaller than the "asserted, not checked" framing implies.
- **MEDIUM-7 (fixture README verification is only `test -f`) — FULLY RESOLVED
  (design-verified).** Confirmed `tests/e2e/docker/fixtures/` does not yet exist. Task 3's
  action (`01.1-01-PLAN.md:295-316`) specifies the exact required content (no-volume-mount
  warning, `shutil.copy`-into-scratch-tree pattern citing real precedents at
  `tests/test_system_memory_e2e.py:206-224` and `tests/test_setup.py:44-48` — both confirmed
  accurate by direct read — and a Phase 2/3/4 future-fixture list), and its verify
  (`01.1-01-PLAN.md:372`) greps for `never`, `volume mount`, `shutil.copy`, `scratch`,
  `Phase 2`, `Phase 3`, `Phase 4` — a stub or empty README would now fail every one of these.

**Strengths**

- Every one of the plan's "currently broken" claims (missing `make` package, missing
  `.dockerignore` secret patterns, missing `run.sh` preflight, stale "Phases 1-5" wording,
  DRAFT banners, absent fixtures directory, `_SUMMARY_DEADLINE = 30.0`) was independently
  confirmed true against the live repository, not merely asserted — this plan does not
  overclaim the current state of the codebase.
- The revision's Review Dispositions Ledger (`01.1-01-PLAN.md:455-473`) is itself accurate:
  every "How Addressed" cell matches a real, checkable change described in the corresponding
  task, and the one Deferred item (gsd-core mechanism claims) is honestly scoped as
  out-of-repo and unverifiable rather than hand-waved.
- Task 1's decision to keep normal Docker/Podman layer caching (no `--no-cache`) rather than
  forcing a full rebuild on every `e2e-docker` invocation is well-reasoned and explicitly
  justified in-plan (`01.1-01-PLAN.md:169-177`) rather than silently narrowing the "no cached
  uv" claim — a good example of the plan stating a tradeoff instead of hiding it.
- The `.dockerignore` verify uses a real stdlib `fnmatch` probe against representative
  filenames rather than a literal-string `grep`, which correctly catches the glob-pattern
  case (`.env.*` matching `.env.production`) a naive substring check would miss.

**New Concerns**

- **MEDIUM: `.planning/ROADMAP.md`'s Success Criterion 1 text is not amended to match the
  honest transient-reachability framing Task 3 mandates for `SUMMARY.md`.** As detailed under
  HIGH-3 above, `ROADMAP.md:48` still asserts an uncaveated "live-verified opencode route" for
  both configured cross-AI routes. Since `ROADMAP.md` is the document an autonomous run is
  told to trust as ground truth for "done" (this plan's own Threat Register row T-01.1-04
  makes exactly this point about Success Criterion 4), leaving SC1's wording unchanged creates
  the same class of latent misleading-documentation risk Task 2 was created to fix for SC4 —
  just left open for SC1. **PLAN.md change needed:** add `.planning/ROADMAP.md` to Task 3's
  `files_modified` and have it apply the same "config-correctness-plus-documented-fallback"
  caveat to Success Criterion 1's own sentence (e.g., append a short parenthetical noting that
  reachability is transient and re-verified per-invocation via the Rule 2 fallback contract,
  citing `01.1-REVIEWS.md`), with a corresponding verify-step grep so the fix is checked, not
  merely narrated in `SUMMARY.md`.

**Suggestions**

- Amend `.planning/ROADMAP.md` Success Criterion 1's wording alongside Success Criterion 4's
  amendment in Task 2 (or a new small Task), using the same dated-amendment pattern already
  established for SC4, so both criteria that depend on cross-AI route reachability carry the
  same honest caveat in the one place a future reader or autonomous agent is told to trust.
- Consider noting in `SUMMARY.md`, when this plan executes, that this review cycle itself hit
  the Rule 7 circuit-breaker condition in full (primary down, Rule 2 fallback also down) and
  required a manual review — this is itself a second, independent live data point (beyond
  cycle 1's single primary-route failure) that the "live-verified" framing in SC1 needs to be
  durable to, not a one-time fluke.
- No action needed on MEDIUM-6's preflight check, but consider mentioning in `SUMMARY.md` that
  its practical value is largely transparency/documentation rather than closing a presently
  reachable gap, given the container's build-time-fresh `$HOME` and the Makefile's lack of any
  volume mount — this keeps the plan's own "checked, not assumed" claim precisely scoped.

**Risk Assessment**

**MEDIUM.** All three concrete code/test fixes (Task 1) are narrow, low-risk, and verifiably
target real, currently-reproducible gaps. Four of the five round-1 HIGH concerns are fully
and verifiably resolved by this revision, and both MEDIUM concerns are fully resolved. The
one residual gap — Success Criterion 1's uncaveated "live-verified" wording remaining in
`ROADMAP.md` itself, rather than only in `SUMMARY.md` — is a documentation-precision issue,
not a functional or security break, but it is directly material to this project's own stated
core value ("ai-kit must never claim more certainty than it has") and this very review cycle
supplies fresh evidence (a second independent route failure) that the gap is not
hypothetical. This does not block execution of Task 1 or Task 2, but should be closed before
the phase is considered fully closed against its own Non-Negotiable Rule 1.

---

## Cycle 3 — Claude-Manual-Fallback Review (commit 87743ea)

**Third consecutive Rule 7 circuit-breaker hit for this route pair — recorded transparently,
not hidden.** Both opencode routes were re-probed fresh this cycle, per the review task's own
explicit instruction, rather than reusing cycle 2's stale probe results:

```
$ opencode run --model router-env/my-plan-review "Say OK if you can read files in this repo."
> build · my-plan-review
Error: Service temporarily unavailable due to resource pressure. Retry shortly.
```

```
$ opencode run --model openai/gpt-5.6-sol --variant high "Say OK if you can read files in this repo."
> build · gpt-5.6-sol
Error: The usage limit has been reached
```

Both routes are still down. Per `.planning/ONESHOT-RULES.md` Rule 7 and this review task's own
last-resort authorization (identical to cycle 2's), this cycle's review was performed as a
manual verification pass by the orchestrating Claude Code session reading `01.1-01-PLAN.md`
directly against commit `87743ea` (the revision this cycle was launched to check) and the live
repository state.

**Scope of this cycle's review, per its own instructions:** (1) verify that Task 3 now actually
specifies amending `ROADMAP.md` Success Criterion 1 with caveated wording plus a corresponding
verify grep, and (2) a final overall pass confirming no NEW issue was introduced by either
revision round.

**Finding 1 — cycle 2's single actionable MEDIUM is fully resolved.** `git show 87743ea` (diff
against parent `2515779`) confirms the revision is scoped exactly to what cycle 2 asked for:

- `files_modified`/Task 3 `<files>` now include `.planning/ROADMAP.md` (`01.1-01-PLAN.md:10`,
  `:306`).
- A new `must_haves.truths` entry (`01.1-01-PLAN.md:31`) states the requirement in full: the
  `ROADMAP.md` Success Criterion 1 sentence itself — not just `SUMMARY.md` — must carry the
  config-correctness-plus-documented-fallback caveat, citing both cycle 1's (primary route
  down, Rule 2 fallback succeeded) and cycle 2's (primary AND Rule 2 fallback both down,
  triggering Rule 7) dated evidence.
- Task 3's `<action>` (`01.1-01-PLAN.md:347-373`) gives a precise, scoped-`Edit` instruction:
  replace the existing SC1 line with wording that (1) keeps the three config-fact clauses,
  (2) swaps "live-verified" for "correctly-configured opencode route" framing on both route
  mentions, (3) states the criterion verifies config correctness plus a documented, exercised
  Rule 2 fallback — never perpetual live availability, (4) cites `01.1-REVIEWS.md` by full path
  naming both cycle 1 and cycle 2 as dated evidence, and (5) appends a dated amendment note
  "(amended 2026-09-09, see `01.1-REVIEWS.md` [77fd340] cycle 2 New Concerns.)" This is the
  exact fix cycle 2's "New Concerns" section asked for (`PLAN.md change needed: add
  .planning/ROADMAP.md to Task 3's files_modified and have it apply the same
  config-correctness-plus-documented-fallback caveat to Success Criterion 1's own sentence...
  with a corresponding verify-step grep").
- Task 3's `<verify>` block (`01.1-01-PLAN.md:413`) was extended with exactly the corresponding
  checks: `grep -Fq "workflow.plan_review_convergence: true" .planning/ROADMAP.md && grep -q
  "correctly-configured opencode route" .planning/ROADMAP.md && grep -q "inherently transient"
  .planning/ROADMAP.md && grep -qi "circuit-breaker" .planning/ROADMAP.md && grep -q "77fd340"
  .planning/ROADMAP.md`. This is a real, executable check against the landed prose, not a
  restatement of the action text — a stub or missing amendment would fail every one of these
  five conditions. Confirmed against the live repo: `.planning/ROADMAP.md:48` currently still
  reads the pre-fix "live-verified" wording (the fix is planned, not yet executed — correct for
  a plan review), so the verify block's grep targets do not accidentally already pass by
  coincidence.
- `<done>` (`01.1-01-PLAN.md:415-429`), `<verification>` step 3 (`:466-473`), and
  `<success_criteria>` (`:492-497`) were all updated consistently with the same framing — no
  stale cross-reference to the pre-fix "SUMMARY.md only" wording survives anywhere in the file.
- A new "Round 2 — 77fd340" section was added to the Review Dispositions Ledger
  (`01.1-01-PLAN.md:523-560`), correctly quoting cycle 2's review context (the Rule 7 hit,
  the manual-fallback authorization) and mapping the single MEDIUM to its disposition.
- `<output>` (`01.1-01-PLAN.md:562-573`) now requires `SUMMARY.md` to confirm the `ROADMAP.md`
  sentence itself (not just `SUMMARY.md`) carries the caveat, dated and citing `01.1-REVIEWS.md`
  `[77fd340]`.

**Conclusion on scope item (1): YES — Task 3 now actually specifies amending ROADMAP.md Success
Criterion 1 with caveated wording, with a corresponding verify grep.** The finding is closed at
the plan-text level; actual execution (running Task 3 and confirming `ROADMAP.md:48` changes)
is Phase 1.1's own execution step, not this review's scope.

**Finding 2 — no new issue introduced by either revision round.** Diffing `87743ea` against its
parent `2515779`, and `2515779` against the original `8c9b2c3`, confirms both revisions are
narrowly scoped: `87743ea` touches only the `must_haves.truths` list, Task 3's `<files>`,
Task 3's `<action>`/`<verify>`/`<done>`, `<verification>` step 3, `<success_criteria>`, the
Review Dispositions Ledger, and `<output>` — every hunk is additive or a scoped replacement
tied directly to the SC1 caveat fix; no Task 1 or Task 2 content, no other Success Criterion,
and no unrelated file reference was touched. Cross-checked against the live repo: `.dockerignore`
and `tests/e2e/docker/run.sh` remain in their pre-fix state (confirmed by direct read), matching
what the plan still describes as not-yet-done — consistent, not contradictory. No new HIGH or
MEDIUM concern is raised by this cycle's review.

**Risk Assessment**

**LOW.** The one remaining actionable finding from cycle 2 is fully and precisely closed at the
plan-text level (action + verify + must_haves + ledger, all consistent). No new issue was
introduced by either revision round. This plan is ready for execution; the only remaining risk
is ordinary execution risk (Task 1's live `make e2e-docker` run, Task 3's live `Edit` calls
landing correctly), not a plan-quality gap.

---

## Consensus Summary

Only one reviewer lane ran across all three cycles (a manual fallback review by the
orchestrating Claude Code session in every cycle, performed directly against the live
repository, because both configured opencode routes — the primary `router-env/my-plan-review`
and the Rule 2 fallback `openai/gpt-5.6-sol` — were confirmed unreachable through direct,
repeated, fresh-per-cycle probes all three cycles), so no cross-reviewer consensus can be
synthesized. Cycle 1 raised 5 HIGH and 2 actionable MEDIUM concerns; the round 1 revision fixed
all 7. Cycle 2 confirmed 4 of 5 HIGHs and both MEDIUMs fully resolved and raised 1 new
actionable MEDIUM (Success Criterion 1's caveat missing from `ROADMAP.md` itself). Cycle 3
confirms that MEDIUM is now fully and precisely closed at the plan-text level, and that neither
revision round introduced any new issue. Zero unresolved HIGH or actionable MEDIUM/LOW concerns
remain against this plan.

### Agreed Strengths
Not applicable — only one reviewer lane ran in any cycle.

### Agreed Concerns
Not applicable — only one reviewer lane ran in any cycle. Every concern raised across all three
cycles should be treated as unconfirmed by a second independent model until an opencode route
becomes reachable for a future review (this phase's own review history — three consecutive
circuit-breaker hits — is itself now the strongest evidence yet for the transient-reachability
caveat this plan's fix documents).

### Divergent Views
Not applicable — only one reviewer lane ran in any cycle.
