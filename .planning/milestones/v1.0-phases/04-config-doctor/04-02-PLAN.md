---
phase: 04-config-doctor
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
cross_ai: true
autonomous: true
requirements: [REQ-config-doctor-diagnostic-checks, REQ-config-doctor-review-screen]
files_modified:
  - tools/config_doctor_checks.py
  - tools/config_doctor_readers.py
  - tools/config_doctor_app.py
  - tests/test_config_doctor.py
  - tests/test_config_doctor_pty.py

estimate:
  tokens: 165000
  raw_tokens: 165000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Every Checks Catalog row `04-RESEARCH.md` documents for Claude Code (rows 2, 2b, 3, 3b, 4), opencode (rows 5, 6, 7, 7b), Codex (rows 8, 9, 10, 10b, 10c, 11), Cursor (rows 13, 14, 15a, 15b, 16, 17, 18, 19), and the cross-runtime rtk-Cursor-integration check (row 12) exists as a `CheckRow` in `CONFIG_DOCTOR_ROWS`, evaluates without raising against a realistic fixture for its runtime, and carries a real `confidence` and `source` citation copied from `04-RESEARCH.md` — never an invented value (ROADMAP SC-1, REQ-config-doctor-diagnostic-checks)."
    - "Rows 17 and 18 (Cursor local CLI chat retention, Cursor CLI telemetry toggle) render `current_display == \"unknown\"` UNCONDITIONALLY, regardless of `cli-config.json`'s actual content — `04-RESEARCH.md` found no key backing either setting, so there is nothing a file read could ever confirm or deny; this is shown as a genuinely open question, never as a false `pass` or `fail` (`04-RESEARCH.md` rows 17/18, Open Questions 2-3, PROJECT.md core value)."
    - "Every row this plan adds has `apply=None` in this wave — Wave 3 is the only plan that upgrades any `CheckRow.apply` field to a real writer, and it does so by editing these SAME row records in place, never by duplicating them. This wave's job is a complete, accurate, confidence-labeled READ-ONLY catalog (REQ-config-doctor-diagnostic-checks' 'informational-only rows never offered as apply actions' contract is trivially true for every row this wave adds, and the RESEARCH-sourced apply disposition — eligible-later vs. permanently informational — is recorded in each row's own `why` text so a human reading the screen already knows which is which before Wave 3 ships)."
    - "The cross-runtime rtk-Cursor-integration row (id `rtk-cursor-integration`) appears in the rendered catalog regardless of whether ANY of the four runtime config files exist — its section is appended UNCONDITIONALLY whenever `CONFIG_DOCTOR_ROWS` has any row with `runtime == \"cross\"`, never gated on a config-file-presence check of any kind; `rtk`'s own absence from PATH is surfaced as a ROW VALUE (the formatted string `\"rtk not installed on PATH\"`), never a section gate (folds in Phase 3's Non-Goals note per `ROADMAP.md` Phase 4 Goal and `tools/hooks/README.md`'s own closing line)."
    - "A single scratch tree carrying realistic, live-verified-shaped fixtures for all four runtimes at once (`settings.json`, `opencode.jsonc`, `config.toml`, `cli-config.json`) produces a catalog with four runtime sections plus the cross-runtime section, and `setup.py --config-doctor` renders all of it under a real PTY without crashing, truncating, or hanging — proven by an executed end-to-end test, not by per-row unit tests alone."
    - "Every new reader function degrades to `UNKNOWN`/`\"unknown\"` on a malformed or type-mismatched value (a `permission` key whose value is a string instead of an object, a `modelParameters` entry whose value is not a list, etc.) rather than raising — `evaluate_row` never lets one bad row crash the whole catalog build for its runtime section."
  artifacts:
    - tools/config_doctor_checks.py
    - tools/config_doctor_readers.py
  key_links:
    - "`evaluate_row`'s `row.read` contract changes from `read(data)` (Wave 1) to `read(ctx)` where `ctx` is a new `ReadContext` NamedTuple (`data`, `env`, `runner`) — this task edits row 1's ALREADY-SHIPPED read function to the new signature in the SAME commit that adds the new rows, and Wave 1's regression suite (`tests.test_config_doctor`) must still pass unmodified in its assertions afterward, proving the signature change is purely additive."
    - "Row 12's live `rtk init --show` probe is a NEW, locally-ported function in `config_doctor_readers.py` (not an import of `tools/hooks/detect.py`) — kept self-contained for the same reason every other reader in this module is self-contained (see 04-01-PLAN.md notes 1), even though `tools/hooks/detect.py` has no `textual` dependency either; its bracket-prefix classification pattern is mirrored, not reused by reference."
    - "`build_catalog`'s four-runtime loop and its new cross-runtime pass are two INDEPENDENT code paths: a section for `runtime == \"cross\"` is appended whenever `CONFIG_DOCTOR_ROWS` has any row with `runtime == \"cross\"`, with NO `os.path.isfile` gate at all — conflating the two would either hide row 12 on a machine with zero AI-CLI config files, or wrongly gate it on an arbitrary one of the four."
---

<objective>
Expand the Checks Catalog from Wave 1's single tracer row to the full set
`04-RESEARCH.md` documents: every Claude Code, opencode, Codex, and Cursor
row, plus the cross-runtime rtk-Cursor-integration check — 25 rows in total,
each confidence-labeled and cited, all read-only in this wave.

Purpose: complete REQ-config-doctor-diagnostic-checks and
REQ-config-doctor-review-screen for every row `04-RESEARCH.md` verified this
milestone (excluding the one row RESEARCH itself flagged as needing explicit
scope confirmation before inclusion — see `<source_audit>`). Every row is
read-only in this wave; Wave 3 decides which rows earn a real apply writer by
editing these same `CheckRow` records in place.

Output: `CONFIG_DOCTOR_ROWS` grows from 1 to 25 records; `config_doctor_readers.py`
gains the per-runtime extraction helpers each new row needs, plus a
`ReadContext` carrying `data`/`env`/`runner` so environment-only rows (Claude
Code telemetry) and subprocess-probed rows (rtk-Cursor-integration) fit the
same engine without a parallel code path; `config_doctor_app.py`'s rendering
is proven against a full, all-four-runtimes-present fixture.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/ONESHOT-RULES.md
@AGENTS.md
@.planning/phases/04-config-doctor/04-CONTEXT.md
@.planning/phases/04-config-doctor/04-RESEARCH.md
@.planning/phases/04-config-doctor/04-01-SUMMARY.md
</context>

<tracked_source_paths>
Every path this plan touches already exists and is git-tracked as of Wave 1:
`tools/config_doctor_checks.py`, `tools/config_doctor_readers.py`,
`tools/config_doctor_app.py`, `tests/test_config_doctor.py`,
`tests/test_config_doctor_pty.py` — verify with `git ls-files` before editing;
this plan performs scoped `Edit` operations, never a whole-file `Write`, since
every one of these files already carries Wave 1's real content.

No path in this plan is a runtime target. All four config files this plan's
readers eventually parse are named only as format references; every test in
this plan runs against a scratch tree (ONESHOT-RULES Rule 5).
</tracked_source_paths>

<failing_direction_contract>
Every runnable `<automated>` command below is followed by a `<fails_when>`
sibling naming the observable output that constitutes failure. `<fails_when>`
binds to the nearest preceding `<automated>`.
</failing_direction_contract>

<deep_work_rules>
Every task below carries `<read_first>`, `<action>`, `<acceptance_criteria>`,
`<verify>` and `<done>`. `<action>` names concrete identifiers and target
states and contains no fenced code blocks.
</deep_work_rules>

<source_audit>
## Multi-Source Coverage Audit (Phase 4, Wave 2 portion)

| # | Source | Item | Covered by |
|---|--------|------|-----------|
| GOAL-SC1 | ROADMAP SC-1 | Every Checks Catalog row across every runtime with a config file present, plus the folded-in rtk-Cursor-integration check | 04-02 T1 (Claude/opencode), T2 (Codex/Cursor), T3 (cross-runtime row 12) |
| GOAL-SC3 | ROADMAP SC-3 | Lower-confidence / no-real-backing-setting rows visibly labeled, never offered as apply | 04-02 T1-T3 (every row's `why` states RESEARCH's own confidence + apply disposition) |
| RES-CLAUDE-2..4 | 04-RESEARCH.md Claude Code table | Rows 2, 2b, 3, 3b, 4 | 04-02 T1 |
| RES-OPENCODE-5..7b | 04-RESEARCH.md opencode table | Rows 5, 6, 7, 7b | 04-02 T1 |
| RES-CODEX-8..11 | 04-RESEARCH.md Codex table | Rows 8, 9, 10, 10b, 10c, 11 | 04-02 T2 |
| RES-CURSOR-13..19 | 04-RESEARCH.md Cursor table | Rows 13, 14, 15a, 15b, 16, 17, 18, 19 | 04-02 T2 |
| RES-CROSS-12 | 04-RESEARCH.md Cross-runtime table | Row 12 (rtk-Cursor-integration) | 04-02 T3 |
| RES-PITFALL4 | 04-RESEARCH.md Common Pitfall 4 | Claude Code's two telemetry systems stay conceptually distinct in the UI copy | 04-02 T1 (row 4's `why` text) |
| RES-PITFALL5 | 04-RESEARCH.md Common Pitfall 5 | Cursor's two sandbox mechanisms (15a / 15b) stay two distinct rows | 04-02 T2 |
| RES-PITFALL6 | 04-RESEARCH.md Common Pitfall 6 | Codex `[memories]` settings (10c) never marketed as `cleanupPeriodDays`-equivalent | 04-02 T2 |
| RES-PITFALL7 | 04-RESEARCH.md Common Pitfall 7 | Cursor `modelParameters` (row 16) treated as informational, undocumented schema | 04-02 T2 |
| RES-OQ1 | 04-RESEARCH.md Open Question 1 | `sandbox-policies/` directory vs. `sandbox.json` — only `sandbox.json` (row 15b) is a citable apply target | 04-02 T2 (row 15b's `why` states the discrepancy explicitly) |
| RES-OQ2/3 | 04-RESEARCH.md Open Questions 2-3 | Cursor rows 17/18 shipped as explicitly `unknown`, not silently skipped | 04-02 T2 |
| RES-A2 | 04-RESEARCH.md Assumption A2 | opencode row 7b's custom-router (`router-env`) applicability is unverified | 04-02 T1 (row 7b's `why` text) |
| CTX-D02 | 04-CONTEXT.md | `scope: model` rows (7b, 10, 16) | 04-02 T1 (7b), T2 (10, 16) |
| CTX-D04 | 04-CONTEXT.md | Row 9 (Codex `features.hooks`) informational only | 04-02 T2 |

**Excluded from this milestone's scope (not a gap):** RESEARCH.md's candidate
row 20 (`rtk`'s own `[tee].max_files`). `04-RESEARCH.md` explicitly frames
this as a further scope question the planner/user should confirm before
locking it in — it is a THIRD TOML target beyond Codex (row 20's own text:
"a new rtk-config-**editing** row is a further scope expansion the
planner/user should explicitly confirm"), it targets a file that is not one
of REQUIREMENTS.md's four named runtimes' config files, and it has zero
upstream documentation backing its "recommended: ~100" claim (Assumptions Log
A1). Per this project's planner-authority limits, this is excluded on
"missing information" grounds (no source confirms it as required scope), not
because it seemed hard. If the user wants it in-scope, it is a small,
mechanically similar follow-up row once confirmed — nothing this wave builds
needs to change to accommodate it later.
</source_audit>

<tasks>

<task type="auto">
  <name>Task 1: Claude Code's remaining rows and opencode's full catalog, plus the `ReadContext` signature revision</name>
  <files>tools/config_doctor_checks.py, tools/config_doctor_readers.py, tests/test_config_doctor.py</files>

  <read_first>
    - `tools/config_doctor_checks.py`, `tools/config_doctor_readers.py` as Wave 1 landed them — read the actual `CheckRow`, `evaluate_row`, `build_catalog`, `get_nested`, `UNKNOWN` definitions before editing.
    - `04-RESEARCH.md` Checks Catalog, Claude Code table rows 2, 2b, 3, 3b, 4 and opencode table rows 5, 6, 7, 7b — the authoritative current-default/recommended/confidence/why/source text for every row this task adds. Quote the citation URLs and the exact recommended values verbatim; do not paraphrase a number.
    - `04-RESEARCH.md` Common Pitfall 3 (promptCacheTtl's billing-tier-dependent default) and Common Pitfall 4 (Claude Code's two distinct telemetry systems) — both directly shape row 2's and row 4's `read`/`why` design.
    - `04-RESEARCH.md` Code Examples section, "opencode per-model options" — the verbatim documented JSONC shape (`provider.<id>.models.<model>.options.{reasoningEffort, textVerbosity, reasoningSummary}` / `.options.thinking.{type, budgetTokens}`) row 7b's reader walks.
    - `04-RESEARCH.md` Assumption A2 — this machine's own `opencode.jsonc` routes through a custom `router-env` provider id, not the literal `"openai"`/`"anthropic"` — row 7b's `why` must state this is unverified for non-documented provider ids, never assert it works.
  </read_first>

  <action>
First, revise `evaluate_row`'s contract: define `ReadContext` in
`tools/config_doctor_checks.py` as a `typing.NamedTuple` with fields `data:
object` (the parsed config dict, or `None` when the section's file state is
`CONFIG_STATE_UNREADABLE`), `env: object` (the env mapping `build_catalog`
received), `runner: object` (a subprocess-runner callable, defaulting to
`subprocess.run` — unused by any row until Task 3, but part of the contract
from this task onward so Task 3 does not need a second signature revision).
Change `evaluate_row(row, ctx)` to call `row.read(ctx)` instead of
`row.read(data)`. Update row 1's (`claude-retention`) existing `read`
function to accept `ctx` and call `get_nested(ctx.data, "cleanupPeriodDays",
default=30)` — a one-line signature change. Update `build_catalog` to
construct a `ReadContext` once per runtime (`data=None` when the section's
state is unreadable, else the parsed dict; `env=env`; `runner=None`, meaning
"use the default" — Task 3 threads a real `runner` kwarg through
`build_catalog(env, runner=None)` when it adds the first subprocess-based
row) and pass it into every `evaluate_row` call for that runtime. Run
`tests.test_config_doctor` after this change and confirm every Wave 1
assertion still passes unmodified — this is the regression proof that the
signature revision is purely additive.

Add five Claude Code rows to `CONFIG_DOCTOR_ROWS`. `claude-prompt-cache-ttl`
(row 2): `read` returns `get_nested(ctx.data, "promptCacheTtl",
default=UNKNOWN)` — UNKNOWN, not a guessed flat default, because the true
default when the key is absent genuinely depends on billing/auth mode in a
way the local file alone cannot reveal (Common Pitfall 3); `recommended`
formatted as the string `'"1h" (only changes anything on the 5-min-default
auth modes)'`; `confidence="HIGH"`; `why` states the exact billing-mode split
research verified (1h on subscription within-plan usage, 5m for API-key/
usage-credits/cloud-provider auth) and that the setting requires Claude Code
v2.1.242+; `source="https://code.claude.com/docs/en/prompt-caching"`.
`claude-subagent-prompt-cache-ttl` (row 2b): `read` returns
`get_nested(ctx.data, "subagentPromptCacheTtl", default="5m")` — a genuine
default this time (always 5 minutes, not billing-dependent, per the same
source doc), so absence is NOT routed through UNKNOWN;
`recommended='"1h" if idle-then-resume subagent/workflow usage is common'`;
`confidence="HIGH"`; same source URL. `claude-sandbox-enabled` (row 3):
`read` returns `get_nested(ctx.data, "sandbox", "enabled", default=False)`;
`recommended=True`; `confidence="HIGH"`; `why` states this is OS-level
(Seatbelt/bubblewrap+socat) filesystem/network isolation and is
security-relevant; `source="https://code.claude.com/docs/en/sandboxing"`.
`claude-sandbox-fail-if-unavailable` (row 3b): `read` returns
`get_nested(ctx.data, "sandbox", "failIfUnavailable", default=False)`;
`recommended` a string noting this is the user's own risk call, not a
universal `True` recommendation (same as row 3b's own RESEARCH framing);
`confidence="HIGH"`; same source URL. `claude-telemetry` (row 4): `read`
returns `ctx.env.get("CLAUDE_CODE_ENABLE_TELEMETRY") or "unset"` — this is
the FIRST row reading `ctx.env` rather than `ctx.data`, proving the
`ReadContext` revision above is load-bearing, not speculative; set
`reads_section_file=False` on this row (Wave 1's `CheckRow` field, unused
until now) — this row's value is fully knowable from `ctx.env` regardless of
whether `settings.json` itself parses, so it must NOT be forced to
`"unknown"` by Wave 1's unreadable-file short-circuit the way every
`ctx.data`-dependent row correctly is;
`recommended='"1" (with exporter env vars) for org/self usage-cost
visibility'`; `confidence="HIGH"`; `why` states, verbatim from research, that
this is DISTINCT from Anthropic's own default-on operational metrics/error-
reporting stream (`DISABLE_TELEMETRY`/`DISABLE_ERROR_REPORTING`, a different
mechanism entirely — Common Pitfall 4) and that this setting's real
provider-conditionality is which API provider is connected through
(off by default on Vertex/Bedrock/Foundry/AWS, on for direct Claude API),
never individual-vs-corporate account tier; `why` ALSO explicitly scopes
this row's claim to the **process environment** value only — Claude Code's
`settings.json` separately supports its own `"env"` block, and a
`CLAUDE_CODE_ENABLE_TELEMETRY` value configured only there (never exported
to the process) is NOT read by this row; state this distinction plainly in
the row's own `why` text rather than silently under-reporting such a case as
`"unset"` with no caveat; `source` the same data-usage doc.
Every new row's `why` closes with one clause stating RESEARCH's own apply
disposition verbatim (e.g. "Apply: yes, security-relevant" for row 3) —
`apply` itself stays `None` for all five; Wave 3 decides which become real.

Add four opencode rows. `opencode-retention` (row 5): `read` returns the
formatted string `"not currently configurable"` unconditionally — there is
no key to check; `recommended="N/A"`; `confidence="HIGH"`; `why` states BOTH
independent sources research verified (GitHub issue #22110 closed as "not
planned"; the full official config reference documents every key and has no
retention/cache-TTL/cleanup-period key anywhere) and explicitly phrases this
as "not currently configurable," never "will never be configurable" — a
disposition, not a permanent guarantee; `source="https://opencode.ai/docs/
config/"`; `apply` stays `None` PERMANENTLY for this row (there is no
setting to ever write — record this explicitly in the `why` text, distinct
from every other row whose `apply=None` is merely "not yet wired"). Read the
opencode config's `"permission"` object for `opencode-permissions` (row 6):
`read` first evaluates `permission = get_nested(ctx.data, "permission",
default=None)`; when `permission` is present but is NOT a `dict` (e.g. a
JSON string or number — a malformed config, not merely an absent key),
`read` returns `UNKNOWN` immediately, before touching any of the 13 action
keys, because there is nothing a per-action default lookup can honestly
report against a non-object value — this is DISTINCT from the ABSENT-key
case (key missing entirely), where each action's own documented default
still legitimately applies. Otherwise (the key is absent, OR present as a
genuine `dict`), `read` builds a compact summary string by checking each of
the 13 documented action keys (`read`, `edit`, `glob`, `grep`, `bash`,
`task`, `skill`, `lsp`, `question`, `webfetch`, `websearch`,
`external_directory`, `doom_loop`) via `get_nested(ctx.data, "permission",
<action>, default=<the action's own documented default: "ask" for
doom_loop/external_directory, "allow" for every other action>)`, joining
only the entries whose value differs from "the rest default to allow" into a
short comma-joined string (e.g. `"bash=ask, doom_loop=ask"`, or the literal
string `"all defaults (see Why for the full action set)"` when nothing
differs); `recommended` a string stating this is a personal risk-tolerance
choice, not one fixed target, per action; `confidence="HIGH"`; `why` lists
the full 13-action documented set and its defaults verbatim, and states
plainly that a non-`dict` `"permission"` value degrades to `unknown` rather
than a guessed per-action default; `source="https://opencode.ai/docs/
permissions/"`.
`opencode-share-mode` (row 7): `read` returns `get_nested(ctx.data, "share",
default="manual")`; `recommended='"disabled" for privacy-conscious users'`;
`confidence="MEDIUM"` (carried forward from the source PRD, not re-fetched
this research session, per RESEARCH's own note); `source="https://
opencode.ai/docs/share/"`. `opencode-model-options` (row 7b, `scope="model"`):
`read` walks `get_nested(ctx.data, "provider")`; when it is a dict, iterate
its `<provider_id>` keys, and within each, `models`'s `<model_id>` keys,
collecting every entry that has an `options` key into a list of `f"{provider_id}/
{model_id}: {options!r}"` strings (best-effort `repr`, since `options`' shape
varies by provider family); join with `"; "`; return the literal string `"no
per-model options configured"` when nothing is found. `recommended` a string
naming the documented example shape (`reasoningEffort`/`textVerbosity`/
`reasoningSummary` for `"openai"`; `thinking.budgetTokens` for `"anthropic"`)
and stating plainly that the documented shape is confirmed only for the
literal provider ids `"openai"`/`"anthropic"` — a custom router provider id
(this machine's own `opencode.jsonc` uses `"router-env"`) is UNVERIFIED to
honor these options at all, per Assumption A2; `confidence="HIGH"` on the
documented shape, `"LOW"` on this machine's own custom-router applicability
— use `"LOW"` as the row's overall `confidence` value, since that is the
honest ceiling for what a reader running against THIS machine's real
`opencode.jsonc` can actually claim; `source="https://opencode.ai/docs/
models/"`.

Extend `tests/test_config_doctor.py` with a `TestClaudeRowsExpanded` class
and a `TestOpencodeRows` class. Build one realistic scratch `settings.json`
fixture carrying values for every new Claude Code key (including an env dict
for the telemetry row's `CLAUDE_CODE_ENABLE_TELEMETRY` case, passed via
`build_catalog`'s own `env` parameter — not `os.environ` directly, so the
test never depends on the real process environment) and one realistic
scratch `opencode.jsonc` fixture (including a `"provider"` block shaped like
this machine's own live file, with a custom `"router-env"` provider id, to
exercise row 7b's low-confidence path for real) and assert every new row's
`id`, `current_display`, `confidence`, and that `source` is a non-empty
string starting with `"https://"`. Add a negative test: an `opencode.jsonc`
whose `"permission"` value is a JSON string instead of an object degrades
row 6's `current_display` to `"unknown"` rather than raising. Add one
further `TestClaudeRowsExpanded` case proving `claude-telemetry`'s
`reads_section_file=False` distinction is real, not just declared: build a
scratch `settings.json` containing deliberately truncated/invalid JSON
(unreadable) alongside an `env` dict carrying
`CLAUDE_CODE_ENABLE_TELEMETRY=1`; call `build_catalog`; assert
`claude-telemetry`'s `current_display == "1"` even though the section's own
`settings.json` is unreadable — proving this row's value survives Wave 1's
degrade short-circuit — while every OTHER Claude Code row in that same
section's `current_display` is still `"unknown"` (proving the short-circuit
still correctly applies to every `reads_section_file=True` row in the very
same unreadable section).
  </action>

  <acceptance_criteria>
    - `CONFIG_DOCTOR_ROWS` contains exactly 10 rows after this task
      (`claude-retention` plus the 5 new Claude rows plus the 4 new opencode
      rows).
    - `ReadContext` has fields `data`, `env`, `runner`; `evaluate_row` and
      every `CheckRow.read` function in the file accept `ctx`, not `data`.
    - `claude-telemetry`'s `read` function is the only one in this task that
      reads from `ctx.env` rather than `ctx.data`.
    - `claude-telemetry` has `reads_section_file=False`; a malformed
      `settings.json` combined with `CLAUDE_CODE_ENABLE_TELEMETRY` set in
      `env` still yields `current_display == "1"` for this row while every
      other Claude Code row in the same section degrades to `"unknown"`.
    - `claude-telemetry`'s `why` text explicitly scopes its claim to the
      process-environment value only, distinct from a `settings.json`-
      configured `"env"` block entry it does not read.
    - `opencode-retention`'s `why` text states explicitly that its `apply`
      will remain `None` permanently (not merely "not yet wired").
    - `opencode-permissions`'s `read` function returns `UNKNOWN` when
      `"permission"` is present but not a `dict`, distinct from the
      absent-key case (which legitimately reports each action's own
      documented default) — verified by a dedicated negative test (cross-AI
      review finding, cycle 2, MEDIUM severity).
    - `opencode-model-options`'s `confidence` is `"LOW"`, and its `why` names
      `"router-env"` as this machine's own unverified custom provider id.
    - `python3 -m unittest tests.test_config_doctor -v` reports at least 20
      tests and 0 failures, including every test that already existed from
      Wave 1 (regression proof for the `ReadContext` signature change).
  </acceptance_criteria>

  <verify>
    <automated>python3 -m unittest tests.test_config_doctor -v</automated>
    <fails_when>non-zero exit, or the summary line reads "Ran 0 tests", or fewer than 20 tests ran, or any test whose name matches Wave 1's own suite (TestPresence, TestUnknownDegrade, TestGateRegistration) fails</fails_when>
  </verify>

  <done>
    All 5 Claude Code rows and all 4 opencode rows evaluate correctly against
    realistic fixtures, `claude-telemetry` proves the `ReadContext.env` path
    works, `opencode-model-options` honestly reports LOW confidence for this
    machine's own custom-router setup rather than a false HIGH, and every
    Wave 1 test still passes after the `read(ctx)` signature revision.
  </done>
</task>

<task type="auto">
  <name>Task 2: Codex's full catalog and Cursor's full catalog, including the two genuinely-unresolved rows shipped as honest "unknown"</name>
  <files>tools/config_doctor_checks.py, tools/config_doctor_readers.py, tests/test_config_doctor.py</files>

  <read_first>
    - `tools/config_doctor_checks.py`, `tools/config_doctor_readers.py` as Task 1 left them — read the landed `ReadContext` and the ten already-added rows before adding more to the same registry.
    - `04-RESEARCH.md` Checks Catalog, Codex table rows 8, 9, 10, 10b, 10c, 11 and Cursor table rows 13, 14, 15a, 15b, 16, 17, 18, 19 — the authoritative text for every row this task adds.
    - `04-RESEARCH.md` Common Pitfall 5 (Cursor's two DISTINCT sandbox mechanisms — `cli-config.json`'s own `sandbox.mode`/`networkAccess` keys, row 15a, versus the separate `sandbox.json` FILE, row 15b — must stay two rows, never merged into one reader) and Common Pitfall 6 (Codex's `[memories]` settings, row 10c, are NOT session-history retention despite the duration-adjacent naming).
    - `04-RESEARCH.md` Open Question 1 — the `~/.cursor/sandbox-policies/` directory discrepancy; row 15b's `why` must state that `sandbox.json` is the only citable apply target and that no official doc names a `sandbox-policies/` directory.
    - `04-RESEARCH.md` Open Questions 2-3 and Common Pitfall 7 — rows 17, 18, and 16's genuinely-unresolved/undocumented status; each must be shipped rather than silently skipped, per PROJECT.md's confidence-labeling constraint.
    - `tools/config_doctor_readers.py::read_toml_checked` (Wave 1) — Codex's `config.toml` reader; every new Codex row's `read` function receives `ctx.data` as the TOML-parsed dict from this same reader.
  </read_first>

  <action>
Add six Codex rows. `codex-sandbox-mode` (row 8): `read` returns
`get_nested(ctx.data, "sandbox_mode", default=UNKNOWN)` — UNKNOWN, not a
guessed value, because research found no documented default and this
machine's own `config.toml` has no `sandbox_mode` key at all (absence is a
live-verified fact, not evidence of a specific fallback behavior);
`recommended='"workspace-write" + approval_policy = "on-request" (or the
newer granular object form)'`; `confidence="MEDIUM"` (enum confirmed, default
unconfirmed); `why` states the enum is `"read-only" | "workspace-write" |
"danger-full-access"` and that `approval_policy` also supports a newer
granular object form (`{granular = {sandbox_approval, rules,
mcp_elicitations, request_permissions, skill_approval}}`), distinct from the
PRD's simple string enum; `source="https://learn.chatgpt.com/docs/
config-file/config-reference"`. `codex-hooks` (row 9): `read` checks BOTH
`get_nested(ctx.data, "features", "hooks")` and the documented deprecated
alias `get_nested(ctx.data, "features", "codex_hooks")`, returning whichever
resolves (or `False` if neither key is present — that IS the documented
default per research); `recommended="N/A — informational only"`;
`confidence="HIGH"`; `why` states, per D-04, this row is informational only,
matching row 5's treatment, and that `features.codex_hooks` is a documented
deprecated alias the reader must check; `apply` stays `None` PERMANENTLY for
this row (state that explicitly, matching `opencode-retention`'s precedent —
D-04 locks this, not merely "not yet"). `codex-model-reasoning-effort` (row
10, `scope="model"`): `read` returns a formatted string combining
`get_nested(ctx.data, "model", default="unknown")` and
`get_nested(ctx.data, "model_reasoning_effort", default=UNKNOWN)` as
`f"model={model}, effort={effort_display}"` (UNKNOWN's own string form is
`"unknown"`, matching every other row's convention) — UNKNOWN for the effort
value itself, because research found no documented default (distinct from
row 3's `False` default, which IS documented); `recommended='"high"/"xhigh"
for complex/multi-file work — research found no per-model mapping backed by
an official source, so this recommendation does not vary by which model is
active despite the scope="model" label; the schema supports a real per-model
mapping once one is sourced, without an architecture change'` (state this
honestly — do not invent a per-model lookup table with no citation);
`confidence="LOW"` (community-sourced default claim only; the enum values
themselves are HIGH, but the row's overall confidence is the honest ceiling);
`why` states the enum (`"minimal" | "low" | "medium" | "high" | "xhigh"`,
`xhigh` model-dependent, Responses API only); same source URL.
`codex-history-max-bytes` (row 10b): `read` returns `get_nested(ctx.data,
"history", "max_bytes", default="unset (no cap)")`; `recommended='set a cap
if disk usage is a concern'`; `confidence="HIGH"`; `why` states this caps
`history.jsonl` by byte size (drops oldest entries), NOT a duration control;
same source URL. `codex-memories-durations` (row 10c): `read` builds a
formatted string from `get_nested(ctx.data, "memories", "max_unused_days",
default=30)`, `get_nested(ctx.data, "memories", "max_rollout_age_days",
default=30)`, and `get_nested(ctx.data, "memories", "min_rollout_idle_hours",
default=6)`; `recommended="N/A — informational only unless [memories] is
confirmed active"`; `confidence="HIGH"` on the values, `why` states — per
Common Pitfall 6, verbatim — that these govern a SEPARATE memory-
consolidation feature, NOT session-history retention, and cites GitHub issue
#6015 ("Codex retains every conversation indefinitely") as confirming Codex
has no actual history-cleanup window; `apply` stays `None`; same source URL
plus `"https://github.com/openai/codex/issues/6015"`. `codex-history-
persistence` (row 11): `read` returns `get_nested(ctx.data, "history",
"persistence", default="save-all")`; `recommended='"none" only if the user
wants it (privacy trade-off) — never auto-recommended'`; `confidence="HIGH"`;
`why` states this is opt-in only, never defaulted to "recommended: apply";
same source URL.

Add eight Cursor rows, reading from `ctx.data` (the `cli-config.json`-parsed
dict via `read_json_checked`, reused from Wave 1 — Cursor's `cli-config.json`
is plain JSON, same reader as Claude Code's `settings.json`). `cursor-
permissions` (row 13): `read` builds `f"allow={get_nested(ctx.data,
'permissions', 'allow', default=[])}, deny={get_nested(ctx.data,
'permissions', 'deny', default=[])}"`; `recommended='lock down per risk
tolerance, same framing as opencode row 6'`; `confidence="HIGH"`; `why`
states entries are typed exact-match strings (`Shell(cmd)`, `Read(glob)`,
`Write(glob)`, `WebFetch(domain)`, `Mcp(server:tool)`) and deny takes
precedence over allow; `source="https://cursor.com/docs/cli/reference/
permissions"`. `cursor-approval-mode` (row 14): `read` returns
`get_nested(ctx.data, "approvalMode", default=UNKNOWN)` (no documented
default found; UNKNOWN is honest here); `recommended='"allowlist" (the
tightest documented mode) if not already set'`; `confidence="HIGH"`;
`source="https://cursor.com/docs/cli/reference/configuration"`. `cursor-
sandbox-cli-config` (row 15a): `read` builds `f"mode={get_nested(ctx.data,
'sandbox', 'mode', default=UNKNOWN)}, networkAccess={get_nested(ctx.data,
'sandbox', 'networkAccess', default=UNKNOWN)}"`; `recommended="N/A —
informational only pending clearer official docs on exact values"`;
`confidence="MEDIUM"` (keys confirmed to exist; exact semantics
unconfirmed); `why` states explicitly this is NOT the same mechanism as row
15b (`sandbox.json`) — Common Pitfall 5 — and must never be read from the
same function; `apply` stays `None`; same source URL as row 14. `cursor-
sandbox-json` (row 15b): `read` cannot read `cli-config.json` at all — this
row reads a SEPARATE file, `<cursor_dir>/sandbox.json`, which
`resolve_runtime_config_paths` does not currently resolve; add a
`cursor_sandbox_json` entry to `resolve_runtime_config_paths`'s return dict
(same `cursor_dir`, filename `sandbox.json`) and thread it through
`build_catalog` as an extra field on `ReadContext` for THIS row only, or
(simpler and preferred) have this row's own `read(ctx)` function accept
`ctx.env` and resolve+read the file itself via `read_json_checked` — set
`reads_section_file=False` on this row for the identical structural reason
as `claude-telemetry` (Task 1): its value is fully knowable independent of
whether `cli-config.json` itself parses, so it must NOT be forced to
`"unknown"` by Wave 1's unreadable-file short-circuit whenever
`cli-config.json` happens to be malformed — document
in the row's own docstring/comment why it is the one row in this catalog
whose `read` function performs its own file I/O rather than relying on the
section-level `data` the engine already parsed, citing Common Pitfall 5 and
Open Question 1 (no `sandbox-policies/` directory doc exists; `sandbox.json`,
singular file, is the only citable target) as the reason a generic per-
runtime `data` blob cannot represent this row. `recommended='"workspace_
readonly" or a scoped networkPolicy, analogous to Claude Code row 3'`;
`confidence="HIGH"` (fully documented schema: `type` defaults
`"workspace_readwrite"` if absent, `networkPolicy.default` defaults
`"deny"`); `why` states the protected always-write-blocked paths
(`.cursor/*.json`, `.git/hooks/**`, `.vscode/**`) and the exact Open-
Question-1 discrepancy (this machine's `~/.cursor/sandbox-policies/`
directory is present but empty; no official doc names it; `sandbox.json` is
the correct target); `source="https://cursor.com/docs/reference/sandbox"`.
`cursor-model-parameters` (row 16, `scope="model"`): `read` first evaluates
`mp = get_nested(ctx.data, "modelParameters", default=None)`; when `mp` is
present but is NOT a `dict` (the shape observed by live inspection is
per-model-id keys mapping to a params object — a non-`dict` value is a
malformed config, not a documented alternate shape), `read` returns
`UNKNOWN` immediately rather than emitting a `repr()` of whatever type it
actually is; when `mp` is absent entirely, `read` returns the literal string
`"no modelParameters configured"`; otherwise (a genuine `dict`), `read`
returns `repr(mp)` (best-effort, since the schema is undocumented — never
assume a fixed per-key shape beyond "it's an object"); `recommended="N/A —
informational only, schema undocumented"`; `confidence="LOW"` (per Pitfall
7: `[VERIFIED: read from a real file]` and `[documented by Cursor]` are NOT
the same confidence tier — this key is entirely absent from the official
Configuration reference); `why` states plainly that a non-`dict`
`modelParameters` value degrades to `unknown` rather than a guessed
`repr()`; `apply` stays `None` PERMANENTLY (state this explicitly, matching
row 5's/row 9's precedent — never gate this behind a future apply path
without a documented schema to validate against); `source` same as row 14.
`cursor-local-retention` (row 17): `read` UNCONDITIONALLY
returns `UNKNOWN`, regardless of `ctx.data`'s content — there is no key to
check at all; `recommended="N/A"`; `confidence="LOW"`; `why` states this is
genuinely open, not confirmed absent (unlike opencode row 5, which has a
closed-not-planned GitHub issue as positive evidence) — CLI chats live under
`~/.cursor/chats`, and the only retention information found concerns cloud-
side storage, not local on-disk history; `apply=None` permanently; `source`
left as an empty string or a short prose note that no citable source exists
(state plainly in `why` rather than fabricating a URL). `cursor-cli-
telemetry` (row 18): same treatment as row 17 — `read` unconditionally
returns `UNKNOWN`; `why` states no CLI docs page documents a telemetry
flag/env var for `cursor-agent`, only the unrelated editor's VS-Code-style
`telemetry.*` settings were found (a different product surface); `apply=None`
permanently. `cursor-attribution` (row 19): `read` builds
`f"attributeCommitsToAgent={get_nested(ctx.data, 'attribution',
'attributeCommitsToAgent', default=True)}, attributePRsToAgent=
{get_nested(ctx.data, 'attribution', 'attributePRsToAgent', default=True)}"`;
`recommended='leave as-is or disable per privacy preference'`;
`confidence="HIGH"`; `why` states this adds a "Made with Cursor"
trailer/footer to commits/PRs; same source URL as row 14.

Extend `tests/test_config_doctor.py` with a `TestCodexRows` class and a
`TestCursorRows` class, covering every new row against a realistic scratch
`config.toml` and a realistic scratch `cli-config.json`/`sandbox.json` pair.
Assert rows 17 and 18 render `current_display == "unknown"` against BOTH an
empty `cli-config.json` (`{}`) AND a `cli-config.json` populated with every
OTHER row's real keys — proving the unconditional-unknown behavior does not
accidentally depend on the file being otherwise empty. Assert row 10c's
`why` text contains the substring `"6015"` (the GitHub issue number, pinning
the citation is actually present, not just described in the plan). Add a
negative test: a `cli-config.json` whose `"modelParameters"` value is a JSON
string instead of an object degrades row 16's `current_display` to
`"unknown"` rather than emitting a `repr()` of the string or raising — the
same class of fix applied to row 6 in Task 1 (cross-AI review finding, cycle
2, MEDIUM severity). Assert
row 15b's reader correctly resolves and reads `sandbox.json` as a file
independent of `cli-config.json`'s own content, including the case where
`cli-config.json` exists but `sandbox.json` does not (row 15b's `read` must
degrade gracefully — document and test what it returns in that case: the
documented `type` default, `"workspace_readwrite"`, since the schema itself
states an absent file means that default, which is a genuine documented
default and therefore not `UNKNOWN`). Also assert row 15b's
`reads_section_file=False` behavior is real: build a scratch
`cli-config.json` containing deliberately truncated/invalid JSON
(unreadable) alongside a valid `sandbox.json`; call `build_catalog`; assert
row 15b's `current_display` correctly reflects `sandbox.json`'s real content
(not `"unknown"`), while every OTHER Cursor row in that same section
degrades to `"unknown"` as expected.
  </action>

  <acceptance_criteria>
    - `CONFIG_DOCTOR_ROWS` contains exactly 24 rows after this task (10 from
      Task 1 plus 6 Codex plus 8 Cursor).
    - `codex-hooks` and `cursor-model-parameters` and `cursor-local-retention`
      and `cursor-cli-telemetry` each state explicitly, in their own `why`
      text, that `apply` stays `None` permanently, distinct from a row whose
      `apply=None` is merely "not yet wired."
    - `cursor-local-retention`'s and `cursor-cli-telemetry`'s `read`
      functions return `UNKNOWN` unconditionally, verified against both an
      empty and a fully-populated `cli-config.json` fixture.
    - `cursor-sandbox-json`'s `read` function performs its own file I/O
      against `sandbox.json`, independent of `cli-config.json`'s content, and
      its own docstring states why (Common Pitfall 5 / Open Question 1).
    - `cursor-sandbox-json` has `reads_section_file=False`; a malformed
      `cli-config.json` combined with a valid `sandbox.json` still yields
      row 15b's real `sandbox.json`-derived value while every other Cursor
      row in that section degrades to `"unknown"`.
    - `cursor-model-parameters`'s `read` function returns `UNKNOWN` when
      `"modelParameters"` is present but not a `dict`, rather than emitting a
      `repr()` of a non-dict value — verified by a dedicated negative test.
    - `python3 -m unittest tests.test_config_doctor -v` reports at least 39
      tests and 0 failures, including every test from Task 1 and Wave 1.
  </acceptance_criteria>

  <verify>
    <automated>python3 -m unittest tests.test_config_doctor -v</automated>
    <fails_when>non-zero exit, or the summary line reads "Ran 0 tests", or fewer than 39 tests ran</fails_when>
    <automated>python3 -c "import sys; sys.path.insert(0, 'tools'); import importlib.util as u; s=u.spec_from_file_location('cdc','tools/config_doctor_checks.py'); m=u.module_from_spec(s); s.loader.exec_module(m); print(len(m.CONFIG_DOCTOR_ROWS))"</automated>
    <fails_when>the printed count is not 24</fails_when>
  </verify>

  <done>
    All 6 Codex rows and all 8 Cursor rows evaluate correctly against
    realistic fixtures; rows 17 and 18 honestly report `unknown` in every
    scenario; rows 9, 16, 17, and 18 are marked as permanently informational
    in their own `why` text rather than "not yet wired"; row 15b correctly
    reads a second, separate file rather than being conflated with row 15a.
  </done>
</task>

<task type="auto">
  <name>Task 3: The cross-runtime rtk-Cursor-integration row, and a full-catalog regression across all four runtimes at once</name>
  <files>tools/config_doctor_checks.py, tools/config_doctor_readers.py, tests/test_config_doctor.py, tests/test_config_doctor_pty.py</files>

  <read_first>
    - `tools/hooks/detect.py` lines 1-97 — `_probe_rtk_hook`'s exact bounded-subprocess, bracket-prefix-classification pattern (`RTK_PROBE_TIMEOUT_SECONDS = 2.0`, `[rtk_path, "init", "--show"]`, `capture_output=True, text=True, timeout=..., check=False, stdin=subprocess.DEVNULL`, never raises). This task PORTS this pattern (does not import `tools/hooks/detect.py`) into `config_doctor_readers.py`, generalized to search for a line naming Cursor specifically.
    - `04-RESEARCH.md` Cross-runtime table, row 12 — the exact citation
      (`tools/hooks/README.md`'s own closing line folding this check in) and
      the "Point-in-time — re-probe... not fixed by this research session"
      caveat about the exact Cursor-specific stdout line text.
    - `.planning/phases/03-tool-substitution-awareness-hook/03-RESEARCH.md`
      lines 527-540 — the actual live-verified `rtk init --show` output
      block (`rtk v0.44.1`): the Cursor line reads `[ok] Cursor hook:
      registered in hooks.json` with **lowercase** `hook`, distinct from the
      separate Claude Code line `[ok] Hook: rtk hook claude (native binary
      command)` with capital `Hook`. This is the exact, verbatim text
      `_probe_rtk_cursor_hook`'s matcher and its test fixture must use — a
      case-sensitive `"Hook"` match can never match the real Cursor line.
    - `tools/hooks/detect.py` lines 73-96 (`_probe_rtk_hook`) — the existing
      matcher's own `"Hook:" in line and "rtk hook" in line` condition,
      which correctly targets the capital-`Hook` Claude Code line;
      `_probe_rtk_cursor_hook`'s matcher must target the Cursor line's
      distinct, lowercase `hook` wording instead, not reuse this condition
      verbatim.
    - `tools/hooks/README.md` — the closing line row 12's `why` text cites
      verbatim.
    - `tools/config_doctor_checks.py`'s `build_catalog` as Task 1/2 left it —
      the four-runtime loop this task extends with an independent
      cross-runtime pass.
    - `tools/config_doctor_app.py` (Wave 1) — the `DataTable` rendering path
      this task's PTY test exercises against a full, all-runtimes-present
      fixture for the first time.
  </read_first>

  <action>
Add `_probe_rtk_cursor_hook(rtk_path, runner)` to `config_doctor_readers.py`,
porting `tools/hooks/detect.py::_probe_rtk_hook`'s exact shape (same timeout
constant value, same subprocess argument list, same never-raises `try`/
`except Exception` wrapper) but scanning `rtk init --show`'s stdout for a
line containing BOTH `"Cursor"` and the LOWERCASE substring `"hook"` — the
live-verified line is `[ok] Cursor hook: registered in hooks.json`
(`03-RESEARCH.md:527-540`), lowercase `hook`, distinct from the separate
Claude Code line `[ok] Hook: rtk hook claude (native binary command)`,
whose capital-`Hook` form `_probe_rtk_hook` already matches on. A
case-sensitive `"Hook"` requirement (as an earlier draft of this plan
specified) would NEVER match the real Cursor line, always reporting
`unreadable` against genuine `rtk` output even when the integration is
registered. Classify a `[ok]`-prefixed matching line as registered, a
`[--]`-prefixed matching line as not-registered, and anything else (no
matching line, or a matching line with neither prefix) as unreadable —
mirroring the SAME three-way classification `_probe_rtk_hook` already uses,
never raising. State explicitly in this function's own docstring, citing
`03-RESEARCH.md:527-540`'s verbatim output block as the source for this
exact line text, AND `04-RESEARCH.md` row 12's own "point-in-time —
re-probe... not fixed by this research session" caveat — a future `rtk`
release could still reword this line, so classification must keep
degrading to unreadable on no match rather than raise or assume.

Change `build_catalog`'s signature to `build_catalog(env, runner=None)`;
default `runner` to `subprocess.run` internally when `None`, and thread it
into every `ReadContext` this function constructs (Task 1 already added the
`runner` field to `ReadContext`; this task is the first to populate it with
something other than the default). Add a cross-runtime pass AFTER the
existing four-runtime loop: if any row in `CONFIG_DOCTOR_ROWS` has
`runtime == "cross"`, evaluate each such row with a `ReadContext(data=None,
env=env, runner=runner)` and append `{"runtime": "cross", "config_path":
None, "rows": [...]}` to `sections` — this pass has NO `os.path.isfile` gate
of any kind; it is independent of whether any of the four per-runtime config
files exist.

Add `rtk-cursor-integration` (row 12, `runtime="cross"`, `scope="runtime"`
— it is not gated by which model is active) to `CONFIG_DOCTOR_ROWS`. `read`
resolves `rtk` via `shutil.which("rtk")`; when absent, returns the formatted
string `"rtk not installed on PATH"`; when present, calls
`_probe_rtk_cursor_hook(rtk_path, ctx.runner)` and formats its result as one
of `"registered"`, `"not registered"`, or `"unreadable (see Why)"`.
`recommended='rtk init -g --agent cursor if reported absent'`;
`confidence="HIGH"` on the mechanism (matching research's own framing —
"HIGH (mechanism), inherently time-varying (result)"); `why` states this
check is point-in-time and MUST be re-probed at build/verify time, never
cached or assumed stable across sessions, and cites this as the fold-in of
Phase 3's Non-Goals note per `tools/hooks/README.md`'s own closing line;
`source="tools/hooks/README.md"` (a repo-relative citation, since this is an
in-repo cross-reference, not an external URL — state this distinction in the
row's own comment). `apply` stays `None` in this wave.

Extend `tests/test_config_doctor.py` with a `TestCrossRuntimeRow` class:
assert `rtk-cursor-integration` appears in `build_catalog`'s output even
when ALL FOUR per-runtime config files are absent (a scratch tree with
nothing at all under any of the four env-var-resolved directories); assert
its `current_display` correctly reflects each of the three states
(`rtk` absent from a scratch PATH via an injected `runner`/`shutil.which`
override — mirror `tests/test_tool_substitution_hook.py`'s `fake_bin`/
`scoped_env` pattern for building a scratch PATH with a stub `rtk`
executable printing a controlled `rtk init --show` block for each of the
three cases — build the "registered" case's stub output from the VERBATIM
verified block in `03-RESEARCH.md:527-540` (including its own `[ok] Hook:
rtk hook claude (native binary command)` decoy line), not a paraphrase, so
the test proves the matcher finds the Cursor line specifically and does not
accidentally match the Claude Code line instead). Add a `TestFullCatalogRegression` class: build ONE scratch
tree with realistic, non-trivial fixture content for all four runtime config
files simultaneously (reusing the fixtures Task 1/Task 2 already built,
combined into one scratch tree) plus a stub `rtk` on a scratch PATH; call
`build_catalog(env, runner=...)`; assert the result has exactly 5 sections
(`claude`, `opencode`, `codex`, `cursor`, `cross`) and exactly 24+1=25 total
rows across them; assert every row's `current_display` is either a non-empty
string or the literal `"unknown"` — never `None`, never a raw Python `repr`
of an internal sentinel leaking through.

Extend `tests/test_config_doctor_pty.py` with
`test_full_catalog_renders_under_pty_without_crashing`: reuse
`TestFullCatalogRegression`'s combined fixture tree; spawn `setup.py
--config-doctor` under a real PTY with `PATH` set to the scratch bin dir
holding the `rtk` stub (prepended to a minimal safe PATH, mirroring
`tests/test_tool_substitution_hook.py`'s `scoped_env` discipline) so the
subprocess probe resolves the stub, not any real `rtk` that might be
installed on the dev machine; use `drive_until` to wait for a marker unique
to the LAST section painted (the cross-runtime row's `id` text or its
`current_display`, whichever the table literally renders); assert the
captured output contains recognizable text from at least one row per
section (five distinct substrings, one per runtime plus cross); send `"q"`;
drain; assert clean exit. This is the test that proves 25 rows across 5
sections render without the TUI crashing, truncating mid-paint, or hanging —
something no per-row unit test can prove on its own.
  </action>

  <acceptance_criteria>
    - `CONFIG_DOCTOR_ROWS` contains exactly 25 rows.
    - `build_catalog(env, runner=...)`'s cross-runtime pass has no
      `os.path.isfile` call anywhere in its code path.
    - `_probe_rtk_cursor_hook`'s matcher requires `"Cursor"` and lowercase
      `"hook"` (never a case-sensitive `"Hook"`), matching the live-verified
      `[ok] Cursor hook: registered in hooks.json` line
      (`03-RESEARCH.md:527-540`) and NOT the separate `[ok] Hook: rtk hook
      claude` line.
    - `_probe_rtk_cursor_hook`'s docstring cites both `03-RESEARCH.md:527-540`'s
      verbatim output block (as the source for the exact Cursor line text)
      and `04-RESEARCH.md` row 12's "not fixed by this research session"
      caveat.
    - `TestFullCatalogRegression` asserts exactly 5 sections and 25 rows
      from one combined fixture tree.
    - `python3 -m unittest tests.test_config_doctor -v` reports at least 47
      tests and 0 failures.
    - `python3 -m unittest tests.test_config_doctor_pty -v` reports at least
      2 tests and 0 failures.
  </acceptance_criteria>

  <verify>
    <automated>python3 -m unittest tests.test_config_doctor -v</automated>
    <fails_when>non-zero exit, or the summary line reads "Ran 0 tests", or fewer than 47 tests ran</fails_when>
    <automated>python3 -m unittest tests.test_config_doctor_pty -v</automated>
    <fails_when>non-zero exit, or fewer than 2 tests ran</fails_when>
    <automated>make lint</automated>
    <fails_when>non-zero exit</fails_when>
  </verify>

  <done>
    The rtk-Cursor-integration row appears in the catalog on a machine with
    zero AI-CLI config files present, reflects all three probe states
    correctly against a stubbed `rtk`, and the full 25-row, 5-section catalog
    renders under a real PTY without crashing. `CONFIG_DOCTOR_ROWS` now holds
    every row `04-RESEARCH.md` documents except the explicitly-excluded row
    20.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `rtk` third-party binary -> `_probe_rtk_cursor_hook` | The probe captures a third-party CLI's human-readable stdout and parses it as a detection signal; untrusted, reverse-engineered text, mirroring Phase 3's already-reviewed `_probe_rtk_hook` |
| Four runtime config files (now fully read) -> the review screen | All 24 new rows read user-controlled file content; every reader must degrade, never raise, on a malformed value |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-05 | Tampering | `_probe_rtk_cursor_hook`'s subprocess call | medium | mitigate | Fixed argument list via `shutil.which`-resolved absolute path; no shell; `stdin=subprocess.DEVNULL`; bounded `RTK_PROBE_TIMEOUT_SECONDS`; never raises (Task 3, mirrors Phase 3's already-reviewed T-03-01) |
| T-04-06 | Denial of Service | A malformed `opencode.jsonc` `"permission"`/`"provider"` value (e.g. a string where an object is expected) reaching row 6's or row 7b's reader | medium | mitigate | Every new reader checks `isinstance(..., dict)`/`isinstance(..., list)` before iterating and degrades to `"unknown"`/a safe default rather than raising a `TypeError`; pinned by Task 1's negative test |
| T-04-07 | Information Disclosure | Rendering Cursor's `permissions.allow`/`deny` lists, which can include filesystem globs and MCP server names | low | accept | This is local, non-secret configuration the user already set; no credential or key material is read by any row in this catalog |
| T-04-SC | Tampering | Supply chain — package-manager installs | n/a | accept | This wave installs zero new packages |
</threat_model>

<notes>
## Planner decisions recorded here (not silent)

1. **Every new row's `apply` stays `None` in this wave, including rows
   `04-RESEARCH.md` labels "Apply: yes."** Wave 2's job is a complete,
   accurate, confidence-labeled READ-ONLY catalog; Wave 3 is where the actual
   writer mechanics (atomic JSON write, JSONC surgical edit, TOML region
   replace) get built and then wired onto the subset of rows that earn a
   real applier. This is not a scope reduction — every row's `why` text
   already states RESEARCH's own apply disposition, so nothing is hidden
   from the reviewer; only the WRITE MECHANISM is deferred to the plan that
   actually builds write mechanisms.

2. **Rows 5, 9, 16, 17, and 18 are marked PERMANENTLY informational**, not
   merely "not yet wired" — distinct from every other row, which Wave 3 may
   or may not choose to make apply-eligible. This distinction is recorded in
   each such row's own `why` text specifically because `04-RESEARCH.md`
   itself gives a structural reason none of these five can ever have a real
   apply target (no setting exists upstream, informational-only per D-04,
   undocumented schema, or genuinely unresolved absence-of-evidence).

3. **Row 10's model-scoped recommendation does not actually vary by which
   Codex model is active**, despite `scope="model"`. `04-RESEARCH.md`
   explicitly reconfirms (not upgrades) the PRD's own low-confidence
   "community-documented as medium" claim and states the exact per-model
   mapping mechanism is "left to the planner" with no sourced table to build
   one from. Inventing a mapping here would fabricate certainty this
   project's own core value forbids. The row still READS the active model
   name (satisfying D-02's structural intent — the schema and the reader
   both support a future per-model table without an architecture change),
   it just does not yet vary its recommendation by it.

4. **`claude-telemetry` and `cursor-sandbox-json` are the first two rows to
   set `reads_section_file=False`** (the `CheckRow` field Wave 1 defined but
   never exercised). Both rows' values are fully knowable independent of
   whether their runtime's OWN section file parses — `claude-telemetry` from
   `ctx.env`, `cursor-sandbox-json` from a wholly separate file — so forcing
   them to `"unknown"` whenever `settings.json`/`cli-config.json` happens to
   be malformed would discard real, available information. Each row's own
   test proves the distinction against a deliberately malformed section
   file. (Addresses cross-AI review finding, MEDIUM severity — the root
   `reads_section_file` mechanism lives in `04-01-PLAN.md`.)

5. **`claude-telemetry`'s `why` text is scoped to the process-environment
   value only**, rather than also reading a `settings.json`-configured
   `"env"` block entry for the same variable. Checking both would require
   resolving an undocumented precedence between the two sources; scoping the
   claim honestly is the simpler, equally-compliant fix per this row's own
   confidence-labeling discipline. (Addresses cross-AI review finding, LOW
   severity.)
</notes>

<verification>
Run from the repository root, in this order:

1. `python3 -m unittest tests.test_config_doctor -v` — the full pure-Python
   suite, now covering all 25 rows.
2. `python3 -m unittest tests.test_config_doctor_pty -v` — both PTY tests,
   including the new full-catalog render.
3. `make lint` — `py_compile` over the (unchanged file list from Wave 1,
   already-registered) modules.
4. `make test` — the full suite, confirming no regression elsewhere in the
   repo.

ONESHOT-RULES Rule 1 and Rule 5 apply exactly as in 04-01-PLAN.md: real
output/exit codes only, and every exercise of every reader runs against a
scratch tree, never uz's real config. The clean-room `make e2e-docker` gate
(Rule 6) is carried by plan 04-03, which closes the phase.
</verification>

<success_criteria>
- `CONFIG_DOCTOR_ROWS` holds exactly 25 `CheckRow` records: the one from
  Wave 1 plus 24 new ones, covering every row `04-RESEARCH.md` documents
  except the explicitly-excluded row 20.
- Every row evaluates without raising against a realistic fixture, and every
  reader degrades to `"unknown"` rather than crashing on malformed input.
- Rows 17 and 18 report `"unknown"` unconditionally; rows 5, 9, 16, 17, and
  18 state in their own `why` text that `apply` will remain `None`
  permanently.
- The cross-runtime rtk-Cursor-integration row appears regardless of any
  per-runtime config file's presence, and correctly reflects all three probe
  states against a stubbed `rtk`.
- A single combined fixture exercising all four runtimes plus the
  cross-runtime row renders correctly both in a pure-Python `build_catalog`
  test and under a real PTY-driven `setup.py --config-doctor` run.
- `make test` and `make lint` are green.
</success_criteria>

<output>
Create `.planning/phases/04-config-doctor/04-02-SUMMARY.md` when done. Record
the final row count (25), the list of row ids whose `apply` is marked
permanently `None`, and the real output and exit codes of every command in
`<verification>`.

Also record, prominently and as an explicit open question for uz — NOT
resolved by this plan and not merely closed inside `<source_audit>`'s prose:
whether to add `04-RESEARCH.md`'s candidate row 20 (`rtk`'s own
`[tee].max_files`, currently defaulting to `20` in `~/.config/rtk/
config.toml`'s `[tee]` block) as a future 26th Config Doctor row. This
exclusion reflects `<source_audit>`'s own scope reasoning (a third TOML
target outside REQUIREMENTS.md's literal four-runtime wording, with no
upstream-documented recommended value), but the candidate value itself
traces directly back to `04-CONTEXT.md`'s Open Research Question 7, where uz
already proposed raising it to roughly `100` — that is uz's own stated
interest, not an invented one, and it deserves to be surfaced back to uz
rather than silently settled in a planning artifact. State plainly in the
SUMMARY, verbatim or close to it: "Open question for uz: include rtk's
`[tee].max_files` (row 20 candidate, uz's own proposed value ~100) as a
future Config Doctor row in a later phase? Currently excluded from Phase 4's
scope — see `<source_audit>` for the full reasoning." (Cross-AI review
finding, cycle 2, LOW severity — carried from cycle 1, where it was raised
but never ledgered or acted on; corrects Round 1's ledger, which incorrectly
claimed no feedback was deferred on this plan.)
</output>

## Review Dispositions Ledger

### Round 1 — 8982557

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| Row-12 rtk-hook probe's line matcher requires case-sensitive `"Cursor"` + `"Hook"`, but live-verified rtk output uses lowercase `"hook"` (`[ok] Cursor hook: registered in hooks.json`, per `03-RESEARCH.md:539`) — the check can never match real output | HIGH | Task 3: matcher changed to `"Cursor"` + lowercase `"hook"`; docstring and test fixture now cite `03-RESEARCH.md:527-540`'s verbatim output block (including the Claude `[ok] Hook: rtk hook claude` decoy line) instead of claiming the text was never captured |
| Wave 1's "unreadable file → every row unknown" short-circuit conflicts with new rows that don't need the section file (`claude-telemetry` reads `ctx.env`; `cursor-sandbox-json` reads a separate file) | MEDIUM | Task 1 sets `claude-telemetry.reads_section_file=False` (04-01's field/mechanism) plus a regression test proving it survives a malformed `settings.json`; Task 2 does the same for `cursor-sandbox-json` against a malformed `cli-config.json` |
| `claude-telemetry` reads only process env, missing a `settings.json`-configured `"env"` block value for `CLAUDE_CODE_ENABLE_TELEMETRY` | LOW | Task 1: row's `why` text now explicitly scopes the claim to the process-environment value only, stating this row does not read a `settings.json`-configured `env` block entry |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| None | Every current actionable finding attributed to this plan was addressed above. |

### Round 2 — d5f98ec

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| Row 6's negative test asserts `current_display` degrades to `"unknown"` when `"permission"` is a JSON string, but the specified `get_nested`-based read instead returns each action's own documented default (never `"unknown"`) — test and implementation contradict each other | MEDIUM | Task 1: `opencode-permissions`'s `read` now checks `isinstance(permission, dict)` first and returns `UNKNOWN` immediately when `"permission"` is present but not a `dict`, distinct from the absent-key case where each action's own default legitimately applies — reconciles the already-planned negative test with the implementation. The same explicit type-guard is applied to row 16's `modelParameters` (Task 2), with a new negative test added |
| Cycle-1's row-20 (`rtk [tee].max_files`) checkpoint finding was dropped rather than resolved — CONTEXT.md's own Open Question 7 proposed a value, but the exclusion was only ever recorded in `<source_audit>`'s prose, never surfaced as a question to uz; Round 1's ledger incorrectly claimed "Deferred: None" | LOW | **Corrects Round 1's ledger inaccuracy** — that finding was raised in cycle 1 but never actually addressed, despite Round 1's "Deferred: None" claim. This round: `<output>` now explicitly instructs the executor to record an open scope question for uz in the SUMMARY — whether to add row 20 as a future 26th row, citing CONTEXT.md Open Question 7's proposed `~100` value — rather than leaving the exclusion closed only inside the audit table |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| None | Every current actionable finding attributed to this plan (cycle 2) was addressed above; the row-20 EXCLUSION itself (as opposed to the finding that it wasn't surfaced as a question) remains out of this milestone's scope per `<source_audit>`'s existing reasoning — the fix here is surfacing the question to uz, not reversing the exclusion. |

### Round 3 (polish) — 6ae3f3d

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|----------------|
| Task 2's second `<verify>` one-liner (`python3 -c "...s.loader.exec_module(m)..."`) execs `config_doctor_checks.py`, whose module-level `import config_doctor_readers` raises `ModuleNotFoundError` from the repo root since `tools/` is never placed on `sys.path` by a bare `python3 -c` invocation — fails spuriously even against a correct implementation | MEDIUM | Task 2's `<verify>` one-liner now prepends `import sys; sys.path.insert(0, 'tools')` before the `importlib.util` load, mirroring 04-01's own `load_checks()` `sys.path` guard |
| must_haves truth #4 said the cross-runtime section "is gated on nothing but `rtk`'s own presence on PATH," contradicting the action/key_links, which append the section unconditionally whenever any `runtime == "cross"` row exists — rtk-absence is a row VALUE, not a section gate | LOW | Truth #4 reworded: the section is appended unconditionally whenever `CONFIG_DOCTOR_ROWS` has any `runtime == "cross"` row; `rtk`'s absence from PATH now explicitly stated as a row value (`"rtk not installed on PATH"`), never a section gate |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| None | Both cycle-3 findings attributed to this plan were addressed above; cycle 3 found 0 HIGH findings across the phase and this is the max-cycles (3) polish pass. |
