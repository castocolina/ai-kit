---
phase: 05-usage-metrics-dashboard
plan: 05
type: execute
wave: 6
depends_on: ["05-04", "05-06"]
cross_ai: true
autonomous: true
requirements: [REQ-usage-metrics-dashboard-ui]
files_modified:
  - skills/ai-kit-usage-metrics/ai_kit_usage_metrics/dashboard.py
  - skills/ai-kit-usage-metrics/SKILL.md
  - tests/test_ai_kit_usage_metrics.py
  - .planning/PROJECT.md
  - .planning/intel/requirements.md
  # [Round 1] Wave moved 5 -> 6, depends_on gained "05-06": this plan is
  # now the sole, final SKILL.md editor and the final skill-judge/
  # whole-repo-gate runner for Phase 5, running strictly AFTER 05-06 so no
  # same-wave file race exists. See must_haves/key_links and the Review
  # Dispositions Ledger for the full rationale.
  # [Round 3] PROJECT.md and .planning/intel/requirements.md added: Task 2
  # also corrects two stale pre-D-10 "dashboard Artifact"/"local export"
  # references left un-amended when D-10 corrected REQUIREMENTS.md/
  # ROADMAP.md.

estimate:
  tokens: 145000
  raw_tokens: 130000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The generated `dashboard.html`, when its embedded JSON is loaded by the SAME JS the file itself ships, can answer the PRD's own canonical example query — 'how many grep-family invocations this month, grouped by session' — entirely from already-embedded data: filtering `family == \"grep\"` + the current-month date preset, then grouping the filtered rows by `session_id`, with no network call and no re-query of any server (REQ-usage-metrics-dashboard-ui, ROADMAP SC-3)."
    - "**[Round 1]** All 5 MVP axes are BOTH filterable AND sortable, not just present in some partial combination: date (filterable via single-day/week/month/custom-range/current-month presets AND sortable via a clickable column header), model (filterable via `<select>` AND sortable), family (filterable via `<select>` AND sortable, PLUS a literal command-text substring filter covering the PRD's own 'how much rg specifically' framing), tokens (a numeric min/max range filter on `tokens_input`+`tokens_output` AND sortable, null-last), price (a numeric min/max range filter AND sortable, null-last) — verified by an executed test asserting each axis has BOTH a filter control AND a sortable header present in the rendered HTML (D-08/D-09, PRD MVP Axes, PRD Functional Acceptance)."
    - "**[Round 1]** The shipped JS's own pure filter/sort/group functions (not a Python re-implementation of their logic) are executed for real via `node`, against fixture data, and produce the exact PRD canonical-query result — conditional on `shutil.which(\"node\")` being present; if `node` is unavailable the test is skipped with an explicit, visible skip reason, never silently treated as passing (PROJECT.md 'never claim more certainty than verified')."
    - "**[Round 1]** `cli.py`'s `dashboard` subcommand already opens the refined DB via `refined_store.open_refined_db_readonly` (added by 05-01 specifically so this wave never needs a read-write connection) — this plan's `dashboard.py::generate(conn, output_path)` never itself opens a connection, so it works correctly against whatever connection (read-only or read-write, e.g. from a test) it is passed, and this plan adds a test asserting a `dashboard.generate` call against a connection opened via `open_refined_db_readonly` succeeds without attempting any write."
    - "**[Round 1]** `inferred_family`/`inferred_confidence` (05-06) and Cursor's `source_confidence` (05-03) are rendered VISIBLY DISTINCT from a mechanically-determined `family` — a row whose `family` is empty but `inferred_family` is set displays the inferred value with an explicit 'inferred (LOW confidence)'-style visual/textual marker, never presented with the same certainty as a decomposer-determined family (PROJECT.md's confidence-visibility rule, `.planning/PROJECT.md:121-125`)."
    - "**[Round 1]** Every data-driven value the inline JS writes into the DOM is constructed via `document.createElement`/`.textContent` assignment ONLY — never via template-string interpolation into `innerHTML` or any other HTML-string-building path — eliminating the prior self-contradiction between the action's own rendering instructions and the threat model's own mitigation claim."
    - "Regenerating the dashboard (`cli.main([\"dashboard\"])` or `[\"run\"]`) NEVER reads from any file other than the refined SQLite database — no direct read of `raw/*.jsonl`, no network call, no filesystem write outside `dashboard.html` itself — confirmed by an executed test that deletes every `raw/*.jsonl` file before calling `dashboard` and asserts the dashboard still regenerates correctly from the refined DB alone (REQ-usage-metrics-dashboard-ui: 'directly from refined data with no live re-parse')."
    - "**[Round 1]** The dashboard's published capability declaration (there is none — this is a locally-regenerated static HTML file with a `file://` open, not a Claude Code Artifact) never wires a hosted-database capability to session content, confirmed by an AST-based scan of `dashboard.py`'s own `import`/`from...import` statements (not a literal-string grep, which can both miss an aliased/indirect import and false-positive on harmless prose) proving zero network-capable modules (`urllib`, `http`, `requests`, `socket`) are ever imported (REQUIREMENTS.md: 'never has a hosted-database capability pointed at session content')."
    - "`SKILL.md` documents the refined schema's exact field list (the same columns Wave 1's `refined_store.py` defined) and the `command_shape` / `control_flow_script`-vs-`unclassified` limitation in plain prose, and passes the `skill-judge` review loop (0 remaining Critical/Important finding, or score >= 96/120, no regression vs. baseline) before this plan is considered complete (PROJECT.md Skill-quality gate, PRD Documentation acceptance criterion)."
    - "**[Round 1]** `SKILL.md` is finalized by THIS plan alone (running last, Wave 6, depending on 05-06) — it folds in 05-06's own classification-loop documentation content (sourced from `05-06-SUMMARY.md`) BEFORE running `skill-judge`, so the reviewed, final file genuinely reflects the WHOLE phase's feature set, never bypassed by a later same-wave or later-plan edit."
    - "**[Round 1]** This plan runs the phase's authoritative final close-out gate — `make test`, `make lint`, `make validate` all exit 0 for the whole repository, plus the real-data `<human-check>` invitation — since it is the last plan to execute in Phase 5 (moved here from 05-06 per the wave reorder)."
  artifacts:
    - skills/ai-kit-usage-metrics/ai_kit_usage_metrics/dashboard.py
    - skills/ai-kit-usage-metrics/SKILL.md
  key_links:
    - "`dashboard.py::generate` queries `refined_commands` directly via its OWN `sqlite3` SELECT statements — it does NOT import or call anything from `raw_store.py`, and it does NOT add any new read/write helper to `refined_store.py` (which stays focused on the WRITE path shared by `refiner.py` and, in 05-06, `classify_loop.py`) — this keeps this plan's own `dashboard.py` edits free of any file-overlap conflict with 05-06's own `refined_store.py`/`classify_loop.py` edits, which have already landed by the time this plan (Wave 6) runs."
    - "The rendered HTML's session-grouping view and the flat per-row table Wave 1 already ships are BOTH present in the output — grouping is an additional rendering mode layered on the SAME embedded JSON array, not a schema or query change, so the canonical example query works whether the user is looking at the grouped or flat view."
    - "**[Round 1, wave reorder]** This plan moves to Wave 6, `depends_on: [\"05-04\", \"05-06\"]` (05-06 unchanged at Wave 5, `depends_on: [\"05-04\"]`). Rationale: the two plans previously shared Wave 5 and both claimed to edit `SKILL.md` and the same test file with 'zero overlap', which was false. Making this plan run strictly after 05-06 turns it into the SOLE, final `SKILL.md` assembler and `skill-judge` runner — 05-06's Task 2 no longer touches `SKILL.md` at all (see 05-06's own Round 1 ledger); instead it hands this plan ready-to-publish documentation prose via `05-06-SUMMARY.md`, which this plan's Task 2 reads and folds in before its own single, final `skill-judge` pass."
    - "**[Round 1]** Extract the filter/sort/group logic the inline `<script>` uses into small, PURE functions with no DOM dependency (`filterRows(rows, criteria)`, `sortRows(rows, key, direction)`, `groupBySession(rows)`) defined as plain top-level `function` declarations inside the SAME inline `<script>` block (still zero external files, zero bundler — just factored so the logic is callable outside a browser context too). The DOM-wiring code (event listeners, `createElement` calls) calls these SAME functions; the test suite ALSO calls these same functions for real via `node -e \"<script source> ; console.log(JSON.stringify(filterRows(...)))\"` (or an equivalent `subprocess.run([\"node\", ...])` invocation), comparing actual `node`-executed output against the PRD canonical-query expectation — this is qualitatively different from a Python re-implementation of the same logic, since a bug in the ACTUAL shipped JS now fails the test directly. Conditional on `shutil.which(\"node\")`; skips with a visible reason if absent, consistent with this project's stdlib-only dependency discipline (no new pinned dependency — `node` is used only if already present, never installed by this plan)."
    - "**[Round 1]** Confidence-visible rendering: the row-rendering function computes a `displayFamily` value — `family` if non-empty, else `inferred_family + \" (inferred, \" + inferred_confidence + \")\"` if `inferred_family` is set, else an empty placeholder — and writes it via `textContent`, never conflating a mechanical and an inferred value in the SAME unlabeled cell. Cursor rows additionally render their own `source_confidence` value in a distinct column/badge, never silently dropped between capture (05-03) and the dashboard (this had been the exact Round 1 finding: 'Cursor confidence field dropped before reaching the dashboard' — the full column list this plan already embeds, per `must_haves`, includes `source_confidence`, so this is a RENDERING addition, not a query/schema change)."
    - "**[Round 1]** All row/cell/option construction in the inline JS uses `document.createElement(tag)` + `.textContent = value` (or `.value =` for `<option>` element values, still not HTML-string-based) — the prior action text's own 'template-string row rendering into a `<tbody>`' phrasing is retracted and replaced by this explicit `createElement`/`textContent`-only rule, resolving the direct contradiction with this plan's own threat-model mitigation (T-05-13)."
    - "**[Round 2]** `groupBySession`'s token/price aggregation dedups on `(session_id, turn_id)` BEFORE summing `tokens_input`/`tokens_output`/`price` — a compound command sharing one `turn_id` across multiple step rows (e.g. `cd src && rg TODO`) contributes that turn's token/price figures to a session total exactly ONCE, not once per step row, matching 05-04's own turn-level attribution rule (05-04-PLAN.md `must_haves`/`key_links`: 05-05 must dedup before summing) and closing the cross-plan double-counting the Round 2 cross-AI review found between this plan and 05-04. Invocation COUNTS still count every matching row — counting and token/price summing are deliberately different reductions over the same filtered set."
    - "**[Round 2]** A row whose `execution_certain` is `False` (an `&&`-gated step per 05-04's `operator`/`execution_certain` columns, T-05-17) renders a visible '(conditional)' marker in its own column/badge, distinct from a row with `execution_certain=True` or `None` — a conditionally-reached step never looks identical to a confirmed invocation, mirroring the same confidence-visibility pattern already applied to `displayFamily`/`source_confidence`."
---

<objective>
Complete the static HTML dashboard: full filter/sort UI across the 5 MVP
axes, session-grouped views, and the canonical "how many grep-family
invocations this month, grouped by session" query answered entirely from
already-embedded data. Finalize and `skill-judge`-review `SKILL.md`.

Purpose: complete REQ-usage-metrics-dashboard-ui. Wave 1 proved the
embed-and-render mechanism on one runtime's simple commands with a flat,
ungrouped table; this wave expands it to the full axis set, across all 5
sources' now-fully-refined data (05-04), with real filter/sort controls and
session grouping — the actual user-facing deliverable ROADMAP Phase 5's
Goal describes.

Output: `dashboard.py`'s `generate()` produces a complete, dependency-free
static HTML dashboard; `SKILL.md` documents the full pipeline and schema and
passes `skill-judge`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/ONESHOT-RULES.md
@AGENTS.md
@.planning/phases/05-usage-metrics-dashboard/05-CONTEXT.md
@docs/prds/ai-kit-usage-metrics-dashboard-v1.0-prd.md
@.planning/phases/05-usage-metrics-dashboard/05-01-SUMMARY.md
@.planning/phases/05-usage-metrics-dashboard/05-04-SUMMARY.md
@.planning/phases/05-usage-metrics-dashboard/05-06-SUMMARY.md
</context>

<tracked_source_paths>
`dashboard.py`, `SKILL.md`, and `tests/test_ai_kit_usage_metrics.py`
already exist and are tracked — scoped `Edit` only, no whole-file `Write`.
</tracked_source_paths>

<source_audit>
## Multi-Source Coverage Audit (Phase 5, Wave 5 — dashboard portion)

| # | Source | Item | Covered by |
|---|--------|------|-----------|
| REQ-1 | REQUIREMENTS.md | REQ-usage-metrics-dashboard-ui (full, amended per D-08/D-09/D-10) | T1, T2 |
| GOAL-SC3 | ROADMAP SC-3 | Session-grouped canonical query, no live re-parse, no hosted-database capability | T1 |
| CTX-D08/D09 | 05-CONTEXT.md | No export/import step; static HTML regenerated with embedded data, not a server | Already Wave 1's own design — this wave only expands the UI, not the mechanism |
| PRD-MVP-AXES | PRD MVP Axes | Date (with presets), model, commands+family, tokens, price — all filterable/sortable | T1 |
| PRD-DOC | PRD Quality Standards / User Acceptance | Refined schema field list + `contains_control_flow` limitation documented in SKILL.md | T2 |
| PROJECT-SKILL-GATE | PROJECT.md | Skill-quality gate — skill-judge review | T2 |

No item is deferred out of this wave's scope.
</source_audit>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Full filter/sort dashboard UI across the 5 MVP axes with session grouping, reading only the refined DB</name>
  <files>skills/ai-kit-usage-metrics/ai_kit_usage_metrics/dashboard.py, tests/test_ai_kit_usage_metrics.py</files>

  <read_first>
    - `dashboard.py` as Wave 1 landed it — the exact `</script`-escaping
      mechanism and atomic-write helper this task extends, never
      re-implements from scratch.
    - `refined_store.py`'s full column list (Wave 1, extended by 05-04's
      `turn_id`/`step_count`/`operator`/`execution_certain`/
      `source_confidence`/`rtk_*` columns, and populated by 05-06's
      `inferred_family`/`inferred_confidence`) — the exact 5 MVP axis
      columns (`date`, `model`, `family`, `tokens_input`/`tokens_output`,
      `price`) plus `session_id`/`runtime`/`command_text`/`command_shape`/
      `inferred_family`/`inferred_confidence`/`source_confidence` this
      task's query and rendered table/controls must expose.
    - PRD MVP Axes section (already read this session) — the exact date
      presets ("single day, week, month, custom range, current month"),
      and the family-axis framing ("records BOTH the literal command run
      AND its family, so a query can ask for 'how much rg specifically'
      or 'how much of the grep family overall' equally well").
    - This plan's own `must_haves.truths` — the exact canonical query this
      task's own test must answer end to end, and the Round 1 fixes for
      all-5-axes-filterable-AND-sortable, real `node`-executed JS,
      confidence-visible rendering, and `createElement`/`textContent`-only
      DOM construction.
    - `refined_store.py::open_refined_db_readonly` (added by 05-01) — the
      exact URI-mode read-only opener `cli.py`'s `dashboard` subcommand
      already calls; this task's own `dashboard.py::generate` takes an
      already-open `conn` as its first argument and never opens its own
      connection, so it is agnostic to whether the caller passed a
      read-only or read-write connection.
  </read_first>

  <behavior>
    - `dashboard.py::generate(conn, output_path)` queries EVERY column
      Wave 1's `refined_store.py` schema (as extended by 05-04 and 05-06)
      defines — not just the 6-column subset Wave 1's own dashboard
      queried — and embeds the full row set as JSON, unchanged
      embedding/escaping mechanism from Wave 1.
    - **[Round 1]** The rendered HTML ships inline, dependency-free JS
      structured as small PURE functions (`filterRows`, `sortRows`,
      `groupBySession`, plus a `displayFamily` helper) called by the
      DOM-wiring code, implementing full filter+sort coverage for ALL 5
      axes: (a) a family filter (`<select>` populated from DISTINCT
      `family` values, plus "all") AND a sortable family column header;
      (b) a model filter (same `<select>` pattern) AND a sortable model
      column header; (c) a literal command-text substring filter
      (`<input type="text">`, case-insensitive substring match against
      `command_text`) — this is the axis's own text-search half, distinct
      from the family `<select>`; (d) a date-range control offering the
      PRD's own presets (single day via a date picker, current week,
      current month, custom start/end) computed client-side against the
      embedded `date` strings (`YYYY-MM-DD`, lexically comparable) AND a
      sortable date column header; (e) numeric min/max range filter
      inputs for `tokens_input`+`tokens_output` (combined "tokens" axis)
      AND sortable `tokens_input`/`tokens_output` column headers; (f) a
      numeric min/max range filter for `price` AND a sortable `price`
      column header — (d)/(e)/(f) reuse the SAME null-last numeric/lexical
      sort comparator (click toggles ascending/descending, `None`/`null`
      values always sort last regardless of direction — never crash a
      sort on a missing price); (g) a "group by session" toggle that
      re-renders the currently-filtered row set aggregated by
      `session_id` — **[Round 2]** `groupBySession` FIRST reduces the
      filtered rows to one row per DISTINCT `(session_id, turn_id)` pair
      (keeping the first `tokens_input`/`tokens_output`/`price` triple
      seen for each `turn_id`, per 05-04's own attribution rule: a
      compound command's step rows sharing one `turn_id` carry that
      turn's token/price figures duplicated onto every step) and only
      THEN sums the deduplicated `tokens_input`/`tokens_output`/`price`
      (where not `None`) per `session_id` — while the "count of matching
      rows" (invocation count) still counts every matching row, since
      invocation counting and token/price summing are deliberately
      different reductions over the SAME filtered set, instead of the
      flat per-row table.
    - **[Round 2]** A row whose `execution_certain` is `False` (an
      `&&`-gated step per 05-04's `operator`/`execution_certain` columns,
      T-05-17) renders a visible "(conditional)" marker in its own
      column/badge, distinct from a row with `execution_certain=True` (or
      `None`, for sources/shapes that never carry the distinction) — a
      conditionally-reached step must never look identical to a confirmed
      invocation, mirroring the SAME confidence-visibility pattern already
      applied to `displayFamily`/`source_confidence`.
    - **[Round 1]** `filterRows`/`sortRows`/`groupBySession` are executed
      FOR REAL via `node` (not merely re-implemented in Python) against
      fixture row data; selecting family `"grep"` + the "current month"
      date preset + the "group by session" toggle, against a fixture
      embedding rows from multiple sessions and multiple families/dates,
      produces a result — via the ACTUAL shipped JS functions, invoked
      through `node` — matching the PRD's own canonical example ("how
      many grep-family invocations this month, grouped by session"). This
      test is skipped (with a visible, explicit reason) only when
      `shutil.which("node")` is `None`.
    - **[Round 1]** A row whose `family` is empty but `inferred_family` is
      set renders `displayFamily` as `"{inferred_family} (inferred,
      {inferred_confidence})"`, visibly distinct from a mechanically
      determined `family` value; a Cursor row's own `source_confidence`
      renders in a distinct column/badge, never dropped silently between
      capture and the dashboard.
    - **[Round 1]** Every data-driven DOM write uses
      `document.createElement` + `.textContent` (or `.value` for
      `<option>`s) — never an `innerHTML` assignment fed by embedded data,
      and never a template-string built directly into a `<tbody>`'s
      `innerHTML`.
    - `dashboard.py::generate`'s OWN Python code touches only the
      `refined.db` SQLite connection it is passed (whether read-only or
      read-write) — no `open()` call anywhere in the module reads
      `raw/*.jsonl` or any other file, and an AST-based scan of the
      module's own `import`/`from...import` statements finds zero
      occurrences of `urllib`, `http`, `requests`, or `socket`.
  </behavior>

  <action>
Extend `dashboard.py::generate` to `SELECT` an explicit full column list
matching `refined_store.py`'s schema exactly (so a future schema change is
a visible diff here too — including `inferred_family`/`inferred_confidence`/
`source_confidence`), embedding the full row set. Build the inline JS as a
single well-commented `<script>` block appended after the existing
data-embedding `<script type="application/json">` tag from Wave 1 — do
NOT introduce a bundler, a CDN `<script src>` tag, or any external JS
library. **[Round 1]** Structure the script as: (1) a block of small, pure,
top-level `function` declarations with NO DOM references —
`filterRows(rows, criteria)` (applies family/model/command-text-substring/
date-range/tokens-range/price-range filters, all optional/composable),
`sortRows(rows, key, direction)` (null-last), `groupBySession(rows)`
(**[Round 2]** dedups on DISTINCT `(session_id, turn_id)` before summing
`tokens_input`/`tokens_output`/`price`, per `<behavior>`'s dedup rule —
row COUNT still counts every matching row), `displayFamily(row)` (per
`<behavior>`'s inferred-labeling rule); (2) a
DOM-wiring block that reads control values, calls the pure functions, and
renders results using `document.createElement`/`.textContent` ONLY — NEVER
`innerHTML` fed by row/cell data, and never a template string built into a
`<tbody>`'s `innerHTML` (this retracts and replaces the prior draft's
"template-string row rendering" instruction, which directly contradicted
this plan's own threat-model mitigation). Build controls for all 5 axes
per `<behavior>`: family `<select>` + sortable family `<th>`; model
`<select>` + sortable model `<th>`; a `command_text` substring `<input
type="text">`; a date-range control (presets + custom start/end) + sortable
date `<th>`; `tokens_input`/`tokens_output` min/max `<input type="number">`
pairs + sortable `<th>`s; `price` min/max `<input type="number">` pair +
sortable `<th>`; a group-by-session toggle. **[Round 2]** Additionally
render an `execution_certain` badge/column: when a row's
`execution_certain` is `False`, write a `"(conditional)"` `textContent`
marker into its own cell/badge (via `document.createElement`, same as
every other data-driven write — never `innerHTML`); rows with
`execution_certain` `True` or `None` render no marker. The date-preset control
computes "current month" as `YYYY-MM` prefix-matching against each row's
own `date` field client-side at render time (using the browser's OWN
current date via `new Date()`) — document in a comment that this means the
SAME dashboard HTML file answers "current month" differently depending on
WHEN it is opened, which is intentional, and distinct from every OTHER
piece of data in the file, which IS fixed at generation time.

Extend `tests/test_ai_kit_usage_metrics.py` with a `TestDashboardFull`
class: seed a scratch `refined.db` with rows spanning >= 3 sessions, >= 2
families (including at least one `grep`-family row: a literal `rg`
invocation, proving family resolution flows all the way through to the
dashboard's own filter options), at least one row with `family=""` but
`inferred_family`/`inferred_confidence` set (05-06's output shape), at
least one Cursor-sourced row with a `source_confidence` value, and dates
inside and outside "this month" (compute "this month" relative to the
test's own execution time, not a hardcoded date, so this test does not
silently go stale); call `dashboard.generate` against a connection opened
via `refined_store.open_refined_db_readonly` (proving the read-only path
this plan's own `must_haves` requires); parse the output HTML's embedded
JSON block back out and assert every row and every column (including the
newly-embedded `inferred_family`/`inferred_confidence`/`source_confidence`)
round-trips correctly; assert the rendered HTML string contains, for EACH
of the 5 axes, BOTH a filter control AND a `data-sort-key` (or equivalent
identifiable attribute) on its column header; assert the inferred-family
row's rendered output contains an "(inferred," marker and the Cursor row's
rendered output contains its `source_confidence` value; **[Round 2]** seed
at least one row with `execution_certain=False` (an `&&`-gated step) and
assert its rendered output contains a "(conditional)" marker not present
on `execution_certain=True`/`None` rows; seed at least two step rows
sharing the SAME `turn_id` (simulating a compound command's two steps),
each carrying identical `tokens_input`/`tokens_output`/`price`, and assert
the "group by session" aggregated output's summed tokens/price for that
session counts that `turn_id`'s figures exactly ONCE, not once per step
row (while the invocation COUNT for that session still counts both rows);
assert zero
occurrences of a data-driven `innerHTML` assignment pattern in
`dashboard.py`'s own emitted `<script>` source (a targeted regex for
`innerHTML\s*=` immediately followed by a template-literal/backtick or a
row-variable reference, distinguishing it from an absent occurrence
entirely). **[Round 1]** Add a
`test_shipped_js_canonical_query_via_node` test: if `shutil.which("node")`
is `None`, call `self.skipTest("node not available — shipped-JS execution
test skipped")`; otherwise extract the `filterRows`/`sortRows`/
`groupBySession` function source from the generated HTML (a simple regex
slice between known markers is acceptable — document the marker
convention in a comment), write a small `node`-executable wrapper script
to a scratch file that loads those functions plus a JSON fixture and
prints the "grep family, this month, grouped by session" result as JSON,
run it via `subprocess.run(["node", wrapper_path], capture_output=True,
text=True, check=True)`, parse stdout, and assert it matches a
hand-computed expected count — this exercises the ACTUAL shipped JS, not
a Python re-implementation. **[Round 2]** Include in that same fixture a
session with two step rows sharing one `turn_id` and identical
`tokens_input`/`tokens_output`/`price` (a compound `&&` command's two
steps); additionally assert the node-executed `groupBySession` result's
summed tokens/price for that session equals the turn's figures counted
ONCE — proving the `(session_id, turn_id)` dedup rule holds in the ACTUAL
shipped JS, not merely in Python test scaffolding. Add a test that deletes every `raw/*.jsonl`
file from a scratch store directory before calling `dashboard.generate`
and asserts the output is unchanged/correct (proving no raw-file
dependency). **[Round 1]** Add an AST-based test
(`ast.parse(open("dashboard.py").read())`, walking `ast.Import`/
`ast.ImportFrom` nodes) asserting no imported module name is `urllib`,
`http`, `requests`, or `socket` — replacing the prior literal-string grep
approach, which can both miss an aliased/indirect import and false-positive
on harmless prose (e.g. a docstring mentioning "no requests library
needed").
  </action>

  <acceptance_criteria>
    - The rendered HTML contains, for EACH of the 5 MVP axes, both a
      filter control and a sortable column header — verified by an
      executed structural test, not merely "a control exists somewhere".
    - **[Round 1]** The shipped JS's own `filterRows`/`sortRows`/
      `groupBySession` functions are executed for real via `node` (when
      available) and produce the correct canonical-query result; the test
      is explicitly, visibly skipped (never silently passed) when `node`
      is absent.
    - **[Round 1]** A row with `inferred_family` set but empty `family`
      renders a visible "(inferred, ...)" marker distinct from a
      mechanical family value; a Cursor row's `source_confidence` renders
      visibly.
    - **[Round 1]** Zero data-driven `innerHTML` assignments exist in the
      generated script — all row/cell/option construction uses
      `createElement`/`textContent` exclusively.
    - **[Round 2]** `groupBySession`'s token/price sums dedup on
      `(session_id, turn_id)` before summing — a compound command's step
      rows sharing one `turn_id` contribute their tokens/price to a
      session total exactly once, not once per step row; invocation
      counts still count every matching row. Verified by both the
      Python-side test and the `node`-executed shipped-JS test.
    - **[Round 2]** A row with `execution_certain=False` renders a visible
      "(conditional)" marker distinct from `execution_certain=True`/`None`
      rows.
    - The full `refined_commands` column set (including
      `inferred_family`/`inferred_confidence`/`source_confidence`)
      round-trips through the embedded JSON.
    - `dashboard.generate` produces byte-identical-in-substance output
      whether or not `raw/*.jsonl` files exist alongside the refined DB,
      and succeeds when passed a connection opened via
      `open_refined_db_readonly`.
    - **[Round 1]** An AST-based scan of `dashboard.py`'s own imports
      finds zero occurrences of `urllib`, `http`, `requests`, or `socket`.
    - `python3 -m unittest tests.test_ai_kit_usage_metrics -v` reports at
      least 100 tests and 0 failures.
  </acceptance_criteria>

  <verify>
    <automated>python3 -m unittest tests.test_ai_kit_usage_metrics -v</automated>
    <fails_when>non-zero exit, or the summary line reads "Ran 0 tests", or fewer than 100 tests ran, or any prior test fails</fails_when>
    <automated>grep -cE '\.innerHTML[[:space:]]*=' skills/ai-kit-usage-metrics/ai_kit_usage_metrics/dashboard.py</automated>
    <fails_when>**[Round 2]** the output is not "0" — an actual `.innerHTML = ...` ASSIGNMENT exists (this plan's own rule is createElement/textContent only, with no exception); a comment or docstring merely naming "innerHTML" in prose does not match this pattern and does not fail the gate (fixes Round 2's finding that the prior bare `grep -c "innerHTML"` would also fail on an explanatory comment)</fails_when>
  </verify>

  <done>
    The generated dashboard HTML exposes BOTH a filter control AND a
    sortable header for all 5 MVP axes plus session grouping; the shipped
    JS's own pure filter/sort/group functions are executed for real via
    `node` (or explicitly, visibly skipped when unavailable) and correctly
    answer the PRD's own canonical example query; **[Round 2]** session
    grouping's token/price sums dedup on `(session_id, turn_id)` before
    summing, never double-counting a compound command's shared turn;
    `execution_certain=False` rows render a visible conditional marker;
    inferred/source
    confidence values render visibly distinct from mechanical ones; every
    data-driven DOM write uses `createElement`/`textContent`, never
    `innerHTML`; the generator never reads `raw/*.jsonl`, works correctly
    against a read-only connection, and an AST-based import scan confirms
    zero network-capable modules anywhere in its source.
  </done>
</task>

<task type="auto">
  <name>Task 2: Finalize SKILL.md (full pipeline + refined schema + command_shape limitation + 05-06's classification-loop docs), pass skill-judge review, and run the phase's final close-out gate</name>
  <files>skills/ai-kit-usage-metrics/SKILL.md, tests/test_ai_kit_usage_metrics.py</files>

  <precondition>The `skill-judge` skill is available in this environment (it is a listed built-in skill in this project's own tooling) — if it cannot be invoked for any reason, record that as a `human_verification` item per ONESHOT-RULES Rule 7 rather than silently marking this task done without a real review pass.</precondition>

  <read_first>
    - `skills/ai-kit-usage-metrics/SKILL.md` as Waves 1-4 progressively
      left it — the existing Scope section's "honest about current
      coverage" pattern this task now finalizes to the full, completed
      end state (all 5 sources, full schema, full dashboard).
    - **[Round 1]** `.planning/phases/05-usage-metrics-dashboard/05-06-SUMMARY.md`
      — this plan runs strictly AFTER 05-06 (Wave 6 depends_on 05-06), so
      this file is guaranteed to exist and contain 05-06's own
      ready-to-publish "Classification refinement loop" documentation
      section (trigger condition, `DEFAULT_MIN_OCCURRENCES` caveat, the
      required `classify` then `dashboard` two-step sequence) — fold that
      content into `SKILL.md` verbatim/lightly-adapted, do not re-derive
      it from scratch.
    - `skills/ai-kit-opencode-providers/SKILL.md` (already read this
      session) — the established structure/tone (frontmatter
      `name`/`description` with explicit trigger phrases, a Scope
      section, clear boundaries on what the skill does NOT do) this
      project's `skill-judge`-passing skills already follow.
    - PRD Quality Standards / User Acceptance sections (already read this
      session) — the EXACT documentation requirement: "the refined
      schema's exact field list and the `contains_control_flow`
      limitation are documented wherever this pipeline's `SKILL.md` (or
      equivalent) lives."
    - `refined_store.py`'s full column list and `decomposer.py`'s
      `command_shape` enum (05-04) — the exact facts `SKILL.md`'s
      documentation must state accurately, not paraphrase loosely.
  </read_first>

  <action>
Rewrite `SKILL.md`'s body (keep the `name: ai-kit-usage-metrics`
frontmatter, refine its `description` if the trigger phrasing needs
sharpening after seeing the full feature set) to document, in plain
prose: (1) what the skill does — runs the local capture -> refine ->
dashboard pipeline against Claude Code, opencode, Codex, and Cursor
session logs plus `rtk`'s own local stores, entirely locally, no
export/import step; (2) how to invoke it — `python3
skills/ai-kit-usage-metrics/ai-kit-usage-metrics.py run` (and the
individual `capture`/`refine`/`dashboard` subcommands for partial runs),
and where the output dashboard HTML lands (`paths.dashboard_html_path`'s
real resolved location, stated as the literal
`~/.local/share/ai-kit/usage-metrics/dashboard.html` default path,
opened via `file://`); (3) the refined schema's EXACT field list
(reproduce `refined_store.py`'s own column names verbatim, one line per
column, with a one-clause description each — including
`inferred_family`/`inferred_confidence`/`source_confidence`); (4) the
`command_shape` limitation, verbatim-accurate to what 05-04 actually
built: `control_flow_script` (a recognized `if`/`for`/`while`/`case`/
`until`/`select` block, captured as one opaque entry, never
decomposed into fabricated sub-commands) vs. `unclassified` (a segment
the decomposer's quote-aware scanner could not structurally close,
distinct from a recognized-but-opaque script block) — and that both
buckets are the intended seed for 05-06's classification refinement
pass, not permanent dead ends; (5) Cursor's explicit low-confidence
caveat and the `store.db`-exclusion rationale, carried forward from
05-03's own documentation; (6) the cron/scheduling non-goal (manual
invocation only this milestone, per D-07 — cite the Backlog 999.2 entry
by name so a reader knows where that gap is tracked); **[Round 1]** (7)
05-06's own "Classification refinement loop" section, folded in from
`05-06-SUMMARY.md` per `<read_first>` — the `classify` subcommand, its
honest real trigger condition, the `DEFAULT_MIN_OCCURRENCES` caveat, and
the required `classify` then `dashboard` sequence to see updated
inferred-confidence values in the dashboard.

**[Round 3]** Separately from `SKILL.md` itself, correct two stale
pre-D-10 references to a "dashboard Artifact"/"local export" design that
predate what this phase actually delivers — a pipeline-regenerated static
HTML file with refined data embedded directly, no export/import step, no
browser-sandboxed Artifact mechanism (already the corrected wording in
`REQUIREMENTS.md`'s `REQ-usage-metrics-dashboard-ui` and `ROADMAP.md`'s
Phase 5 Success Criterion #3, per D-10, amended 2026-09-08): (a)
`PROJECT.md`'s Rejected Alternatives bullet reading "A hosted-database
capability behind the usage-metrics dashboard Artifact — session data
must never leave the local machine; the dashboard reads a local export
only." is reworded to match `REQUIREMENTS.md`'s own D-10 phrasing — no
export/import step, no browser-sandboxed Artifact mechanism, never a
hosted-database capability pointed at session content; (b)
`.planning/intel/requirements.md`'s `REQ-usage-metrics-dashboard-ui`
description and acceptance bullets are amended the same way, so this
intel snapshot stops disagreeing with the authoritative requirement it
mirrors.

Invoke the `skill-judge` skill against `skills/ai-kit-usage-metrics/SKILL.md`
ONLY AFTER all of (1)-(7) above — including 05-06's folded-in section —
are present, so the reviewed file is genuinely the final, complete one
(this is the direct Round 1 fix for "the final material SKILL.md edit
bypasses the skill-quality gate": this plan is now the ONLY plan that
edits `SKILL.md`, and it runs `skill-judge` last, over the fully-assembled
content, never appended-to afterward by another same-wave or later plan).
Address every Critical/Important finding it reports by editing `SKILL.md`
directly (never by weakening the finding's own bar) and re-run until 0
remaining Critical/Important findings, or a score >= 96/120 with no
regression vs. this skill's own prior baseline (there is no prior
baseline for a brand-new skill — treat the FIRST `skill-judge` run's own
score as this skill's baseline, and the requirement is simply "0
remaining Critical/Important" for a first-time skill, matching how
Phase 2's `ai-kit-opencode-providers` skill was gated).

Add a small `TestSkillDocumentation` test to
`tests/test_ai_kit_usage_metrics.py`: read `SKILL.md`'s own text and
assert it contains every one of `refined_store.py`'s real column names
(parsed from the actual Python source via a simple regex/AST scan of the
`CREATE TABLE` statement, not a hardcoded duplicate list that could drift)
— an executed proof that the documented schema and the real schema stay
in sync, not just true at the moment this task was written. **[Round 1]**
Also assert `SKILL.md`'s text contains the literal substring `classify`
and the word `trigger` (a lightweight, non-brittle proof that 05-06's
classification-loop section was genuinely folded in, not merely read and
discarded).

**[Round 1]** Finally — since this plan is the LAST plan to execute in
Phase 5 (Wave 6, depends_on 05-04 and 05-06) — run the phase's
authoritative close-out gate: `make test`, `make lint`, and `make
validate` for the whole repository, all must exit 0. This responsibility
moved here from 05-06's own Task 2 per the wave reorder (see this plan's
own Review Dispositions Ledger); 05-06's own final task now runs only a
narrower, plan-scoped regression check, not the whole-repository gate.
  </action>

  <acceptance_criteria>
    - `SKILL.md` documents every real `refined_commands` column name,
      verified by an executed test comparing against the actual schema
      source, not a hand-maintained duplicate.
    - `SKILL.md` explicitly distinguishes `control_flow_script` from
      `unclassified` in prose matching 05-04's actual implementation.
    - **[Round 1]** `SKILL.md` includes 05-06's own classification-loop
      documentation, folded in from `05-06-SUMMARY.md`, verified by an
      executed test.
    - `skill-judge` reports 0 remaining Critical/Important findings for
      `skills/ai-kit-usage-metrics/SKILL.md` (or the `human_verification`
      precondition fallback is recorded if the skill is genuinely
      unavailable in this environment) — run against the FULLY assembled
      file, including 05-06's folded-in section.
    - **[Round 1]** `make test`, `make lint`, and `make validate` all exit
      0 for the whole repository — the actual, final phase-close gate for
      Phase 5, now run here since this plan executes last.
    - **[Round 3]** `PROJECT.md` and `.planning/intel/requirements.md` no
      longer contain the stale "dashboard Artifact"/"local export"
      phrasing for the usage-metrics dashboard — both now match
      `REQUIREMENTS.md`'s already-amended (D-10) wording.
    - `python3 -m unittest tests.test_ai_kit_usage_metrics -v` reports at
      least 102 tests and 0 failures.
  </acceptance_criteria>

  <verify>
    <automated>python3 -m unittest tests.test_ai_kit_usage_metrics -v</automated>
    <fails_when>non-zero exit, or the summary line reads "Ran 0 tests", or fewer than 102 tests ran, or any prior test fails</fails_when>
    <automated>grep -c "classify" skills/ai-kit-usage-metrics/SKILL.md</automated>
    <fails_when>the output is "0" (05-06's classification-loop section was not folded into the final SKILL.md)</fails_when>
    <automated>grep -l "no export/import step" .planning/PROJECT.md .planning/intel/requirements.md | wc -l</automated>
    <fails_when>the output is not "2" (both docs must carry the corrected, D-10-matching wording — a missing file or an un-amended one means the stale "dashboard Artifact"/"local export" text is still present)</fails_when>
    <automated>make test && make lint && make validate</automated>
    <fails_when>non-zero exit from any of the three, or any hook line ends with "Failed"</fails_when>
  </verify>

  <done>
    `SKILL.md` accurately and completely documents the finished pipeline,
    the full refined schema, the `command_shape` limitation, AND 05-06's
    classification-loop documentation, folded in before — not after —
    this plan's own single, final `skill-judge` review, which reports 0
    remaining Critical/Important findings (or the unavailability fallback
    is explicitly recorded). **[Round 3]** `PROJECT.md` and
    `.planning/intel/requirements.md` no longer describe the dashboard as
    an "Artifact" reading a "local export," matching the actual delivered
    pipeline-regenerated static HTML design. The whole-repository
    `make test`/`make lint`/`make validate` gate is green, genuinely
    closing Phase 5.
    <human-check>For uz, after this autonomous run and entirely at uz's
    discretion: run `python3 skills/ai-kit-usage-metrics/ai-kit-usage-metrics.py run`
    against your real `~/.claude`/`~/.local/share/opencode`/
    `~/.local/share/rtk`/`~/.codex`/`~/.cursor` data, open the generated
    `dashboard.html`, and confirm it answers a real query you care about
    — including trying the family/model/command-text/date/tokens/price
    filters and the group-by-session toggle for real. This is never run
    automatically by this phase's own execution (ONESHOT-RULES Rule 5) —
    it is your own, separate, later action. **[Round 1]** This check
    moved here from 05-06's own Task 2, since this plan now runs last in
    the phase.</human-check>
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Refined DB content → embedded dashboard JSON → inline JS rendering | Unchanged from Wave 1's own T-05-01 (the `</script` escaping mitigation); this wave's expanded column set and controls do not introduce a new embedding point — everything still flows through the SAME single escape-then-embed function. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-13 | Tampering | The expanded dashboard's inline JS reading MORE embedded fields (e.g. `command_text` now potentially rendered in a detail view) than Wave 1's original 6-column table | medium | mitigate | **[Round 1]** Every field the inline JS writes into the DOM uses `document.createElement` + `.textContent`/`.value` assignment ONLY — `innerHTML` is never used for ANY data-driven content, with NO exception for "module-controlled" values either, resolving the prior draft's own internal contradiction (the action text previously described "template-string row rendering into a `<tbody>`" while this mitigation claimed `textContent`-only). Pinned by Task 1's `<verify>`: **[Round 2]** a scoped `grep -cE '\.innerHTML[[:space:]]*='` (an actual assignment, not a bare substring match that a comment could also trip) must show exactly `0` — an absolute rule for real assignments, not a conditional one — so a `command_text` value containing HTML-meaningful characters can never inject markup into the rendered page. |
| T-05-17 | Repudiation (of execution certainty) | Dashboard rendering of `execution_certain` (05-04's `&&`-gated-step flag) | medium | mitigate | **[Round 2]** A row whose `execution_certain` is `False` renders a visible "(conditional)" marker via `createElement`/`textContent`, distinct from a confirmed row — a step this pipeline never actually observed executing (because a preceding `&&` gate could have short-circuited it) is never presented with the same certainty as a directly-observed invocation. Mirrors T-05-19's `displayFamily` pattern; pinned by an executed test in Task 1. Cross-referenced from 05-04's own T-05-17 registration of this column. |
| T-05-19 | Spoofing (of certainty) | Dashboard rendering of `inferred_family`/`inferred_confidence`/`source_confidence` alongside mechanically-determined `family` | medium | mitigate | **[Round 1]** `displayFamily` never merges an inferred value into the same unlabeled cell as a mechanical `family` value — an inferred value always renders with an explicit "(inferred, {confidence})" marker, and Cursor's own `source_confidence` renders in its own distinct column/badge, so a user can never mistake a LOW-confidence guess for a decomposer-certain fact (PROJECT.md's confidence-visibility rule). Pinned by an executed test in Task 1. |
</threat_model>

<verification>
Run `python3 -m unittest tests.test_ai_kit_usage_metrics -v` and confirm at
least 102 tests pass with 0 failures. Confirm `skill-judge`'s review output
shows 0 remaining Critical/Important findings for the FULLY assembled
`SKILL.md` (including 05-06's folded-in classification-loop section).
**[Round 1]** Run `make test`, `make lint`, and `make validate` for the
whole repository and confirm all exit 0 — this is the actual, final
phase-close gate for Phase 5, moved here from 05-06 per the wave reorder,
since this plan (Wave 6) is now the last plan to execute.
</verification>

<success_criteria>
- REQ-usage-metrics-dashboard-ui is fully satisfied: all 5 MVP axes are
  BOTH filterable AND sortable, session grouping works, the PRD's own
  canonical query is answerable from embedded data alone via the ACTUAL
  shipped JS (executed via `node` when available), no live re-parse, no
  hosted-database capability (verified by AST-based import scan).
- Inferred/source confidence values render visibly distinct from
  mechanically-determined ones; all data-driven DOM writes use
  `createElement`/`textContent`, never `innerHTML`.
- **[Round 2]** Session token/price totals dedup on `(session_id,
  turn_id)` before summing, never double-counting a compound command's
  shared turn; `execution_certain=False` rows render a visible conditional
  marker.
- `SKILL.md` accurately documents the finished pipeline, including 05-06's
  classification-loop section, and passes `skill-judge` as the single,
  final reviewed version of the file.
- All 4 of Phase 5's requirements (`REQ-usage-metrics-raw-capture`,
  `REQ-usage-metrics-refinement-pipeline`, `REQ-usage-metrics-dashboard-ui`,
  `REQ-usage-metrics-classification-refinement-loop`) are complete, and the
  whole-repository `make test`/`make lint`/`make validate` gate is green,
  closing Phase 5.
</success_criteria>

<output>
Create `.planning/phases/05-usage-metrics-dashboard/05-05-SUMMARY.md` when done
</output>

## Review Dispositions Ledger

### Round 1 — 9490f7a

**Review Feedback Addressed**

| Concern | Severity | How Addressed |
|---------|----------|----------------|
| All 5 axes are not both filterable and sortable — the draft provided family/model/date filters and sorting only for tokens/price, lacking literal-command filtering, token/price filters, and sorting for date/model/command/family | HIGH | Task 1's behavior/action rewritten so EVERY axis gets both a filter control and a sortable column header: family (`<select>` + sortable `<th>`), model (`<select>` + sortable `<th>`), a new literal command-text substring `<input>` (the missing text-search half of the family/command axis), date (presets + custom range, now also a sortable `<th>`), and new numeric min/max range filters for tokens and price (in addition to their existing sortable headers). Verified by a dedicated structural test asserting both a filter control AND a sortable header exist for each of the 5 axes. |
| The shipped JavaScript is not executed by any test — the must-have claims the shipped JS answers the canonical query, but the test only reimplements the logic in Python | HIGH | The inline script is now factored into small pure functions (`filterRows`, `sortRows`, `groupBySession`, `displayFamily`) with no DOM dependency, and a new `test_shipped_js_canonical_query_via_node` test extracts and executes those ACTUAL functions via `node` (via `subprocess.run`), asserting the real shipped JS produces the canonical-query result — not merely a Python reimplementation. Conditional on `shutil.which("node")`; explicitly, visibly `skipTest`s (never silently passes) when `node` is unavailable, per this project's stdlib-only dependency discipline. |
| The dashboard's read-only database claim lacks a read-only connection path — 05-05 does not define a separate read-only opener for the `dashboard` subcommand | HIGH | Resolved upstream: 05-01's Round 1 revision already added `refined_store.py::open_refined_db_readonly` (a URI-mode read-only opener) and wired `cli.py`'s `dashboard` subcommand to use it. This plan's `dashboard.py::generate` takes an already-open connection and never opens its own, so it works correctly against the read-only connection `cli.py` now passes — pinned by a new test explicitly calling `generate` against a connection opened via `open_refined_db_readonly`. |
| Inferred classifications are embedded but not visibly labeled — PROJECT.md's confidence rule requires lower-confidence results to be visible, but the draft did not specify displaying `inferred_family` with `inferred_confidence` | HIGH | Added a `displayFamily` rendering rule: when `family` is empty but `inferred_family` is set, the rendered value is `"{inferred_family} (inferred, {inferred_confidence})"`, visibly distinct from a mechanical value; Cursor's own `source_confidence` renders in its own distinct column/badge. Verified by an executed test. New threat T-05-19 registers this as a mitigated Spoofing-of-certainty risk. |
| Safe DOM rendering is contradictory — the action proposed "template-string row rendering into `<tbody>`" while the threat mitigation claimed `textContent`-only | HIGH | Retracted the template-string instruction entirely. The action now mandates `document.createElement` + `.textContent`/`.value` for ALL data-driven DOM writes, with NO exception (T-05-13's mitigation updated to match exactly, removing its prior "or only occurrences that assign a module-controlled value" carve-out). Pinned by Task 1's `<verify>` requiring `grep -c "innerHTML"` to be exactly `0`, unconditionally. |
| Source-string absence is not sufficient proof of no network capability — a literal grep can miss aliased/indirect imports and false-positive on harmless prose | MEDIUM | Replaced the literal-string grep with an AST-based test (`ast.parse` + walking `ast.Import`/`ast.ImportFrom` nodes) asserting no imported module name is `urllib`, `http`, `requests`, or `socket` — the same AST-based discipline already used for 05-03's Cursor-capture sqlite3-import check. |
| There is no real browser or DOM verification — presence of `<select>`/`<option>` strings does not establish usable behavior, date controls, sorting, or safe rendering | MEDIUM | Partially addressed, partially deferred — see Deferred table below. The `node`-executed shipped-JS test (see the first row above) now proves the actual filter/sort/group LOGIC is correct, which is the highest-value slice of this concern; full interactive DOM/browser behavior (event-listener wiring, real rendering, click handling) remains unverified by an automated test. |
| Plans 05-05 and 05-06 both edit `SKILL.md` and the same test file in the same wave despite claiming no overlap | HIGH (cross-referenced from 05-06's own concern list) | Wave reorder: this plan moves to Wave 6, `depends_on: ["05-04", "05-06"]`, running strictly after 05-06. This plan is now the SOLE `SKILL.md` editor and the sole `skill-judge` runner, folding in 05-06's classification-loop documentation (sourced from `05-06-SUMMARY.md`) before its own single, final review pass — resolving both the file-race and the "final edit bypasses the quality gate" concern together. |

**Review Feedback Deferred**

| Concern | Reason |
|---------|--------|
| No real browser or DOM verification (MEDIUM) — full interactive behavior (event listeners actually firing, real rendering, click-driven sort/filter/group in an actual DOM) is not exercised by an automated test | Deferred. Adding genuine browser/DOM verification would require a headless-browser dependency (e.g. Playwright, Selenium, jsdom via an npm toolchain) that this project's stack does not currently carry and that ONESHOT-RULES' and PROJECT.md's stdlib-only / no-new-dependency discipline weighs against introducing for a single dashboard's test coverage. The `node`-executed pure-function test (see Addressed table) closes the highest-value part of this gap — the actual shipped filter/sort/group LOGIC is now proven correct via real JS execution — leaving only DOM-wiring/rendering behavior unverified by automation. The `<human-check>` in this plan's own Task 2 (moved here from 05-06, since this plan now runs last) gives uz a concrete, low-cost manual verification point for the remaining gap: opening the real dashboard and exercising every control by hand. |

### Round 2

**Review Feedback Addressed**

| Concern | Severity | How Addressed |
|---------|----------|----------------|
| Cross-plan (05-04 + 05-05): `groupBySession` summed `tokens_input`/`tokens_output`/`price` directly per `session_id` without first deduping on `turn_id`, even though 05-04's own `must_haves`/`key_links` require 05-05 to dedup before summing — a compound command sharing one `turn_id` across step rows (e.g. `cd src && rg TODO`) got its tokens/price counted twice in session totals | HIGH | `groupBySession` now reduces to one row per DISTINCT `(session_id, turn_id)` pair BEFORE summing `tokens_input`/`tokens_output`/`price`; row-count aggregation (invocations) is unaffected, since it is a deliberately different reduction over the same filtered set. Added to `must_haves.truths`, `<behavior>`, `<action>` (both the Python-side `TestDashboardFull` test and the `node`-executed shipped-JS test now seed a shared-`turn_id` fixture and assert the dedup holds), `acceptance_criteria`, `<done>`, and `success_criteria`. |
| T-05-17's `execution_certain=False` distinction (05-04's `&&`-gated-step flag) is never rendered in the dashboard, so conditional steps look like confirmed invocations | MEDIUM | Added an `execution_certain=False` -> visible "(conditional)" marker rendering rule (`createElement`/`textContent`, matching the `displayFamily` pattern) to `must_haves.truths`, `<behavior>`, `<action>`, `acceptance_criteria`, `<done>`, and a new T-05-17 threat-register row in this plan (cross-referencing 05-04's own T-05-17) confirming the mitigation is implemented, not just declared upstream. Test coverage added to `TestDashboardFull`. |
| Task 1's `<verify>` `grep -c "innerHTML"` must be exactly 0 hits, but a comment merely naming the ban (e.g. "never use innerHTML") would also fail the gate — the rule should be "no innerHTML assignment," not "the substring never appears" | MEDIUM | `<verify>` now runs `grep -cE '\.innerHTML[[:space:]]*='` — scoped to an actual assignment pattern, not a bare substring — so an explanatory comment/docstring mentioning "innerHTML" no longer trips the gate. T-05-13's mitigation text updated to match. |

**Review Feedback Deferred**

| Concern | Reason |
|---------|--------|
| (none) | Every Round 2 finding against this plan (1 HIGH, 2 MEDIUM) was incorporated. |

### Round 3 (polish) — 539513e

**Review Feedback Addressed**

| Concern | Severity | How Addressed |
|---------|----------|----------------|
| Dashboard rendering of `source_confidence` is specified in this plan, but 05-04 never actually populated the column — the badge test could pass on a hand-seeded `refined.db` fixture and still show nothing on real `run` output | MEDIUM | Resolved at the correct source: 05-04's own Round 3 revision now threads `source_confidence` from the raw envelope onto every refined row (`envelope.get("source_confidence") or "high"`), so this plan's existing rendering and `TestDashboardFull` badge test are now backed by data the pipeline genuinely produces, not just a hand-seeded fixture. No behavioral change needed in THIS plan; recorded here for cross-plan traceability, mirroring the Round 2 pattern used for the `groupBySession` dedup fix. |
| `PROJECT.md`/`.planning/intel/requirements.md` still describe the dashboard as an "Artifact" reading a "local export," stale against D-10's already-corrected `REQUIREMENTS.md`/`ROADMAP.md` wording (amended 2026-09-08) | LOW | Task 2's `<action>` gains a small, explicit docs-correction step (files_modified now includes both docs): `PROJECT.md`'s Rejected Alternatives bullet and `.planning/intel/requirements.md`'s `REQ-usage-metrics-dashboard-ui` section are both reworded to match `REQUIREMENTS.md`'s D-10 phrasing — no export/import step, no browser-sandboxed Artifact mechanism. New `<acceptance_criteria>`/`<verify>` gate confirms both files carry the corrected wording. |

**Review Feedback Deferred**

| Concern | Reason |
|---------|--------|
| No real browser/DOM click test | Already deferred in Round 1 (see that round's own Deferred table for the full rationale — no new headless-browser dependency this milestone; the `node`-executed pure-function test plus the `<human-check>` remain the coverage). Cycle 3 re-raised this as "still true," not a new finding — reaffirmed as deferred, not re-litigated; this is the max-cycles (3) polish pass, per the milestone's review-cycle policy. |
