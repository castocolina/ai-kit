---
phase: 01.1-autonomous-run-infrastructure-inserted
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/e2e/docker/Dockerfile
  - tests/e2e/docker/run.sh
  - .dockerignore
  - tests/test_wizard_pty.py
  - .planning/ROADMAP.md
  - .planning/ONESHOT-RULES.md
  - .planning/ONESHOT-AUTONOMOUS.md
  - tests/e2e/docker/fixtures/README.md
autonomous: true
requirements: []

estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - ".planning/config.json carries the configured cross-AI routes for both plan-review convergence and execution delegation (workflow.plan_review_convergence, review.default_reviewers/review.models, workflow.cross_ai_execution, workflow.cross_ai_command) — confirmed present and correctly-valued this phase, not redone. Live reachability of any route is inherently transient, not a permanent guarantee: this very phase's own cross-AI plan review found the primary plan-review route unreachable twice ('Service temporarily unavailable due to resource pressure') and the Rule 2 fallback engaged and succeeded, producing `01.1-REVIEWS.md` itself as the evidence the fallback contract works end-to-end."
    - "make e2e-docker builds a genuinely fresh container — the absence of ~/.claude, ~/.cursor, ~/.config/opencode, and ~/.codex is explicitly asserted by a run.sh preflight check (fails loudly if any exists), not merely assumed — and its build context is free of credential-shaped files (.dockerignore excludes .env, .env.*, *.pem, *.key) — then passes make test + make lint inside it."
    - "ONESHOT-AUTONOMOUS.md and ONESHOT-RULES.md are internally consistent with real Makefile targets and config.json, with no stale phase-numbering references — including the 'Phases 1-5' wording this phase's own review found stale, not just the earlier 'Phase 0.1' fix — and their opening DRAFT banners carry a stated, accurate disposition instead of a blanket unexamined label."
    - "ROADMAP.md's Phase 1.1 Success Criterion 4 names verification evidence achievable entirely within Phase 1.1's own scope — this phase's own 01.1-REVIEWS.md, produced by a real, live invocation of the plan-review convergence mechanism against this very phase's plan — never a forward-looking claim that would require Phase 1.2 to be planned (and convergence invoked against it) before Phase 1.1 itself has closed."
    - "ROADMAP.md's Phase 1.1 Success Criterion 1 sentence itself — not just this plan's SUMMARY.md — carries the same config-correctness-plus-documented-fallback caveat already applied to Success Criterion 4: route reachability is inherently transient, not a permanent guarantee, evidenced by 01.1-REVIEWS.md's own dated record of both cycle 1 (primary route unreachable, Rule 2 fallback engaged and succeeded) and cycle 2 (both the primary route and the Rule 2 fallback route unreachable simultaneously, triggering the Rule 7 circuit-breaker manual-review path)."
    - "A documented, reusable fixture convention exists under tests/e2e/docker/fixtures/ for later phases to add runtime-specific E2E fixtures without guessing at config shapes ahead of their own research — its required content (no-volume-mount warning, copy-into-scratch-tree pattern, named future-phase list) is verified by grepping for that content, not just by the file's existence."
  artifacts:
    - tests/e2e/docker/Dockerfile
    - tests/e2e/docker/run.sh
    - .dockerignore
    - tests/test_wizard_pty.py
    - tests/e2e/docker/fixtures/README.md
    - .planning/ROADMAP.md
    - .planning/ONESHOT-RULES.md
    - .planning/ONESHOT-AUTONOMOUS.md
  key_links:
    - "Dockerfile apt-get install list -> run.sh's make test / make lint steps actually finding make on PATH inside the container"
    - ".dockerignore's credential-shaped exclusion patterns -> Dockerfile's COPY --chown=aikit:aikit . /home/aikit/ai-kit build-context assembly, before any secret-shaped file could ever enter the image"
    - "run.sh's preflight assertion -> the real, checked absence of the four AI-CLI config dirs inside the container, printed as evidence rather than assumed"
    - "TestPhase2E2E.test_pick_skill_and_confirm_creates_symlink's local _SUMMARY_DEADLINE -> its 4 drive_until call sites inside that one test"
    - "ROADMAP.md Success Criterion 4 wording -> this phase's own 01.1-REVIEWS.md as the real, in-phase-scope evidence that plan-review convergence actually fires when invoked"
    - "tests/e2e/docker/fixtures/README.md's documented convention -> Phase 2/3/4's future shutil.copy-into-scratch-tree fixture additions"
---

<objective>
Close out Phase 1.1 (Autonomous-Run Infrastructure, INSERTED) by making its four ROADMAP.md
Success Criteria genuinely true and evidenced, per 01.1-RESEARCH.md's live-reproduced findings.
Most of the "wiring" (`.planning/config.json`'s cross-AI keys, `ONESHOT-AUTONOMOUS.md`/
`ONESHOT-RULES.md`, the `Makefile`'s `e2e-docker` target, the Dockerfile/run.sh skeleton) already
exists and is committed — this plan does NOT rebuild any of it. It closes the two concrete,
live-reproduced gaps research found (a missing `make` package in the container image, and a PTY
test deadline that reproducibly times out under constrained container CPU), formally amends one
inaccurate ROADMAP wording (Success Criterion 4, per the D-01/ONESHOT-RULES.md Rule 4 dated-
amendment precedent), establishes the D-09 fixture convention (directory + doc, no runtime
fixtures yet), and verifies (without redoing) the two success criteria already satisfied by prior
session work (commits 5c00e2a, 6161b81).

Revision note (this version): `01.1-REVIEWS.md` [d896183] (cross-AI plan review, fallback route
`openai/gpt-5.6-sol` after the primary `router-env/my-plan-review` route was confirmed unreachable
twice this cycle) raised 5 HIGH and 2 actionable MEDIUM concerns against the prior draft of this
plan. This revision addresses every one of them directly in Tasks 1-3 below and in this file's
Review Dispositions Ledger (after `<success_criteria>`): Task 1 now also hardens `.dockerignore`
against credential-shaped files and adds a real preflight assertion of container freshness to
`run.sh` (HIGH-5, MEDIUM-6); Task 2's Success Criterion 4 amendment is re-grounded in this phase's
own already-completed convergence run instead of a Phase-1.2-targeting invocation that would
itself violate the roadmap's phase-ordering rule, and its verify step now checks the real
convergence-run artifact's structure, not just the amended roadmap prose (HIGH-1, HIGH-2); Task 3
now also fixes a second stale phase-numbering reference in `ONESHOT-RULES.md`, states an explicit
disposition for both files' DRAFT banners, reframes Success Criterion 1's confirmation as
config-correctness-plus-documented-fallback rather than an unconditional live-reachability claim,
and strengthens the fixture README's verify to check required content, not just file existence
(HIGH-3, HIGH-4, MEDIUM-7).

Round 2 revision note: `01.1-REVIEWS.md` [77fd340] — a cycle 2 review performed as a manual
verification pass against the live repository, because both the primary `router-env/my-plan-
review` route and the Rule 2 fallback `openai/gpt-5.6-sol` route were confirmed unreachable this
cycle (the named Rule 7 circuit-breaker condition; recorded transparently in this file's Review
Dispositions Ledger, not hidden) — confirmed four of Round 1's five HIGH concerns and both MEDIUM
concerns fully resolved, and raised one further actionable MEDIUM concern: Success Criterion 1's
transient-reachability caveat landed in `SUMMARY.md`'s planned content but never in
`.planning/ROADMAP.md`'s own Success Criterion 1 sentence — the document an autonomous run is told
to trust as ground truth. Task 3 below now also amends that sentence directly, grounded in both
cycle 1's and cycle 2's route outages as dated evidence, and this file's Review Dispositions Ledger
records the fix under "Round 2 — 77fd340".

Discretion note (per this task's own instructions): this plan does NOT carry `cross_ai: true` in
its frontmatter. Its three tasks are narrow, session-context-dependent fixes (a live-reproduced
container bug, a live-reproduced test flake, and a precise dated ROADMAP amendment matching an
established in-repo pattern) best executed by the local executor that already holds this
session's research findings — not delegated to a fresh cross-AI session that would need to
re-derive them. Had this plan opted in, `execute-phase.md` would delegate to `workflow.cross_ai_
command`, falling back per D-05 to `opencode run --model xai/grok-4.6` if the primary
`router-env/my-coding` route failed; `cross_ai: true` remains the right default for Phase 1.2
onward, once this plan's own work has made that delegation path real. Separately, this plan is
itself subject to D-02's mandatory plan-review convergence before execution — if the primary
`router-env/my-plan-review` reviewer route fails, D-03's documented fallback
(`opencode run --model openai/gpt-5.6-sol` at high reasoning effort) applies, exactly as it did
this cycle; neither fallback model is ever substituted for the other's role.

Per D-11, every artifact this plan produces stays English-only (`AGENTS.md`'s Non-Negotiable
Rules, unmodified for this phase like every other). No *runtime* config file is written by any
task below — Task 3 only reads `.planning/config.json` to confirm existing values, never writes
it — so AGENTS.md's atomic-config-write rule (`tempfile.mkstemp()` + `os.replace()`) has no write
surface to apply to in this plan; every write this plan does make (`.planning/ROADMAP.md`,
`.planning/ONESHOT-RULES.md`, `.planning/ONESHOT-AUTONOMOUS.md`, `tests/e2e/docker/fixtures/
README.md`, `tests/e2e/docker/Dockerfile`, `tests/e2e/docker/run.sh`, `.dockerignore`,
`tests/test_wizard_pty.py`) is a planning/documentation or dev-tooling artifact, not runtime CLI
config, and uses scoped `Edit` calls rather than a full-file rewrite.

Purpose: prove — with real command output, not claims — that Phases 1.2-5 can run unattended
under `/gsd-autonomous`, per `.planning/ONESHOT-RULES.md`.
Output: a green `make e2e-docker` run with a hardened build context and an asserted-fresh
container, a corrected `ROADMAP.md`, corrected `ONESHOT-*.md` files with a stated DRAFT
disposition, and the `tests/e2e/docker/fixtures/` convention documented and content-verified for
later phases.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/config.json
@.planning/ONESHOT-RULES.md
@.planning/ONESHOT-AUTONOMOUS.md
@.planning/phases/01.1-autonomous-run-infrastructure-inserted/01.1-CONTEXT.md
@.planning/phases/01.1-autonomous-run-infrastructure-inserted/01.1-RESEARCH.md
@.planning/phases/01.1-autonomous-run-infrastructure-inserted/01.1-REVIEWS.md
@AGENTS.md
@tests/e2e/docker/Dockerfile
@tests/e2e/docker/run.sh
@.dockerignore
@tests/test_wizard_pty.py
@tests/test_system_memory_e2e.py
@tests/test_setup.py
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Fix Dockerfile `make` gap, harden `.dockerignore` + `run.sh` clean-room guarantees, fix PTY deadline flake, verify `make e2e-docker` green end-to-end</name>
  <files>tests/e2e/docker/Dockerfile, tests/e2e/docker/run.sh, .dockerignore, tests/test_wizard_pty.py</files>
  <precondition>podman (or docker) is present on PATH and can build/run images without sudo — already confirmed this session (01.1-RESEARCH.md Environment Availability: rootless Podman 5.8.4). If absent, this is an ONESHOT-RULES.md Rule 7 circuit breaker, not a task failure to work around.</precondition>
  <action>
    Per D-08 (this phase's own success criterion 2) and RESEARCH.md Pitfall 1 (live-reproduced
    exit 127 this session): in `tests/e2e/docker/Dockerfile`, add `make` to the
    `apt-get install -y --no-install-recommends` package list on the line currently reading
    `bash git curl ca-certificates shellcheck` so it becomes
    `bash git curl ca-certificates shellcheck make`. This is a Debian-archive OS package
    installed through the same already-trusted `apt-get` call as the existing packages — not an
    npm/pip/cargo dependency, so the Package Legitimacy Gate does not apply (RESEARCH.md Package
    Legitimacy Audit: N/A). Do not add any other package or change any other Dockerfile line.

    Per `01.1-REVIEWS.md` [d896183] HIGH concern 5 (Docker build-context secret-disclosure risk
    understated — the image copies the entire build context via `COPY --chown=aikit:aikit .
    /home/aikit/ai-kit`, and the current `.dockerignore` excludes `.venv`/`.git`/
    `.claude/worktrees`/`__pycache__`/`*.pyc`/`.aikit`/`uv.lock.bak` but nothing credential-shaped):
    append a new stanza to `.dockerignore` excluding `.env`, `.env.*`, `*.pem`, and `*.key` —
    credential-shaped filenames that could exist untracked in a working tree without ever being
    committed, and would otherwise be copied straight into the clean-room image. Add a short
    comment above the new lines explaining why (untracked secret material must never enter a build
    context, even one that's never had a real secret committed to git). Do not remove or alter any
    existing `.dockerignore` line.

    Per `01.1-REVIEWS.md` MEDIUM concern 6 ("genuinely fresh" is asserted, not checked — `run.sh`
    never asserts absence of the four AI-CLI config dirs, and the Makefile's `e2e-docker` target
    builds without `--no-cache`): in `tests/e2e/docker/run.sh`, immediately after the existing
    `echo "==> repo: $(pwd)"` line and before the existing `echo "==> uv sync ..."` line, insert a
    preflight block that loops over `"$HOME/.claude"`, `"$HOME/.cursor"`, `"$HOME/.config/opencode"`,
    and `"$HOME/.codex"`, printing a `FATAL:` message to stderr and exiting 1 if any of them exists
    — turning the "no pre-existing AI-CLI config" half of Success Criterion 2 into something this
    script actually checks and fails loudly on, not an assumption. Deliberately do NOT add
    `--no-cache` to the Makefile's `e2e-docker` target: the specific guarantee that matters — the
    real absence of pre-existing AI-CLI config directories — is what the new preflight check now
    asserts directly and unconditionally on every run; a rebuild reusing Docker/Podman's own layer
    cache for the `uv`-install `RUN` step (when the Dockerfile itself hasn't changed) is ordinary
    image-build behavior, not a clean-room violation, and forcing `--no-cache` into every
    `e2e-docker` invocation would meaningfully slow the gate for no corresponding safety gain. Note
    this stated resolution in the plan's SUMMARY.md rather than silently narrowing the "no cached
    uv" phrasing without explanation.

    Per RESEARCH.md Pitfall 2 / Open Question 2 (reproduced twice live, deterministic): in
    `tests/test_wizard_pty.py`, inside `TestPhase2E2E.test_pick_skill_and_confirm_creates_symlink`
    (class `TestPhase2E2E` starts at line 347; the test method's local deadline block is around
    line 399-401), raise the local `_SUMMARY_DEADLINE` from `30.0` to `60.0` — a blanket increase,
    per RESEARCH.md's own recommendation, since `drive_until` polls via `select()` rather than
    sleeping a fixed amount, so a wider ceiling costs nothing on a fast host and only matters when
    the container is genuinely CPU-constrained. Leave `_BOOT_DEADLINE` (45.0) and `_DRAIN_DEADLINE`
    (30.0) in that same test untouched, and do not touch any deadline constant in `TestPhase1E2E`,
    `TestPhase3E2E`, or `TestCurlBashE2E` — this fix is scoped to the one reproduced flake, per
    ONESHOT-RULES.md Rule 6 ("a real gate, not optional polish" — narrow, targeted fixes, not a
    blanket rewrite of unrelated test timing).

    Per D-07 (real-machine-mutation policy is absolute for this milestone): all verification of
    every fix above happens through `make e2e-docker` itself — the clean-room Podman/Docker
    container — never against the real dev-box `~/.claude`/`~/.cursor`/`~/.config/opencode`/
    `~/.codex`. Run `make e2e-docker` for real (Podman is the engine available on this machine, per
    the Makefile's docker/podman auto-detect) and let it run to completion. Capture the full
    output, including the new preflight line. This is Success Criterion 2 — it must be evidenced
    with real command output and exit code in this plan's SUMMARY.md, not claimed as "should pass
    now".
  </action>
  <verify>
    <automated>python3 -c "
import fnmatch
patterns = [l.strip() for l in open('.dockerignore') if l.strip() and not l.strip().startswith('#')]
tests = ['.env', '.env.production', 'secret.pem', 'id_rsa.key']
missing = [t for t in tests if not any(fnmatch.fnmatch(t, p) for p in patterns)]
assert not missing, f'.dockerignore does not exclude: {missing}'
print('dockerignore-secrets: PASS')
" && grep -q ".claude" tests/e2e/docker/run.sh && grep -q ".cursor" tests/e2e/docker/run.sh && grep -q ".config/opencode" tests/e2e/docker/run.sh && grep -q ".codex" tests/e2e/docker/run.sh && grep -qi "FATAL" tests/e2e/docker/run.sh && make e2e-docker</automated>
  </verify>
  <done>
    The stdlib fnmatch probe prints "dockerignore-secrets: PASS" (the new `.dockerignore` patterns
    genuinely cover credential-shaped filenames, not just a literal-string coincidence); `run.sh`
    contains the new preflight block referencing all four AI-CLI config dirs and a `FATAL` abort
    path; and `make e2e-docker` exits 0: the preflight step prints its check with no abort, the
    `uv sync` step completes, `make test` reports its full unittest run (all `tests.test_*` modules
    plus `tests/test_install.sh`) with zero failures, and `make lint` completes with no errors —
    all inside the container, printed as "==> ai-kit clean-room E2E: PASS" by `run.sh`'s final
    line.
  </done>
  <reversibility rating="reversible">All four changes (an added apt package, two new `.dockerignore` patterns, a preflight assertion block, and a widened test-timeout constant) can be reverted or re-tuned later without any downstream impact; `drive_until` already polls rather than sleeping, so the wider ceiling has no cost on a fast host, and the preflight check only ever fires on a genuine clean-room violation.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Formally amend ROADMAP.md Phase 1.1 Success Criterion 4 — grounded in this phase's own convergence evidence</name>
  <files>.planning/ROADMAP.md</files>
  <action>
    Per RESEARCH.md Pitfall 3 (live-verified by reading `gsd-core`'s `workflows/plan-phase.md` in
    full: `workflow.plan_review_convergence` is read there only inside REVISION_CONFLICT handling,
    never to auto-invoke the convergence loop; the loop itself only runs via the explicit
    `/gsd-plan-review-convergence <phase>` command or `/gsd-autonomous --converge`/`--cross-ai`,
    per `workflows/plan-review-convergence.md`'s own Config Gate and `workflows/autonomous.md`'s
    `--converge` routing) and per D-02 (plan-review convergence is mandatory for every `PLAN.md`
    in this milestone — this criterion exists to prove that mechanism actually fires): the current
    wording of `.planning/ROADMAP.md`'s Phase 1.1 Success Criterion 4 (see line 51 as currently
    committed) attributes the convergence-triggering effect to a bare `/gsd-plan-phase 1.2`
    invocation on its own — a claim `gsd-core`'s actual behavior cannot satisfy, since that bare
    invocation never triggers convergence, regardless of config.

    Per `01.1-REVIEWS.md` [d896183] HIGH concerns 1 and 2: a prior draft of this amendment fixed
    the "bare `/gsd-plan-phase`" problem but introduced a new one — it proposed verifying this
    criterion by invoking the convergence loop against Phase 1.2, which cannot legitimately run
    yet (Phase 1.2 has no `PLAN.md`), and doing so before Phase 1.1 has closed directly contradicts
    this same `ROADMAP.md`'s own rule that Phase 1.1 must complete before Phase 1.2 is planned. The
    prior draft's own verify step also only grepped the amended prose itself, never exercising any
    real evidence that the convergence mechanism had actually fired for a real plan. Both problems
    are fixed the same way: ground the proof entirely within Phase 1.1's own scope, using evidence
    that already exists — this very phase's plan went through a real, live convergence run this
    session (the primary `router-env/my-plan-review` route was probed and found unreachable twice,
    the Rule 2 fallback to `openai/gpt-5.6-sol` engaged and succeeded), and that run produced
    `.planning/phases/01.1-autonomous-run-infrastructure-inserted/01.1-REVIEWS.md` — a real
    artifact with real frontmatter (`reviewed_at`, `plans_reviewed`, `model_sources` naming the
    fallback route) and a real per-reviewer body (Strengths, Concerns, Risk Assessment) that no
    bare `/gsd-plan-phase` invocation could ever produce. That artifact is the live-in-scope proof
    this criterion needs.

    Follow this project's own established dated-amendment pattern (the same shape as the existing
    "(amended 2026-09-08, see `05-CONTEXT.md` D-08/D-09/D-10)" notes already in this file, and the
    "Phase Numbering" note at the top of the Phases section that documents D-01's own renumbering)
    — per ONESHOT-RULES.md Rule 4, this requires a formal, dated amendment, not a silent
    reinterpretation. Using `Edit` (scoped replacement, never a full-file `Write`), replace the
    existing Success Criterion 4 line with wording that: (1) keeps `workflow.plan_review_
    convergence: true` as a stated config fact; (2) names the real entry points generically
    (`/gsd-plan-review-convergence <phase>` or `/gsd-autonomous --converge`/`--cross-ai`) as what
    actually fires the mechanism when invoked; (3) cites `.planning/phases/01.1-autonomous-run-
    infrastructure-inserted/01.1-REVIEWS.md` by its full path as the concrete, already-completed,
    in-scope proof — a real convergence run against this phase's own `01.1-01-PLAN.md`, including
    the live-probed primary-route failure and the successful Rule 2 fallback; (4) states plainly
    that a bare `/gsd-plan-phase` invocation never triggers it; and (5) appends a dated note
    "(amended 2026-09-09, see `01.1-CONTEXT.md` D-02 and `01.1-RESEARCH.md` Pitfall 3 for why a
    bare `/gsd-plan-phase` invocation never auto-invokes convergence; re-amended same day per
    `01.1-REVIEWS.md` [d896183] HIGH concerns 1-2, which found the first amendment's own proposed
    verification action would itself require invoking convergence against a not-yet-planned Phase
    1.2 before Phase 1.1 closes — this wording instead grounds the proof in Phase 1.1's own
    already-completed convergence run)". Do not touch any other Success Criterion or any other
    Phase's section.
  </action>
  <verify>
    <automated>grep -q "01.1-REVIEWS.md" .planning/ROADMAP.md && grep -q "amended 2026-09-09" .planning/ROADMAP.md && grep -q "d896183" .planning/ROADMAP.md && ! grep -qF '4. Running `/gsd-plan-phase 1.2` after this phase closes visibly triggers plan-review convergence (not silently skipped).' .planning/ROADMAP.md && test -f .planning/phases/01.1-autonomous-run-infrastructure-inserted/01.1-REVIEWS.md && grep -q "^reviewed_at:" .planning/phases/01.1-autonomous-run-infrastructure-inserted/01.1-REVIEWS.md && grep -q "^plans_reviewed:" .planning/phases/01.1-autonomous-run-infrastructure-inserted/01.1-REVIEWS.md && grep -qi "fallback" .planning/phases/01.1-autonomous-run-infrastructure-inserted/01.1-REVIEWS.md && grep -qi "Risk Assessment" .planning/phases/01.1-autonomous-run-infrastructure-inserted/01.1-REVIEWS.md && echo PASS</automated>
  </verify>
  <done>
    `.planning/ROADMAP.md`'s Phase 1.1 Success Criterion 4 names the real, generic entry points
    (`/gsd-plan-review-convergence <phase>` / `/gsd-autonomous --converge`), cites
    `01.1-REVIEWS.md` by full path as the concrete in-scope proof, carries a dated amendment note
    citing `01.1-CONTEXT.md`/`01.1-RESEARCH.md`/`01.1-REVIEWS.md` [d896183], the old unverifiable
    wording is gone, and `01.1-REVIEWS.md` itself is confirmed (by this task's own verify) to carry
    real structural evidence of a live convergence run — not a stub.
  </done>
  <reversibility rating="reversible">A ROADMAP wording amendment can itself be further amended later using the same dated-note pattern if gsd-core's behavior changes (RESEARCH.md's own "Valid until" note flags this) or if a future phase wants to add its own convergence-run citation once it closes.</reversibility>
</task>

<task type="auto">
  <name>Task 3: Fix stale ONESHOT wording + state DRAFT-banner disposition, document D-09 fixture convention (content-verified), and confirm SC-1 with honest transient-reachability framing</name>
  <files>.planning/ONESHOT-RULES.md, .planning/ONESHOT-AUTONOMOUS.md, tests/e2e/docker/fixtures/README.md, .planning/ROADMAP.md</files>
  <action>
    Per D-09 (fixture depth deferred to the phases that need it): create the
    `tests/e2e/docker/fixtures/` directory containing only a `README.md` that documents the
    shared convention — no runtime-specific fixture file (no fake `~/.claude/settings.json`,
    `~/.cursor/hooks.json`, `opencode.jsonc`, or Codex TOML) is added by this phase. The README
    must state: (1) `tests/e2e/docker/Dockerfile`'s existing `COPY --chown=aikit:aikit .
    /home/aikit/ai-kit` already bakes any file under this directory into the clean-room image at
    build time — never add a `podman run -v` volume mount for a fixture, and never volume-mount
    any of uz's real `~/.claude`/`~/.cursor`/`~/.config/opencode`/`~/.codex` (that would defeat
    the harness's entire purpose, per ONESHOT-RULES.md Rules 5/6); (2) the actual pattern for
    using a fixture is `shutil.copy`-into-a-scratch-tree, following the existing precedent in
    `tests/test_system_memory_e2e.py:210-224` (copying from a fixed repo-relative path into a
    `tempfile`-rooted scratch tree before pointing `CLAUDE_CONFIG_DIR`/`AI_KIT_DIR`/
    `XDG_CONFIG_HOME` at it) and `tests/test_setup.py:44-48`'s `FIXTURE_DIR =
    os.path.join(os.path.dirname(__file__), "fixtures")` constant convention — never a live
    in-place reference; (3) a short "Adding a fixture (future phases)" section naming Phase 2
    (`opencode.jsonc`), Phase 3 (Claude `settings.json` + Cursor `hooks.json`), and Phase 4 (Codex
    TOML) as the phases that will each add their own fixture under
    `tests/e2e/docker/fixtures/<runtime>/<file>`, grounded in that phase's own research per D-09
    — not guessed at now. Per `01.1-REVIEWS.md` [d896183] MEDIUM concern 7 (the README's
    verification was only `test -f`, so an empty or stub README would have passed), write the
    README so it genuinely contains all three required elements above in prose the executor's
    own verify step below can grep for — do not satisfy this task by creating a near-empty file.

    Then, per this phase's Success Criteria 1 and 3 (already satisfied by prior-session work —
    commits `5c00e2a`, `6161b81` — do not redo or re-author any of it, but do not overclaim it
    either): confirm `.planning/config.json` still carries `workflow.plan_review_convergence:
    true`, `workflow.cross_ai_execution: true`, `workflow.cross_ai_command: "opencode run --model
    router-env/my-coding"` (D-02/D-04), and `review.default_reviewers: ["opencode"]` /
    `review.models: {"opencode": "router-env/my-plan-review"}` (D-02/D-06). Per `01.1-REVIEWS.md`
    HIGH concern 3, record this confirmation as what it actually is — config values are correct
    (a static check), not a fresh live-reachability proof for this exact moment: a configured
    route records intent, and reachability is inherently transient, as this very phase's own
    cross-AI plan review demonstrated first-hand (the primary `router-env/my-plan-review` route
    returned "Service temporarily unavailable due to resource pressure" twice this cycle, and the
    Rule 2 fallback to `openai/gpt-5.6-sol` engaged and succeeded — proving the fallback contract
    itself works end-to-end, which is the property that actually matters when a primary route is
    transiently down). State this framing explicitly in SUMMARY.md rather than asserting
    unconditional live reachability.

    Per `01.1-REVIEWS.md` [77fd340] cycle 2 New Concerns (MEDIUM — a second, independent instance
    of the same class of gap Task 2 already closes for Success Criterion 4): the confirmation above
    records the transient-reachability framing in `SUMMARY.md`, but `.planning/ROADMAP.md`'s own
    Phase 1.1 Success Criterion 1 sentence (line 48 as currently committed) — the document an
    autonomous run is told to trust as ground truth for "done", per this same plan's Threat
    Register row T-01.1-04 — is left unedited and still commits to an unconditional-availability
    claim for both configured cross-AI routes, with no caveat that such reachability is inherently
    transient. This is now doubly material: cycle 2's own manual review (see the Review
    Dispositions Ledger below) found BOTH the primary `router-env/my-plan-review` route AND the
    Rule 2 fallback `openai/gpt-5.6-sol` route down simultaneously — a second, independent data
    point beyond cycle 1's single primary-route failure, confirming the caveat is not a one-time
    fluke. Using `Edit` (scoped replacement, never a full-file `Write`), replace
    `.planning/ROADMAP.md`'s existing Success Criterion 1 line with wording that: (1) keeps the
    three config-fact clauses (`workflow.plan_review_convergence: true`, `review.default_
    reviewers`/`review.models`, and `workflow.cross_ai_execution: true` with `cross_ai_command`)
    but swaps the prior unconditional-availability adjective for a "correctly-configured opencode
    route" framing on both route mentions; (2) states, following the same config-correctness-
    plus-documented-fallback pattern Task 2 already applies to Success Criterion 4, that this
    criterion verifies config correctness plus a documented, exercised Rule 2 fallback — never
    perpetual live availability, since route reachability is inherently transient; (3) cites
    `.planning/phases/01.1-autonomous-run-infrastructure-inserted/01.1-REVIEWS.md` by full path as
    the dated evidence, naming both cycle 1 (the primary route unreachable twice, Rule 2 fallback
    to `openai/gpt-5.6-sol` engaging and succeeding) and cycle 2 (the primary route AND the Rule 2
    fallback route unreachable simultaneously, triggering the Rule 7 circuit-breaker manual-review
    path); and (4) appends a dated note "(amended 2026-09-09, see `01.1-REVIEWS.md` [77fd340]
    cycle 2 New Concerns.)" Do not touch any other Success Criterion or any other Phase's section —
    this is the same narrow, scoped-`Edit` discipline Task 2 already applies to Success Criterion 4.

    Per `01.1-REVIEWS.md` HIGH concern 4: `.planning/ONESHOT-RULES.md`'s Per-Phase Checklist item 1
    (`.planning/ONESHOT-RULES.md:30`) currently names, as the phases that "already have" a
    `{N}-CONTEXT.md` from prior sessions, a bare phase-range integer pair that predates the D-01
    renumber (original Phase 1 split into 1.1 and 1.2) — this is a second stale phase-numbering
    reference that the earlier "Phase 0.1"→"Phase 1.1" fix (commit 6161b81) did not touch, since
    it was never a `0.1` string to begin with. Using `Edit`, correct that bare range so it reads as
    `1.2-5` (Phase 1.1 is this phase itself, discussed inline rather than carried over from a prior
    session, so it should not be counted among the phases that "already have" one). Before editing,
    grep both `.planning/ONESHOT-RULES.md` and `.planning/ONESHOT-AUTONOMOUS.md` for any further
    stale bare-integer phase-range wording beyond this one instance (for example a phrase naming
    "Phase 1" immediately followed by "through" or "to" rather than ".1"/".2") — this phase's own
    research found no other instance, but if the executor's own grep surfaces one, fix it with the
    same dated discipline rather than skipping it silently.

    Per `01.1-REVIEWS.md` HIGH concern 4 (continued — the DRAFT banners): both
    `.planning/ONESHOT-AUTONOMOUS.md` and `.planning/ONESHOT-RULES.md` currently open with a bolded
    "DRAFT — not yet reviewed or run" sentence as the first sentence of their opening paragraph.
    This phase's own execution is the first real exercise of two of the playbook's rules — Rule 2
    (plan-review convergence, exercised for real by this very plan producing `01.1-REVIEWS.md`) and
    Rule 6 (the clean-room E2E gate, exercised for real by Task 1's `make e2e-docker` run) — while
    Phases 1.2 through 5 have not yet been run under this playbook at all. This is the planner's
    explicit, stated disposition (per the reviewer's own framing that this must be a stated
    decision, not silence): the blanket DRAFT label is imprecise rather than simply wrong, so
    replace it with a precise statement instead of either leaving it unchanged or silently deleting
    it. Using `Edit` in BOTH files, replace only that bolded opening sentence (leave the rest of
    the paragraph that follows it — the "General, phase-agnostic execution rules..." /
    "Copy-paste driver for a single autonomous run..." text — untouched) with: "**Phase 1.1 has
    exercised this playbook's Rule 2 (plan-review convergence) and Rule 6 (clean-room E2E gate) for
    real while closing itself — see `01.1-REVIEWS.md` and this phase's own `make e2e-docker` run.
    Phases 1.2 through 5 have not yet run under this playbook; treat those sections as
    reviewed-but-unexercised until each phase actually closes.**" Use the identical sentence in
    both files.

    Record all of the above — the fixture README's content, the SC-1 transient-reachability
    framing, the SC-3 stale-wording fix, and the DRAFT-banner disposition — in this plan's
    SUMMARY.md.
  </action>
  <verify>
    <automated>test -f tests/e2e/docker/fixtures/README.md && grep -qi "never" tests/e2e/docker/fixtures/README.md && grep -qi "volume mount" tests/e2e/docker/fixtures/README.md && grep -q "shutil.copy" tests/e2e/docker/fixtures/README.md && grep -qi "scratch" tests/e2e/docker/fixtures/README.md && grep -q "Phase 2" tests/e2e/docker/fixtures/README.md && grep -q "Phase 3" tests/e2e/docker/fixtures/README.md && grep -q "Phase 4" tests/e2e/docker/fixtures/README.md && grep -q '"plan_review_convergence": true' .planning/config.json && grep -q '"cross_ai_execution": true' .planning/config.json && grep -q '"cross_ai_command": "opencode run --model router-env/my-coding"' .planning/config.json && grep -q '"default_reviewers": \["opencode"\]' .planning/config.json && grep -q '"opencode": "router-env/my-plan-review"' .planning/config.json && ! grep -q "Phase 0\.1" .planning/ONESHOT-AUTONOMOUS.md .planning/ONESHOT-RULES.md && grep -q "Phases 1.2-5 already have" .planning/ONESHOT-RULES.md && grep -q "Rule 2 (plan-review convergence) and Rule 6 (clean-room E2E gate) for real" .planning/ONESHOT-AUTONOMOUS.md && grep -q "Rule 2 (plan-review convergence) and Rule 6 (clean-room E2E gate) for real" .planning/ONESHOT-RULES.md && grep -Fq "workflow.plan_review_convergence: true" .planning/ROADMAP.md && grep -q "correctly-configured opencode route" .planning/ROADMAP.md && grep -q "inherently transient" .planning/ROADMAP.md && grep -qi "circuit-breaker" .planning/ROADMAP.md && grep -q "77fd340" .planning/ROADMAP.md && echo PASS</automated>
  </verify>
  <done>
    `tests/e2e/docker/fixtures/README.md` exists and its content is verified (not just its
    existence) to document the no-volume-mount warning, the copy-into-scratch-tree convention, and
    the Phase 2/3/4 future-fixture list; `.planning/config.json`'s `plan_review_convergence`,
    `cross_ai_execution`, `cross_ai_command`, `review.default_reviewers`, and `review.models` keys
    are confirmed already correct (Success Criterion 1) with the transient-reachability framing
    recorded in SUMMARY.md AND landed verbatim in `.planning/ROADMAP.md`'s own Success Criterion 1
    sentence itself — "correctly-configured opencode route" replacing the prior unconditional-
    availability claim, with a dated amendment note citing `01.1-REVIEWS.md` [77fd340] cycle 2 New
    Concerns — rather than an unconditional-availability claim confined to SUMMARY.md alone;
    `ONESHOT-AUTONOMOUS.md`/`ONESHOT-RULES.md` are confirmed free of stale "Phase 0.1" references,
    the "Phases 1-5" reference is corrected to "Phases 1.2-5", and both files' DRAFT banners carry
    the same stated, accurate disposition sentence (Success Criterion 3) — all recorded in
    SUMMARY.md.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Host dev machine -> container build context | `tests/e2e/docker/Dockerfile`'s `COPY --chown=aikit:aikit . /home/aikit/ai-kit` copies the full repo (minus `.dockerignore` exclusions) into the clean-room image this plan builds and runs |
| Committed `.planning/config.json` -> shell execution | `workflow.cross_ai_command`/`cross_ai_timeout` are read by `gsd-core` and executed as a real shell command during later phases' plan execution — unmodified by this plan, but this plan's Task 3 re-confirms the values |
| `.planning/ROADMAP.md` wording -> autonomous-run agent behavior | An autonomous `/gsd-autonomous` run agent reads and trusts ROADMAP.md's Success Criteria text as ground truth for what "done" means — Task 2 corrects a wording gap that could otherwise mislead that trust |
| `tests/e2e/docker/run.sh` "clean-room" claim -> autonomous-run agent trust | The playbook and ROADMAP.md both assert the container is genuinely fresh; Task 1 turns that assertion into a checked precondition instead of an assumed one |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01.1-01 | Tampering | `tests/e2e/docker/Dockerfile` apt-get install list (Task 1) | low | accept | Adding `make` is a Debian-archive OS package installed via the same already-trusted `apt-get` call as the existing `bash git curl ca-certificates shellcheck` packages — no new trust tier introduced. Package Legitimacy Gate is explicitly N/A per RESEARCH.md (npm/pip/cargo ecosystems only). |
| T-01.1-02 | Information Disclosure | Dockerfile `COPY --chown=aikit:aikit . /home/aikit/ai-kit` build context (Task 1) | medium | mitigate | `01.1-REVIEWS.md` [d896183] HIGH concern 5: git-hygiene alone was the only stated control, but an untracked secret file (never committed, so git-hygiene never sees it) could still enter the build context. Task 1 adds explicit `.env`/`.env.*`/`*.pem`/`*.key` patterns to `.dockerignore`, verified this task by a stdlib `fnmatch` probe against representative secret-shaped filenames, closing the gap rather than merely accepting it. |
| T-01.1-03 | Tampering | `.planning/config.json`'s `cross_ai_command`/`cross_ai_timeout`, executed as a shell command by `gsd-core` (verified, not modified, by Task 3) | medium | accept | Same trust boundary as any command already in a committed `Makefile` — accepted project-level risk per RESEARCH.md Security Domain; unchanged by this plan. |
| T-01.1-04 | Tampering (documentation / process-trust integrity) | `.planning/ROADMAP.md` Phase 1.1 Success Criterion 4 wording (Task 2) | medium | mitigate | Task 2 formally amends the criterion to cite `01.1-REVIEWS.md` as the real, in-scope, already-completed proof that convergence fires — per `01.1-REVIEWS.md` HIGH concerns 1-2, deliberately not a Phase-1.2-targeting invocation that would itself violate this file's own phase-ordering rule — per `ONESHOT-RULES.md` Rule 4's dated-amendment pattern. Prevents a future autonomous-run agent from acting on either a false "already fires" assumption or an unrunnable verification instruction. |
| T-01.1-05 | Denial of Service (test-suite reliability, not a security-critical asset) | `tests/test_wizard_pty.py` `TestPhase2E2E.test_pick_skill_and_confirm_creates_symlink`'s `_SUMMARY_DEADLINE` (Task 1) | low | accept | Blanket increase 30s -> 60s trades a small risk of masking a genuine future performance regression (RESEARCH.md Assumption A1) for eliminating a reproducible false-failure under constrained container CPU; `drive_until` polls via `select()` so the wider ceiling costs nothing on a fast host. |
| T-01.1-06 | Spoofing (a stale or pre-populated container could present itself as clean-room when it isn't) | `tests/e2e/docker/run.sh`'s "genuinely fresh" claim (Task 1) | medium | mitigate | `01.1-REVIEWS.md` MEDIUM concern 6: the claim was asserted in comments/docs but never checked at runtime. Task 1 adds a preflight block to `run.sh` that fails loudly (exit 1) if any of `~/.claude`/`~/.cursor`/`~/.config/opencode`/`~/.codex` exists before any test runs — turning an assumed guarantee into a checked one on every single invocation, independent of Docker/Podman's own layer-cache behavior for unrelated `RUN` steps. |

</threat_model>

<verification>
Run, in order, after all three tasks complete:
1. `make e2e-docker` — full clean-room rebuild + preflight assertion + `make test` + `make lint`,
   expect exit 0 (Success Criterion 2, evidenced by Task 1's own verify, including the stdlib
   fnmatch `.dockerignore` probe and the `run.sh` preflight-block grep checks).
2. `grep -n "01.1-REVIEWS.md\|amended 2026-09-09" .planning/ROADMAP.md` — confirm the amended
   Success Criterion 4 wording is present and cites the real, in-scope proof artifact (Success
   Criterion 4), then confirm `01.1-REVIEWS.md` itself carries real structural evidence of a live
   convergence run (Task 2's own verify).
3. `test -f tests/e2e/docker/fixtures/README.md` plus the content-grep checks, the
   `.planning/config.json`/`ONESHOT-*.md` checks, and the `.planning/ROADMAP.md` Success Criterion
   1 caveat checks from Task 3's verify — confirm Success Criteria 1 and 3 remain true (with SC-1
   framed, in `ROADMAP.md`'s own Success Criterion 1 sentence and not just SUMMARY.md, as
   config-correctness-plus-documented-fallback citing `01.1-REVIEWS.md` [77fd340] cycle 2, not
   unconditional live reachability), the fixture README's required content is present (not just
   the file), and both `ONESHOT-*.md` files carry the corrected phase range and the stated DRAFT
   disposition.

All four of Phase 1.1's ROADMAP.md Success Criteria are evidenced by real command output or a
real, already-existing artifact captured in this plan's SUMMARY.md — no criterion is marked done
on a self-reported claim (per `.planning/ONESHOT-RULES.md` Non-Negotiable Rule 1), and no
criterion's proof requires an action (like invoking convergence against an unplanned phase) that
would itself violate this project's own rules.
</verification>

<success_criteria>
- `make e2e-docker` builds and passes `make test` + `make lint` inside a genuinely fresh
  container — the absence of the four AI-CLI config dirs is asserted by `run.sh`'s new preflight
  check, not assumed — with a build context hardened against credential-shaped files (Success
  Criterion 2).
- `.planning/ROADMAP.md`'s Phase 1.1 Success Criterion 4 names the real convergence entry points
  and cites this phase's own `01.1-REVIEWS.md` as in-scope, already-completed proof, with a dated
  amendment note (Success Criterion 4).
- `tests/e2e/docker/fixtures/README.md` documents the D-09 copy-into-scratch-tree convention, with
  its required content verified by grep, and no runtime-specific fixture files added yet.
- Success Criteria 1 and 3 are independently re-confirmed true (not re-implemented), with Success
  Criterion 1 framed honestly as config-correctness-plus-documented-fallback — landed in
  `.planning/ROADMAP.md`'s own Success Criterion 1 sentence itself, with a dated amendment note
  citing `01.1-REVIEWS.md` [77fd340] cycle 2, not merely narrated in SUMMARY.md — rather than
  unconditional live reachability, and Success Criterion 3's ONESHOT-*.md files corrected for both
  the "Phase 0.1" and the "Phases 1-5" stale references, with a stated DRAFT-banner disposition.
- No task in this plan mutated uz's real `~/.claude`, `~/.cursor`, `~/.config/opencode`, or
  `~/.codex` (D-07) — all verification ran inside the `make e2e-docker` container or read-only
  config inspection.
</success_criteria>

## Review Dispositions Ledger

### Round 1 — d896183

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|----------------|
| Success Criterion 4 rewritten but never behaviorally verified — Task 2's verify only greps amended roadmap prose | HIGH | Task 2: verify now also checks `01.1-REVIEWS.md`'s real structural markers (`reviewed_at`, `plans_reviewed`, fallback mention, Risk Assessment section) — the actual artifact produced by a real convergence run, not just the amended sentence. |
| Proposed Success Criterion 4 creates a phase-ordering conflict — verifying it would require invoking convergence against Phase 1.2 before Phase 1.1 closes | HIGH | Task 2: new wording cites Phase 1.1's own already-completed `01.1-REVIEWS.md` as the proof; the generic entry-point description no longer names a Phase-1.2-targeting invocation as the verification action. |
| Success Criterion 1 checked as static config only, not live reachability — this review's own probe found the primary plan-review route down | HIGH | Task 3: SC-1 confirmation reframed as config-correctness-plus-documented-fallback; SUMMARY.md must record the transient primary-route failure and the successful Rule 2 fallback rather than claiming unconditional live reachability. |
| Success Criterion 3 falsely treated as already satisfied — `ONESHOT-RULES.md` still says "Phases 1-5", and both `ONESHOT-*.md` files are still labeled DRAFT | HIGH | Task 3: fixes the "Phases 1-5" → "Phases 1.2-5" stale reference (with a scan for any further instance), and replaces both files' DRAFT banner with a stated, accurate disposition instead of leaving it silently unchanged. |
| Threat model understates Docker build-context secret-disclosure risk — `.dockerignore` doesn't exclude `.env`/credential files | HIGH | Task 1: adds `.env`/`.env.*`/`*.pem`/`*.key` to `.dockerignore`, verified by a stdlib `fnmatch` probe against representative secret-shaped filenames; threat register row T-01.1-02 changed from accept to mitigate. |
| "Genuinely fresh" container claim asserted, not checked — `run.sh` never asserts absence of the four AI-CLI config dirs; Makefile builds without `--no-cache` | MEDIUM | Task 1: adds a `run.sh` preflight block that fails loudly if any of the four dirs exists; deliberately keeps normal layer caching (not `--no-cache`) with an explicit stated rationale rather than silently narrowing the claim. New threat register row T-01.1-06. |
| Fixture README verification is only `test -f`, not content-checked | MEDIUM | Task 3: verify now greps for the no-volume-mount warning, the `shutil.copy` pattern, the "scratch" tree language, and the Phase 2/3/4 future-fixture list — an empty or stub README now fails verification. |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| gsd-core mechanism claims (convergence gating, execution delegation gating) could not be independently verified by this review lane — its sandbox could not reach `gsd-core`'s own source outside this repository | Not actionable within this plan: `01.1-RESEARCH.md`'s claims about `gsd-core`'s `workflows/plan-phase.md`/`plan-review-convergence.md`/`autonomous.md` are already backed by direct, full-file reads with line citations from this session (the strongest evidence obtainable without vendoring `gsd-core` source into this repository, which is out of this phase's scope). No plan action changes this; future re-verification is already flagged in `01.1-RESEARCH.md`'s own "Valid until" note if `gsd-core` is updated. |

### Round 2 — 77fd340

**Review context (recorded transparently, not hidden):** both configured cross-AI routes were
unreachable this cycle. The primary `opencode run --model router-env/my-plan-review` route was
probed directly and returned "Service temporarily unavailable due to resource pressure" on two
separate attempts. The Rule 2 fallback `opencode run --model openai/gpt-5.6-sol --variant high`
was then also probed directly (once via a full prompt-file run, once via a short probe roughly 20
minutes later) and both times returned "The usage limit has been reached." Per
`.planning/ONESHOT-RULES.md` Rule 7, this is the named circuit-breaker condition (a route AND its
Rule 2 fallback both unreachable). Per this review task's own explicit last-resort authorization,
cycle 2's review was therefore performed as a manual verification pass instead of a fresh
cross-AI-dispatched review: the orchestrating Claude Code session read `01.1-01-PLAN.md` directly
against the live repository and every file it references, and wrote `01.1-REVIEWS.md`'s
`claude-manual-fallback` section itself, per Rule 7's own named last-resort path — not a
substitution of one configured model for another, and no `.planning/config.json` value was
changed to effect it. This is itself a second, independent data point (beyond cycle 1's single
primary-route failure) that a correctly-configured cross-AI route can be transiently unavailable —
exactly the caveat this round's fix below adds to `.planning/ROADMAP.md` itself.

Cycle 2 confirmed four of Round 1's five HIGH concerns and both MEDIUM concerns fully and
verifiably resolved by the Round 1 revision (see `01.1-REVIEWS.md` [77fd340]'s "Round 1
Disposition Verification" section for the file-by-file confirmation), and raised one further
actionable MEDIUM concern, addressed below.

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|----------------|
| `.planning/ROADMAP.md`'s own Success Criterion 1 sentence still asserts an uncaveated unconditional-availability claim for both cross-AI routes — the Round 1 fix applied the config-correctness-plus-documented-fallback caveat to `SUMMARY.md` only, never to the authoritative `ROADMAP.md` text an autonomous run is told to trust | MEDIUM | Task 3: `.planning/ROADMAP.md` added to `files_modified` and to Task 3's `<files>`; Task 3's action now amends SC1's sentence with the same config-correctness-plus-documented-fallback pattern already used for SC4, citing `01.1-REVIEWS.md` (both cycle 1's and cycle 2's route outages as dated evidence) and a dated amendment note; Task 3's verify now greps `.planning/ROADMAP.md` for the landed caveat markers (`correctly-configured opencode route`, `inherently transient`, `circuit-breaker`, `77fd340`). |

### Review Feedback Deferred
No concern deferred this round — the single actionable finding is fully addressed above. The
review's three non-blocking "Suggestions" are substantially satisfied without a separate action:
placing the SC1 fix in Task 3 (rather than folding it into Task 2 alongside SC4, which the review
itself offered only as one of two options) keeps Task 2 scoped to SC4 and sits next to Task 3's
pre-existing SC1 confirmation paragraph; noting the Rule 7 circuit-breaker hit and MEDIUM-6's
transparency-only framing in `SUMMARY.md` was already required by this plan's `<output>`
instructions before this round and remains so, now extended to cover cycle 2's circuit-breaker hit
too.

<output>
Create `.planning/phases/01.1-autonomous-run-infrastructure-inserted/01.1-01-SUMMARY.md` when done,
including: the full real `make e2e-docker` output (or its pass/fail tail plus exit code) as
evidence for Success Criterion 2, including the new preflight-block output line; the stated
rationale for not adding `--no-cache` to the `e2e-docker` Makefile target; the transient-
reachability framing for Success Criterion 1 (primary route failure + successful fallback in
cycle 1, and BOTH the primary route and the Rule 2 fallback route down simultaneously in cycle 2 —
not an unconditional live reachability claim), including confirmation that
`.planning/ROADMAP.md`'s own Success Criterion 1 sentence itself (not just this SUMMARY.md) now
carries that caveat, dated 2026-09-09 and citing `01.1-REVIEWS.md` [77fd340]; and confirmation
that the fixture README's required content (not just its existence) was checked.
</output>
