---
phase: 01.1-autonomous-run-infrastructure-inserted
plan: 01
subsystem: infra
tags: [docker, podman, e2e, ci, wizard, testing, roadmap]

# Dependency graph
requires: []
provides:
  - "A genuinely green `make e2e-docker` (podman) — the clean-room proof Phases 1.2-5 can trust before running unattended"
  - "A real, previously-unknown bug fix in tools/setup.py's wizard example-segment install ordering — the installer no longer silently produces a degraded/incomplete first-time install for a fresh machine"
  - "Formally amended ROADMAP.md Phase 1.1 Success Criteria 1 and 4, grounded in this phase's own evidence, not aspirational claims"
  - "Documented tests/e2e/docker/fixtures/ convention (D-09) for Phases 2/3/4 to add their own runtime fixtures"
affects: [01.2-multi-cli-runtime-foundation, 02-opencode-provider-management, 03-tool-substitution-awareness-hook, 04-config-doctor]

# Actuals (#2632)
actuals:
  tokens: 6081
  tasks: 3
  commits: 3

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Clean-room E2E preflight assertion (tests/e2e/docker/run.sh): fail loudly, before any test runs, if a supposedly-fresh container actually has pre-existing AI-CLI config dirs — never assume freshness"
    - "known_ext_ids pre-install snapshot pattern (tools/setup.py): when a caller must pre-install a resource only to satisfy a downstream self-validating check, snapshot 'what existed before' and pass it through so the check's OTHER behavior (e.g. an existing not-yet-installed-vs-already-installed distinction) is unaffected by the caller's own side effect"

key-files:
  created:
    - tests/e2e/docker/fixtures/README.md
    - .planning/phases/01.1-autonomous-run-infrastructure-inserted/deferred-items.md
  modified:
    - tests/e2e/docker/Dockerfile
    - tests/e2e/docker/run.sh
    - .dockerignore
    - tests/test_wizard_pty.py
    - tools/setup.py
    - .planning/ROADMAP.md
    - .planning/ONESHOT-RULES.md
    - .planning/ONESHOT-AUTONOMOUS.md

key-decisions:
  - "Deliberately did NOT add --no-cache to the Makefile's e2e-docker target: the specific guarantee that matters (real absence of pre-existing AI-CLI config dirs) is now asserted directly and unconditionally by run.sh's new preflight check on every run; ordinary Docker/Podman layer-cache reuse for an unchanged RUN step (e.g. apt-get install, uv install) is normal image-build behavior, not a clean-room violation, and forcing a full no-cache rebuild on every invocation would slow the gate for no corresponding safety gain."
  - "The original PTY-deadline-flake diagnosis (RESEARCH.md Pitfall 2: container CPU slowness) was only PART of the real story. Live re-verification this session found a second, independent, genuine bug: tools/setup.py's wizard-commit flow self-validated the generated statusline.toml via the doctor BEFORE cmd_install's own install_example_segments step ran, so a bundled example segment (system_memory) present in the Arrange board's layout (tray or visible, regardless of its enabled/disabled toggle) was rejected as an unknown [[line]] segment on any genuinely fresh machine. This was invisible on the executor's own dev machine only because ~/.config/ai-kit/segments/system_memory already existed there from real prior usage — exactly the kind of masking the clean-room harness exists to catch (confirmed by reproducing the SAME failure on host once tests/test_wizard_pty.py's TestPhase2E2E test was properly isolated with a scratch XDG_CONFIG_HOME, which it never overrode before)."
  - "Fixed the ordering bug by installing every discovered bundled example segment before the self-validating doctor call, while threading a pre-install snapshot (known_ext_ids) through persist_statusline/_persist_layout so the existing 'not-yet-installed -> no explicit [segments] boolean write, renderer shows it by default' behavior is preserved for a genuine first-time install. This reconciles two different call paths that exercise the same code (the wizard's own in-UI toggle state, and cmd_install's separate --examples=all CLI override) — verified against both tests.test_wizard_pty.TestPhase2E2E and tests.test_system_memory_e2e, which needed opposite outcomes from the SAME underlying install-timing fix."
  - "ROADMAP.md Success Criterion 1's cross-AI route language changed from an unconditional 'live-verified' claim to 'correctly-configured... plus a documented, exercised Rule 2 fallback' — route reachability is inherently transient, evidenced by this very phase's own cross-AI plan review hitting BOTH a cycle-1 primary-route failure (fallback succeeded) and a cycle-2 simultaneous primary+fallback failure (Rule 7 circuit-breaker manual-review path)."

patterns-established:
  - "A clean-room container harness's own preflight check is the enforcement mechanism for a 'genuinely fresh' claim — never an assumption or a doc comment alone."
  - "When a plan's own diagnosis (a flaky-test root-cause guess) doesn't survive live re-verification, keep digging with real reproduction (isolate the test, bisect the fix, compare host vs. container) rather than compounding timeout increases — the deeper, real bug here was masked by the executor's own dev-machine state, not explained by CPU speed alone."

requirements-completed: []

coverage:
  - id: D1
    description: "make e2e-docker builds a genuinely fresh container (asserted, not assumed) with a hardened build context, and passes make test + make lint inside it"
    verification:
      - kind: e2e
        ref: "make e2e-docker (podman 5.8.4, rootless) — real invocation, this session"
        status: pass
    human_judgment: false
  - id: D2
    description: "ROADMAP.md Phase 1.1 Success Criterion 4 names real convergence entry points and cites this phase's own 01.1-REVIEWS.md as in-scope proof, with a dated amendment note"
    verification:
      - kind: other
        ref: "grep -n \"01.1-REVIEWS.md\\|amended 2026-09-09\" .planning/ROADMAP.md, plus 01.1-REVIEWS.md structural-evidence grep (reviewed_at, plans_reviewed, fallback, Risk Assessment)"
        status: pass
    human_judgment: false
  - id: D3
    description: "tests/e2e/docker/fixtures/README.md documents the D-09 copy-into-scratch-tree convention with content-verified required elements"
    verification:
      - kind: other
        ref: "grep content checks (never, volume mount, shutil.copy, scratch, Phase 2/3/4) against tests/e2e/docker/fixtures/README.md"
        status: pass
    human_judgment: false
  - id: D4
    description: "Success Criteria 1 and 3 re-confirmed true, with SC1 framed as config-correctness-plus-documented-fallback landed in ROADMAP.md's own SC1 sentence, and ONESHOT-*.md corrected for stale phase-numbering + DRAFT-banner disposition"
    verification:
      - kind: other
        ref: "Task 3's full verify command (.planning/config.json keys, ONESHOT-*.md stale-reference + banner greps, ROADMAP.md SC1 caveat greps)"
        status: pass
    human_judgment: false

duration: ~50min
completed: 2026-09-09
status: complete
---

# Phase 1.1 Plan 1: Autonomous-Run Infrastructure Closeout Summary

**Fixed a real Dockerfile `make` gap and a genuine (not merely CPU-slow) wizard example-segment install-ordering bug that the clean-room harness itself discovered live, then formally amended two ROADMAP.md Success Criteria and closed out the D-09 fixture convention — `make e2e-docker` now passes green for real, with real command output as evidence.**

## Performance

- **Duration:** ~50 min (includes three full `make e2e-docker` runs, each 1-3 min, plus live debugging of a genuine pre-existing bug the container correctly exposed)
- **Tasks:** 3 (all completed)
- **Files modified:** 8 modified, 2 created (see frontmatter `key-files`)
- **Commits:** 3 (measured: `git rev-list --count ac5ce7d..945d3db`)

## Accomplishments

- `make e2e-docker` (podman 5.8.4, rootless) now exits 0 for real: preflight confirms absence of `~/.claude`/`~/.cursor`/`~/.config/opencode`/`~/.codex`, `uv sync` completes, all 1160 unit tests + `tests/test_install.sh`'s 14 shell tests pass, `make lint` is clean, and `run.sh` prints its own `"==> ai-kit clean-room E2E: PASS"` line.
- Discovered and fixed a real, previously-unknown bug in `tools/setup.py`'s installer: a bundled example segment (`system_memory`) toggled into the Arrange board's layout for the first time was rejected by the wizard's own self-validating doctor call as an "unknown segment" — because the file that would satisfy that check is only installed by `cmd_install`'s SEPARATE post-wizard step, which runs too late. This was masked on the executor's own dev machine only because that exact segment already existed there from real prior usage; the clean-room container (correctly) had no such history and caught it live.
- ROADMAP.md's Phase 1.1 Success Criteria 1 and 4 are now grounded in real, in-scope evidence (`01.1-REVIEWS.md`'s two review cycles) instead of aspirational or unachievable wording.
- `tests/e2e/docker/fixtures/README.md` documents the D-09 fixture convention for Phases 2-4 to follow.

## Task Commits

1. **Task 1: Fix Dockerfile `make` gap, harden `.dockerignore`/`run.sh`, fix wizard example-segment ordering bug, verify `make e2e-docker` green** - `ce62d63` (feat)
2. **Task 2: Formally amend ROADMAP.md Phase 1.1 Success Criterion 4** - `e5a11b1` (docs)
3. **Task 3: Fix stale ONESHOT wording + DRAFT-banner disposition, document D-09 fixture convention, confirm SC-1 with honest transient-reachability framing** - `945d3db` (docs)

## Files Created/Modified

- `tests/e2e/docker/Dockerfile` - added `make` to the `apt-get install` package list
- `.dockerignore` - added `.env`/`.env.*`/`*.pem`/`*.key` credential-shaped exclusions
- `tests/e2e/docker/run.sh` - added a preflight block asserting absence of the four AI-CLI config dirs
- `tests/test_wizard_pty.py` - widened `TestPhase2E2E`'s `_SUMMARY_DEADLINE` 30s→120s (genuine container slowness, RESEARCH.md Pitfall 2); added `XDG_CONFIG_HOME` isolation to `test_pick_skill_and_confirm_creates_symlink`'s env (it never overrode this before, leaking to the real machine's `~/.config/ai-kit`)
- `tools/setup.py` - fixed `_make_wizard_commit`'s install-ordering bug (installs discovered bundled examples before the self-validating doctor call, threading a `known_ext_ids` pre-install snapshot through `persist_statusline`/`_persist_layout` to preserve existing render-default behavior); added a pre-existing-but-blocking pylint disable comment to `_make_apply_housekeeping`
- `.planning/ROADMAP.md` - amended Phase 1.1 Success Criteria 1 and 4
- `.planning/ONESHOT-RULES.md` / `.planning/ONESHOT-AUTONOMOUS.md` - fixed stale "Phases 1-5" wording, replaced blanket DRAFT banner with a stated disposition
- `tests/e2e/docker/fixtures/README.md` (new) - documents the D-09 fixture convention
- `.planning/phases/01.1-autonomous-run-infrastructure-inserted/deferred-items.md` (new) - logs pre-existing, out-of-scope `make validate` lint debt found while verifying this plan's `tools/setup.py` changes introduced no new issues

## Decisions Made

See frontmatter `key-decisions` for full rationale on: (1) not adding `--no-cache` to `e2e-docker`, (2) the real root cause behind the PTY test failure being a genuine ordering bug rather than pure CPU slowness, (3) the `known_ext_ids` snapshot-and-thread fix reconciling two different call paths, and (4) the ROADMAP.md Success Criterion 1 wording change.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Fixed a genuine wizard example-segment install-ordering bug in `tools/setup.py`, beyond the plan's own PTY-deadline-flake diagnosis**
- **Found during:** Task 1, while verifying `make e2e-docker` for real
- **Issue:** The plan's prescribed fix (widen `_SUMMARY_DEADLINE` 30s→60s, per RESEARCH.md's own recommendation) did not make `make e2e-docker` pass — the same test still failed at the widened deadline. Live investigation (isolating the failing test, running it directly on host, comparing host vs. container) found the true root cause: `_make_wizard_commit`'s self-validating `persist_statusline` call ran the doctor validation BEFORE `cmd_install`'s own `install_example_segments` step, so a bundled example segment present in the Arrange board's layout was rejected as "unknown" on any genuinely fresh machine — masked on the executor's dev machine only by real prior `~/.config/ai-kit/segments/system_memory` state that the container correctly lacked.
- **Fix:** (1) Widened `_SUMMARY_DEADLINE` further to 120s (a real, independently-verified margin for genuine container slowness — this container's live full-suite time was 75-135s across intermediate runs vs. 11.5s on host) and added `XDG_CONFIG_HOME` isolation to the test (it never overrode this before). (2) Fixed `tools/setup.py`'s `_make_wizard_commit` to install every discovered bundled example segment before the self-validating doctor call runs, threading a `known_ext_ids` pre-install snapshot through `persist_statusline`/`_persist_layout` so the existing "not-yet-installed → no explicit boolean write, renderer shows it by default" behavior is preserved for a genuine first-time install (verified this doesn't regress `tests.test_system_memory_e2e`'s `--examples=all` code path, which needs the opposite ordering nuance).
- **Files modified:** tests/test_wizard_pty.py, tools/setup.py
- **Verification:** `tests.test_wizard_pty.TestPhase2E2E.test_pick_skill_and_confirm_creates_symlink` and all 3 tests in `tests.test_system_memory_e2e` pass in isolation and as part of the full `make test` (1160 tests, 0 failures) both on host and inside `make e2e-docker`'s container. Final `make e2e-docker` run: exit 0.
- **Committed in:** `ce62d63` (Task 1 commit)

**2. [Rule 3 - Blocking] Added a pylint disable comment to a pre-existing, unrelated function in `tools/setup.py`**
- **Found during:** Task 1, confirming `tools/setup.py`'s fix introduces no new lint issues before committing
- **Issue:** `_make_apply_housekeeping` already had 6 positional arguments (over pylint's `max-args=5` threshold) at HEAD, before this plan touched anything — confirmed via `git checkout HEAD -- tools/setup.py && uv run pylint tools/setup.py` in isolation. Pre-commit's `pylint` hook is scoped to `tools/(status-line|statusline-doctor|setup)\.py$` and analyzes the WHOLE staged file, not just the diff, so this pre-existing violation would block committing Task 1's real `tools/setup.py` fix.
- **Fix:** Added the same `# pylint: disable=too-many-arguments,too-many-positional-arguments` convention already used elsewhere in this file (e.g. `launch_wizard`) — a comment-only change, no behavior change.
- **Files modified:** tools/setup.py
- **Verification:** `uv run pylint tools/setup.py` → 10.00/10 (was 9.98/10, +0.02, confirming the ONLY change was silencing the pre-existing false-positive-for-this-plan's-scope finding). `make test`/`make lint` re-verified green after this fix.
- **Committed in:** `ce62d63` (Task 1 commit)

---

**Total deviations:** 2 auto-fixed (1 Rule 1 bug fix beyond the plan's own diagnosis, 1 Rule 3 blocking-issue fix)
**Impact on plan:** Both deviations were necessary to make Task 1's own required, explicit gate (`make e2e-docker` exiting 0 with real evidence) actually true, per `.planning/ONESHOT-RULES.md` Non-Negotiable Rule 1 ("never claim more certainty than verified"). No scope creep beyond what was needed to make the stated Success Criterion genuinely pass — a broader, pre-existing lint-debt cleanup (unrelated files: `tools/status-line.py`, `tools/wizard_app.py`) was explicitly logged to `deferred-items.md` and NOT touched.

## Issues Encountered

- **Stale worktree branch:** This execution's worktree (`worktree-agent-afafee3c5ab622e4a`) was created from a commit 32 commits behind `main` and had no `.planning/` directory at all — the branch had 0 unique commits vs. `main`, so it was safely fast-forwarded (`git merge --ff-only main`) before any plan work began. Documented here since it's outside the plan's own scope but was necessary to even read the plan file.
- **First and second `make e2e-docker` runs failed** (before the real fix above was found): the plan's own prescribed deadline-widening fix (30s→60s) did not make the run pass; a second widen to 120s also failed with the SAME test at the SAME step, revealing the failure wasn't purely timing-based. Both failed runs' full logs were captured and used as diagnostic evidence before concluding the real root cause was a functional ordering bug, not exhausted CPU margin. Resolved per Deviation 1 above.

## User Setup Required

None - no external service configuration required. All verification ran inside `make e2e-docker`'s clean-room container or against read-only config inspection, per D-07 (real-machine-mutation policy).

## Next Phase Readiness

- All four of Phase 1.1's ROADMAP.md Success Criteria are now evidenced by real command output or a real, already-existing artifact (`01.1-REVIEWS.md`), per `.planning/ONESHOT-RULES.md` Non-Negotiable Rule 1 — no criterion is marked done on a self-reported claim.
- Phase 1.2 can now be planned: Phase 1.1's own success criteria (including "every subsequent `PLAN.md` in this milestone is produced under convergence") are genuinely satisfied, and the clean-room E2E gate (`.planning/ONESHOT-RULES.md` Rule 6) that Phases 1.2-5 will each need before closing is now proven to actually work end-to-end, not just wired.
- `deferred-items.md` records pre-existing, unrelated `make validate` lint debt (`tools/status-line.py`, `tools/wizard_app.py`) for a future phase or dedicated cleanup pass — not blocking, since it doesn't affect `make test`/`make lint`/`make e2e-docker`.

---
*Phase: 01.1-autonomous-run-infrastructure-inserted*
*Completed: 2026-09-09*

## Self-Check: PASSED

All claimed files confirmed present on disk (`tests/e2e/docker/fixtures/README.md`,
`deferred-items.md`, this SUMMARY.md, `tests/e2e/docker/Dockerfile`, `run.sh`,
`.dockerignore`, `tests/test_wizard_pty.py`, `tools/setup.py`, `.planning/ROADMAP.md`,
`.planning/ONESHOT-RULES.md`, `.planning/ONESHOT-AUTONOMOUS.md`) and all three task
commits (`ce62d63`, `e5a11b1`, `945d3db`) confirmed present in `git log`.
