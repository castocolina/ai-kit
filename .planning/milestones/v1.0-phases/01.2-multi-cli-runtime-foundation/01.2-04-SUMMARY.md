---
phase: 01.2-multi-cli-runtime-foundation
plan: 04
subsystem: wizard+cli
tags: [native-runtime, wizard, skill-judge, detect-current-runtime]

# Dependency graph
requires:
  - "detection.detect_current_runtime(env=None) -> claude/opencode/cursor/codex/unknown, fixed check order, never cached"
  - "config_io.validate_reviewer_fields accepts/rejects native_runtime (functionally-reachable schema)"
provides:
  - "cli.py detect-current-runtime subcommand prints {native_runtime: <value>}, including unknown"
  - "ai-kit-spec-config Step 2.7 shows (running under: <value>) on the native-entry ask"
  - "ai-kit-spec-config Step 3 persists native_runtime on native entries only"
affects: [01.2-multi-cli-runtime-foundation]

# Actuals
actuals:
  tokens: unknown
  tasks: 2
  commits: 1

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "detect-current-runtime: zero-arg CLI subcommand wrapping detect_current_runtime(env_fn()), never TTL'd"
    - "SKILL.md Process routing layer + references/ for the 8-item CLI catalog procedure (MANDATORY load + native-only Do-NOT-load)"

key-files:
  created:
    - skills/ai-kit-spec-config/references/cli-model-selection.md
    - .planning/phases/01.2-multi-cli-runtime-foundation/01.2-04-SUMMARY.md
  modified:
    - skills/ai-kit-spec-review/ai_kit_spec/cli.py
    - skills/ai-kit-spec-config/SKILL.md
    - tests/test_ai_kit_spec.py
    - .planning/ROADMAP.md
    - .planning/phases/01.2-multi-cli-runtime-foundation/01.2-CONTEXT.md

key-decisions:
  - "SC3/D-08 'ranked candidate output' resolves to Step 2.7's native-entry ask, not Step 2.2's ranked CLI-candidate block — a native entry never goes through Step 2.2."
  - "unknown is displayed and persisted as the literal string unknown (D-04 applied to display, not just persistence)."
  - "Step 2.2's 8-item catalog procedure extracted to references/cli-model-selection.md so SKILL.md stays under the skill-judge D5 500-line bar (was 614)."

patterns-established:
  - "Wizard native-entry flow: live detect-current-runtime -> inline (running under: <value>) annotation -> native_runtime written only on cli-omitted reviewers."

requirements-completed:
  - REQ-multi-cli-runtime-detection

coverage:
  - id: D1
    description: "detect-current-runtime prints {native_runtime: claude} under CLAUDECODE=1 and {native_runtime: unknown} when no signal matches"
    requirement: REQ-multi-cli-runtime-detection
    verification:
      - kind: unit
        ref: "tests/test_ai_kit_spec.py::TestMainCli.test_detect_current_runtime_prints_json"
        status: pass
      - kind: unit
        ref: "tests/test_ai_kit_spec.py::TestMainCli.test_detect_current_runtime_prints_json_for_unknown"
        status: pass
    human_judgment: false
  - id: D2
    description: "Step 2.7 computes, displays, and carries native_runtime; Step 3 persists it on native entries only"
    requirement: REQ-multi-cli-runtime-detection
    verification:
      - kind: other
        ref: "skills/ai-kit-spec-config/SKILL.md Step 2.7 / Step 3"
        status: pass
    human_judgment: true
  - id: D3
    description: "skill-judge gate: no remaining Critical/Important, score >= 96/120, no regression vs baseline 107/120"
    requirement: REQ-multi-cli-runtime-detection
    verification:
      - kind: other
        ref: "skill-judge review of skills/ai-kit-spec-config/SKILL.md — 108/120 Grade A, Critical 0, Important 0"
        status: pass
    human_judgment: true
  - id: D4
    description: "ROADMAP SC3 and CONTEXT D-08 dated 2026-09-09 amendments resolving ranked candidate output to Step 2.7"
    requirement: REQ-multi-cli-runtime-detection
    verification:
      - kind: other
        ref: ".planning/ROADMAP.md SC3; 01.2-CONTEXT.md Amendment (2026-09-09)"
        status: pass
    human_judgment: false

# Metrics
duration: unknown
completed: 2026-09-09
status: complete
---

# Phase 1.2 Plan 04: Wire native_runtime into the spec-config wizard

**The wizard's native-entry flow now live-detects the host runtime, labels it inline as `(running under: <value>)` — including the literal `unknown` case — and persists `native_runtime` on native `[[reviewers]]` entries only.**

## Accomplishments

- New `detect-current-runtime` CLI subcommand (zero arguments, no cache/TTL) prints `{"native_runtime": "<value>"}` via `detect_current_runtime(env_fn())`.
- `ai-kit-spec-config/SKILL.md` Step 2.7 runs that subcommand, shows `(running under: <native_runtime>)` next to the native-entry ask, and defers host-identity prose to the live-detected value instead of asserting Claude Code unconditionally.
- Step 3 writes `native_runtime` only on native entries, always as an explicit string including `"unknown"`.
- ROADMAP Success Criterion 3 and CONTEXT D-08 each carry a dated 2026-09-09 amendment resolving "ranked candidate output" to Step 2.7.
- skill-judge: baseline 107/120 (Grade B, `f6411c3`) → 108/120 (Grade A). Critical 0, Important 0. No regression.

## Task Commits

- **Tasks 1-2 (CLI subcommand + wizard wiring + skill-judge gate)** — `f5c0efb` (feat)

**Plan metadata:** this SUMMARY, written after `f5c0efb`.

## Files Created/Modified

- `skills/ai-kit-spec-review/ai_kit_spec/cli.py` — import, parser, handler for `detect-current-runtime`
- `tests/test_ai_kit_spec.py` — `test_detect_current_runtime_prints_json` and `_for_unknown`
- `skills/ai-kit-spec-config/SKILL.md` — Step 2.7/Step 3 wiring; Step 2.2 routing stub
- `skills/ai-kit-spec-config/references/cli-model-selection.md` — extracted Step 2.2 8-item procedure
- `.planning/ROADMAP.md` — SC3 dated amendment
- `.planning/phases/01.2-multi-cli-runtime-foundation/01.2-CONTEXT.md` — D-08 dated amendment
- `.planning/phases/01.2-multi-cli-runtime-foundation/01.2-04-SUMMARY.md` — this file

## Deviations from Plan

**Step 2.2 extracted to `references/cli-model-selection.md`.** The plan's Task 1 file list did not include a new reference file. After the Step 2.7/Step 3 edits, SKILL.md was 614 lines with no `references/` of its own — skill-judge D5 scores 0-5 for a dump over 500 lines with no layering. Sibling skills (`ai-kit-spec-execute-superpowers`, `ai-kit-spec-execute-gsd`) already use MANDATORY-load + Do-NOT-load. The 8-item CLI catalog procedure moved to the reference; SKILL.md keeps the order-matters framing, the per-CLI keep/decline ask (so the native-only skip is a real predicate), and a MANDATORY load trigger naming the live landmines that skipping the file reproduces. Native-only runs Do NOT load it. This is a Task 2 quality-gate fix, not a scope change: every Task 1 must-have (subcommand, annotation, unknown display, host-conditional prose, Step 3 persistence, dated SC3/D-08 notes) is still in SKILL.md.

No other deviations. Tests were written first and watched fail (`invalid choice: 'detect-current-runtime'`) before the handler was added.

## Issues Encountered

None that blocked the work. skill-judge D5 would have been a Critical/Important structural finding at 614 lines; extracting Step 2.2 cleared it.

## User Setup Required

None.

## Next Phase Readiness

Phase 1.2 ROADMAP Success Criterion 3 is true under this plan's dated amendment. REQ-multi-cli-runtime-detection's wizard-labeling + skill-judge acceptance criterion is satisfied. Remaining Phase 1.2 work is already complete (01.2-01/02/03); this was the last open plan.

## skill-judge report (ai-kit-spec-config)

### Summary
- **Baseline** (pre-Task-1, last recorded independent run): 107/120 (Grade B) at `f6411c3`
- **Post-change**: 108/120 (90%, Grade A)
- **Pattern**: Process (Step 0→4) with Tool-level rigor at each factory call
- **Knowledge Ratio**: E:A:R ≈ 75:20:5
- **Verdict**: Expert wizard knowledge, now properly layered; no Critical/Important findings; no regression vs 107.

### Dimension Scores

| Dimension | Score | Max | Notes |
|-----------|-------|-----|-------|
| D1: Knowledge Delta | 18 | 20 | Live landmines (codex `gpt-5.6-sol-high`, cursor-agent ~200 ids, native auto-priority reorder). Tiny activation remainder in Step 0 path resolution. |
| D2: Mindset vs Mechanics | 13 | 15 | Step 2 opening judgment calls (quota reliability, purpose match). Not a full ask-yourself block at every step. |
| D3: Anti-Pattern Quality | 14 | 15 | NEVER list with WHY, including the new `(running under: unknown)` never-omit rule. |
| D4: Specification Compliance | 14 | 15 | Valid frontmatter; description has WHAT/WHEN/keywords (`--check-only`, `--local`, `--global`, review-spec.toml). |
| D5: Progressive Disclosure | 13 | 15 | SKILL.md 337 lines; MANDATORY load + Do-NOT-load on `references/cli-model-selection.md`. One reference, not a full tree. |
| D6: Freedom Calibration | 14 | 15 | Factory subcommands for fragile writes; medium freedom on ladder/CLI selection. |
| D7: Pattern Recognition | 9 | 10 | Self-declared Process + Tool hybrid, now matching the official Process length band. |
| D8: Practical Usability | 13 | 15 | `--check-only` probe-quota shadowing, render-toml WARNING path, unknown-runtime display, native-only skip. |

**Critical issues:** none.
**Important issues:** none.

## Verification (captured)

```
$ python3 -m unittest tests.test_ai_kit_spec.TestMainCli.test_detect_current_runtime_prints_json tests.test_ai_kit_spec.TestMainCli.test_detect_current_runtime_prints_json_for_unknown -v
test_detect_current_runtime_prints_json (tests.test_ai_kit_spec.TestMainCli.test_detect_current_runtime_prints_json) ... ok
test_detect_current_runtime_prints_json_for_unknown (tests.test_ai_kit_spec.TestMainCli.test_detect_current_runtime_prints_json_for_unknown) ... ok

----------------------------------------------------------------------
Ran 2 tests in 0.004s

OK
```

RED (before `cli.py` handler existed):

```
test_detect_current_runtime_prints_json ... ERROR
test_detect_current_runtime_prints_json_for_unknown ... ERROR
review-spec: error: argument command: invalid choice: 'detect-current-runtime'
FAILED (errors=2)
```

```
$ make test
----------------------------------------------------------------------
Ran 1181 tests in 11.703s

OK (skipped=19)
14 passed, 0 failed   (tests/test_install.sh)
```

```
$ make lint
shellcheck tools/install.sh tests/test_install.sh
python3 -m py_compile tools/setup.py tools/status-line.py tools/statusline-doctor.py
(exit 0)
```

## Self-Check: PASSED

- [x] `detect-current-runtime` prints `{"native_runtime": "<value>"}`, including `"unknown"`
- [x] Step 2.7 shows `(running under: <value>)`, including `(running under: unknown)` never omitted
- [x] Step 2.7 host-identity prose defers to the live-detected value
- [x] Step 3 documents `native_runtime` persistence on native entries only
- [x] ROADMAP SC3 and CONTEXT D-08 carry dated 2026-09-09 amendment notes
- [x] skill-judge 108/120 Grade A, Critical 0, Important 0, no regression vs 107
- [x] `make test` and `make lint` both pass
- [x] Production commit exists (`f5c0efb`)

---
*Phase: 01.2-multi-cli-runtime-foundation*
*Completed: 2026-09-09*
