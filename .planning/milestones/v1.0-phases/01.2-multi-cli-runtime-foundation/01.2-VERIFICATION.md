---
phase: 01.2-multi-cli-runtime-foundation
verified: 2026-09-09T18:58:25Z
status: passed
score: 4/4 must-haves verified
covered_files:
  - .planning/phases/01.2-multi-cli-runtime-foundation/01.2-01-PLAN.md
  - .planning/phases/01.2-multi-cli-runtime-foundation/01.2-01-SUMMARY.md
  - .planning/phases/01.2-multi-cli-runtime-foundation/01.2-02-PLAN.md
  - .planning/phases/01.2-multi-cli-runtime-foundation/01.2-02-SUMMARY.md
  - .planning/phases/01.2-multi-cli-runtime-foundation/01.2-03-PLAN.md
  - .planning/phases/01.2-multi-cli-runtime-foundation/01.2-03-SUMMARY.md
  - .planning/phases/01.2-multi-cli-runtime-foundation/01.2-04-PLAN.md
  - .planning/phases/01.2-multi-cli-runtime-foundation/01.2-04-SUMMARY.md
  - .planning/phases/01.2-multi-cli-runtime-foundation/01.2-REVIEW.md
  - .planning/REQUIREMENTS.md
  - tools/install.sh
  - tests/test_install.sh
  - skills/ai-kit-spec-review/ai_kit_spec/detection.py
  - skills/ai-kit-spec-review/ai_kit_spec/model_catalog.py
  - skills/ai-kit-spec-review/ai_kit_spec/config_io.py
  - skills/ai-kit-spec-review/ai_kit_spec/cli.py
  - tests/test_ai_kit_spec.py
  - skills/ai-kit-spec-config/SKILL.md
  - skills/ai-kit-spec-config/references/cli-model-selection.md
  - docs/research/opencode-sidebar-status-bar-viability.md
covered_digest: "v1:sha256:728164eed7005e87fc122e2db10592fa99fc66448e6f94eae899ec1098254182"
behavior_unverified: 0
overrides_applied: 0
---

# Phase 1.2: Multi-CLI Runtime Foundation Verification Report

**Phase Goal:** ai-kit's installer and model-catalog tooling are hardened and runtime-aware as the
project expands beyond Claude Code toward opencode and Codex.
**Verified:** 2026-09-09T18:58:25Z
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Installer's primary git path only ever fetches the target branch's history (single-branch), while the tarball-fallback and `git pull --ff-only` paths stay byte-identical (SC1) | ✓ VERIFIED | `tools/install.sh:107` literally reads `git clone --single-branch --branch "$REPO_BRANCH" --depth 1 "$url" "$INSTALL_DIR"`. Line 100 (`git -C "$INSTALL_DIR" pull --ff-only`) and lines 129/131 (`curl \| tar xz --strip-components=1 -C "$tmp"` / `wget -qO- \| tar xz ...`) are unchanged. `bash tests/test_install.sh` → 16 passed, 0 failed (independently re-run), including the FAKEGIT `--single-branch` argv assertions and an embedded `shellcheck` pass. |
| 2 | Asking "which CLI is ai-kit running under right now" gets a reliable, empirically-confirmed answer via `native_runtime`, validated in both `model_catalog.py` and `config_io.py`, mutually exclusive with a real `cli` value (SC2, per 2026-09-09 amendment) | ✓ VERIFIED | `detect_current_runtime()` exists in `skills/ai-kit-spec-review/ai_kit_spec/detection.py:35-71` — walks a fixed `_RUNTIME_ENV_SIGNALS` order (`CLAUDECODE`→`OPENCODE`→`CURSOR_AGENT`→`CODEX_VERSION`), returns `"unknown"` when none match, no `@cache`/`lru_cache`/TTL wrapper anywhere in the module (confirmed by direct grep — only `detect_installed_clis`/`build_runtimes_snapshot` reference `RUNTIMES_TTL_SECONDS`). `model_catalog.py:103-110` and `config_io.py:126-132` both independently implement `_VALID_NATIVE_RUNTIME = {"claude","opencode","codex","cursor","unknown"}` closed-set rejection AND the `native_runtime`+non-null-`cli` mutual-exclusion rejection, read directly from source (not inferred from tests). |
| 3 | `ai-kit-spec-config`'s wizard actually surfaces `native_runtime` in its native-entry flow (Step 2.7/Step 3), backed by a `detect-current-runtime` CLI subcommand (SC3, per 2026-09-09 amendment) | ✓ VERIFIED | `cli.py:496` registers `sub.add_parser("detect-current-runtime")`; handler at `cli.py:712-713` prints `json.dumps({"native_runtime": detect_current_runtime(env_fn())})`. `SKILL.md` Step 2.7 (lines 237-249) runs that exact subcommand, renders `(running under: <native_runtime>)` inline (including the literal `unknown` case, never omitted per its own text), and Step 3 (lines 270-283) documents `native_runtime` is written "ONLY on native entries, never on a CLI-sourced entry ... always written as an explicit string including `"unknown"`, never omitted." |
| 4 | A committed markdown research report enumerates opencode's documented plugin hooks, assesses sidebar/status-bar viability, cross-references both `anomalyco/opencode#5971` and `#23539`, and gives an explicit recommendation (SC4) | ✓ VERIFIED | `docs/research/opencode-sidebar-status-bar-viability.md` exists (87 lines), is a real synthesis with 5 substantive sections (hook surface, #5971, #23539, Codex, Recommendation), cites both issues 3 times each (`grep -c` confirmed), gives an explicit "wait for upstream" recommendation with a concrete re-investigation trigger (linked PR or maintainer implementation comment). Not a stub — content is specific (named proposed APIs, issue status, source citations). |

**Score:** 4/4 truths verified (0 present, behavior-unverified)

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `tools/install.sh` | `--single-branch` on primary clone; other paths untouched | ✓ VERIFIED | Confirmed by direct read + `bash tests/test_install.sh` (16/16 pass) |
| `tests/test_install.sh` | FAKEGIT harness asserts `--single-branch`; WR-02 regression test | ✓ VERIFIED | Independently re-run, 16 passed 0 failed, includes tarball-failure-no-orphan test |
| `skills/ai-kit-spec-review/ai_kit_spec/detection.py` | `detect_current_runtime()`, uncached | ✓ VERIFIED | Read directly; no cache/TTL wrapper |
| `skills/ai-kit-spec-review/ai_kit_spec/model_catalog.py` | `native_runtime` schema validation + cli mutual-exclusion | ✓ VERIFIED | Lines 23, 39, 93-110 read directly |
| `skills/ai-kit-spec-review/ai_kit_spec/config_io.py` | `native_runtime` schema validation + cli mutual-exclusion (functionally-reachable) | ✓ VERIFIED | Lines 97, 126-132 read directly; WR-03 dict-guard also present (lines 108-114) |
| `skills/ai-kit-spec-review/ai_kit_spec/cli.py` | `detect-current-runtime` subcommand | ✓ VERIFIED | Lines 26, 496, 712-713 |
| `skills/ai-kit-spec-config/SKILL.md` | Step 2.7/3 wizard wiring; passes skill-judge | ✓ VERIFIED | Content read directly (lines 60-283); skill-judge 108/120 Grade A per SUMMARY (LLM-judge — not independently re-run, see note below), 337 lines under the 500-line D5 bar |
| `skills/ai-kit-spec-config/references/cli-model-selection.md` | Extracted Step 2.2 procedure (skill-judge fix) | ✓ VERIFIED | Exists, 305 lines |
| `docs/research/opencode-sidebar-status-bar-viability.md` | Real synthesis report | ✓ VERIFIED | Read in full; substantive, not a stub |

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| `SKILL.md` Step 2.7 | `cli.py`'s `detect-current-runtime` | `python3 "$TOOLS_PY" detect-current-runtime` invocation, output captured as `native_runtime` | ✓ WIRED | Literal command present in SKILL.md line 241; subcommand exists and prints matching JSON shape |
| Step 2.7's captured `native_runtime` | Step 3's written `[[reviewers]]` entry | Prose explicitly ties the two together, mirroring `purpose`'s existing carry-forward pattern | ✓ WIRED | SKILL.md lines 249, 280-282 |
| `validate_reviewer_fields`/`validate_catalog_entry` cross-field checks | ROADMAP SC2's "never set on entries with a real cli value" | Machine-enforced rejection, not prose-only | ✓ WIRED | Verified in source, both files, confirmed by `TestRenderAndWriteToml.test_render_toml_rejects_native_runtime_with_non_null_cli` and `TestValidateCatalogEntry.test_native_runtime_rejected_when_cli_is_present`, both independently re-run and passing |
| `tools/install.sh`'s clone line | `tests/test_install.sh`'s FAKEGIT harness | argv capture + assertion | ✓ WIRED | Re-run: 16/16 pass |

### Behavioral Spot-Checks / Independent Test Re-Runs

| Command | Result | Status |
|---------|--------|--------|
| `python3 -m unittest tests.test_ai_kit_spec` (full suite, run independently, not trusting SUMMARY) | Ran 459 tests, OK (exit 0) | ✓ PASS |
| `bash tests/test_install.sh` (full suite, run independently) | 16 passed, 0 failed (exit implied 0) | ✓ PASS |
| `make lint` | shellcheck + py_compile clean, exit 0 | ✓ PASS |
| `python3 -m unittest tests.test_ai_kit_spec.TestDetectCurrentRuntime tests.test_ai_kit_spec.TestValidateCatalogEntry tests.test_ai_kit_spec.TestRenderAndWriteToml -v` | 56 tests, OK | ✓ PASS |
| Direct source read of `detection.py`, `model_catalog.py`, `config_io.py`, `cli.py`, `SKILL.md` against each literal SC wording | Matches claims | ✓ PASS |
| Direct read of `docs/research/opencode-sidebar-status-bar-viability.md` for stub-vs-substance | Substantive, specific content | ✓ PASS |

Note: `skill-judge`'s 108/120 score (SUMMARY-reported) is an LLM-driven qualitative review, not independently re-run here per this phase's scope constraints (no `tools/setup.py` wizard invocation, no live config-doctor apply). The SKILL.md content itself was independently read and confirmed to substantively deliver the SC3 wiring regardless of the score's exact reproducibility.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| — | — | No `TBD`/`FIXME`/`XXX`/`TODO`/`HACK`/`PLACEHOLDER` markers found in any phase-modified file (one `XXXXXX` match in `install.sh` is `mktemp`'s own template syntax, not a debt marker) | — | None |

**Code review (`01.2-REVIEW.md`):** 3 Warnings (WR-01 silent vendor-key corruption on slugified-empty creator name, WR-02 orphaned temp dir on tarball fetch failure, WR-03 dict-valued reviewer field silently corrupted to a TOML string) + 2 Info items, 0 Critical. Independently confirmed all 3 Warnings are fixed in current source:
- WR-02 fix (`731b1fa`): `tools/install.sh:126-133` now registers `trap 'rm -rf "$tmp"' EXIT` around the tarball fetch, cleared before the atomic swap. `tests/test_install.sh` gained a genuine failure-path regression test ("tarball fetch failure leaves no orphan .ai-kit.* temp dir") which passes.
- WR-01 fix (`d469951`): `cli.py:364` now reads `elif aa_match and (_slugify((aa_match.get("model_creator") or {}).get("name", "")) or None):` — the slugified result, not the raw name, gates the assignment.
- WR-03 fix (`786d7fc`): `config_io.py:108-114` now rejects any dict-valued reviewer field up front, before `_toml_value` would silently stringify it.
- shellcheck-lint follow-up (`1e04c53`): fixed a shellcheck SC2016 finding in WR-02's own regression test; `make lint` is clean (exit 0), independently re-run.

All four fix commits (`731b1fa`, `d469951`, `786d7fc`, `1e04c53`) exist in `git log` and their diffs match their stated intent.

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| REQ-multi-cli-install-single-branch | 01.2-01 | `--single-branch` on primary clone | ✓ SATISFIED | tools/install.sh:107; checkbox `[x]` and traceability row `Complete` in REQUIREMENTS.md |
| REQ-multi-cli-runtime-detection | 01.2-02, 01.2-04 | `detect_current_runtime()` + `native_runtime` schema + wizard wiring | ✓ SATISFIED (code) | Verified directly in source across both plans; REQUIREMENTS.md checkbox is still `[ ]` and traceability row still reads `Pending` — a bookkeeping lag, not a code gap (see Gaps Summary) |
| REQ-multi-cli-opencode-ui-research | 01.2-03 | Research report | ✓ SATISFIED (code) | Report exists and is substantive; REQUIREMENTS.md checkbox is still `[ ]` and traceability row still reads `Pending` — same bookkeeping lag |

No orphaned requirements: all three IDs mapped to this phase in REQUIREMENTS.md's traceability table are claimed by exactly one plan each (01.2-01/02/04, 01.2-03).

### Human Verification Required

None. All four success criteria are independently verifiable via source inspection and re-run test suites; no visual, real-time, or external-service-dependent behavior is in scope for this phase (the live `opencode`/`codex`/`cursor-agent` runtime-detection subprocess probes were already honestly documented as blocked-by-account-state in 01.2-02's own SUMMARY, not silently upgraded to "confirmed" — verified by reading detection.py's docstring, which still says "source-inspected-only" for `cursor`/`codex`, matching that honest disclosure).

### Gaps Summary

No code-level gaps found against any of the four ROADMAP Success Criteria. All four are genuinely implemented, wired, and covered by independently re-run tests — not just claimed in SUMMARY.md.

One non-blocking bookkeeping discrepancy: `.planning/REQUIREMENTS.md`'s checkboxes and traceability-table status column for `REQ-multi-cli-runtime-detection` and `REQ-multi-cli-opencode-ui-research` still read `[ ]`/`Pending` even though the underlying work (verified above) is complete and dated-amendment notes for both already exist inline in the same file. This looks like an artifact of 01.2-03's own documented worktree-isolation block (which explicitly could only land the amendment *prose*, not the checkbox state) and possibly a similar gap for 01.2-02/04. This is metadata bookkeeping, not a functional or goal-achievement gap — flagged here for the orchestrator's phase-completion bookkeeping pass (explicitly out of scope for this verifier per the task's own instructions not to touch ROADMAP.md/STATE.md; REQUIREMENTS.md checkbox sync is the natural place for the orchestrator to close this during phase completion).

---

_Verified: 2026-09-09T18:58:25Z_
_Verifier: Claude (gsd-verifier)_
