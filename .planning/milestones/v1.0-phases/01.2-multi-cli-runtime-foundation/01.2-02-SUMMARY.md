---
phase: 01.2-multi-cli-runtime-foundation
plan: 02
subsystem: detection+config
tags: [runtime-detection, model-catalog, review-spec, schema-validation]

# Dependency graph
requires: []
provides:
  - "detection.detect_current_runtime(env=None) -> claude/opencode/cursor/codex/unknown, fixed check order, never cached"
  - "model_catalog.validate_catalog_entry accepts/rejects native_runtime (forward-compatible schema)"
  - "config_io.validate_reviewer_fields accepts/rejects native_runtime (functionally-reachable schema)"
  - "both validators reject native_runtime co-present with a non-null cli"
affects: [01.2-multi-cli-runtime-foundation, 01.2-04-PLAN.md]

# Actuals
actuals:
  tokens: unknown
  tasks: 3
  commits: 1

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "detect_current_runtime(): fixed-order env-var walk, never TTL'd/cached — distinct from detect_installed_clis()/build_runtimes_snapshot()'s caching pattern"
    - "Schema addition mirrored across two validators (model_catalog.py forward-compatible-only, config_io.py functionally-reachable) rather than picking one, per 01.2-RESEARCH.md's own fallback recommendation"

key-files:
  created:
    - .planning/phases/01.2-multi-cli-runtime-foundation/01.2-02-SUMMARY.md
  modified:
    - skills/ai-kit-spec-review/ai_kit_spec/detection.py
    - skills/ai-kit-spec-review/ai_kit_spec/model_catalog.py
    - skills/ai-kit-spec-review/ai_kit_spec/config_io.py
    - tests/test_ai_kit_spec.py
    - AGENTS.md
    - .planning/REQUIREMENTS.md
    - .planning/ROADMAP.md

key-decisions:
  - "Open Question 1 resolved: native_runtime attaches to BOTH model_catalog.py (forward-compatible only — no live code path produces a catalog entry with a cli key) AND config_io.py (functionally-reachable — cli:None/omitted genuinely exists today only in review-spec.toml's native [[reviewers]] entries)."
  - "detect_current_runtime() deliberately uncached/no-TTL, unlike sibling detection functions — answers a fixed-for-process-lifetime question, cheap to recompute."
  - "Task 3 live smoke test genuinely attempted for codex/cursor-agent; both blocked by account state (expired codex refresh token, cursor-agent usage limit) — docstrings stay source-inspected-only per the plan's own accepted-gap contract, not upgraded to live-verified."

patterns-established:
  - "A present-but-empty env-var value (e.g. CLAUDECODE=\"\") is treated as falsy/absent via plain truthiness (env.get(var)), never a bare `var in env` presence check."

requirements-completed:
  - REQ-multi-cli-runtime-detection

coverage:
  - id: D1
    description: "detect_current_runtime() returns the fixed-order-first truthy signal among CLAUDECODE/OPENCODE/CURSOR_AGENT/CODEX_VERSION, or 'unknown' when none match, including the empty-string-is-falsy and both-signals-set-uses-fixed-order cases"
    requirement: REQ-multi-cli-runtime-detection
    verification:
      - kind: unit
        ref: "tests/test_ai_kit_spec.py::TestDetectCurrentRuntime (8 cases)"
        status: pass
    human_judgment: false
  - id: D2
    description: "native_runtime is validated as an optional closed-set field in both model_catalog.py and config_io.py, and rejected when co-present with a non-null cli"
    requirement: REQ-multi-cli-runtime-detection
    verification:
      - kind: unit
        ref: "tests/test_ai_kit_spec.py::TestValidateCatalogEntry (native_runtime cases)"
        status: pass
      - kind: unit
        ref: "tests/test_ai_kit_spec.py::TestRenderAndWriteToml (native_runtime cases)"
        status: pass
    human_judgment: false
  - id: D3
    description: "AGENTS.md/.planning/REQUIREMENTS.md/.planning/ROADMAP.md formally amended (2026-09-09) documenting the cli:None-lives-in-review-spec.toml structural finding"
    requirement: REQ-multi-cli-runtime-detection
    verification:
      - kind: other
        ref: "git diff 0cba99a^..0cba99a -- AGENTS.md .planning/REQUIREMENTS.md .planning/ROADMAP.md"
        status: pass
    human_judgment: false
  - id: D4
    description: "Live smoke-test attempt for codex/cursor-agent detection signals, honest outcome recorded"
    requirement: REQ-multi-cli-runtime-detection
    verification:
      - kind: manual
        ref: "codex exec ... env | grep CODEX_VERSION (exit 0, blocked: expired/already-used refresh token); cursor-agent -p ... env | grep CURSOR_AGENT (exit 0, blocked: usage limit)"
        status: pass
    human_judgment: true

# Metrics
duration: unknown (multi-attempt cross-AI dispatch, see Deviations)
completed: 2026-09-09
status: complete
---

# Phase 1.2 Plan 02: Multi-CLI Runtime Detection + native_runtime Schema Summary

**`detect_current_runtime()` identifies which CLI hosts the current process via a fixed env-var check order; `native_runtime` is now a validated, optional field in both `model_catalog.py` and `config_io.py`, mutually exclusive with a non-null `cli`.**

## Accomplishments

- `detection.detect_current_runtime(env=None) -> claude/opencode/cursor/codex/unknown`, uncached, walking `_RUNTIME_ENV_SIGNALS` in fixed documented order (`CLAUDECODE`, `OPENCODE`, `CURSOR_AGENT`, `CODEX_VERSION`).
- `_VALID_NATIVE_RUNTIME` closed-set validation added to both `model_catalog.py::validate_catalog_entry` and `config_io.py::validate_reviewer_fields`, each with a defensive `native_runtime` + non-null `cli` mutual-exclusion rejection.
- `AGENTS.md`, `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md` formally amended (2026-09-09) resolving Open Question 1.
- Task 3's live smoke test genuinely attempted against both source-inspected-only signals; both blocked by account state, honestly documented rather than silently upgraded.

## Task Commits

- **Tasks 1-3 (detection + schema + docs amendments)** — `0cba99a` (feat)

**Plan metadata:** committed alongside `0cba99a` (this SUMMARY).

## Files Created/Modified

- `skills/ai-kit-spec-review/ai_kit_spec/detection.py` — `detect_current_runtime()` + `_RUNTIME_ENV_SIGNALS`
- `skills/ai-kit-spec-review/ai_kit_spec/model_catalog.py` — `native_runtime` schema (forward-compatible)
- `skills/ai-kit-spec-review/ai_kit_spec/config_io.py` — `native_runtime` schema (functionally-reachable)
- `tests/test_ai_kit_spec.py` — `TestDetectCurrentRuntime` + `native_runtime` cases in `TestValidateCatalogEntry`/`TestRenderAndWriteToml`
- `AGENTS.md`, `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md` — dated amendments
- `.planning/phases/01.2-multi-cli-runtime-foundation/01.2-02-SUMMARY.md` — this file

## Deviations from Plan

### Process deviation (orchestration, not code)

**Cross-AI execution required multiple dispatch attempts before landing cleanly.** Two prior `Agent()`-dispatched subagents (`a2b8ac2f65e69a4fe`, `a2685b724ac7f3813`) each backgrounded an `opencode run` process and then incorrectly waited for a task-notification — a capability only the top-level orchestrator session has, not subagents. Both returned `status: completed` without the work actually finishing. The orchestrator (this session, directly) took over: found three concurrent, unsupervised `opencode run` processes writing to the same working tree from earlier stuck-subagent retries (`candidate_summary_02.md`, `candidate_summary_02b.md`, `plan02_retry2.log`), killed all of them to eliminate the concurrent-write risk, verified the resulting partial working-tree state via `ast.parse` (no syntax corruption) and the full test suite (455/455 passing), determined Tasks 1 and 2's code + tests were already complete and coherent, and personally finished the two remaining gaps: the three doc amendments and Task 3's live smoke-test attempt (both probes genuinely run, both blocked by account state). No production code required rework — only the missing documentation and the smoke-test step were completed directly.

**Impact on plan:** No scope change, no code rework. All must-haves satisfied.

## Issues Encountered

Multiple cross-AI dispatch attempts got stuck on the async-notification misunderstanding described above; resolved by direct orchestrator intervention rather than a third subagent dispatch.

## User Setup Required

None — no external service configuration required. (codex/cursor-agent auth/quota remain in their pre-existing degraded state; re-running Task 3's verify command later will pick up live-verification once either account issue clears.)

## Next Phase Readiness

Ready for 01.2-04 (wizard wiring), which consumes `native_runtime` via `config_io.py`'s now-validated schema. Phase 1.2 ROADMAP Success Criterion 2 is true for this plan's scope.

## Verification (captured)

```
$ python3 -m unittest tests.test_ai_kit_spec.TestDetectCurrentRuntime tests.test_ai_kit_spec.TestValidateCatalogEntry tests.test_ai_kit_spec.TestRenderAndWriteToml -v
Ran 55 tests in 0.002s
OK

$ make test
14 passed, 0 failed   (tests/test_install.sh)
(unittest suite: 455 tests, OK)

$ make lint
(clean, exit 0)

$ codex exec "Run exactly this shell command and nothing else, then stop: env | grep CODEX_VERSION"
exit 0; blocked: "Failed to refresh token: ... refresh token was already used"

$ cursor-agent -p "Run exactly this shell command and nothing else, then stop: env | grep CURSOR_AGENT"
exit 0; blocked: "ActionRequiredError: You've hit your usage limit"
```

## Self-Check: PASSED

- [x] `detect_current_runtime()` exists, uncached, fixed check order, all 8 behavior cases pass
- [x] `native_runtime` validated in both `model_catalog.py` and `config_io.py`
- [x] `native_runtime` + non-null `cli` rejected in both validators
- [x] `AGENTS.md`/`REQUIREMENTS.md`/`ROADMAP.md` carry dated 2026-09-09 amendments
- [x] Task 3 live smoke test genuinely attempted; honest outcome recorded, no false "confirmed" claim
- [x] `make test` and `make lint` both pass
- [x] Production commit exists (`0cba99a`)

---
*Phase: 01.2-multi-cli-runtime-foundation*
*Completed: 2026-09-09*
