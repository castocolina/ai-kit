---
phase: 01.2-multi-cli-runtime-foundation
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/research/opencode-sidebar-status-bar-viability.md
  - .planning/REQUIREMENTS.md
autonomous: true
cross_ai: true
requirements:
  - REQ-multi-cli-opencode-ui-research

estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A committed markdown file enumerates every opencode plugin hook documented today (event hooks, custom tools, agents, MCP configs, env injection) and states plainly that none of them support custom UI rendering."
    - "The report cross-references both anomalyco/opencode#5971 (sidebar panels — D-06's mandatory reference) and anomalyco/opencode#23539 (status-bar widgets — included because the phase's own goal names both sidebar and status-bar, per 01.2-RESEARCH.md's own recommendation to extend D-06's mandate)."
    - "Codex is mentioned with exactly one 'not applicable here' line, per D-06 — no dedicated Codex analysis section."
    - "The report gives an explicit recommendation ('wait for upstream') with a concrete re-investigation trigger (either issue gaining a linked PR or a maintainer implementation comment) rather than a vague 'monitor this' statement."
    - "No source code file is modified by this plan, per D-06 — the deliverable is documentation only."
  artifacts:
    - docs/research/opencode-sidebar-status-bar-viability.md
  key_links:
    - "anomalyco/opencode#5971 and #23539's own text (already fetched and quoted in 01.2-RESEARCH.md) -> the report's Recommendation section's 'wait for upstream' conclusion"
    - "REQUIREMENTS.md's REQ-multi-cli-opencode-ui-research traceability line -> the report's real committed path"
---

<objective>
Produce the committed markdown research report REQ-multi-cli-opencode-ui-research requires:
enumerate opencode's documented plugin hooks, assess sidebar/status-bar viability, cross-reference
`anomalyco/opencode#5971` (and, per 01.2-RESEARCH.md's own extension of D-06's mandate,
`anomalyco/opencode#23539`), and give an explicit recommendation. Per D-06, this phase makes
**no code changes** for this requirement — the research is already substantively complete (see
01.2-RESEARCH.md's "opencode Plugin Hooks" and "Code Examples > Report skeleton" sections); this
plan's job is synthesizing that already-gathered research into the committed deliverable, not new
discovery.

Purpose: give the project a decision-ready record of opencode's sidebar/status-bar plugin-API
state, so this doesn't get silently re-investigated from scratch in a future phase.
Output: `docs/research/opencode-sidebar-status-bar-viability.md`, committed, with an explicit
recommendation.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01.2-multi-cli-runtime-foundation/01.2-RESEARCH.md
@.planning/PROJECT.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Write the opencode sidebar/status-bar plugin viability report</name>
  <files>docs/research/opencode-sidebar-status-bar-viability.md</files>
  <action>
  Create the new `docs/research/` directory and write
  `docs/research/opencode-sidebar-status-bar-viability.md`, using 01.2-RESEARCH.md's own
  "Architecture Patterns > opencode Plugin Hooks (research deliverable content)" section as the
  primary source (the research is already complete this session — synthesize it, do not
  re-research it). Sections, in this order:

  1. **Current hook surface** — enumerate opencode's documented plugin hooks (event hooks,
     custom tools, agents, MCP configs, env injection), citing opencode.ai/v2/docs/build/plugins
     and opencode.ai/v2/docs/build/plugins/cli, and state plainly that none of them support
     custom UI rendering in the sidebar or status bar today.
  2. **anomalyco/opencode#5971 (sidebar panels)** — status (open, unimplemented as of
     2026-09-09, assigned to `@thdxr`, no linked PR or substantive maintainer comment), the
     proposed `SidebarPanel` interface (collapsible sections, static or 5s-polled dynamic
     content), and its own named example use cases — per D-06's mandatory cross-reference.
  3. **anomalyco/opencode#23539 (status-bar widgets)** — status (open, unimplemented, same lack
     of maintainer engagement), the proposed `tui.statusbar.widget` hook (widget id,
     positioning, render function, refresh interval), its references to `#8619`/`#18969`, and
     the note that opencode's TUI is built on the Bubble Tea framework. Include this section
     even though D-06 names only #5971 as mandatory — the phase's own goal explicitly says
     "sidebar/status-bar," and 01.2-RESEARCH.md's own recommendation is that omitting the
     status-bar issue would understate the actual state of upstream interest.
  4. **Codex** — exactly one line: "Not applicable here." Per D-06, no dedicated Codex analysis
     section.
  5. **Recommendation** — "wait for upstream": no stable, shipped opencode plugin hook exists
     today that could back a custom sidebar/status-bar element even as a workaround (both
     issues' own framing confirms this), so there is no hook to prototype against. State a
     concrete re-investigation trigger: either issue gaining a linked PR or a maintainer
     implementation comment — not a vague "monitor this" statement.

  Do not modify any source code file in this task, per D-06.
  </action>
  <verify>
    <automated>test -f docs/research/opencode-sidebar-status-bar-viability.md && grep -c 'anomalyco/opencode#5971' docs/research/opencode-sidebar-status-bar-viability.md && grep -c 'anomalyco/opencode#23539' docs/research/opencode-sidebar-status-bar-viability.md && grep -ic 'not applicable' docs/research/opencode-sidebar-status-bar-viability.md</automated>
  </verify>
  <done>
  The report exists at the committed path, covers all 5 required sections, cross-references
  both GitHub issues by number, gives Codex exactly one "not applicable" line, and ends with an
  explicit "wait for upstream" recommendation plus a concrete re-investigation trigger; `git
  status` shows zero source-code files touched by this task.
  </done>
</task>

<task type="auto">
  <name>Task 2: Confirm no code changes and cross-link the report from REQUIREMENTS.md</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
  Run `git status`/`git diff --stat` scoped to this plan's own changes and confirm the only
  files touched are the new report under `docs/research/` and this task's own edit to
  `.planning/REQUIREMENTS.md` — per D-06's "no code changes" constraint, no `.py`/`.sh`/`.md`
  skill file may appear in that diff. Add a short pointer in `.planning/REQUIREMENTS.md`'s
  `REQ-multi-cli-opencode-ui-research` line, linking to the report's real committed path
  (`docs/research/opencode-sidebar-status-bar-viability.md`), so a future reader of
  REQUIREMENTS.md finds the deliverable without searching `docs/` — mirroring how other
  amended requirement lines in this file already carry inline dated notes.
  </action>
  <verify>
    <automated>grep -q 'docs/research/opencode-sidebar-status-bar-viability.md' .planning/REQUIREMENTS.md</automated>
  </verify>
  <done>
  `.planning/REQUIREMENTS.md`'s `REQ-multi-cli-opencode-ui-research` entry references the
  report's real path; no file outside `docs/research/` and `.planning/REQUIREMENTS.md` was
  modified by this plan.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None introduced | This plan is documentation-only (D-06: no code changes) — no new trust boundary, no new attack surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01.2-04 | N/A | `docs/research/opencode-sidebar-status-bar-viability.md` | low | accept | Documentation-only deliverable, no code path, no runtime behavior — nothing to mitigate beyond the existing "no code changes" constraint this plan's own Task 2 verifies. |

</threat_model>

<verification>
`test -f docs/research/opencode-sidebar-status-bar-viability.md` passes; the report contains
both GitHub issue references and the "not applicable" Codex line; `git diff --stat` for this
plan shows only the report and the REQUIREMENTS.md pointer changed.
</verification>

<success_criteria>
Phase 1.2 ROADMAP Success Criterion 4 is true: a committed markdown research report exists
enumerating opencode's documented plugin hooks, assessing sidebar/status-bar viability,
cross-referencing `anomalyco/opencode#5971` (and `#23539`), and giving an explicit
recommendation.
</success_criteria>

<output>
Create `.planning/phases/01.2-multi-cli-runtime-foundation/01.2-03-SUMMARY.md` when done
</output>
